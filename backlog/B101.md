# B101 — Make ARC PlanChunker Graduation Reliable Across Puzzle Variants

- **State:** complete
- **Priority:** P1
- **Plan:** backlog/plans/B-101-arc-planchunker-reliable-graduation.md
- **Dependencies:** B95, B97, B99, B100

## Problem Statement

`PlanChunker` can now graduate from `explore` to `directional` in some live ARC runs, but the
graduation logic is still too brittle across puzzle variants. In one run, player+goal evidence was
strong enough to produce a directional chunk; in the next run, similarly strong role evidence still
left the solver stuck in `Explore: try unexplored action to gather more information`.

That means the solver has crossed the "I know who the player is and what the target likely is"
threshold, but the chunker is not using that knowledge consistently.

## What It Does

Harden chunk graduation so it behaves consistently once solve evidence is strong enough:

- define an explicit graduation equation for `explore -> directional`
- treat `player + goal + exploration coverage + role confidence` as first-class graduation signals
- allow directional chunking even when evidence is not perfect, if it is clearly stronger than
  continued pure exploration
- keep exploration as a fallback when solve evidence is genuinely weak or contradictory
- surface why the chunk graduated or stayed in explore inside solve summaries/debug output

## Acceptance Criteria

1. `PlanChunker` uses an explicit graduation score or thresholded decision, not a fragile set of
   ad hoc checks.
2. When `player` and `goal` both exceed configured confidence thresholds and exploration coverage is
   mostly complete, chunking prefers `directional` over `explore`.
3. Solve summaries/debug output record the reason a chunk stayed `explore` or graduated to
   `directional`.
4. Add at least 4 tests covering:
   - graduation with strong player+goal evidence
   - refusal to graduate with weak/contradictory evidence
   - partial exploration coverage with strong roles
   - a regression based on a live-run pattern where graduation previously failed
5. In a live puzzle-1 smoke run where player+goal evidence is present, the final strategy summary
   should not default to `Explore...` unless the debug reason explicitly says the evidence was
   insufficient or contradictory.

## Outcome

The solve phase stops randomly falling back to exploration after it already knows enough to attempt
directed movement.

## Validation Note (2026-03-31)

- Implemented files: `agents/arc3/solver.py`, `agents/arc3/runner.py`, `tests/test_arc3_solver.py`, `tests/test_arc3_durable_runner.py`
- Tests run:
  - `.venv/bin/pytest -q tests/test_arc3_solver.py tests/test_arc3_durable_runner.py tests/test_arc3_hypothesis.py tests/test_arc3_orchestrator.py`
- Result: `114 passed, 1 warning`
- Regression checks: live smoke run `codex_real_smoke_25` exported a directional chunk with explicit graduation reason and score
