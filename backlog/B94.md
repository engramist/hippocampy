# B94 - ARC Path Hypothesis Composition and Testing

State: complete

Plan: backlog/plans/B-94-arc-path-hypotheses.md

### B94 · ARC Path Hypothesis Composition and Testing

**Problem:** Action facts tell us what a single operator seems to do, but the real ARC search problem often lives at the path level: which short sequence or branch changes the board in a useful way. Right now the system can collect operator evidence without having a clean artifact for claims like "ACTION5 then ACTION6 opens a new region" or "ACTION7 after ACTION6 returns to a prior state."

**What this card builds:** A lightweight path-hypothesis layer on top of action facts. Path hypotheses should describe short sequences, their observed result, and whether they appear valuable, low-value, or ineffective.

**Scope:**
- Build short path hypotheses from recent transition sequences
- Keep path hypotheses separate from action facts
- Track path confidence and value separately from action-level consistency
- Surface path hypotheses in prompts and debug output
- Allow later cards to promote strong path hypotheses into durable SideQuests artifacts

**Depends on:** B88 (hypothesis engine), B93 (action fact promotion)

## Files to Create/Modify

- `agents/arc3/hypothesis.py`
- `agents/arc3/orchestrator.py`
- `tests/test_arc3_hypothesis.py`
- `tests/test_arc3_orchestrator.py`

## Acceptance Criteria

1. ARC can summarize recent 2-step or 3-step paths as explicit path hypotheses
2. Path hypotheses include:
   - action sequence
   - observed result summary
   - confidence
   - value status
3. Action facts remain separate from path hypotheses in code and prompt output
4. Prompt output can show at least one recent path hypothesis during a live run
5. Tests cover path-hypothesis generation and prompt surfacing

## Outcome

ARC reasoning moves from isolated operator facts toward actual sequence-level experimentation, which is much closer to how the puzzle should be explored.

## Validation Note (2026-03-31)

- Implemented files: `tests/test_arc3_hypothesis.py`
- Tests run:
  - `.venv/bin/pytest -q tests/test_arc3_hypothesis.py tests/test_arc3_orchestrator.py`
- Result: `88 passed, 1 warning`
- Regression checks: added explicit 3-step path-hypothesis coverage while preserving separate prompt surfacing for action facts vs. path hypotheses
