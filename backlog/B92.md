# B92 - ARC Step-Level SideQuests Write Trace

State: complete
Completed: 2026-04-01
Validation: 98/98 tests passed (test_b92_write_trace.py + solver/orchestrator + B124 regression suite)

Plan: backlog/plans/B-92-arc-write-trace.md

### B92 · ARC Step-Level SideQuests Write Trace

**Problem:** ARC debug output currently shows prompts, actions, and environment effects, but it does not show what SideQuests actually saved on each step. That makes it hard to verify whether the explore phase is producing durable knowledge or just generating text in the local loop. We need to see the exact SideQuests writes associated with each action so we can debug memory quality instead of guessing.

**What this card builds:** A step-level write trace for ARC runs. Each step should expose the SideQuests artifacts created or updated during that turn, including:
- raw turn notifications sent to SideQuests
- new or updated hypotheses
- lessons or concepts extracted from the step
- promoted action facts or blocked/no-op facts
- whether the write was tentative, confirmed, or refuted

**Scope:**
- Add an ARC debug export section that captures the SideQuests write payloads associated with each step
- Keep the trace compact and deterministic enough to compare between runs
- Include identifiers so we can map prompt step -> action -> SideQuests write(s)
- Avoid dumping full graph rows; surface the write intent and saved summary instead

**Depends on:** B88 (hypothesis engine), current ARC debug export

## Files to Create/Modify

- `agents/arc3/runner.py`
- `agents/arc3/orchestrator.py`
- `benchmarks/arc3/PROMPT_STRATEGY.md`
- `tests/test_arc3_durable_runner.py`

## Acceptance Criteria

1. `submission_results_single.json` includes a per-step SideQuests write trace
2. Each step can show:
   - action id
   - observed effect summary
   - SideQuests write summaries created from that step
3. The trace distinguishes tentative vs confirmed vs refuted knowledge when available
4. At least one real ARC smoke run can be inspected end to end without reading SideQuests internals directly
5. Tests cover the new export structure

## Outcome

ARC debugging becomes memory-visible. We can inspect not just what the agent saw and did, but what SideQuests actually retained from each step.
