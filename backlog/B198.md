# B198: ARC Agent — Proactive Warning Integration (Active Context Push)

**Status**: ready
**Assigned**: TBD
**Depends on**: B195 (active context push)
**Complexity**: Small (1-2 hours)
**Priority**: P2 (prevents repeated mistakes without agent effort)
**Plan**: backlog/plans/B-198-arc-proactive-warnings.md

## Problem Statement

After B195 extends `notify_turn` to return `proactive_context`, the ARC orchestrator needs to actually read and act on these warnings. Currently, `notify_turn` responses are stored in trace events but not used for decision-making.

## Root Causes

1. Orchestrator calls `notify_turn` but ignores the response content beyond recording it
2. No mechanism to inject proactive warnings into the solve prompt or hypothesis context
3. Proactive warnings about failed actions are lost

## What This Card Does

1. **Parse `proactive_context`** from `notify_turn` response in orchestrator's step processing
2. **Inject warnings into solve context**: Add `proactive_warnings` field to `hypothesis_context` passed to `SolveEngine.solve()`
3. **Solve engine uses warnings**: If a warning says "ACTION4 was ineffective in similar puzzles," the plateau policy and autopilot should penalize ACTION4
4. **Trace logging**: Record proactive warnings in `_execution_trace` for eval

## Ecosystem Layer

- `agents/arc3/orchestrator.py` (Agent Runtime — parse and use proactive_context)
- `agents/arc3/solver.py` (Agent Runtime — use warnings in scoring)

**Cross-layer compliance**: No cross-layer violations.

## Files to Create/Modify

- **MODIFY** `agents/arc3/orchestrator.py` — Parse `proactive_context` from notify_turn response, add to hypothesis_context
- **MODIFY** `agents/arc3/solver.py` — Use `proactive_warnings` in `_score_action_families()` to penalize warned-against actions
- **CREATE** `tests/test_b198_arc_proactive_warnings.py` — Tests for warning parsing, injection, action penalization
- No changes to `docs/tool-catalog.md`

## Acceptance Criteria

- [ ] `proactive_context` parsed from `notify_turn` response
- [ ] Warnings injected into `hypothesis_context["proactive_warnings"]`
- [ ] `_score_action_families()` penalizes actions flagged in warnings
- [ ] Warnings recorded in execution trace
- [ ] Graceful when `proactive_context` is empty or absent
- [ ] All existing ARC tests pass

## Outcome

ARC agents automatically avoid repeating known mistakes. The brain warns about failed strategies, and the solver incorporates those warnings into action scoring.
