# B205 — Evolving Perceive Questions: Context-Aware SideQuests Ingestion Per Step

- **State:** ready
- **Priority:** P2
- **Plan:** backlog/plans/B-205-evolving-perceive-questions.md
- **Ecosystem Layer(s):** Agent Runtime & Harness
- **Cross-layer violations:** None — changes are in orchestrator.py (perception content) and runner.py (timeline labeling). No KuzuDB schema changes.
- **Dependencies:** B202 (Per-Step PERCEIVE) — must be complete; `perceive_step_response()` is the insertion point.

## Problem Statement

The per-step perceive phase (added by B202) injects a structured server response summary into SideQuests via `notify_turn`. However, that summary is stateless — it reports what happened (grid delta, reward) without framing it in terms of what the agent already knows (archetype, victory condition, active strategy chunk).

This matters because SideQuests ingestion is **semantic** — the context around a fact determines how well it's retrieved later. A `notify_turn` that says:

```
[STEP RESPONSE] Step 5, action=ACTION2. State: NOT_FINISHED. Reward: 0.0. Grid: 4 cells changed.
```

is less retrievable and less useful than:

```
[STEP RESPONSE] Step 5, action=ACTION2. Puzzle type: space (conf=0.57). Victory: unknown. 
Strategy: Plateau Exploitation → ACTION2. State: NOT_FINISHED. Reward: 0.0. 
Grid: 4 cells changed, direction=right. Expected movement, no progress. 
Available: ACTION1, ACTION2, ACTION3.
```

Similarly, the `phase_question` timeline label for PERCEIVE never changes from `"What kind of puzzle is this and what is the likely win condition?"` — even at step 15 when the archetype is already classified. The question should evolve from discovery to refinement.

## What It Does (Scope)

### 1. Enrich `perceive_step_response()` notify_turn content (high impact)

In `orchestrator.perceive_step_response()` (added by B202), read orchestrator state to build a context-aware response summary before the `notify_turn` call:

- **Step 0 / no context:** Use discovery framing ("What kind of puzzle is this?")
- **Step > 0 with context:** Use hypothesis-testing framing that includes:
  - Archetype + confidence (from `_solve_context`)
  - Victory condition (from `_solve_context`)
  - Active strategy chunk description
  - Prior action + outcome (from `_step_history[-1]`)
  - Whether the result matched expectation (reward > 0 vs. 0)
  - Grid delta interpretation

This is assembled from already-available in-memory fields — no new LLM calls, no KuzuDB queries.

### 2. Store the contextual question on `_last_response_perception` (medium impact)

Add a `"phase_question"` key to the dict returned by `perceive_step_response()` and stored in `_last_response_perception`. This allows `_phase_answer_for("perceive")` (B204) and timeline exports to surface the evolved question.

### 3. Make `phase_question` for PERCEIVE context-aware in timeline (low impact, cosmetic)

The current `_phase_question_for("perceive")` is a static method with no access to orchestrator state. Rather than refactoring the method signature, read `orchestrator._last_response_perception.get("phase_question")` in `_phase_answer_for()` (already an instance method with orchestrator access) and include it in the timeline answer string.

The static `_phase_question_for` can remain unchanged — the question evolution lives in the perception content and the phase_answer, which is the right place.

### Not in scope
- LLM calls to generate the contextual question.
- KuzuDB writes for question evolution tracking (timeline already captures this via notify_turn ingest).
- Changing the structure of `perceive_step_response()` beyond enriching the summary string.

## Files to Create/Modify

| File | Action |
|------|--------|
| `agents/arc3/orchestrator.py` | **Modify** — Enrich `response_summary` in `perceive_step_response()` with archetype/victory/chunk context; add `"phase_question"` to perception dict |

### Adapter/allow-list impact
- No new tools introduced.
- No tool-catalog changes.

## Acceptance Criteria

1. At step > 0, `perceive_step_response()` `notify_turn` content includes the current archetype (name + confidence) if known.
2. At step > 0, the summary includes the active strategy chunk description if one is active.
3. At step > 0, the summary includes whether the action matched expectation ("expected movement" / "no effect" / "unexpected change").
4. `_last_response_perception["phase_question"]` is set to a non-generic string at step > 0 (e.g., `"Did ACTION2 advance toward the victory condition? Archetype=space, chunk=Plateau Exploitation."`).
5. Bootstrap perceive (step 0) summary is unchanged — still uses discovery framing.
6. No LLM calls are made in `perceive_step_response()`.
7. `pytest tests/test_arc3_orchestrator.py::test_perceive_step_response` passes (may need update to check enriched content).
8. Smoke test: `master_timeline_filtered.md` perceive entries show archetype context in their `what` field at step > 0.

## Outcome

Each per-step perceive ingestion into SideQuests carries enough context to be retrieved meaningfully in future steps. The timeline shows the agent's question evolving from "what is this puzzle?" at step 0 to "did this action advance our strategy?" at step 5+, making the learning progression visible and the SideQuests knowledge base more useful.
