# B80 - Fix Step 1b Filter Aggressiveness After O4 Fix

State: ready
Priority: MEDIUM
Review Finding: R2-O2
Plan: backlog/plans/B-80-step1b-filter-fix.md

### B80 · Fix Step 1b Filter Aggressiveness After O4 Fix

**Problem:** The Review #1 O4 fix (defer Step 1b storage until after Step 2 noise filtering) is correct, but the filter checks `r["head"].lower() in surviving_texts`. This drops relations where the head entity is a noun chunk that didn't pass through NER. Step 1b verb patterns can produce valid relations with noun-chunk heads, but `surviving_texts` only contains NER entities.

**What it does:** Adjusts the Step 1b post-filter to also include noun-chunk texts from Step 1b heads/tails.

**Depends on:** None

**Files to modify:**
- `mcp_engine/loop/orchestrator.py` — adjust surviving_texts filter logic

**Adapter allow-list propagation check:** No tool changes — N/A.

**Acceptance Criteria:**
- Step 1b relations with noun-chunk heads that weren't NER entities are preserved (not filtered out)
- Relations where BOTH head and tail are noise are still filtered
- `pytest tests/ -k "step1b or orchestrator" -q` passes

**Outcome:** Valid Step 1b relations with noun-chunk heads are no longer incorrectly discarded.
