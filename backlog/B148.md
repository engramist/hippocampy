# B148: Preserve Grounded Player/Goal and Victory Confidence Under Late-Run Drift

**Status**: complete ✅ DONE (2026-04-01)
**Assigned**: TBD  
**Depends on**: B147  
**Complexity**: Medium (3–5 hours)  
**Priority**: P0 (next live-run blocker after B147)  
**Related**: B141, B142, B143, B144, B145, B146, B147  
**Plan**: backlog/plans/B-148-preserve-grounding-under-drift.md

## Problem Statement

B147 fixed the threshold-flapping case for plateau activation, but the latest verified live smoke shows a deeper failure mode: in some late-run traces, the player/goal grounding and victory confidence themselves collapse too far for plateau mode to engage at all.

Evidence from `live_qwen25_7b_b147verify_1775257893`:
- `correct = false`, `steps = 15`, `final_state = NOT_FINISHED`
- the final summary still ends with `CHUNK: Explore: try unexplored action to gather more information [explore]`
- verified late-run confidences fall much lower than the sticky sustain window:
  - `player = 0.45`
  - `goal = 0.35`
  - `final_victory_confidence = 0.35`
- the live trace shows the run degrading back into generic exploration rather than preserving the strongest earlier grounded interpretation

This means the next bottleneck is now **late-run grounding collapse**: once the solver has previously established a plausible player, goal, and victory condition, noisy later frames should not immediately erase that structure unless there is strong contradictory evidence.

## What This Card Builds

1. **Recent-best grounding persistence**: keep the strongest recent `player` / `goal` assignments available through noisy late frames rather than letting weak refreshes drag them down too aggressively.

2. **Contradiction-aware demotion**: only demote previously grounded roles when there is explicit contradictory evidence, not merely lower-confidence ambiguity.

3. **Victory-confidence stabilization**: preserve the most credible recent `reach_goal` interpretation across late-run drift unless the evidence clearly changes.

4. **Regression for the live collapse pattern**: add a failing regression covering the observed drop from usable grounding into low-confidence `explore` mode.

## Files to Create/Modify

- `agents/arc3/solver.py` — preserve recent-best grounding / victory evidence under late-run drift and refine demotion rules
- `tests/test_arc3_solver.py` — regressions for late-run grounding persistence and contradiction-aware demotion
- `tests/test_arc3_orchestrator.py` — optional coverage if solve-context propagation or summary output needs assertions

## Ecosystem / Layer Compliance

- Layer: ARC solve engine (`agents/arc3/solver.py`) with optional solve-context propagation checks in orchestrator tests
- No new tools, adapter changes, or schema migrations expected
- Additive persistence / demotion refinement only; no cross-layer import violations
- Must comply with `docs/arc-harness-rules.md` and `docs/ecosystem-rules.md`

## Acceptance Criteria

- [ ] Previously grounded player/goal roles are not demoted during late-run drift unless strong contradictory evidence appears
- [ ] Victory confidence remains stable enough to preserve the current solve policy when evidence is noisy but not contradictory
- [ ] The live collapse pattern from `live_qwen25_7b_b147verify_1775257893` is reproduced by regression and prevented
- [ ] Late-run summaries do not fall back to generic `Explore` solely because weak refreshes lowered role confidence
- [ ] Existing B143 coordinate policy and B145–B147 plateau behavior remain intact
- [ ] Focused ARC tests and broader regressions pass with no regressions

## Outcome Statement

When complete, the solver will preserve its strongest grounded understanding through noisy late frames, allowing plateau and exploitation logic to stay engaged instead of collapsing back into low-confidence exploration.