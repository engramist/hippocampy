# B172: Persist VictoryCondition to KuzuDB

**Status**: ready
**Assigned**: TBD
**Depends on**: B170 (hypothesis persistence — victory conditions relate to hypotheses)
**Complexity**: Small (1-2 hours)
**Priority**: P1 (victory condition is critical solve state with no persistence)
**Plan**: backlog/plans/B-172-victory-condition-kuzu.md

## Problem Statement

`SolveEngine._victory_condition` (`solver.py:1754`) stores the inferred victory condition (e.g. "reach the goal", "visit all intermediates then interact with goal") as a Python dataclass. This is one of the most important pieces of solve state — it determines when the agent thinks it has won.

There is no KuzuDB node type for victory conditions. This knowledge is lost between solves and invisible to graph queries.

## Root Causes

1. `VictoryCondition` dataclass (`solver.py:67-74`) was built as ephemeral solve state
2. No schema was ever created for it

## What This Card Does

1. **Add `VictoryCondition` node type to schema**: `condition_id`, `task_id`, `level`, `condition_type`, `description`, `confidence`, `evidence_steps`, `created_at`, `last_updated_step`
2. **Add `INFERRED_FROM` relationship**: `VictoryCondition → Hypothesis` (links victory inference to supporting hypotheses)
3. **Add `REQUIRES_ENTITY` relationship**: `VictoryCondition → GridEntity` (e.g. "must visit intermediate X")
4. **Persist to KuzuDB**: When SolveEngine infers or updates victory condition, write to KuzuDB
5. **Load from KuzuDB**: At start of solve, check if a victory condition was already inferred for this task/level

## Ecosystem Layer

- `agents/arc3/solver.py` (Agent Runtime — victory condition writer)
- `mcp_engine/schema.py` (Data Products — new node type + 2 relationships)

**Cross-layer compliance**: No cross-layer violations.

## Files to Create/Modify

- **MODIFY** `mcp_engine/schema.py` — Add `VictoryCondition` node type and relationships
- **MODIFY** `agents/arc3/solver.py` — Persist victory condition to KuzuDB on inference/update
- **CREATE** `tests/test_b172_victory_condition_persistence.py` — Tests for CRUD and relationship creation
- No changes to `docs/tool-catalog.md`

## Acceptance Criteria

- [ ] `VictoryCondition` node type exists in schema
- [ ] Inferring a victory condition writes a node to KuzuDB
- [ ] Updating confidence or evidence updates the node
- [ ] `INFERRED_FROM` edges link to supporting Hypothesis nodes
- [ ] `REQUIRES_ENTITY` edges link to GridEntity nodes involved in the condition
- [ ] When KuzuDB unavailable, falls back to in-memory-only
- [ ] All existing ARC tests pass

## Outcome

Victory condition inference is persisted to the knowledge graph. Cross-level and cross-puzzle patterns ("puzzles with intermediates always require visit-then-interact") become queryable.
