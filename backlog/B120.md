# B120 - Entity-Enriched Observation in Prompt Blocks

State: complete
Completed: 2026-04-01
Validation: 118/118 tests passed (test_arc3_orchestrator.py + solver + B111 + B124 + B92 regressions)

Plan: backlog/plans/B-120-entity-enriched-observation.md

### Problem

`_summarize_puzzle_structure()` and `_format_observation_section()` report raw grid data (colors,
shapes, spatial sketch) but include zero entity-role context. The OBSERVATION block in the prompt
packet says things like "color 5 (12 cells)" — not "color 5 = PLAYER at row 3, col 7".

Without entity annotations, the LLM cannot ground its action choices in what entities are doing.
Even when `ObjectRoleMapper` runs later in `solve`, its results never flow back into the
observation or puzzle structure summaries that seed SideQuests memory or the prompt.

### What This Card Builds

Enrich observation summaries and prompt blocks with entity role data:

1. `_summarize_puzzle_structure()` includes entity roles alongside color descriptions.
2. `_format_observation_section()` annotates colors with known roles.
3. `build_action_packet()` adds an ENTITY_CONTEXT section to the prompt when entity map exists.
4. Available actions get reasoning hints tied to entity positions when roles are known.

### Depends on

- B119 (entity map must exist from bootstrap)
- B117 (typed decision packets)

## Files to Create/Modify

- `agents/arc3/orchestrator.py` — update `_summarize_puzzle_structure()`, `_format_observation_section()`, `build_action_packet()`
- `docs/arc-harness-rules.md` — add ENTITY_CONTEXT to prompt block ownership, update block-by-phase matrix
- `tests/test_arc3_orchestrator.py` — test entity annotations in observation and prompt output

## Acceptance Criteria

1. `_summarize_puzzle_structure()` output includes entity roles when `_entity_map` is populated.
2. `_format_observation_section()` annotates color lines with roles (e.g., "color 5 (PLAYER, 12 cells)").
3. When entity map exists, prompt packet includes a block with entity role summary.
4. When entity map is empty, observation falls back to current behavior (no crash).
5. Tests validate enriched vs. fallback observation output.
6. `docs/arc-harness-rules.md` updated to reflect new block.

### Adapter Allow-List Check

No new tools introduced. No tool-catalog update needed.

## Outcome

The LLM sees entity-grounded context in every prompt, enabling strategy even when actions are
constrained or when the environment has complex multi-entity dynamics.
