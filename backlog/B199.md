# B199: ARC Agent — Knowledge Gap-Aware Exploration Budget

**Status**: ready
**Assigned**: TBD
**Depends on**: B193 (metacognition gaps)
**Complexity**: Small (1-2 hours)
**Priority**: P2 (more exploration for unknown puzzle types)
**Plan**: backlog/plans/B-199-arc-gap-aware-exploration.md

## Problem Statement

After B193 creates KnowledgeGap nodes, the ARC agent should use this information to decide how much exploration budget to allocate. A puzzle archetype with rich accumulated knowledge needs less exploration than one with a knowledge gap.

Currently, B168's exploration phase has a fixed budget (4 deterministic + 4 LLM-guided steps). This should be adaptive.

## Root Causes

1. Exploration budget is hardcoded, not adaptive to knowledge state
2. Orchestrator doesn't query `get_knowledge_gaps` before solving
3. No mechanism to increase exploration for unfamiliar archetypes

## What This Card Does

1. **Pre-solve gap check**: In `runner.py`, call `get_knowledge_gaps` before puzzle solve
2. **Adaptive exploration budget**: If a KnowledgeGap exists for the puzzle's archetype, double the Phase 2b exploration budget (LLM-guided steps)
3. **If no gap**: Use default exploration budget (proven recipe available via B197)
4. **Trace logging**: Record gap-aware budget adjustment in execution trace

## Ecosystem Layer

- `agents/arc3/runner.py` (Agent Runtime — gap check and budget adjustment)
- `agents/arc3/orchestrator.py` (Agent Runtime — configurable exploration budget)

**Cross-layer compliance**: Agent Runtime reads from Data Products via BrainClientProtocol.

## Files to Create/Modify

- **MODIFY** `benchmarks/arc3/adapter.py` — Add `get_knowledge_gaps` to BrainClientProtocol (delegates to existing tool)
- **MODIFY** `agents/arc3/runner.py` — Call `get_knowledge_gaps`, pass budget to orchestrator
- **MODIFY** `agents/arc3/orchestrator.py` — Accept `exploration_budget_multiplier` config, apply to Phase 2b
- **CREATE** `tests/test_b199_gap_aware_exploration.py` — Tests for gap detection, budget adjustment, default fallback
- No changes to `docs/tool-catalog.md`

## Acceptance Criteria

- [ ] `get_knowledge_gaps` queried before each puzzle solve
- [ ] Exploration budget doubled when KnowledgeGap exists for archetype
- [ ] Default exploration budget used when no gap
- [ ] Budget adjustment recorded in execution trace
- [ ] NoOpBrainClient returns empty gaps (default budget)
- [ ] All existing ARC tests pass

## Outcome

The agent spends more time exploring unfamiliar puzzle types and less time on well-understood ones. Exploration effort is proportional to ignorance.
