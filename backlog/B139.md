# B139: Break Low-Value Explore Loops in `space` Puzzles After Goal Inference

**Status**: complete ✅ DONE (2026-04-01)
**Assigned**: TBD  
**Depends on**: B132, B133, B134, B135, B136, B138  
**Complexity**: Medium (2–4 hours)  
**Priority**: P1 (HIGH — plumbing is healthy, but the live solver still stalls at zero reward)  
**Plan**: backlog/plans/B-139-space-solver-quality.md

## Problem Statement

The post-fix live ARC smoke is now much healthier operationally, but the solver is still failing on the actual puzzle.

Fresh evidence from the latest real-api smoke (`LIVE_ID=live_smoke_1775194418`) shows:

```text
Task 1: arc_eval_001
Correct: False
Steps: 15
final_state: NOT_FINISHED
```

And the solve summary now clearly shows the remaining bottleneck:
- archetype inferred as `space` with confidence `0.65`
- victory condition inferred as `reach_goal` with confidence `0.83`
- object roles include a high-confidence `player=0` and `goal=9`
- `no_progress_step_count = 15`
- the agent still decays into low-value `ACTION3` / `ACTION4` / `ACTION6` / `ACTION7` behavior
- the final active chunk remains `Explore: try unexplored action to gather more information`
- `dissonance` remains `True`

So the recent bug-card work fixed **plumbing, provenance, recall gating, replanning, and trace visibility** — but the remaining problem is now clearly **solver quality**. The agent understands enough to identify the puzzle as a goal-reaching space puzzle, yet it does not reliably convert that inference into goal-directed actions.

## What This Card Builds

1. Adds a stronger transition from `explore` mode to `goal-directed` mode once:
   - player and goal roles are both known with high confidence,
   - exploration coverage is complete enough,
   - and repeated low-value actions indicate exploration has plateaued.
2. Introduces a geometry-aware action bias for `space` puzzles that scores actions by whether they appear to reduce inferred player→goal distance or preserve a consistent movement trend.
3. Penalizes repeated low-value action churn across the whole action set, not just single-action repetition, so the agent stops bouncing among equally bad options.
4. Emits trace evidence such as `geometry_bias`, `goal_distance_delta`, or `explore_to_directional_promotion` so the policy shift is auditable in `agent_trace`.

## Files to Create/Modify

- `agents/arc3/solver.py` — improve chunk graduation/action ranking for `space` + `reach_goal` puzzles
- `agents/arc3/orchestrator.py` — surface the new directional/geometry decision signals in trace output
- `tests/test_arc3_solver.py` — add regression coverage for promotion out of low-value explore loops
- `tests/test_b139_space_goal_policy.py` — targeted regression for the current failure mode

## Ecosystem / Layer Compliance

- Layer: ARC agent solve engine (`agents/arc3/solver.py`) + orchestration policy (`agents/arc3/orchestrator.py`)
- No cross-layer import violations.
- Builds directly on the observability/provenance work completed in B130/B132–B138.
- Complies with `docs/arc-harness-rules.md` and `docs/ecosystem-rules.md`.

## Acceptance Criteria

- [ ] A regression test reproduces the current failure mode: player+goal known, but policy remains stuck in low-value exploration
- [ ] Once the regression is fixed, the solver promotes to a directional/goal-driven chunk instead of staying in `Explore: try unexplored action...`
- [ ] Relevant ARC suites pass with no regressions
- [ ] A fresh live smoke shows at least one meaningful goal-directed adjustment after victory inference, rather than 15 straight no-progress steps
- [ ] `submission_results_single.json` or `agent_execution_trace.json` includes clear evidence for why the goal-directed action was chosen

## Outcome Statement

After this card, the ARC stack stops failing for purely tactical reasons after the infrastructure is already working. The remaining bottleneck becomes actual puzzle reasoning quality instead of exploration churn and low-value action decay.