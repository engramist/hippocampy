# B149: Action Fatigue — Penalize Exhausted Zero-Reward Actions During Exploitation

**Status**: complete ✅ DONE (2026-04-01)
**Assigned**: TBD
**Depends on**: None (benefits from B142 evidence-floor, but independent)
**Complexity**: Medium (2–4 hours)
**Priority**: P0 (root cause of 15-step plateau stalls on a single action)
**Related**: B141 (blocked actions), B142 (graduation evidence floor), B144–B146 (plateau mode)
**Plan**: backlog/plans/B-149-action-fatigue.md

## Problem Statement

Once the agent finishes initial exploration and enters exploitation, it repeatedly selects the same action (e.g. ACTION6) for the entire remaining run — even when that action produces zero reward on every attempt.

Existing guards do not cover this case:

| Guard | Why it doesn't fire |
|-------|---------------------|
| B133 repetition gate | Only fires when action + frame hash are identical. If the frame changes even slightly (e.g. a timer tick), the gate skips. |
| B141 blocked-action set | Only blocks an action after 5 **consecutive global** no-progress steps. If the agent alternates between two zero-reward actions, neither reaches the threshold. |
| Plateau locked-family (B144–B146) | Deliberately locks the agent onto the "top ranked" action family. If that family is wrong, the lock amplifies the mistake. |
| Exploration policy | Only forces untested actions. Once `initial_exploration_complete`, this path is dead. |

Evidence from live run:
- `no_progress_step_count: 15` with plateau locked on ACTION6
- Every step in the trace shows ACTION6 with reward 0
- Dissonance never fires because graduation stays inflated (addressed by B142, but action selection is still the bottleneck)

The agent needs a mechanism to track per-action fatigue and suppress exhausted actions so it can try alternatives.

## What This Card Builds

1. **Per-action fatigue counter**: Track consecutive zero-reward uses of each action during the current chunk. Stored as `Dict[str, int]` on the orchestrator.

2. **Fatigue penalty in action enforcement**: When the fatigue count for an action exceeds a threshold (default: 3), the action is demoted in `_enforce_action_policy` — either replaced with the next-best ranked action, or with an untested/least-recently-used alternative.

3. **Plateau lock escape hatch**: When the plateau locked-family itself has fatigue ≥ threshold, temporarily unlock and allow the secondary family (or a rotation to a third option) instead of forcing the locked choice.

4. **Fatigue reset on chunk switch**: When the active chunk changes (new strategy), reset all fatigue counters to zero. The new plan deserves a fresh evaluation.

5. **Trace events**: Emit `action_fatigue_override` when fatigue forces a switch, with the fatigued action, its count, and the replacement.

## Files to Create/Modify

- `agents/arc3/orchestrator.py` — add fatigue tracking dict, fatigue-aware logic in `_enforce_action_policy`, fatigue reset on chunk switch, trace events
- `tests/test_b149_action_fatigue.py` — new test file for fatigue tracking, penalty application, plateau escape, chunk-reset, and no-false-positive cases

## Ecosystem / Layer Compliance

- Layer: ARC agent orchestration internals (`agents/arc3/orchestrator.py`)
- No cross-layer import violations. All changes are within the orchestrator's action enforcement policy.
- No new tools, adapter changes, or schema migrations.
- Complies with `docs/arc-harness-rules.md` and `docs/ecosystem-rules.md`.

## Acceptance Criteria

- [ ] Per-action fatigue counter increments on each zero-reward step for the executed action
- [ ] After 3 consecutive zero-reward uses of the same action, `_enforce_action_policy` switches to an alternative
- [ ] When the plateau locked-family is fatigued, the secondary family is used instead
- [ ] Fatigue counters reset to zero when the active chunk changes
- [ ] `action_fatigue_override` trace event is emitted when fatigue forces a switch
- [ ] Actions that produce reward do NOT accumulate fatigue (counter resets to 0)
- [ ] Existing test suites pass with no regressions

## Outcome Statement

After this card, the agent will stop perseverating on a single zero-reward action during exploitation. Instead, when an action proves unproductive after 3 attempts, the agent rotates to alternatives — breaking plateau stalls without requiring full replanning.
