# B169: KuzuDB as Single Source of Truth for Object Roles

**Status**: ready
**Assigned**: TBD
**Depends on**: B168 (graph-based exploration agent)
**Complexity**: Medium (3–4 hours)
**Priority**: P0 (role disconnect causes B168 graph-inferred roles to be silently discarded)
**Plan**: backlog/plans/B-169-kuzu-role-source-of-truth.md

## Problem Statement

Object roles (player, goal, wall, intermediate) are stored in two disconnected places:

1. **`SolveEngine._object_roles`** — an in-memory `Dict[int, ObjectRole]` on a Python object. All 20+ readers in solver.py and orchestrator.py read from here.
2. **KuzuDB `GridEntity` nodes** — have `inferred_role` and `role_confidence` columns, written by B168's `EntityGraphBuilder`.

These two stores are never synchronized. B168 writes high-confidence roles (e.g. player=color_10 at 0.95) to KuzuDB, and `merge_graph_roles()` copies them into `_object_roles`. But `seed_bootstrap_roles()` and `_merge_persistent_roles()` only write to `_object_roles` — KuzuDB never sees their updates.

The in-memory dict should be a **read-through cache** over KuzuDB, not a competing source of truth.

## Root Causes

1. `_object_roles` predates B168/KuzuDB — it was the only role store before the graph existed.
2. `seed_bootstrap_roles()` writes directly to the dict, bypassing KuzuDB entirely.
3. `_merge_persistent_roles()` does complex merge logic (B148 grounding, conflict resolution, demotion) entirely in-memory.
4. Orchestrator reads (player position, goal position, intermediate count) were recently moved from `_solve_context["object_roles"]` to `solve_engine._object_roles` but still bypass the graph.

## What This Card Does

1. **All role writers go through KuzuDB first**: `seed_bootstrap_roles()`, `_merge_persistent_roles()`, and `merge_graph_roles()` write to `GridEntity.inferred_role` / `GridEntity.role_confidence` in KuzuDB.
2. **`_object_roles` becomes a read-through cache**: A new `_sync_roles_from_db()` method queries KuzuDB and populates `_object_roles`. Called once per step at the start of `step()`.
3. **All 20+ readers stay unchanged**: They keep reading `_object_roles` (now a cache, not source of truth). No async conversion needed.
4. **Merge logic moves to Cypher**: The B148 grounding rules, conflict resolution, and demotion logic are expressed as KuzuDB merge operations, so the graph is always authoritative.

## Ecosystem Layer

- `agents/arc3/solver.py` (ARC agent layer — role writers become KuzuDB writers)
- `agents/arc3/orchestrator.py` (ARC agent layer — `merge_graph_roles` simplified)
- `agents/arc3/entity_graph.py` (ARC agent layer — new role persistence methods)
- `mcp_engine/schema.py` (Data Products & Memory — no schema changes, `GridEntity` already has the columns)

**Cross-layer compliance**: Role data flows through `KuzuClient` (layer 4) to `GridEntity` nodes (layer 5). Agent logic stays in layer 2. No cross-layer violations.

## Files to Create/Modify

- **MODIFY** `agents/arc3/entity_graph.py` — Add `persist_role(color_id, role, confidence, position)` and `load_all_roles() -> Dict[int, ObjectRole]` methods
- **MODIFY** `agents/arc3/solver.py` — `seed_bootstrap_roles()` and `_merge_persistent_roles()` write to KuzuDB via EntityGraphBuilder; add `_sync_roles_from_db()` called at start of `step()`
- **MODIFY** `agents/arc3/orchestrator.py` — `merge_graph_roles()` simplified to just call `entity_graph.persist_role()` (the cache sync happens automatically at next step)
- **MODIFY** `agents/arc3/runner.py` — Pass `entity_graph` reference to SolveEngine so it can call persist/load methods
- **CREATE** `tests/test_b169_kuzu_role_source.py` — Tests for role persistence, cache sync, merge conflict resolution via KuzuDB
- No changes to `docs/tool-catalog.md` (no new MCP tools)

## Acceptance Criteria

- [ ] `seed_bootstrap_roles()` writes roles to KuzuDB `GridEntity` nodes (not just in-memory dict)
- [ ] `_merge_persistent_roles()` writes merge results to KuzuDB
- [ ] `_sync_roles_from_db()` populates `_object_roles` from KuzuDB at start of each step
- [ ] B168 `merge_graph_roles()` writes only to KuzuDB (no direct `_object_roles` mutation)
- [ ] After `_sync_roles_from_db()`, `_object_roles` matches KuzuDB state exactly
- [ ] B148 grounding rules (confidence >= 0.7 preservation) still work correctly
- [ ] Demotion of extra primaries (single player, single goal) still works
- [ ] When KuzuDB is unavailable (NoOp mode), falls back to in-memory-only path — existing behavior preserved
- [ ] All existing ARC tests pass (`pytest tests/test_arc3_*.py -q`)
- [ ] Smoke test shows roles in both KuzuDB and `_object_roles` are consistent

## Outcome

KuzuDB is the single source of truth for object roles. The in-memory dict is a per-step cache. All writers (bootstrap, per-step refinement, B168 graph inference) go through KuzuDB. This eliminates the dual-store disconnect that caused B168's high-confidence inferences to be silently lost.
