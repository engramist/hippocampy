# B118 - ARC Ledger-Driven Runtime Pruning

State: complete
Completed: 2026-04-01
Validation: 143/143 tests passed (test_b118_ledger_pruning.py + solver + orchestrator + B111 + B124 + B92 regressions)

Plan: backlog/plans/B-118-arc-ledger-driven-runtime-pruning.md

### Problem

We now have `sidequests_ledger`, but the harness still does not adapt to what the ledger teaches
us:

- which SideQuests calls are expensive
- which are low-value
- which repeatedly fail to improve action quality

### What This Card Builds

Ledger-driven pruning for ARC runtime behavior:

- use ledger evidence to suppress or down-rank expensive low-value calls
- prioritize reads/writes that correlate with meaningful action improvements
- make pruning decisions visible in debug output

### Depends on

- B111
- B108

## Files to Create/Modify

- `agents/arc3/orchestrator.py`
- `agents/arc3/runner.py`
- `benchmarks/arc3/PROMPT_STRATEGY.md`
- `tests/test_arc3_orchestrator.py`
- `tests/test_arc3_durable_runner.py`

## Acceptance Criteria

1. The harness can identify high-latency / low-value SideQuests call patterns from the ledger.
2. The harness can suppress or down-rank those call patterns in later steps/runs.
3. Pruning decisions are visible in debug/export output.
4. Tests cover pruning decisions from synthetic ledger data.
5. A live smoke run shows reduced unnecessary SideQuests overhead without breaking solve behavior.

## Outcome

The ARC harness starts adapting its own runtime behavior based on measured SideQuests value rather
than static heuristics alone.
