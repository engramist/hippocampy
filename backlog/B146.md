# B146: Stabilize Plateau Lock and Keep Chunk/Action/Trace in Sync

**Status**: complete ✅ DONE (2026-04-01)
**Assigned**: TBD  
**Depends on**: B145  
**Complexity**: Medium (3–5 hours)  
**Priority**: P0 (remaining end-to-end plateau-policy blocker)  
**Related**: B141, B142, B143, B144, B145  
**Plan**: backlog/plans/B-146-stabilize-plateau-lock-sync.md

## Problem Statement

B145 materially improved the live behavior by replacing the generic `Explore` chunk with a real `plateau_exploitation` chunk, but the latest verified smoke shows that the plateau lock is still not fully stable or self-consistent.

Evidence from `live_qwen25_7b_b145verify_1775255395`:
- `correct = false`, `steps = 15`, `final_state = NOT_FINISHED`
- final strategy summary now shows `CHUNK: Plateau Exploitation: commit to top-ranked ACTION2 [plateau_exploitation]`
- however the same final summary ends with `LOCKED FAMILY: ACTION4`
- late actions still churn across families (`ACTION6`, `ACTION6`, `ACTION6`, `ACTION3`, `ACTION6`, `ACTION3`)
- the live run therefore shows a mismatch between:
  - the plateau chunk description,
  - the reported locked family,
  - and the actual executed actions

This means the remaining bottleneck is now **plateau lock stability and synchronization**: once plateau mode locks onto a family, the solver, orchestrator, trace, and executed actions should all agree on that family unless an explicit unlock condition fires.

## What This Card Builds

1. **Stable locked-family state**: persist one authoritative `plateau_locked_family` instead of recomputing/overwriting it opportunistically across later steps.

2. **Explicit unlock policy**: only allow the locked family to change when fresh evidence crosses a defined threshold or the current family is explicitly exhausted.

3. **Chunk/trace/action synchronization**: ensure `active_chunk.description`, `strategy_summary`, `LOCKED FAMILY`, and the actual chosen action family stay aligned.

4. **Regression for the observed mismatch**: lock in the bug fix so a run can no longer report `top-ranked ACTION2` while actually ending on `LOCKED FAMILY: ACTION4` and dispatching a different family.

## Files to Create/Modify

- `agents/arc3/solver.py` — persist the authoritative plateau family in solve context and keep summary text synchronized with it
- `agents/arc3/orchestrator.py` — enforce the stored locked family consistently and only unlock on explicit reasons
- `tests/test_arc3_solver.py` — regression for summary/chunk/locked-family consistency
- `tests/test_arc3_orchestrator.py` — regression for stable family locking and controlled unlock behavior

## Ecosystem / Layer Compliance

- Layer: ARC solve engine (`agents/arc3/solver.py`) + ARC orchestration policy (`agents/arc3/orchestrator.py`)
- No new tools, adapter changes, or schema migrations expected
- Additive policy consistency fix only; no cross-layer import violations
- Must comply with `docs/arc-harness-rules.md` and `docs/ecosystem-rules.md`

## Acceptance Criteria

- [ ] Once plateau mode locks a family, that family remains the authoritative choice until an explicit unlock/exhaustion reason fires
- [ ] `active_chunk`, `strategy_summary`, `LOCKED FAMILY`, and actual executed late-step actions remain consistent about which family is active
- [ ] Any family change in plateau mode emits an explicit unlock reason in trace output
- [ ] The specific B145 mismatch (`CHUNK: ... ACTION2` while `LOCKED FAMILY: ACTION4`) is covered by a regression and cannot recur
- [ ] Existing B143 `ACTION6` coordinate behavior remains intact when `ACTION6` is the locked family
- [ ] Focused ARC tests and broader regressions pass with no regressions

## Outcome Statement

When complete, plateau mode will not just create an exploitation chunk — it will keep one coherent, auditable family lock across the solver and orchestrator, eliminating the current live mismatch between the reported policy and the actions actually executed.