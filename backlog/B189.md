# B189: Puzzle Scheduler

**Status**: ready
**Assigned**: TBD
**Depends on**: B180 (budget), B184 (circuit breaker)
**Complexity**: Medium (2-4 hours)
**Priority**: P2 (infrastructure for sustained improvement)
**Plan**: backlog/plans/B-189-puzzle-scheduler.md

## Problem Statement

`DurableARCRunner.run()` processes puzzles in a simple for-loop (line ~53 of runner.py). All puzzles get equal treatment — trivially easy ones and impossibly hard ones get the same step budget and execution priority. There's no health checking, no difficulty ordering, and no concurrency control.

Enterprise orchestration needs "scheduling and lifecycle management" — managed creation, task assignment, health checking, and scaling. The ARC analog is a puzzle scheduler that orders by difficulty, enforces health checks, and supports configurable concurrency.

## Root Causes

1. Runner iterates `for task in tasks:` with no ordering
2. No difficulty estimation for puzzles
3. No health check before starting a puzzle (is LLM responsive? is memory under limit?)
4. No concurrency support (single-threaded for-loop)

## What This Card Does

1. **New `PuzzleScheduler` class** in `agents/arc3/scheduler.py`
2. Orders puzzles by estimated difficulty (historical solve data from KuzuDB, archetype, grid size)
3. Skips previously-solved puzzles (configurable)
4. Health checks before each puzzle: LLM ping, memory check, API connectivity
5. Configurable concurrency via `asyncio.Semaphore` (default 1 for sequential, but ready for B187)
6. Reports ETA based on average puzzle solve time

## Ecosystem Layer

- `agents/arc3/scheduler.py` (Agent Runtime — new file)
- `agents/arc3/runner.py` (Agent Runtime — replace for-loop with scheduler)

**Cross-layer compliance**: No cross-layer violations.

## Files to Create/Modify

- **CREATE** `agents/arc3/scheduler.py` — PuzzleScheduler class with ordering, health checks, concurrency
- **MODIFY** `agents/arc3/runner.py` — Replace for-loop with `scheduler.run_batch(tasks)`
- **CREATE** `tests/test_b189_scheduler.py` — Tests for ordering, health checks, skip logic
- No changes to `docs/tool-catalog.md`

## Acceptance Criteria

- [ ] Puzzles ordered by estimated difficulty (easy first)
- [ ] Previously-solved puzzles skipped when configured
- [ ] Health check runs before each puzzle; skips puzzle if LLM unresponsive
- [ ] Concurrency configurable (default 1 = sequential)
- [ ] ETA reported after first puzzle completes
- [ ] All existing ARC tests pass

## Outcome

Easy puzzles solved first (maximizes early score). Unhealthy infrastructure detected before wasting tokens. Ready for concurrent execution when B187 lands.
