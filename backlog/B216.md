# B216 — Loop Detection Signal Must Propagate Into Route/Replan Decisions

- **State:** ready
- **Priority:** P1
- **Plan:** backlog/plans/B-216-loop-detection-route-propagation.md
- **Ecosystem Layer(s):** Agent Runtime
- **Cross-layer violations:** None.
- **Dependencies:** B215.

## Problem Statement

`Am I looping?` is issued via `current_truth` in the execute phase every step, but the loop signal never changes routing behavior. Replan fires on a periodic timer (every ~3 steps, steps 6/9/12/15/18) and emits "Graduation dropped to 0.20 (reason: evidence_floor)" — but each replan returns the router to the same ACTION6 decision. The loop detection machinery fires but is structurally disconnected from the route phase.

Confirmed from `master_timeline.json`:
- `current_truth` query "Am I looping?" appears in execute phase on every step 5–20.
- Route decision is unchanged across all 20 steps regardless of loop signal.
- `_hypothesis_context.loop_detected` is read in the runner (runner.py:359, 766, 968, 1498, 1690) but the replan trigger (runner.py:1901–1910) only uses it to gate escalation, not to change which action family is selected.

## What It Does (Scope)

1. When a loop is detected (same action family chosen for ≥ K consecutive steps with zero net board change), the route phase must be forced into exploration mode — specifically, the currently-locked action family must be blacklisted for the next chunk selection.
2. Add a `loop_detected_action_blacklist` to the route context that prevents re-selecting a family that caused the detected loop.
3. Emit a trace event: "loop_escape: blacklisting ACTION_X for next chunk selection".
4. Reset the blacklist after a successful state change (n_cells_changed > 0).

Out of scope:
- Rewriting the loop detection heuristic itself.
- Changes to the `current_truth` tool.

## Files to Create/Modify

| File | Action |
|------|--------|
| `agents/arc3/solver.py` | **Modify** — add loop-escape blacklist field; apply it in plateau/chunk selection |
| `agents/arc3/runner.py` | **Modify** — pass loop signal into route context when loop_detected fires (around line 1901) |
| `tests/test_arc3_solver.py` | **Modify** — add test: loop detection blacklists current family for next route decision |

### Adapter/allow-list impact
- No tool changes.

## Acceptance Criteria

1. When same action family is chosen for ≥ 3 consecutive steps with zero board change, loop detection triggers route-level blacklist.
2. Route phase selects a different action family on the step following loop detection.
3. Blacklist resets after any step where `n_cells_changed > 0`.
4. Trace event is emitted identifying the blacklisted family and reason.
5. In a smoke run, `route` phase decisions show family variation after a detected loop.

## Outcome Statement

Loop detection becomes actionable: when the agent is stuck repeating the same action, routing is structurally forced to try something different rather than continuing the loop indefinitely.
