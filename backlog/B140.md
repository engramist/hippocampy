# B140 - M7 Quest Tree Navigation & Historical Fork UX

State: ready

Plan: backlog/plans/B-140-quest-tree-navigation.md

## Problem Statement

M7 (Memory Control Panel) is planned with "graph visualization, soft-lock UI, merge rollback" but has no explicit spec for how users navigate the Quest/Session/Message tree or fork new SideQuests from historical context. The graph schema already supports this (Session → Message → SENT_IN provenance, branch_quest tool, WORKING_ON edges), but without a dedicated tree navigation UX the Memory Control Panel will be a flat list of nodes rather than an explorable timeline.

Inspiration: pi-mono coding-agent's tree-structured session history demonstrates that visual tree navigation with branch/fork affordances is a high-value UX pattern for AI memory systems. The key insight is that users need to see *where* they are in the quest tree and be able to fork from any historical point — not just the current session.

## What It Does (Scope)

Add a Quest Tree view to the M7 Memory Control Panel (FastAPI + static frontend) with three capabilities:

### 1. Quest Tree Visualization
- Render MainQuest → SideQuest → Session hierarchy as an interactive tree (similar to a git commit graph)
- Each Session node shows: `purpose`, `started_at`, `last_active_at`, artifact count
- Each Quest node shows: `status`, `purpose`, artifact count, `pathway_strength` distribution
- Color-code by status: active (green), completed (gray), archived (dim)
- Edges: `BELONGS_TO`, `WORKING_ON`, `SENT_IN` relationships rendered as tree branches

### 2. Historical Context Drill-Down
- Click any Session node to expand its Message timeline
- Click any Message node to see its neighborhood: artifacts it `ESTABLISHED`, `MergeEvent`s it `TRIGGERED`, concepts extracted
- Show confidence and pathway_strength on each artifact inline
- Filter by artifact type (Decision, Constraint, Requirement, ActionItem, Plan)

### 3. Fork from History
- "Fork SideQuest" button on any Message or Session node
- Clicking it calls `branch_quest` with context seeded from the selected historical point
- The new SideQuest's `purpose` is pre-populated from the source Message/Session context
- Fork point is recorded via a new `FORKED_FROM` relationship: `(SideQuest)-[FORKED_FROM {message_id, session_id, forked_at}]->(Message | Session)`

## Dependencies

- **B102** (Local Graph Visibility UI Using Kuzu Explorer) — B102 established the optional Kuzu Explorer debugging workflow (`tools/graph_viewer/`), including starter Cypher queries for Sessions, Messages, Plans, Lessons, Hypotheses, and neighborhood traversals. B140 should reuse and extend these queries as the data layer for the Quest Tree API endpoints rather than writing new Cypher from scratch. The B102 starter queries (especially #1 recent sessions, #2 message provenance, #7 current-memory slice, #8 concept neighborhood) map directly to B140's three capabilities.
- M7 Memory Control Panel infrastructure (FastAPI `web/server.py` + `web/static/`)
- Existing schema: Session, Message, MainQuest, SideQuest, SENT_IN, WORKING_ON, BELONGS_TO, ESTABLISHED
- `branch_quest` MCP tool (M5)

### B102 → B140 Migration Path

B102 is an optional Docker-based read-only debugging tool. B140 graduates the most useful parts of that workflow into the built-in M7 Memory Control Panel:

| B102 (Optional Docker Debug) | B140 (Built-in M7 Panel) |
|-------------------------------|--------------------------|
| Kuzu Explorer via Docker | FastAPI endpoints in `web/server.py` |
| Manual Cypher queries | Structured JSON API responses |
| Raw graph browser | Purpose-built Quest Tree visualization |
| Read-only snapshot copy | Read-only Kuzu connection (same as B102's safety model) |
| Starter query pack | API endpoints wrapping those queries |

B102's `tools/graph_viewer/` remains available as an advanced debug escape hatch. B140 makes the 80% use case (quest/session/message navigation) accessible without Docker.

## Files to Create/Modify

- `web/static/quest-tree.js` — Tree visualization component (D3.js or similar lightweight lib)
- `web/static/quest-tree.css` — Tree styling
- `web/server.py` — New API endpoints:
  - `GET /api/quest-tree` — returns Quest/Session/Message hierarchy as JSON tree
  - `GET /api/session/{session_id}/messages` — returns Message timeline with artifact links
  - `GET /api/message/{message_id}/neighborhood` — returns 1-hop graph neighborhood
  - `POST /api/fork-quest` — calls `branch_quest` with historical context
- `web/static/index.html` — Add Quest Tree panel/tab to Memory Control Panel
- `mcp_engine/graph/kuzu_client.py` — Add `FORKED_FROM` relationship table (if not already present)
- `tests/test_quest_tree_api.py` — API endpoint tests
- `docs/tool-catalog.md` — No new MCP tools (this is web UI only), but document the `FORKED_FROM` relationship if added

## Adapter / Tool Allow-List Check

No new MCP tools introduced. The fork action reuses the existing `branch_quest` tool internally. No adapter allow-list changes needed.

## Ecosystem Layer

This card operates in the **web** layer (`web/server.py`, `web/static/`). Read-only Kuzu access for visualization queries. The `POST /api/fork-quest` endpoint delegates to the Brain Daemon via IPC (same as MCP tools), not direct Kuzu writes. No cross-layer violations.

## Acceptance Criteria

1. `GET /api/quest-tree` returns a JSON tree with MainQuest → SideQuest → Session hierarchy, including `purpose`, `status`, `started_at`, and artifact counts for each node.
2. `GET /api/session/{id}/messages` returns chronologically ordered Messages with links to artifacts they established.
3. `GET /api/message/{id}/neighborhood` returns 1-hop artifacts (Decisions, Constraints, etc.) with confidence and pathway_strength.
4. `POST /api/fork-quest` creates a new SideQuest linked to the specified historical Message/Session via `FORKED_FROM`.
5. Quest Tree renders in the browser as an interactive tree with expand/collapse on Session nodes.
6. Fork button is visible on Session and Message nodes and successfully creates a new SideQuest when clicked.
7. All API endpoints have tests covering happy path and empty-state responses.

## Outcome

The Memory Control Panel becomes a navigable timeline of the user's decision history, not just a flat node list. Users can see the full quest tree, drill into any historical point, and fork new SideQuests from past context — making the graph's temporal structure directly explorable.
