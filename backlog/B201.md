# B201 — Durable Phase-State Machine: Named Solve Phases for ARC Orchestrator

- **State:** ready
- **Priority:** high
- **Plan:** backlog/plans/B-201-durable-phase-state-machine.md
- **Ecosystem Layer(s):** Agent Orchestration & Control Plane, Agent Runtime & Harness
- **Cross-layer violations:** None — phase enum and controller live in the Control Plane (runner.py); Agent Runtime (orchestrator.py, solver.py, hypothesis.py) reads phase but does not own transitions.

## Problem Statement

The ARC solver lacks an explicit, durable phase-state machine. The current runner stamps `current_phase` as bare strings (`"bootstrap"`, `"hypothesize"`, `"solve"`, `"act"`, `"ingest"`) inline without a formal enum, defined transition table, or gate conditions. This has three consequences:

1. **No enforced phase ordering.** The runner hardcodes a sequence, but nothing prevents an agent or future code from skipping phases or looping incorrectly. Phase transitions are implicit in control flow, not inspectable state.

2. **No firm completion gates between phases.** The solver gets stuck cycling actions (explore mode) because there is no durable gate that says "action exploration is done, advance to map modeling." Graduation scoring (`_graduation_assessment`) is a soft heuristic that doesn't enforce phase handoffs.

3. **No REPLAN escalation.** When the solver detects stalls (loops, zero-reward streaks), there is no mechanism to loop back to an earlier phase (world modeling or re-hypothesizing). It just keeps trying the same approach.

The system has phase-completion *signals* scattered across files (e.g. `initial_exploration_complete`, `positions_known`, `archetype_confidence`, `graduation_score`) but no single controller that consumes them to enforce transitions.

**Code audit note:** There are TWO parallel phase-stamping loops in `DurableARCRunner` — `_run_puzzle()` and `_run_puzzle_with_brain()` — both must be updated. The checkpoint system does NOT persist phase state. 13 read sites use `getattr(brain, "current_phase", "bootstrap")` for ledger/trace recording.

## What It Does (Scope)

Introduce a named, durable 7-phase state machine for ARC solving:

| Phase | Purpose | Replaces |
|---|---|---|
| **PERCEIVE** | Intake server info, seed API knowledge, initial observation | `"bootstrap"` (pre-perceive) |
| **MODEL** | Build world/map understanding — topology, edges, entity roles, spatial patterns | `"bootstrap"` (post-perceive, during plan) |
| **HYPOTHESIZE** | Infer game type (archetype), victory condition, strategy candidates | `"hypothesize"` |
| **ROUTE** | Select strategy/chunk, plan next action sequence | `"solve"` |
| **EXECUTE** | Take action, interact with environment | `"act"` |
| **EVALUATE** | Ingest result, score reward, check win/loss | `"ingest"` (no `"evaluate"` string exists today) |
| **REPLAN** | Detect loops/stalls, escalate, loop back to MODEL or HYPOTHESIZE | New (currently missing) |

**Note:** `finalization` is post-loop cleanup, not a solve phase. It remains a bare string.

### Key design decisions
- `SolvePhase` is a Python `Enum` in a new module `agents/arc3/phase.py`.
- `PhaseController` is a class in the same module that owns:
  - Current phase (durable, checkpointable via `to_checkpoint()`/`from_checkpoint()`)
  - Transition table (which phase can follow which)
  - Gate conditions (what signals must be true to advance)
  - Phase history log (for diagnostics and write traces)
- `DurableARCRunner` delegates phase stamping to `PhaseController` instead of inline string assignments.
- **Backward compat shim**: `brain.current_phase = phase_ctrl.phase_name` after each advance, so all 13 getattr read sites and `LedgerBrainClient._record()` keep working.
- Gate conditions reuse existing signals — no new LLM calls or expensive computation.
- Step budgets force-advance if gates don't open within N steps (prevents infinite waiting).

### Phase transition table

The per-step loop is **HYPOTHESIZE → ROUTE → EXECUTE → EVALUATE → HYPOTHESIZE** (not → ROUTE), reflecting the actual code where `hypothesize()` runs every step.

| From → To | Gate condition |
|---|---|
| PERCEIVE → MODEL | Initial observation received, API knowledge seeded |
| MODEL → HYPOTHESIZE | `initial_exploration_complete` OR step ≥ MODEL_BUDGET (force) |
| HYPOTHESIZE → ROUTE | `archetype_confidence ≥ 0.3` OR step ≥ HYPOTHESIS_BUDGET (force) |
| ROUTE → EXECUTE | Active chunk selected |
| EXECUTE → EVALUATE | Action submitted, frame response received |
| EVALUATE → HYPOTHESIZE | Normal loop continuation (not done, no stall) |
| EVALUATE → REPLAN | `loop_detected` OR `no_progress_steps ≥ 3` |
| REPLAN → MODEL | Exploration not complete — need more world understanding |
| REPLAN → HYPOTHESIZE | Archetype confidence < 0.3 — hypothesis was wrong |
| REPLAN → ROUTE | Just pick a new strategy |

### Not in scope
- Changing the prompt templates or LLM reasoning.
- Changing KuzuDB schema (phases are runtime state, not persistent graph facts).
- Changing the ARC API interface or harness.
- Performance tuning gate thresholds (that's a follow-up card).

## Dependencies

- None blocking. This card builds on existing code in `runner.py`, `orchestrator.py`, `solver.py`, `hypothesis.py`.
- Soft dependency on existing signals: `initial_exploration_complete`, `positions_known`, `archetype_confidence`, `victory_confidence`, `zero_reward_streak`, `loop_detected`.

## Files to Create/Modify

| File | Action |
|---|---|
| `agents/arc3/phase.py` | **Create** — `SolvePhase` enum, `PhaseController` class, transition table, gate conditions, checkpoint support |
| `agents/arc3/runner.py` | **Modify** — Replace inline `current_phase = "..."` in BOTH `_run_puzzle` AND `_run_puzzle_with_brain`, add REPLAN logic, add `_should_replan`/`_replan_target` helpers, update tool allowed_phases map |
| `agents/arc3/checkpoint.py` | **Modify** — Add optional `phase_state: dict` field to `TaskCheckpoint` |
| `agents/arc3/orchestrator.py` | **Modify** — Accept + read `PhaseController` for prompt mode selection |
| `agents/arc3/solver.py` | **Modify** — Add signal accessor methods (`is_exploration_complete`, `has_minimum_model`, `has_hypothesis`, `has_active_chunk`) |
| `docs/arc-harness-rules.md` | **Modify** — Update phase list, block-by-phase matrix, tool allowed_phases |
| `docs/ecosystem-rules.md` | **Modify** — Update phase references in layer mapping |
| `tests/test_phase_controller.py` | **Create** — 28 unit tests for phase transitions, gates, checkpoints, cycles |
| `tests/test_b111_ledger.py` | **Modify** — Update `"bootstrap"` assertion to `"perceive"` |
| `tests/test_b92_write_trace.py` | **Modify** — Update `"bootstrap"` assertions to `"perceive"` |

### Adapter/allow-list impact
- No new tools introduced.
- No tool-catalog changes needed.
- `benchmarks/arc3/adapter.py` — no code changes needed (backward compat shim handles it).
- Phase names in `allowed_phases` for tool contracts in `runner.py` and `arc-harness-rules.md` must be updated to new names. `finalization` stays as bare string in tool contracts.

## Acceptance Criteria

1. `SolvePhase` enum exists with exactly 7 members: PERCEIVE, MODEL, HYPOTHESIZE, ROUTE, EXECUTE, EVALUATE, REPLAN.
2. `PhaseController` enforces legal transitions — attempting an illegal transition raises `IllegalPhaseTransition`.
3. `PhaseController.advance()` checks gate conditions and only advances when gates are satisfied (or force-advance is used with logged warning).
4. **Both** `_run_puzzle` and `_run_puzzle_with_brain` use `PhaseController` — no bare string `current_phase` assignments remain.
5. Backward compat: `brain.current_phase` remains a string, all 13 getattr read sites and `LedgerBrainClient` keep working.
6. Phase state is checkpointable via `to_checkpoint()` / `from_checkpoint()`.
7. Phase history is logged and available in write traces / orchestration report.
8. REPLAN phase fires when `loop_detected` or `no_progress_steps >= 3`.
9. Step budgets force-advance from MODEL and HYPOTHESIZE if gates don't open.
10. A live smoke test (`run_single_puzzle.py --live-smoke --num-puzzles 1`) completes without error and shows phase transitions in output.
11. `pytest tests/test_phase_controller.py` passes with ≥25 test cases.
12. `pytest tests/test_b111_ledger.py tests/test_b92_write_trace.py` pass with updated phase assertions.
13. Existing tests in `tests/test_arc3_durable_runner.py` still pass.

## Outcome

The ARC solver has an explicit, inspectable, durable phase-state machine that prevents the solver from getting stuck in a single mode. Phase transitions are governed by measurable gate conditions with step-budget timeouts, making the orchestration deterministic and debuggable. The REPLAN phase provides the missing escalation path that today causes the solver to cycle actions endlessly.
