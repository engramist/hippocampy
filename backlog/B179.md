# B179: Victory Condition Stays "unknown" at 0.0 Confidence — Lower Inference Threshold

**Status**: ready
**Assigned**: TBD
**Depends on**: None
**Complexity**: Small (1-2 hours)
**Priority**: P2 (agent never infers what winning means, but other fixes may solve puzzles first)
**Plan**: backlog/plans/B-179-victory-condition-inference.md

## Problem Statement

`VictoryHypothesizer.CALL_THRESHOLD` at `solver.py:1009` is set to 0.65 — the victory condition inference LLM call only fires when `_archetype_confidence >= 0.65`. In the traced puzzle:

- Archetype was set to `SPATIAL_NAVIGATION` at confidence ~0.6 (below 0.65)
- Victory condition never upgraded from the default `None` → stayed "unknown" at 0.0 confidence
- The second trigger path (`should_replan and victory_condition.confidence < 0.5` at line 2403) also never fired because `should_replan` wasn't set (the dissonance disconnect from B177)

With no victory condition, the agent has no concept of what "winning" looks like. It can't assess whether it's making progress toward a goal state, and the finish phase of the pattern tracker has nothing to compare against.

## Root Causes

1. `CALL_THRESHOLD = 0.65` is too high — many puzzles have archetype confidence in the 0.5-0.65 range
2. The fallback path depends on `should_replan` which is broken (B177)
3. No time-based fallback: after N steps without a victory condition, the agent should attempt inference regardless of confidence

## What This Card Does

1. **Lower CALL_THRESHOLD to 0.45**: Match the archetype classifier's practical output range. Most spatial navigation puzzles land at 0.5-0.6 confidence.
2. **Add step-based fallback**: After 15 steps without a victory condition, attempt inference regardless of archetype confidence. The agent has enough observation data by then.
3. **Add zero-progress trigger**: If `_consecutive_no_progress_steps >= 5` and no victory condition exists, attempt inference. Being stuck is itself evidence that the agent doesn't understand the win state.

## Ecosystem Layer

- `agents/arc3/solver.py` (Agent Runtime — victory condition inference trigger)

**Cross-layer compliance**: No cross-layer violations.

## Files to Create/Modify

- **MODIFY** `agents/arc3/solver.py` — Lower `CALL_THRESHOLD`, add step-based and zero-progress fallback triggers at lines 2399-2407
- **CREATE** `tests/test_b179_victory_condition_inference.py` — Tests for lowered threshold, step-based trigger, zero-progress trigger
- No changes to `docs/tool-catalog.md`

## Acceptance Criteria

- [ ] `CALL_THRESHOLD` lowered from 0.65 to 0.45
- [ ] Victory condition inference fires when archetype confidence is in 0.45-0.65 range
- [ ] After 15 steps with no victory condition, inference fires regardless of archetype confidence
- [ ] After 5 consecutive zero-progress steps, inference fires if no victory condition exists
- [ ] Trace events log which trigger path activated the inference
- [ ] All existing ARC tests pass

## Outcome

The agent infers a victory condition within the first 15 steps instead of running 146 steps with "unknown" at 0.0 confidence. Even imprecise win-state knowledge (e.g., "reach the green region") is better than none — it gives the pattern tracker and autopilot a target to evaluate against.
