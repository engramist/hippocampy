# B207 — Break Plateau Lock Repetition After Replan Exhaustion

- **State:** ready
- **Priority:** P2
- **Plan:** backlog/plans/B-207-plateau-lock-exhaustion.md
- **Ecosystem Layer(s):** Agent Runtime
- **Cross-layer violations:** None — runtime chunk/plateau policy only.
- **Dependencies:** B176, B177

## Problem Statement

Plateau mode can repeatedly lock to the same action family when rewards are uniformly zero. Even after replans, the lock is re-applied, causing repeated no-op execution and preventing exploration recovery.

## What It Does (Scope)

Add lock-exhaustion logic to plateau policy so repeated replan cycles with the same locked family force an unlock.

Behavior:
- Track consecutive replan/zero-progress episodes while the same family is locked.
- After threshold (for example 3), clear lock and require fresh exploration chunk selection.
- Emit trace reason for forced unlock.

Out of scope:
- Rewriting action ranking model.
- New LLM calls.

## Files to Create/Modify

| File | Action |
|------|--------|
| `agents/arc3/solver.py` | **Modify** — add plateau exhaustion counters and forced unlock path |
| `tests/test_arc3_solver.py` | **Modify** — add test for forced unlock after repeated no-progress replans |

### Adapter/allow-list impact
- No tool changes.
- No `docs/tool-catalog.md` updates required.

## Acceptance Criteria

1. Same-family plateau lock does not persist indefinitely under repeated no-progress replans.
2. Forced unlock occurs after configured threshold and is traceable.
3. Next chunk after forced unlock is exploration-capable (not immediate re-lock to same family without new evidence).
4. New test validates the unlock path.
5. Existing plateau policy tests continue passing.

## Outcome Statement

The agent can escape deadlocked plateau loops and re-enter evidence-gathering behavior instead of repeating the same ineffective action family.
