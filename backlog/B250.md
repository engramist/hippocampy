# B250 - Tabular Ingestion Pipeline

State: complete -- independently re-verified 2026-08-04 against real Kuzu + real Ollama LLM (not mocks): all four previously-unbuilt criteria (LLM summary, key-fact extraction, DESCRIBED_BY_DATASET linkage + current_truth retrieval, change detection/archive-on-change) confirmed genuinely working end-to-end, including through the full B250->B251->B252 pipeline. See "Independent Re-Verification (2026-08-04)" below.
Priority: P1

Plan: backlog/plans/B-250-tabular-ingestion-pipeline.md

### Problem

When a user uploads a spreadsheet or CSV, Campy should not store it wholesale without understanding it, nor should it discard the tabular structure by chunking it into text. It needs to do both: preserve the full queryable data AND extract knowledge into the graph.

### What This Card Does

Extends the document ingestion pipeline to handle tabular files (CSV, XLSX, TSV) with intelligent two-layer storage: full data in SQLite and extracted knowledge (summary, schema classification, key facts) in the Kuzu graph.

### Dependencies

- B249 complete (Dataset node + tabular store)

### Design Decisions

| Decision | Choice | Rationale |
|---|---|---|
| Parsing library | pandas (already a dependency via data science stack) | Handles CSV, XLSX, TSV; familiar API |
| Classification | Reuse gist/schema.org pipeline on column headers + sample rows | Consistent with existing Campy classification |
| Key fact extraction | LLM summarizes dataset + extracts notable values | Totals, constraints, outliers become graph Concepts |
| Change detection | SHA256 hash of file (same as `ingest_document`) | Idempotent re-upload |

### Files to Create/Modify

| File | Action |
|---|---|
| `mcp_engine/tabular_ingest.py` | CREATE - tabular ingestion logic |
| `mcp_engine/ingest.py` | MODIFY - add tabular extensions to ALLOWED_EXTENSIONS, dispatch to tabular path |
| `mcp_engine/tabular_store.py` | MODIFY - may need helpers for ingestion |
| `tests/test_tabular_ingest.py` | CREATE - ingestion tests with sample CSV/XLSX fixtures |
| `tests/fixtures/` | CREATE - sample tabular test files |

### Acceptance Criteria

- [x] Uploading a CSV creates a Dataset node with correct schema_json, row_count, column_count.
- [x] Full tabular data is queryable via `query_table()`.
- [x] LLM summary is stored on Dataset node description. — real LLM call (`create_llm_client_for_step(config, "tabular_ingest")`); falls back to the old template on any failure or missing LLM.
- [x] Key facts (totals, notable values) are extracted as Concept nodes linked via `DESCRIBED_BY_DATASET`.
- [x] Extracted Concepts participate in normal `current_truth` retrieval. — same minimal-Concept shape (embedding, `archived=false`) as `orchestrator.py::_ensure_concept_exists`; verified structurally retrievable via the same cosine-similarity query shape `current_truth`/`bundle_compiler` use.
- [x] Re-uploading same file (unchanged hash) returns `already_current: true`.
- [x] Re-uploading changed file archives old Dataset node and creates new one.
- [x] XLSX files with multiple sheets are handled (one Dataset per sheet or configurable).
- [x] Full test suite passes.

### Validation Commands

```bash
.venv/bin/pytest -q tests/test_tabular_ingest.py tests/test_tabular_store.py
.venv/bin/pytest -q
```

### Audit Findings (2026-08-03 re-verification, historical — all four gaps are now closed, see Implementation below)

The existing test suite for this card (`tests/test_tabular_ingest.py`) is **100% mock-based** — every test uses `mock_db = AsyncMock()` with `db.execute = MagicMock(return_value=None)`. None of it exercises a real KuzuDB. Per the audit brief, that is not sufficient evidence a criterion is met, so I ran a real end-to-end smoke test: a real `KuzuClient` against a throwaway on-disk DB, a real `Dataset` node table, and `ingest_tabular()` called on an actual 3-row CSV.

**Bug found and fixed (trivial, isolated, no test regressions):** the initial real run failed with `Binder exception: Expression $created_at has data type STRING but expected TIMESTAMP. Implicit cast is not supported.` — `_ingest_single_dataset()` in `campy/brain/sensory_cortex/tabular_ingest.py` builds the `CREATE (d:Dataset {...})` Cypher by interpolating every field as `key: $key`, including the two TIMESTAMP-typed columns (`created_at`, `last_accessed_at`), which are passed as ISO-format strings. Kuzu 0.11.3 does not implicitly cast STRING → TIMESTAMP (this is the exact same class of issue `ingest.py` already works around for Document nodes by wrapping with `timestamp($created_at)` — see `campy/brain/sensory_cortex/ingest.py` line ~395). Because every existing test mocks `db.execute`, this Binder exception was never triggered and every real (non-mocked) tabular ingestion into Kuzu was silently failing at the Dataset-node-creation step — the `except Exception` swallows it into `{"error": ...}`, which the caller in `ingest_tabular()` just skips over without surfacing.

  Fix applied: wrapped `created_at`/`last_accessed_at` in `timestamp(...)` when building the Cypher property list (same file, `_ingest_single_dataset`). Verified the fix resolves the crash and re-ran the full suite with no regressions:
  ```
  .venv/bin/python -m pytest -q tests/test_tabular_ingest.py tests/test_tabular_store.py   # 30 passed
  .venv/bin/python -m pytest -q                                                             # 1600 passed, 59 skipped, 2 xfailed
  ```

**Real gaps found via the same real-DB smoke test, still unresolved (not fixed — these need real implementation work, not a one-liner):**

- **"LLM summary is stored on Dataset node description" — not implemented.** `_ingest_single_dataset()` sets `description` to a hardcoded f-string template (`f"Dataset from {file_path.name} sheet '{sheet_name}' with {row_count} rows and {col_count} columns"`), not an LLM call. The code comment admits this directly: `# Generate a description (simplified - LLM generation would require async LLM call)`.
- **"Key facts (totals, notable values) are extracted as Concept nodes linked via DESCRIBED_BY_DATASET" — not implemented at all.** `facts_extracted` is hardcoded to `0` with the comment `# Could extract facts from data summary`. Confirmed via the smoke test: after ingesting a real CSV, `MATCH (c:Concept) RETURN count(c)` → `0`, `MATCH ()-[r:DESCRIBED_BY_DATASET]->() RETURN count(r)` → `0`. The `DESCRIBED_BY_DATASET` edge type exists in the schema and is *read* by `bundle_compiler.py` (B252), but nothing anywhere in the codebase ever *creates* it.
- **"Extracted Concepts participate in normal current_truth retrieval" — cannot be true**, since no Concepts are ever created by this pipeline (follows directly from the previous point).
- **"Re-uploading same file (unchanged hash) returns already_current: true" — not implemented.** `ingest_tabular()` hardcodes `results["already_current"] = False` unconditionally. Confirmed via smoke test: ingesting the identical CSV twice returns `already_current: False` both times, with two distinct new `dataset_id`s and two separate Dataset nodes left in the graph (`MATCH (d:Dataset) RETURN count(d)` → `2` after two ingests of the same unchanged file). The existing test `test_change_detection_same_hash` doesn't actually assert `already_current is True` — its own docstring/comment admits "hash check is not yet fully implemented."
- **"Re-uploading changed file archives old Dataset node and creates new one" — not implemented.** Follows from the above: there is no lookup-by-`content_hash` or archiving logic at all; every call to `ingest_tabular()` unconditionally `CREATE`s a brand-new Dataset node with a fresh UUID.

**Criteria confirmed working via the real-DB smoke test:**
- Uploading a CSV creates a Dataset node with correct `schema_json`, `row_count`, `column_count` (verified with real values: 3 rows, 3 columns, schema listing all 3 columns with dtypes).
- Full tabular data is queryable via `query_table()` (round-tripped the same 3 rows back out through the real SQLite file).
- XLSX multi-sheet handling (`per_sheet` vs `first_only` strategy) is implemented and covered by real pandas/openpyxl-backed tests (`tests/test_tabular_ingest.py::TestXLSXIngestion`, 3/3 passed with pandas actually installed — previously silently skipped, same pyproject.toml gap noted on B249).

Net: roughly half of this card's acceptance criteria are genuinely met; the other half (LLM summarization, fact extraction, change detection, archiving) were never implemented despite being explicit, checked-off-sounding criteria. Reopening as needs-work rather than fixing in place, per audit instructions, since these require real design/implementation work (LLM call wiring, fact-extraction heuristics, hash-based dedup/archive flow), not a trivial patch.

### Implementation (2026-08-03)

All four unresolved gaps implemented in `campy/brain/sensory_cortex/tabular_ingest.py`, following the graceful-degradation and node-creation conventions already established elsewhere in the codebase rather than inventing new ones:

1. **LLM summary** — `_generate_description()` calls `create_llm_client_for_step(config, "tabular_ingest")` (the existing B16 per-step-override factory; `"tabular_ingest"` added to `provider.py`'s documented step names) with a prompt built from column names/dtypes, row count, and a 3-row sample. On any failure or missing LLM client, falls back to the original hardcoded template — same graceful-degradation contract as `loop/step7_5_lesson.py::extract_lessons`.
2. **Key fact extraction** — `_extract_and_link_key_facts()` prompts the LLM for a JSON list of `{"text": "..."}` facts (totals/constraints/patterns), then creates one minimal `Concept` node per fact (same shape as `orchestrator.py::_ensure_concept_exists` — real embedding, `archived=false`, `confidence=0.75`) plus a `DESCRIBED_BY_DATASET` edge (`extraction_method: 'llm'`) to the Dataset. Malformed/non-JSON responses or a missing LLM client return `0` facts without raising, matching `extract_lessons`'s convention.
3. **Extracted Concepts participate in `current_truth` retrieval** — follows directly from (2): the fact Concepts use the exact same properties (`embedding`, `archived=false`, real `Concept` label) that `current_truth`/`bundle_compiler`'s cosine-similarity queries already select on. Verified directly with the same query shape those callers use, rather than standing up the full `current_truth` quest-resolution stack for a unit test.
4. **Change detection + archive-on-change** — added `Dataset.source_key` (new schema migration, `campy/brain/hippocampus/schema.py`'s `_MIGRATIONS` list), a deterministic `sha256(file_path::sheet_name)` identity mirroring `ingest.py`'s `_stable_doc_id` for Document nodes. `_find_active_dataset()` looks up the most recent non-archived Dataset by `source_key`: same `content_hash` → returns `already_current: true` and skips all reprocessing (no SQLite write, no LLM calls); different hash → `_archive_dataset()` soft-deletes the old node (`archived=true`, SQLite table and any fact Concepts left in place — "archive, don't destroy," same as `ingest.py`'s `_archive_old_extracts`) before creating the replacement with a fresh `dataset_id`. Content-based ingestion (`ingest_tabular_from_content`, from B251) always writes to a fresh random temp path, so pasted content naturally never collides with a prior dataset here — this only fires for genuine same-path re-uploads, by design.
   - Multi-sheet XLSX: the whole-file `already_current` flag is `true` only if *every* sheet was unchanged - a file where even one sheet changed still did real work and is reported as such.
   - `source_key` is scoped per-file-path — two different files never collide on change detection even if uploaded in the same run (explicit regression test).

**Verification:** TDD throughout, real Kuzu DB (not `db.execute` mocks) in `tests/test_tabular_ingest_facts_and_dedup.py` (11 new tests): LLM-summary success/fallback/exception paths, fact-extraction success/structural-retrievability/no-LLM/malformed-response paths, unchanged re-upload → `already_current`, changed re-upload → archive-and-recreate, old fact Concepts surviving archiving, and two different files not colliding. Beyond the test suite, ran one additional real end-to-end smoke test against a **real local LLM** (Ollama, the confirmed default per the B304-B307 eval) through the full production schema (`init_schema`): ingested a real CSV, got a genuinely contextual 2-sentence summary and 3 extracted facts as real Concept nodes, re-ingested unchanged (correctly returned `already_current: true`, same `dataset_id`), then re-ingested with edited content (correctly archived the old Dataset and created a new one with an updated summary and row count) — this is the same style of real, non-mocked verification the original audit used to find the crash bug, now confirming the fix end-to-end rather than just against synthetic test doubles.

Existing `tests/test_tabular_ingest.py` (mocked, 13 tests) and `tests/test_tabular_store.py` pass unchanged — the mock's `db.execute = MagicMock(return_value=None)` makes `_find_active_dataset()`'s lookup raise, which is caught and treated as "no existing dataset" (same as `ingest.py`'s `_get_existing_hash` convention), so mocked tests keep exercising the "always new" path exactly as before.

Full suite: `.venv/bin/python -m pytest -q` → 1681 passed, 31 skipped, 2 xfailed (was 1670 after B251; +11 new tests here, no regressions).

### Independent Re-Verification (2026-08-04)

The 2026-08-03 "Implementation" section above was written and merged in the same session as the fix, without independent verification — commit `901d83b`. Per this session's audit brief ("a commit claiming a fix does NOT mean the fix is real"), everything above was re-derived from scratch against a real on-disk KuzuDB (full production schema via `init_schema`, not a hand-rolled `CREATE NODE TABLE` fixture) and, where the criterion depends on it, a real local LLM (Ollama `llama3.1:8b`, the confirmed default per B304-B307) — no `MockDB`, no `db.execute` mocking, anywhere in this pass.

- **LLM summary — confirmed real.** Ingested a real 5-row CSV through `ingest_tabular()`; `_generate_description()` returned a genuinely contextual, non-templated sentence ("This dataset appears to represent employee information, including their department and salary history...") from the actual Ollama call, not the fallback template.
- **Key fact extraction + `DESCRIBED_BY_DATASET` — confirmed real, not just structurally plausible.** The same real ingest produced 3 LLM-extracted facts as real `Concept` nodes. Queried the graph directly: `MATCH ()-[r:DESCRIBED_BY_DATASET]->() RETURN count(r)` → `3` (previously, before this fix, this was always `0` per the original audit). `_GRAPH_REL_TYPES`/edge shape checked directly against `campy/brain/hippocampus/schema.py` — matches.
- **Extracted Concepts participate in `current_truth` retrieval — confirmed with an actual `current_truth()` call, not just "same query shape."** The 2026-08-03 Implementation note only claimed structural equivalence; this pass ran the real `current_truth()` handler (`campy/brain/thalamus/tools/retrieval.py`) against a query matching one of the extracted facts verbatim and got it back as the top result with `similarity: 0.9999...`, alongside the other two extracted facts ranked below it. This criterion is now verified functionally, not just by code inspection.
- **Change detection + archive-on-change (file-path re-uploads) — confirmed real** with a dedicated real-Kuzu script: ingest → `already_current: False`, new `dataset_id`; re-ingest unchanged → `already_current: True`, same `dataset_id`, `MATCH (d:Dataset) RETURN count(d)` stays at `1`; re-ingest with edited content → `already_current: False`, new `dataset_id`, old node correctly flipped to `archived: true` (1 archived + 1 active `Dataset` node afterward, verified by direct Cypher count).
- **Caveat, not a bug (already documented in the Implementation section, reconfirmed here):** `ingest_tabular_from_content()` (the B251 content-paste path) always writes to a fresh random temp path, so re-pasting identical content never triggers `already_current` — dedup only fires for genuine same-file-path re-uploads. This is a real, intentional scope boundary, not a broken promise: the acceptance criterion's wording ("Re-uploading same file...") is about file uploads, which do work.
- **End-to-end pipeline (B250 -> B251 -> B252), the strongest evidence available:** ran `ingest_data({"content": <csv>})` (B251's raw-content router) -> tabular pipeline (B250) -> `compile_context()` (B252) in one real-Kuzu session. The `DESCRIBED_BY_DATASET` edges B250 now creates were correctly picked up by B252's previously-stubbed `_stage_tabular_data`, and the fact `Concept`s were surfaced by `_stage_semantic_context` — full chain confirmed working, not just each half in isolation. See B252's card for the full bundle output.
- Full suite (this repo's actual current HEAD, with `pandas`/`psutil` installed to unskip the tabular tests that were silently skipped in the environment): `.venv/bin/python -m pytest -q` → **1700 passed, 59 skipped, 2 xfailed, 0 failed**. `tests/test_tabular_ingest_facts_and_dedup.py`'s 11 real-Kuzu tests specifically: all pass.
- Not independently re-checked in this pass (unchanged from the original audit, no reason to suspect regression): XLSX multi-sheet handling, `query_table()` round-trip. Both still pass in the full suite.

**Environment note (not a card defect, flagging for whoever runs this next):** `pandas`/`openpyxl` are not in `pyproject.toml` as dependencies anywhere (confirmed by grep) — a fresh venv silently skips every tabular test rather than failing loudly. This was already flagged on B249; still true, still not fixed, out of scope for this card.

### Outcome

Users can upload spreadsheets and CSVs to Campy. The data is preserved for querying AND understood by the graph for retrieval.
