# B84 - Remove or Use Session content_embedding Dead Data

State: complete
Priority: LOW
Review Finding: R2-S2
Plan: backlog/plans/B-84-content-embedding-dead-data.md

### B84 · Remove or Use Session content_embedding Dead Data

**Problem:** `Session.content_embedding` (`FLOAT[384]`) is written during hippocampus routing and in `notify_turn`, but no query ever reads it. Dead data accumulating in every Session node.

**What it does:** Either implements session-scoped similarity search using `content_embedding`, or removes the column and writes.

**Depends on:** None

**Files to modify:**
- `mcp_engine/schema.py` — remove column if unused, OR
- `mcp_engine/tools/__init__.py` — add session similarity search using content_embedding
- `mcp_engine/hippocampus.py` — remove writes if column is removed

**Adapter allow-list propagation check:** No tool changes — N/A.

**Acceptance Criteria:**
- Either: `content_embedding` is read by at least one query (with a use case), OR
- `content_embedding` column, writes, and related code are removed
- `pytest -q` passes

**Outcome:** No dead data accumulating in the graph.
