# B174: Persist ChunkLedgerEntry to KuzuDB via Plan/PlanStep Nodes

**Status**: ready
**Assigned**: TBD
**Depends on**: None (Plan/PlanStep nodes already exist and have writers)
**Complexity**: Small (1-2 hours)
**Priority**: P2 (chunk execution history has no persistence — limits post-solve analysis)
**Plan**: backlog/plans/B-174-chunk-ledger-kuzu.md

## Problem Statement

`SolveEngine._chunk_ledger` (`solver.py:1757`) stores a `List[ChunkLedgerEntry]` tracking the execution history of plan chunks — which chunks ran, how many steps they consumed, what graduation score they achieved, whether they were replaced by dissonance.

This data lives only in memory. After a solve completes, the chunk execution history is lost. This limits:
- Post-solve analysis of what worked vs didn't
- Cross-puzzle learning about which chunk strategies succeed
- Debugging graduation/dissonance issues (B142)

## Root Causes

1. `ChunkLedgerEntry` (`solver.py:104-109`) was built as ephemeral per-solve tracking
2. `Plan` and `PlanStep` nodes exist in KuzuDB with active writers, but chunk execution metadata (graduation score, step count, dissonance trigger) is not captured

## What This Card Does

1. **Extend `PlanStep` or add `ChunkExecution` node type**: Capture `chunk_family`, `steps_consumed`, `graduation_score_final`, `evidence_at_end`, `dissonance_triggered`, `replaced_by`, `created_at`
2. **Persist to KuzuDB**: When a chunk completes (graduation, dissonance, or level end), write execution record
3. **Link to Plan**: `EXECUTED_AS` relationship from `Plan → ChunkExecution`
4. **Load for analysis**: Enable querying chunk history for post-solve reports

## Ecosystem Layer

- `agents/arc3/solver.py` (Agent Runtime — chunk ledger writer)
- `mcp_engine/schema.py` (Data Products — new node type or PlanStep extension)

**Cross-layer compliance**: No cross-layer violations.

## Files to Create/Modify

- **MODIFY** `mcp_engine/schema.py` — Add `ChunkExecution` node type and `EXECUTED_AS` relationship
- **MODIFY** `agents/arc3/solver.py` — Persist ChunkLedgerEntry to KuzuDB on chunk completion
- **CREATE** `tests/test_b174_chunk_ledger_persistence.py` — Tests for persistence and relationship creation
- No changes to `docs/tool-catalog.md`

## Acceptance Criteria

- [ ] `ChunkExecution` node type (or PlanStep extension) exists in schema
- [ ] Completing a chunk writes execution record to KuzuDB
- [ ] Record includes graduation score, steps consumed, evidence, dissonance trigger
- [ ] `EXECUTED_AS` edges link chunk executions to their parent Plan
- [ ] When KuzuDB unavailable, falls back to in-memory-only
- [ ] All existing ARC tests pass

## Outcome

Chunk execution history is persisted. Post-solve analysis can query "which chunk strategies led to progress?" across puzzles.
