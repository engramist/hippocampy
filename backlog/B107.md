# B107 - ARC Meta-Harness Proposer Loop

State: complete

Plan: backlog/plans/B-107-arc-meta-harness-proposer-loop.md

### Problem

Once the experience store and candidate runner exist, we still need the proposer loop that
generates harness edits, selects what to evaluate next, and learns from prior results.

### What This Card Builds

The first ARC meta-harness proposer loop:

- read prior candidate lineage, failures, and score summaries from SideQuests
- propose bounded harness mutations
- hand candidates to the external evaluation loop
- retrieve compact comparisons from prior runs instead of raw trace dumps
- keep humans in the architecture role, not the per-iteration harness-tuning role

### Depends on

- B104
- B105
- B106

## Files to Create/Modify

- `benchmarks/arc3/README.md`
- `docs/ARCHITECTURE.md`
- `backlog/masterBacklogTracker.md`

## Acceptance Criteria

1. The proposer loop is documented as a separate outer-loop role from the ARC harness.
2. The loop defines:
   - candidate proposal
   - candidate evaluation
   - result storage
   - retrieval from prior runs
   - next-candidate selection
3. The design keeps SideQuests as the memory backend, not the harness itself.
4. The docs define at least one bounded search policy for the proposer.
5. The proposer loop stays compatible with external coding executors such as Gemini or Haiku.

## Outcome

SideQuests and ARC move from a manually tuned harness toward an actual meta-harness system that can
iteratively improve the ARC harness.

## Validation Note (2026-03-31)

- Implemented files: `docs/ARCHITECTURE.md`, `benchmarks/arc3/README.md`
- Tests run:
  - `.venv/bin/pytest -q tests/test_arc3_orchestrator.py tests/test_arc3_hypothesis.py tests/test_arc3_action_fact_promotion.py tests/test_arc3_meta_harness_query.py tests/test_arc3_durable_runner.py`
- Result: `113 passed, 1 warning`
- Regression checks: proposer loop docs now define retrieve/propose/evaluate/store/select plus a bounded search policy for coding-agent executors
