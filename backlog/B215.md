# B215 — Plateau Lock Must Not Latch on Unexplored Action Space

- **State:** ready
- **Priority:** P0 (blocking — agent committed to ACTION6 at step 5 with 1 action explored; looped for 15 steps)
- **Plan:** backlog/plans/B-215-plateau-lock-minimum-exploration.md
- **Ecosystem Layer(s):** Agent Runtime
- **Cross-layer violations:** None.
- **Dependencies:** B207.

## Problem Statement

In the smoke run, the agent explored exactly one action (ACTION6) and immediately entered "Plateau Exploitation: commit to top-ranked ACTION6" at step 5. It then repeated this choice for steps 5–20 with an identical board state on every step (same color counts, same 1-pixel change at col 58). The plateau lock never released because ACTION6 remained top-ranked — there was no competition, since no other actions were ever tried.

B207 added forced unlock after N replan cycles, but the root problem is earlier: the plateau lock engages before sufficient exploration has occurred. An action should not be plateau-locked when it is the only action that has been tried.

Observed from `master_timeline.json`:
- Step 4: first ACTION6 attempt ("Explore: try unexplored action")
- Step 5: immediately "Plateau Exploitation: commit to top-ranked ACTION6"
- Steps 5–20: identical route decision, identical board state, zero progress

## What It Does (Scope)

Add a minimum-exploration gate to the plateau lock entry condition in `solver.py`:
- Plateau lock may only engage after at least N distinct action families have been tried (proposed default: N=3, or all available actions if fewer than 3 exist).
- If the exploration gate is not met, route falls through to exploration chunk selection regardless of plateau score.
- Emit a trace reason when plateau lock is deferred due to insufficient exploration.

Out of scope:
- Changing plateau lock release/threshold logic (B207).
- Action ranking model changes.

## Files to Create/Modify

| File | Action |
|------|--------|
| `agents/arc3/solver.py` | **Modify** — add `_min_explored_before_plateau` gate at plateau lock entry (around line 2869) |
| `tests/test_arc3_solver.py` | **Modify** — add test: plateau lock does not engage when fewer than N actions have been explored |

### Adapter/allow-list impact
- No tool changes.
- No `docs/tool-catalog.md` updates required.

## Acceptance Criteria

1. Plateau lock does not engage when only 1 distinct action family has been tried.
2. Plateau lock engages normally when ≥ N action families have been observed.
3. Trace emits a "plateau deferred: insufficient exploration (X of N actions tried)" reason when gate prevents lock.
4. Existing plateau lock tests continue passing.
5. In a smoke run, the router does not reach "Plateau Exploitation" before at least 3 distinct actions appear in the ledger.

## Outcome Statement

The agent is forced to build a minimum evidence base before committing to a single action. Plateau exploitation becomes a convergence signal rather than a premature trap triggered by the first action tried.
