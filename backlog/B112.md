# B112 - Enforce Chunk Policy Only for Live, Valid Guidance Chunks

State: complete
Completed: 2026-04-01
Validation: 73/73 tests passed (test_b112_enforcement_gate.py + core solver/orchestrator suites)

Plan: backlog/plans/B-112-arc-valid-chunk-enforcement-gate.md

### Problem

The latest live ARC smoke run exposed a new enforcement bug:

- chunk policy override is firing on chunks whose description/source is still exploratory
- the exported `active_chunk.estimated_actions` is already empty
- the rationale still says `policy override: enforcing directional chunk ...`

That means the current enforcement gate is too loose. It is treating any active chunk as hard
guidance, even when:

- the chunk source is not actually directional (or otherwise intended to constrain action choice)
- the chunk has no remaining valid actions in the current state
- the chunk is effectively exhausted and should be abandoned instead of enforced

### What This Card Builds

A stricter chunk-enforcement gate for the ARC harness:

- only enforce chunk policy when the chunk source is genuinely guidance-producing
- only enforce when the chunk still has non-empty valid actions for the current state
- never override from an exhausted explore chunk
- keep enforcement rationale accurate about what kind of chunk is being applied

### Depends on

- B109

## Files to Create/Modify

- `agents/arc3/orchestrator.py`
- `agents/arc3/solver.py`
- `tests/test_arc3_orchestrator.py`
- `tests/test_arc3_solver.py`

## Acceptance Criteria

1. Chunk policy is enforced only when:
   - the chunk source is `directional` or another explicitly guidance-grade source
   - and the chunk has at least one valid remaining action in `available_actions`
2. Exhausted explore chunks do not trigger policy overrides.
3. Override rationale reflects the actual chunk source instead of always saying `directional chunk`.
4. If a chunk has no valid remaining actions in the current state, it is treated as stale and not
   enforced.
5. Tests cover:
   - valid directional chunk enforcement
   - no enforcement for exhausted explore chunks
   - no enforcement for chunks with no valid actions
   - accurate rationale text for enforced chunk types
6. A live smoke run no longer shows `policy override` being applied from an empty explore chunk.

## Outcome

Chunk enforcement becomes trustworthy: only real, still-usable guidance chunks can constrain action
choice, while stale or exploratory chunks are allowed to expire without hijacking policy.
