# B105 - ARC Meta-Harness Query Surface for Candidate Comparison

State: complete

Plan: backlog/plans/B-105-arc-meta-harness-query-surface.md

### Problem

Even with a structured experience store, the proposer still needs a fast way to inspect what
worked, what failed, and how two harness candidates differ. The Meta-Harness paper explicitly notes
that a small CLI over results is helpful because raw history gets cumbersome.

### What This Card Builds

A small, focused query surface for meta-harness work:

- list top ARC harness candidates by score/budget tradeoff
- compare two harness candidates by diff, metrics, and failure clusters
- retrieve failed runs by puzzle family, failure mode, or budget regression
- expose the results through a small CLI and/or internal helper layer tuned for coding-agent use

### Depends on

- B104

## Files to Create/Modify

- `benchmarks/arc3/model_eval.py`
- `benchmarks/arc3/README.md`
- `tools/graph_viewer/README.md`
- `backlog/masterBacklogTracker.md`

## Acceptance Criteria

1. There is a documented query surface for candidate comparison that is smaller and more structured
   than raw file navigation.
2. The query surface supports at least:
   - top candidates
   - pairwise candidate comparison
   - runs by failure mode
   - runs by runtime/token regression
3. The docs show example queries/commands for a coding agent proposer.
4. The output is compact and comparison-oriented rather than dumping full traces by default.
5. The design keeps the graph viewer optional and read-only.

## Outcome

The future meta-harness proposer has a small, queryable interface for learning from prior harness
runs without brute-force log scraping.

## Validation Note (2026-03-31)

- Implemented files: `benchmarks/arc3/model_eval.py`, `benchmarks/arc3/README.md`, `tools/graph_viewer/README.md`, `tests/test_arc3_meta_harness_query.py`
- Tests run:
  - `.venv/bin/pytest -q tests/test_arc3_meta_harness_query.py tests/test_arc3_orchestrator.py tests/test_arc3_hypothesis.py`
- Result: `92 passed, 1 warning`
- Regression checks: added compact candidate-comparison helpers without affecting existing ARC hypothesis/orchestrator behavior
