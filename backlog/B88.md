# B88 - Hypothesis Engine: Game-Theory-Driven Puzzle Solving

State: complete

### B88 · Hypothesis Engine: Game-Theory-Driven Puzzle Solving

**Problem:** The current ARC orchestrator jumps straight from Perceive → Plan → Act with no explicit hypothesis formation. The agent doesn't model the game environment, discover invariants, or reason about what actions *do*. `_draft_plan_steps()` is a static 3-step template. The energy bar heuristic (`_estimate_energy`) hardcodes rows 60-62, which is LockSmith-specific — other ARC-AGI-3 games have different or no HUD layouts. Without a hypothesis layer, the agent's "plans" are guesses, not systematic exploration informed by game theory.

**What this card builds:** A **Hypothesis Engine** that sits between Perceive and Plan in the cognitive loop, applying Combinatorial Game Theory principles. The agent now:

1. **Forms hypotheses** about the game's rules, HUD layout, and action semantics from grid observations
2. **Tracks state transitions** in an ephemeral in-memory directed graph (the game tree)
3. **Detects invariants** — pixels/regions that don't change across actions (walls, HUD, decorations)
4. **Updates hypothesis confidence** using Bayesian-style evidence accumulation
5. **Distills durable knowledge** from the ephemeral graph into SideQuests (Kùzu) on WIN/GAME_OVER boundaries
6. **Balances explore vs. exploit** using energy-aware strategy selection

**Cognitive architecture alignment (Baddeley model):**

| Component | Role | Storage | Lifespan |
|-----------|------|---------|----------|
| State Transition Graph | Visuospatial sketchpad | In-memory (Python dict) | Single puzzle attempt |
| Hypothesis tracking | Episodic buffer | In-memory + flush to Kùzu | Attempt → durable on boundary |
| Confirmed rules / Lessons | Long-term memory | Kùzu (SideQuests) | Cross-puzzle, cross-session |
| Working Memory (B18) | Central executive | Kùzu (LOADED edges) | Session → handoff |

**Game theory foundations (Combinatorial Game Theory):**

| Concept | Game Theory Term | Implementation |
|---------|-----------------|----------------|
| The grid state | Node in directed graph | StateNode in in-memory graph |
| A legal action | Edge in directed graph | Transition edge with action label |
| "What does ACTION3 do?" | Hypothesis about transition function | Hypothesis object with confidence |
| "Row 61 never changes" | Invariant detection | InvariantDetector scanning across frames |
| "Don't try that again" | Branch pruning / Amygdala Reflex | Existing OUTCOME_SIGNAL + new hypothesis valence |
| "Energy is low, stop exploring" | Explore/exploit tradeoff | Energy-aware policy in HypothesisManager |

**New loop:** Perceive → **Hypothesize** → Plan → Act → Evaluate

**Depends on:** B87 (orchestrator ✅), B67 (register_plan ✅), B69 (valence ✅), B18 (working memory ✅)

---

## Files to Create

### `agents/arc3/hypothesis.py` (new)

Core module. Contains:

- **`StateNode`** — dataclass: `grid_hash`, `step`, `key_features` (color counts, shape summary), `energy_estimate`
- **`Transition`** — dataclass: `from_hash`, `to_hash`, `action`, `step`, `observed_effects` (diff summary)
- **`Hypothesis`** — dataclass: `id`, `description`, `category` (action_semantic | hud_element | rule | invariant), `confidence` (0.0–1.0), `support_count`, `contradiction_count`, `status` (active | confirmed | refuted | pruned)
- **`StateGraph`** — in-memory directed graph (dict-of-dicts): add_state(), add_transition(), detect_loops(), get_path(), get_unexplored_actions()
- **`InvariantDetector`** — compares grid regions across N frames: finds rows/cols/regions that never change (walls, HUD, decorations) vs. always change (player, dynamic objects)
- **`HypothesisManager`** — orchestrates everything: generate_hypotheses(), update_confidence(), get_best_hypothesis(), get_exploration_action(), distill_to_brain(), energy_policy()

### `tests/test_arc3_hypothesis.py` (new)

~15 tests covering StateGraph, InvariantDetector, HypothesisManager, confidence updates, distillation.

## Files to Modify

### `agents/arc3/orchestrator.py`

- Add `hypothesize()` method between `perceive()` and `plan()`
- Wire `HypothesisManager` into constructor
- `_draft_plan_steps()` consumes hypothesis context instead of static template
- `build_action_prompt()` includes HYPOTHESIS section with active hypotheses + confidence
- Replace hardcoded `_estimate_energy()` reference with hypothesis-driven HUD detection
- `reset_for_retry()` resets the StateGraph but preserves refuted hypotheses

### `benchmarks/arc3/adapter.py`

- Remove hardcoded `_estimate_energy()` method (replaced by InvariantDetector + hypothesis)
- Keep `energy_estimate` field in observation but populate from HypothesisManager output

### `agents/arc3/api_knowledge.py`

- Replace LockSmith-specific energy chunk (#9) with generic hypothesis-driven HUD discovery chunk

### `benchmarks/arc3/schema.py`

- Add `frame_hash: str` and `invariant_regions: list` to ARC3Observation TypedDict

## Kùzu Schema Additions

New node table for durable hypothesis storage:

```
Hypothesis {
    id STRING PRIMARY KEY,
    description STRING,
    category STRING,        -- action_semantic | hud_element | rule | invariant
    confidence FLOAT,
    game_type STRING,       -- "LockSmith", "unknown", etc.
    task_id STRING,
    status STRING,          -- confirmed | refuted
    evidence_count INT32,
    text_raw STRING,        -- for embedding + vector search
    embedding FLOAT[384]
}
```

New relationship tables:
```
HYPOTHESIZED_IN: Hypothesis → Session
CONFIRMS: Observation → Hypothesis (with weight FLOAT)
CONTRADICTS: Observation → Hypothesis (with weight FLOAT)
GENERALIZES: Hypothesis → Hypothesis (cross-game rule transfer)
PRODUCED_HYPOTHESIS: Plan → Hypothesis
```

These allow `analogical_search` and `recall_relevant_lessons` to surface confirmed hypotheses from past games — e.g., "In a previous game, INT<10> was always a wall."

## Acceptance Criteria

1. `StateGraph` correctly builds directed graph from frame sequence, detects loops (revisiting a grid_hash)
2. `InvariantDetector` identifies static regions (walls/HUD) vs. dynamic regions from ≥3 frames
3. `HypothesisManager.generate_hypotheses()` produces ≥1 hypothesis per action taken (action semantics)
4. Confidence updates: supporting observation increases confidence, contradiction decreases it
5. Hypotheses with confidence < 0.2 are auto-pruned; confidence > 0.8 are marked confirmed
6. `distill_to_brain()` writes confirmed/refuted hypotheses to Kùzu as Hypothesis nodes with embeddings
7. `analogical_search` returns relevant confirmed hypotheses from prior games
8. `orchestrator.hypothesize()` is called between perceive() and plan() and its output shapes the plan
9. Energy detection works without hardcoded row numbers — InvariantDetector finds the HUD dynamically
10. Explore/exploit policy: when energy < 0.3, only high-confidence (>0.7) hypotheses are pursued
11. `build_action_prompt()` includes HYPOTHESIS section showing top hypotheses + confidence levels
12. Loop detection: if StateGraph shows the agent revisited a grid_hash, a WARNING is added to the prompt
13. All existing ARC3 tests (49) continue to pass
14. ≥15 new tests covering the hypothesis engine components
15. No new tools introduced — this is internal agent logic, not MCP tool surface

## Outcome

The ARC agent becomes a **systematic explorer** rather than a prompt-guesser. It discovers game rules through structured experimentation, remembers what it learns across games, and balances curiosity against survival pressure. This is the "scientific method" loop that Gemini correctly identified: Observe → Hypothesize → Test → Update. SideQuests provides the durable memory that makes hypotheses accumulate into genuine understanding.

## Validation Note (2026-03-31)

- Implemented files: `agents/arc3/hypothesis.py`, `agents/arc3/orchestrator.py`, `tests/test_arc3_hypothesis.py`
- Tests run:
  - `.venv/bin/pytest -q tests/test_arc3_orchestrator.py`
  - `.venv/bin/pytest -q tests/test_arc3_hypothesis.py`
  - `.venv/bin/pytest -q tests/test_arc3_hypothesis.py tests/test_arc3_orchestrator.py tests/test_arc3_durable_runner.py tests/test_arc3_solver.py`
- Result: `126 passed, 1 warning` on final integration pass
- Regression checks: hypothesis prompts now surface confirmed/active/pruned hypotheses and low-confidence hypotheses are pruned instead of lingering as false certainty
