# B163: Reduce ArchetypeClassifier MIN_OBSERVATIONS from 5 to 2

**Status**: ready
**Assigned**: TBD
**Depends on**: None
**Complexity**: Small (1 hour)
**Priority**: P1 (HIGH — 5 wasted steps with no strategic guidance on a 15-step budget)
**Plan**: backlog/plans/B-163-faster-archetype-classification.md

## Problem Statement

The `ArchetypeClassifier` in `agents/arc3/solver.py` has `MIN_OBSERVATIONS = 5`, meaning it returns `(UNKNOWN, 0.0)` for the first 5 steps. On a 15-step budget, this wastes 33% of the run with zero strategic guidance — every step runs in `fallback` mode with `ranked_action_families: []`.

In the live smoke:
- Steps 0-4: archetype = `unknown`, confidence = 0.0
- Steps 5+: archetype = `space`, confidence gradually rises to 0.65
- Victory condition never exceeds `unknown` (conf 0.10)

By the time the classifier starts working, most actions have already been wasted.

## What This Card Does

1. **Reduces `MIN_OBSERVATIONS` from 5 to 2** — enough to see one action's effect before classifying.
2. **Adds a "fast-track" classification path**: if the bootstrap grid analysis (B162) or first action produces strong spatial signals (multiple distinct regions, directional movement), skip the observation count gate entirely.
3. **Lowers the `LOCK_THRESHOLD` from 0.65 to 0.55** — allow earlier commitment to a strategy.

## Ecosystem Layer

- `agents/arc3` (ARC agent layer — solver.py)

## Files to Create/Modify

- `agents/arc3/solver.py` — update `ArchetypeClassifier` constants and add fast-track path
- `tests/test_arc3_solver.py` — update tests that assert on MIN_OBSERVATIONS behavior
- `tests/test_b163_faster_classification.py` — test fast-track classification with strong initial signals

## Acceptance Criteria

- [ ] `ArchetypeClassifier.MIN_OBSERVATIONS` is 2
- [ ] `ArchetypeClassifier.LOCK_THRESHOLD` is 0.55
- [ ] When bootstrap grid analysis shows ≥3 distinct colored regions, classifier returns a non-UNKNOWN archetype after 1 observation
- [ ] All existing solver tests pass (`pytest tests/test_arc3_solver.py -q`)
- [ ] No regression in archetype accuracy (tested against existing mocked scenarios)

## Outcome

The agent gets strategic guidance by step 2 instead of step 5, gaining 3 more useful steps on its limited budget.
