# B197: ARC Agent — Load Procedures Before Solving (Procedural Memory Integration)

**Status**: ready
**Assigned**: TBD
**Depends on**: B194 (procedural memory), B180 (cost tracker)
**Complexity**: Medium (2-4 hours)
**Priority**: P1 (highest impact on solve rate — reuse proven strategies)
**Plan**: backlog/plans/B-197-arc-procedure-loading.md

## Problem Statement

Even after B194 creates reusable Procedures from successful solve patterns, the ARC orchestrator doesn't know they exist. The orchestrator bootstraps each puzzle from scratch — archetype classification, role mapping, victory hypothesis, chunk planning — without checking if a proven recipe already exists for this puzzle type.

## Root Causes

1. `ARCOrchestrator.__init__` doesn't query for applicable Procedures
2. `SolveEngine.solve()` always starts from zero — no procedure-guided mode
3. `DurableARCRunner._run_puzzle()` doesn't call `recall_procedures` before creating orchestrator
4. `BrainClientProtocol` doesn't expose `recall_procedures` (needs adding to adapter)

## What This Card Does

1. **Add `recall_procedures` to `BrainClientProtocol`**: All adapters implement it
2. **Pre-solve procedure lookup**: In `runner.py`, before creating the orchestrator, call `recall_procedures(archetype_hint)` to get applicable procedures
3. **Procedure-guided mode in SolveEngine**: If a procedure is loaded, use its steps as the initial plan chunk sequence instead of generating from scratch
4. **Procedure tracking**: Record `APPLIED_PROCEDURE` edge when a procedure is used. After puzzle completes, report success/failure to update procedure's `success_rate`
5. **Fallback**: If no procedure exists or procedure steps fail, fall back to normal solve behavior

## Ecosystem Layer

- `agents/arc3/runner.py` (Agent Runtime — pre-solve lookup)
- `agents/arc3/orchestrator.py` (Agent Runtime — procedure-guided mode)
- `agents/arc3/solver.py` (Agent Runtime — procedure as initial chunk)
- `benchmarks/arc3/adapter.py` (Eval Infra — recall_procedures in protocol)

**Cross-layer compliance**: Agent Runtime reads from Data Products via BrainClientProtocol. No cross-layer violations.
**No shadow stores**: Procedure data flows through BrainClientProtocol from KuzuDB.

## Files to Create/Modify

- **MODIFY** `benchmarks/arc3/adapter.py` — Add `recall_procedures` to BrainClientProtocol, implement in all clients
- **MODIFY** `agents/arc3/runner.py` — Call `recall_procedures` before orchestrator creation
- **MODIFY** `agents/arc3/orchestrator.py` — Accept and use procedure in solve loop setup
- **MODIFY** `agents/arc3/solver.py` — Create initial PlanChunk from procedure steps
- **CREATE** `tests/test_b197_arc_procedure_loading.py` — Tests for procedure loading, guided mode, fallback
- No changes to `docs/tool-catalog.md`

## Acceptance Criteria

- [ ] `recall_procedures` called before each puzzle solve
- [ ] If applicable Procedure found, its steps become the initial PlanChunk sequence
- [ ] `APPLIED_PROCEDURE` edge created linking Plan → Procedure with success flag
- [ ] Procedure success_rate updated after puzzle completes
- [ ] Fallback to normal solve when no Procedure exists
- [ ] Fallback to normal solve when Procedure steps fail (dissonance detected)
- [ ] NoOpBrainClient returns empty procedures (no crash)
- [ ] All existing ARC tests pass

## Outcome

ARC agents start with proven recipes when available. A spatial navigation puzzle that was solved before can reuse the winning strategy immediately instead of spending 15+ steps rediscovering it.
