# B113 - Make Directional Chunks Produce Actionable Steps Early and Persistently

State: complete
Completed: 2026-04-01
Validation: 81/81 tests passed (test_b113_actionable_chunks.py + test_arc3_solver.py + test_arc3_orchestrator.py + B109/B112 regression suite)

Plan: backlog/plans/B-113-arc-actionable-directional-chunks.md

### Problem

After fixing invalid explore-chunk enforcement, the latest live ARC smoke run exposed the next
solve-phase gap:

- the final solve summary ends with a `directional` chunk
- the chunk source is correct
- but `active_chunk.estimated_actions` is empty on most late steps
- the chosen action still collapses to repeated local behavior (`ACTION6` 15 times)
- chunk rationale stays `UNTESTED` instead of showing stable actionable guidance

So the problem is no longer “the wrong chunk is being enforced.” The problem is:

- directional chunks are not being turned into actionable step sequences early enough
- or they are being exhausted/emptied too quickly to guide behavior across the critical late steps

### What This Card Builds

A stronger directional chunk materialization path:

- generate actionable directional step sequences earlier once player/goal evidence is sufficient
- keep those step sequences available long enough to guide behavior across multiple live steps
- ensure chunk execution history reflects real planned guidance instead of empty chunk shells

### Depends on

- B109
- B112

## Files to Create/Modify

- `agents/arc3/solver.py`
- `agents/arc3/orchestrator.py`
- `tests/test_arc3_solver.py`
- `tests/test_arc3_orchestrator.py`

## Acceptance Criteria

1. When a directional chunk is active, `estimated_actions` is non-empty at the steps where the
   chunk is intended to guide action choice.
2. Directional chunks are generated early enough to affect late-step decision-making rather than
   appearing only as empty summary shells.
3. Directional chunk execution history is visible in the export as real actionable guidance.
4. The harness does not silently collapse to repeated `UNTESTED` actions while a directional chunk
   is supposedly active.
5. Tests cover:
   - directional chunk retains actionable steps across multiple guided decisions
   - chunk state is not prematurely emptied before the action policy can use it
   - late-step solve context reflects non-empty actionable directional chunks
6. A live smoke run shows directional chunks with usable `estimated_actions` during the guided
   phase, not only at the final step.

## Outcome

Directional chunks stop being mostly descriptive summaries and become durable, actionable guidance
that can influence multiple real puzzle steps.
