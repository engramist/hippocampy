# B103 — Resolve Conflicting Persistent ARC Roles in SolveEngine

- **State:** complete
- **Priority:** P0
- **Plan:** backlog/plans/B-103-arc-role-state-conflict-resolution.md
- **Dependencies:** B95, B97, B99, B101

## Problem Statement

Live ARC runs show that object-role inference is now good enough to identify plausible `player`
and `goal` candidates, but the persistent role-state logic in `SolveEngine` is too naive.

Right now, SolveEngine merges step-level roles using a simple "highest confidence wins forever"
rule. That causes mutually exclusive roles to accumulate or overwrite each other incorrectly:

- a previously detected `player` can later be overwritten by `goal`
- stale old `goal` assignments remain in the persistent role map even when a new dominant goal
  candidate appears
- final solve summaries can contain multiple `goal` labels at once

This is not just a role-mapper problem. The root cause is that persistent role-state management in
`SolveEngine` does not understand role-class conflicts or exclusivity.

## What It Does

Add explicit persistent role conflict resolution to the ARC solve layer:

- stop using pure merge-by-confidence for mutually exclusive roles
- define precedence / conflict rules for `player`, `goal`, and other exclusive role classes
- prevent a `player` from being trivially overwritten by `goal`
- demote or remove stale prior goals when a new dominant goal candidate emerges
- enforce a single primary `player` and a single primary `goal` in solve summaries
- make role-state transitions visible in solve/debug output when a conflict is resolved

## Acceptance Criteria

1. Persistent role-state management no longer uses raw confidence-only merge semantics for mutually
   exclusive roles.
2. A `player` role is not overwritten by a later `goal` label without an explicit conflict rule
   permitting it.
3. At most one primary `player` and one primary `goal` appear in final solve summaries.
4. Stale prior goals are demoted, removed, or clearly marked non-primary when a better goal
   candidate dominates.
5. Add at least 4 tests covering:
   - player preserved against later conflicting goal label
   - stale goal replaced by stronger new goal
   - single-primary-goal enforcement
   - a regression based on the live run where color `15` flipped from `player` to `goal`
6. In a live puzzle-1 smoke run, exported solve summaries end with one primary player and one
   primary goal instead of goal accumulation or role drift.

## Outcome

The solve phase maintains a coherent world model over time instead of accumulating contradictory
role assignments.

## Validation Note (2026-03-31)

- Implemented files: `agents/arc3/solver.py`, `tests/test_arc3_solver.py`
- Tests run:
  - `.venv/bin/python -m py_compile agents/arc3/solver.py tests/test_arc3_solver.py`
  - `.venv/bin/pytest -q tests/test_arc3_solver.py tests/test_arc3_durable_runner.py`
  - `.venv/bin/pytest -q tests/test_arc3_hypothesis.py tests/test_arc3_orchestrator.py tests/test_arc3_durable_runner.py tests/test_arc3_solver.py`
- Result: `126 passed, 1 warning` on final integration pass
- Regression checks: live smoke run `submission_results_single.json` now ends with `PRIMARY ROLES: player=1, goal=2` and no goal accumulation
