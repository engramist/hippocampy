# B156: ARC Level-Aware Orchestration Loop — In-Game Knowledge Pipeline

**Status**: blocked (depends on B152 verification)
**Assigned**: TBD
**Depends on**: B150, B151, B152, B157
**Complexity**: Medium (2–4 hours)
**Priority**: P0 (glue that connects level transitions to learning)
**Related**: codereviewOpus2.md (all sections)
**Plan**: backlog/plans/B-156-arc-two-phase-router.md
**Phase**: Orchestration — wires level transitions to analysis/hypothesis/verification pipeline

## Problem Statement

B157 provides the multi-level game loop and level transition events. B150 provides grid diff analysis. B151 generates game rule hypotheses. B152 verifies them. But nothing wires these together. The orchestrator needs a level-transition handler that:

1. At level transition: captures level data (B157), runs grid analysis (B150), generates hypotheses (B151), verifies against all solved levels (B152)
2. Based on verification confidence: sets the prompt mode (B153) and exploration policy (B154) for the next level
3. Preserves the existing navigation/exploration machinery as active fallback

### What to keep from the old approach

The following components are **retained** for exploration fallback:
- `ActionFact` tracking and `observed_action_effects` — essential for learning what actions do
- `HypothesisManager` / `StateGraph` — useful for detecting loops and tracking state
- `_enforce_action_policy()` — useful for preventing repeated actions (modified by B154)
- `DissonanceDetector` — useful for detecting stalls (with reduced thresholds from B154)
- `PlanChunker` — useful for structuring multi-step execution

The following are **candidates for future removal** (not this card):
- `GameArchetype` enum (race/space/chase/displace) — wrong mental model for ARC
- `ArchetypeClassifier` — produces incorrect classifications
- `ObjectRoleMapper` (player/enemy/goal/wall) — ARC has no players or enemies

## What This Card Builds

1. **`_on_level_transition()` knowledge pipeline** in orchestrator:
   ```
   Level N solved → B157 captures (start_grid, end_grid, actions)
       → B150: diff_grids(start, end) → GridDiff, update LevelPattern
       → B151: hypothesize(level_pattern, solved_levels) → GameRuleHypothesis[]
       → B152: verify_against_solved_levels(hypothesis, all_solved) → confidence
       → Set prompt mode (B153) and exploration policy (B154) for level N+1
   ```

2. **Confidence-based mode routing** for each level:
   - Confidence > 0.8: `execution` mode — apply rule directly, minimal exploration
   - Confidence 0.4-0.8: `rule_application` mode — show insights, moderate exploration
   - Confidence < 0.4 or level 1: `navigation` mode — existing exploration fallback

3. **Progressive knowledge accumulation**:
   - `_game_rule_hypothesis: Optional[GameRuleHypothesis]` — current best understanding
   - `_rule_confidence: float` — increases as more levels verify the hypothesis
   - `_action_semantics: Dict[str, str]` — action effect map, updated per level
   - These persist across levels (B157 preserves cross-level state)

4. **Level 1 bootstrap**: No prior levels solved → full exploration mode. Game rule hypothesis is None. ActionFact tracking starts from scratch (or from B155 memory if available).

5. **Trace events**: Emit trace events for each step of the knowledge pipeline — grid analysis duration, hypothesis count, verification confidence, mode selection. Feeds debug export.

## Files to Create/Modify

- `agents/arc3/orchestrator.py` — Wire `_on_level_transition()` to analysis/hypothesis/verification pipeline; add confidence-based mode routing; add progressive knowledge attributes
- `agents/arc3/runner.py` — Ensure level transition events reach orchestrator (B157 provides the hook)
- `tests/test_b156_level_orchestration.py` — NEW: test knowledge pipeline, mode routing, progressive confidence

## Ecosystem / Layer Compliance

- Layer: Agent Orchestration (runner) + Agent Runtime (orchestrator)
- No cross-layer violations. No imports from `mcp_engine/`.
- No deletion of existing code — additive only.
- Complies with `docs/arc-harness-rules.md` and `docs/ecosystem-rules.md`.

## Acceptance Criteria

- [x] Level transitions trigger the full knowledge pipeline (B150→B151→B152)
- [x] Confidence routing selects the correct prompt/exploration mode
- [x] Knowledge accumulates across levels (action semantics, rule hypotheses)
- [x] Level 1 starts in exploration/navigation mode
- [x] High-confidence levels enter execution mode
- [x] Fallback mode runs the existing navigation loop unchanged
- [x] All knowledge pipeline steps logged in trace events
- [x] No existing code deleted — all old machinery remains available
- [x] All tests pass, no regressions

## Outcome Statement

After this card, level transitions are the agent's learning moments. After solving level N, the agent analyzes what happened, hypothesizes the game's rules, verifies against all solved levels, and configures itself for level N+1. Early levels are tutorials. Later levels are informed by accumulated knowledge. The existing exploration machinery remains as fallback for uncertain situations.
