# B115 - ARC Decision Guard Before Real Move Execution

State: complete
Completed: 2026-04-01
Validation: 90/90 tests passed (test_b115_decision_guard.py + solver/orchestrator + B109-B114 regression suites)

Plan: backlog/plans/B-115-arc-decision-guard.md

### Problem

Even with better chunking and action facts, the harness can still propose moves that:

- repeat known loops
- violate stronger active guidance
- spend a step on stale or contradicted hypotheses

The harness needs an explicit pre-execution guard between "candidate move" and "real move."

### What This Card Builds

A decision guard for ARC action execution:

- inspect the candidate action before it is sent to the environment
- block or critique actions that repeat known bad patterns
- allow self-correction before a real puzzle step is wasted

### Depends on

- B109
- B112
- B114

## Files to Create/Modify

- `agents/arc3/orchestrator.py`
- `agents/arc3/solver.py`
- `tests/test_arc3_orchestrator.py`
- `tests/test_arc3_solver.py`

## Acceptance Criteria

1. Candidate actions pass through a decision guard before execution.
2. The guard can reject or revise actions that:
   - repeat known loops
   - violate stronger active chunk guidance
   - contradict locked evidence
3. Guard outcomes are visible in debug output.
4. Tests cover:
   - rejected loop move
   - rejected stale-guidance move
   - allowed valid move
5. A live smoke run shows guard behavior without breaking the run/export path.

## Outcome

The ARC harness can self-correct before wasting a real environment move.
