# B183: Meta-Supervisor Agent

**Status**: ready
**Assigned**: TBD
**Depends on**: B184 (circuit breaker for reliable LLM calls)
**Complexity**: Large (4-6 hours)
**Priority**: P1 (replaces step-count escalation with trajectory-aware supervision)
**Plan**: backlog/plans/B-183-meta-supervisor.md

## Problem Statement

The no-progress escalation ladder in `orchestrator.py:1674-1702` is purely step-count based: tier 1 at 3 steps, tier 2 at 5, tier 3 at 8. It cannot distinguish between "agent is cycling between 2 states" (hopeless) and "agent is slowly approaching the goal" (worth continuing). It also resets every 8 steps (B177 fixes this), but even with B177, the ladder has no trajectory awareness.

Enterprise orchestration calls for "supervision hierarchies" — higher-level agents that monitor and course-correct workers. The ARC analog is a meta-supervisor that reads the execution trace and makes strategy-level decisions.

## Root Causes

1. Escalation ladder uses only `_consecutive_no_progress_steps` — no trajectory analysis
2. No component evaluates whether the agent's overall strategy is working
3. Decisions are local (per-step) not global (across the trajectory)

## What This Card Does

1. **New `PuzzleSupervisor` class** in `agents/arc3/supervisor.py`
2. Runs every 5 steps, reads `_execution_trace` and `_step_history`
3. Returns one of four decisions:
   - `CONTINUE` — agent is making progress (centroid moving, new states discovered)
   - `NUDGE` — inject a hint into next prompt (e.g., "try unexplored region")
   - `RESET_STRATEGY` — wipe archetype/victory/roles, force re-inference from scratch
   - `ABANDON` — puzzle is hopeless, save tokens (emit `budget_exceeded` or `strategy_exhausted`)
4. **Rule-based first**: Pattern matching on trace events (oscillation detection, state novelty, centroid velocity). Only calls LLM for ambiguous cases.
5. **Replaces escalation ladder** in orchestrator.py (lines 1674-1702) with `supervisor.evaluate()`

## Ecosystem Layer

- `agents/arc3/supervisor.py` (Agent Runtime — new file)
- `agents/arc3/orchestrator.py` (Agent Runtime — replace escalation ladder)

**Cross-layer compliance**: No cross-layer violations. Supervisor is Agent Runtime.

## Files to Create/Modify

- **CREATE** `agents/arc3/supervisor.py` — PuzzleSupervisor class with rule-based evaluation + optional LLM escalation
- **MODIFY** `agents/arc3/orchestrator.py` — Replace escalation ladder (lines 1674-1702) with supervisor.evaluate() call; wire supervisor decisions into solve loop
- **MODIFY** `agents/arc3/runner.py` — Pass supervisor instance to orchestrator; handle ABANDON decision
- **CREATE** `tests/test_b183_supervisor.py` — Tests for each decision type, rule-based patterns, LLM fallback
- No changes to `docs/tool-catalog.md`

## Acceptance Criteria

- [ ] Supervisor returns CONTINUE when centroid is moving toward target
- [ ] Supervisor returns NUDGE when agent is stuck but hasn't tried all actions
- [ ] Supervisor returns RESET_STRATEGY when agent has been oscillating for 15+ steps
- [ ] Supervisor returns ABANDON when 30+ steps with zero progress and all strategies tried
- [ ] Rule-based evaluation runs without LLM call (fast path)
- [ ] LLM escalation only fires for ambiguous cases (configurable threshold)
- [ ] Existing escalation behavior preserved as regression baseline
- [ ] Trace events show supervisor decisions and reasoning
- [ ] All existing ARC tests pass

## Outcome

The agent has a supervisor that understands trajectory quality, not just step counts. Hopeless puzzles are abandoned early (saving tokens). Stuck agents get strategic nudges instead of blindly repeating the same escalation cycle.
