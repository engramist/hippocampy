# B217 — Victory Condition Must Converge: Bootstrap from Archetype and Grid Structure

- **State:** ready
- **Priority:** P1
- **Plan:** backlog/plans/B-217-victory-condition-convergence.md
- **Ecosystem Layer(s):** Agent Runtime
- **Cross-layer violations:** None.
- **Dependencies:** B214, B215, B216.

## Problem Statement

`victory_condition=unknown` with `conf=0.10` persisted across all 20 steps of the smoke run. The agent never established a goal. `archetype=space (conf=0.57)` was stable from step 4 onward, but this archetype knowledge was not used to derive a candidate victory condition. The hypothesize phase repeatedly produced "Archetype=space; victory_condition=unknown" without progress.

Contributing factors:
- `recall_lessons` returned 0 every step (blocked by B214), so no prior knowledge of what "space" archetypes typically require.
- The model phase never synthesized a concrete hypothesis from the grid structure (64×64, colors 0/1/3/5/9/11, player=3 stable in row 1) even though this information was consistently available.
- The perceive phase question "Did ACTION6 advance toward the victory condition?" is unanswerable when `victory_condition=unknown`, so perceive always evaluates to no-progress without narrowing the goal.

## What It Does (Scope)

1. Add archetype-seeded victory bootstrapping: when `archetype` is known with confidence ≥ 0.5 and `victory_condition` is still unknown after step 3, inject a default candidate hypothesis derived from the archetype (e.g. for "space": "navigate player to goal tile").
2. Add a model-phase structural scan: on the first `model` call where player role is identified but victory is unknown, extract goal-color candidates from the grid color inventory and propose a candidate victory condition.
3. Store the candidate via `upsert_lesson` (domain=archetype, lesson_type=hypothesis) so it survives to `recall_lessons` in the hypothesize phase.

Out of scope:
- LLM prompt rewriting (use existing prompt injection hooks).
- Changing archetype detection logic.

## Files to Create/Modify

| File | Action |
|------|--------|
| `agents/arc3/solver.py` | **Modify** — add archetype-seeded victory bootstrap in hypothesize phase (when conf ≥ 0.5, step > 3, victory unknown) |
| `agents/arc3/runner.py` | **Modify** — inject archetype→victory default into model context when conditions met |
| `tests/test_arc3_solver.py` | **Modify** — add test: after archetype stabilizes, victory condition receives a candidate before step 5 |

### Adapter/allow-list impact
- No tool changes.
- No `docs/tool-catalog.md` updates required.

## Acceptance Criteria

1. When archetype is known with conf ≥ 0.5 and victory is still unknown after step 3, a candidate victory condition is injected into the hypothesize context.
2. The injected candidate appears in the route phase's goal context on the following step.
3. The candidate is stored via `upsert_lesson` and retrievable by `recall_lessons` (depends on B214 being fixed first).
4. In a smoke run, `phase_answer` for hypothesize shows a non-unknown victory condition by step 5.
5. Existing archetype/victory tests continue passing.

## Outcome Statement

The agent always has a working hypothesis about its goal, derived from stable archetype knowledge, rather than remaining goal-blind for the entire run. This unblocks the perceive→evaluate feedback loop that requires a known victory condition to assess action progress.
