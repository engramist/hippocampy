# B147: Make Plateau Activation Sticky Under Confidence Jitter

**Status**: complete ✅ DONE (2026-04-01)
**Assigned**: TBD  
**Depends on**: B146  
**Complexity**: Medium (2–4 hours)  
**Priority**: P0 (next live-run blocker after B146)  
**Related**: B141, B142, B143, B144, B145, B146  
**Plan**: backlog/plans/B-147-sticky-plateau-activation.md

## Problem Statement

B146 improved plateau lock consistency, but the latest verified live smoke shows a new failure mode: the plateau policy sometimes never activates at all because the grounding confidence jitters just below the hard threshold.

Evidence from `live_qwen25_7b_b146verify_1775256911`:
- `correct = false`, `steps = 15`, `final_state = NOT_FINISHED`
- the final strategy summary reverted to `CHUNK: Explore: try unexplored action to gather more information [explore]`
- no stable plateau lock appears in the late-run summaries
- the verified object roles show `player` confidence of `0.6975`, just below the current `>= 0.7` plateau gate
- this means a tiny confidence wobble is enough to suppress the entire late-run plateau policy even after the run is clearly stalled

The remaining bottleneck is now **plateau activation brittleness**: once the run has enough evidence that player/goal grounding is broadly established, small confidence jitter should not drop the solver back out of plateau mode and generic exploration.

## What This Card Builds

1. **Sticky plateau activation**: once plateau mode becomes eligible, keep it active through small confidence dips rather than requiring the exact `>= 0.7` check every time.

2. **Grounding hysteresis**: use a slightly lower sustain threshold or recent-history grounding evidence so plateau mode does not flap on/off around one borderline value.

3. **Regression for the borderline case**: add a failing regression covering the live scenario where `player≈0.6975`, `goal≈0.95`, and the run is already in a prolonged zero-reward streak.

4. **Trace visibility**: expose whether plateau mode was activated via fresh grounding vs. sticky/hysteresis carry-forward.

## Files to Create/Modify

- `agents/arc3/solver.py` — make plateau activation sticky under minor confidence jitter and expose the reason in solve context
- `tests/test_arc3_solver.py` — regressions for borderline grounded confidence and hysteresis behavior
- `tests/test_arc3_orchestrator.py` — optional coverage if solve context propagation needs explicit assertions

## Ecosystem / Layer Compliance

- Layer: ARC solve engine (`agents/arc3/solver.py`) with possible orchestrator propagation checks
- No new tools, adapter changes, or schema migrations expected
- Additive gating refinement only; no cross-layer import violations
- Must comply with `docs/arc-harness-rules.md` and `docs/ecosystem-rules.md`

## Acceptance Criteria

- [ ] Plateau mode still activates or remains active for prolonged zero-reward streaks when grounding confidence is only slightly below the original threshold (for example `player≈0.6975`)
- [ ] Plateau activation does not flap on/off across adjacent late steps due to tiny confidence jitter
- [ ] Trace/strategy output makes clear whether plateau mode came from direct grounding or sticky hysteresis
- [ ] The `live_qwen25_7b_b146verify_1775256911` failure mode is covered by a regression and cannot recur for the same borderline-confidence pattern
- [ ] Existing B143 coordinate logic and B145/B146 plateau-lock consistency behavior remain intact
- [ ] Focused ARC tests and broader regressions pass with no regressions

## Outcome Statement

When complete, plateau mode will engage reliably in the late-stage stalled runs it was designed for, instead of silently disappearing because a grounded role confidence dipped a few thousandths below a hard threshold.