# B132: Harden Mental Sandbox JSON Parsing and Fallback Attribution

**Status**: complete ✅ DONE (2026-04-01)
**Assigned**: TBD  
**Depends on**: None  
**Complexity**: Small-Medium (1–2 hours)  
**Priority**: P1 (HIGH — LLM is live, but sandbox output is still brittle and often falls back)  
**Plan**: TBD

## Problem Statement

The original "`openai` is not installed" diagnosis is now stale.

Fresh evidence shows the local LLM is available:
- `requirements.txt` already contains `openai==1.58.1`
- `pyproject.toml` already includes `openai>=1.50.0`
- the latest live smoke showed successful `POST http://localhost:11434/v1/chat/completions "200 OK"`

However, `master_timeline.json` still contains repeated `mental_sandbox_parse_error` and `mental_sandbox_fallback_query_llm` events. So the current issue is **not** provider initialization — it is that the mental sandbox is frequently receiving malformed or non-JSON model output and dropping into fallback behavior.

Evidence from the current timeline:
```text
multiple events: mental_sandbox_parse_error
multiple events: mental_sandbox_fallback_query_llm
latest live run: local LLM requests return 200 OK
```

This weakens action quality and makes it hard to tell whether a choice came from the sandbox or the fallback path.

## What This Card Builds

1. Tightens the sandbox prompt/response contract so JSON output is easier to parse reliably.
2. Adds structured recovery when the first sandbox parse fails (e.g. extract JSON object, retry once, then fall back).
3. Emits a clearer decision-source marker showing whether the final action came from:
   - the sandbox,
   - a parse-recovery path,
   - or the fallback query path.
4. Improves error details in the trace so malformed model output can be diagnosed quickly.

## Files to Create/Modify

- `agents/arc3/orchestrator.py` — harden `_mental_sandbox()` parsing and trace events
- `agents/arc3/prompts.py` — make the sandbox JSON contract stricter and more explicit
- `tests/test_arc3_orchestrator.py` — add regressions for malformed-but-recoverable sandbox output

## Ecosystem / Layer Compliance

- Layer: ARC orchestration internals (`agents/arc3/`)
- No cross-layer import violations.
- Does not change ARC harness phase ownership.
- Complies with `docs/ecosystem-rules.md` and `docs/arc-harness-rules.md`.

## Acceptance Criteria

- [ ] A fresh run shows materially fewer `mental_sandbox_parse_error` events
- [ ] `master_timeline.json` clearly distinguishes sandbox vs fallback decisions
- [ ] Malformed-but-recoverable sandbox output no longer immediately degrades to the generic fallback path
- [ ] `pytest -q tests/` passes with no regressions

## Outcome Statement

After this card, the mental sandbox is robust enough to use the live LLM consistently. Decision quality improves because malformed responses are recovered or clearly attributed instead of silently collapsing into fallback behavior.
