# B123 - ARC Python REPL Sandbox Tool for Grid Logic Verification

State: complete
Completed: 2026-04-01
Validation: 95/95 tests passed (test_arc3_repl_sandbox.py + orchestrator/solver + B109-B115 regressions)

Plan: backlog/plans/B-123-arc-repl-sandbox.md

Ecosystem Layer: Runtime (Layer 3) — ARC Agent tool, non-world-mutating

### Problem

The ARC agent currently reasons about grid transformations (rotation, translation, pattern matching)
using pure LLM text generation. ARC puzzles are fundamentally algorithmic — the agent guesses pixel
moves when it should be verifying logic. The mental sandbox (B114) provides a reasoning loop, but
it only compares action facts and hypotheses; it cannot execute code to test a transformation.

### What This Card Builds

A harness-local Python REPL tool the agent can invoke inside the mental sandbox loop:

1. `repl_test` sandbox tool — accepts a short Python snippet (≤50 lines), executes it in a
   restricted subprocess, returns stdout/stderr.
2. Restrictions: no file I/O, no network, no imports beyond `numpy`, `collections`, `itertools`,
   `json`, `math`. Timeout: 2 seconds.
3. The agent can write a small function (e.g., rotate a grid, count objects, test a hypothesis)
   and verify the output before spending a real ARC move.
4. Integrates with `_mental_sandbox_loop()` — the REPL is one of the sandbox tools alongside
   `peek_action_consequences`.
5. REPL results appear in `thinking_trace` for debug export.

### Depends on

- B114 (mental sandbox loop must exist)

### Priority

P1 — Highest-impact single improvement for ARC score. Moves agent from "guessing pixels" to
"verifying logic."

## Files to Create/Modify

- `agents/arc3/repl_sandbox.py` — **CREATE**: restricted Python REPL executor (subprocess + timeout + import whitelist)
- `agents/arc3/orchestrator.py` — **MODIFY**: add `repl_test` as a sandbox tool in `_mental_sandbox_loop()`
- `agents/arc3/prompts.py` — **MODIFY**: add `REPL_SANDBOX_INSTRUCTION` prompt constant (Layer 4 compliant)
- `tests/test_arc3_repl_sandbox.py` — **CREATE**: test safe execution, timeout, blocked imports, output capture

## Acceptance Criteria

1. Agent can invoke `repl_test` with a Python snippet inside the sandbox loop.
2. Snippet executes in a subprocess with 2-second timeout.
3. Blocked imports (os, subprocess, socket, etc.) raise a clear error, not a crash.
4. REPL output (stdout + stderr) appears in `thinking_trace[].result`.
5. No sandbox tool call mutates the ARC environment or SideQuests state.
6. `pytest tests/test_arc3_repl_sandbox.py -q` passes.

## Outcome

The agent can test grid logic computationally before committing a real move, dramatically reducing
wasted steps on incorrect spatial reasoning.
