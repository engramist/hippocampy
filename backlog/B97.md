# B97 — Object Role Detection Without Reward Signal

- **State:** complete
- **Priority:** P0 (blocks directed solve behavior)
- **Plan:** backlog/plans/B-97-object-role-no-reward.md
- **Dependencies:** B95 (SolveEngine exists)

## Problem Statement

`ObjectRoleMapper.update()` only assigns PLAYER role when `reward > 0.3`, and WALL only when there are no `changed_regions`. In ARC-AGI-3 there is **no per-step reward** — the only reward signal is WIN at the end. Result: all object roles stay `unknown` for the entire run, so `PlanChunker` has no target to navigate toward and defaults to pure exploration.

## What It Does

Replaces the reward-gated heuristics with **evidence fusion from grid-change patterns**:

- **PLAYER**: Color whose centroid or changed-region signature consistently co-varies with the currently inferred operator effect. This is an evidence rule, not a hard-coded mapping from `ACTION1..ACTION4` to compass directions.
- **WALL**: Color that appears exclusively in `static_rows` (already computed by `InvariantDetector`) OR never changes across any observed transition.
- **GOAL**: Color that is: (1) small pixel count, (2) stationary across frames, (3) not in static/HUD rows, (4) not the background (color 0), and (5) becomes more plausible as a target when nearby action effects or path hypotheses converge on it.
- **BACKGROUND**: Color 0 (black) — most frequent, fills empty space.

`SolveEngine` should orchestrate any SideQuests retrieval needed to support this work. `ObjectRoleMapper`
should stay mostly logic-only and consume the already-gathered hypothesis/observation data.

## Files to Modify

- `agents/arc3/solver.py` — rewrite `ObjectRoleMapper` class

## Acceptance Criteria

1. After several consistent operator observations, PLAYER role is assigned to the strongest evidence-backed moving color with confidence ≥ 0.70.
2. Colors exclusively in `static_rows` are assigned WALL with confidence ≥ 0.65.
3. Small-count, stationary, non-background, non-static-row colors are assigned GOAL with confidence ≥ 0.55.
4. Role assignments accumulate across steps (higher-confidence new evidence replaces lower).
5. `test_object_role_mapper_detects_player_from_movement` passes.
6. `test_object_role_mapper_detects_wall_from_static_rows` passes.
7. `test_object_role_mapper_detects_goal_heuristic` passes.
8. All existing B95 tests still pass (94 total).

## Outcome

The solve engine correctly identifies player and goal objects within the first 5–8 steps, enabling `PlanChunker` to generate BFS paths toward the goal rather than defaulting to random exploration.

## Validation Note (2026-03-31)

- Implemented files: `agents/arc3/solver.py`, `tests/test_arc3_solver.py`
- Tests run: focused ARC solver/durable-runner suites during B95/B97 integration
- Result: passing locally before downstream role-state issues were identified
- Regression checks: live smoke runs now detect non-`unknown` player/goal roles, which confirms the no-reward role-mapping path landed
