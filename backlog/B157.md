# B157: ARC Multi-Level Game Progression — Continue After WIN

**Status**: complete ✅ DONE (2026-04-01)
**Assigned**: TBD
**Depends on**: None
**Complexity**: Medium (2–4 hours)
**Priority**: P0 (CRITICAL BLOCKER — without this, agent only attempts level 1 of 8)
**Related**: codereviewOpus2.md, submission_results_single.json
**Plan**: backlog/plans/B-157-arc-multi-level-progression.md

## Problem Statement

The ARC-AGI-3 API serves multi-level games. The test game has `win_levels: 8` — the agent must solve all 8 levels for full credit. Currently the runner **exits immediately on the first WIN state** (runner.py:322-326):

```python
if state == "WIN":
    success = True
    break  # ← Bug: exits game after solving just level 1
```

The agent never attempts levels 2-8. With `levels_completed: 0` across all 15 steps in the smoke test, the agent didn't even solve level 1 — but even if it did, it would stop there.

This is the single biggest architectural gap: the runner treats ARC-AGI-3 as a single-puzzle game when it's actually a multi-level progression.

### ARC-AGI-3 Game Model

- Each game has N levels (typically 7-8), progressing from simple to complex
- Early levels are implicit tutorials — they teach the game's rules
- Later levels apply those rules in harder configurations
- Scoring is **weighted by level index** — later levels count more
- RHAE scoring: `(human_actions / ai_actions)^2` per level, weighted average across levels
- The agent learns what the game is about by PLAYING the early levels

This means early levels are effectively "training examples" — but discovered through interaction, not given statically.

## What This Card Builds

1. **Level-aware game loop** in runner.py: After WIN, check if `levels_completed < win_levels`. If more levels remain, continue playing (the API serves the next level automatically after WIN).

2. **Level transition tracking**: Capture grid snapshots at level boundaries:
   - `_level_start_grid`: Grid state when level N begins
   - `_level_end_grid`: Grid state when level N is won
   - `_level_action_sequence`: Actions taken to solve level N
   - `_solved_levels`: List of (start_grid, end_grid, actions) tuples

3. **Per-level step budgeting**: Distribute the step budget across levels instead of burning all steps on level 1.

4. **Level-aware reset**: On GAME_OVER within a level, retry that level (not the whole game). On persistent failure, skip to next level if API allows.

5. **Level context in orchestrator**: Pass `current_level`, `levels_completed`, `solved_levels` to the orchestrator so it can use prior level knowledge.

## Files to Create/Modify

- `agents/arc3/runner.py` — Multi-level game loop, level transition tracking, per-level budgeting
- `agents/arc3/orchestrator.py` — Accept level context, expose `_solved_levels` for downstream use
- `tests/test_b157_multi_level_progression.py` — NEW: test level progression, transition capture, budget distribution

## Ecosystem / Layer Compliance

- Layer: Agent Orchestration (runner)
- No cross-layer violations. No imports from `mcp_engine/`.
- Complies with `docs/arc-harness-rules.md` and `docs/ecosystem-rules.md`.

## Acceptance Criteria

- [ ] Runner continues playing after WIN if `levels_completed < win_levels`
- [ ] Grid snapshots captured at level start and level end (WIN)
- [ ] Action sequences recorded per level for cross-level analysis
- [ ] Step budget distributed across levels (not all burned on level 1)
- [ ] Level context passed to orchestrator
- [ ] GAME_OVER triggers per-level retry, not full game restart
- [ ] All tests pass, no regressions

## Outcome Statement

After this card, the agent actually plays the full game — all 8 levels instead of just level 1. Solved early levels provide "training data" for later levels, enabling the cross-level learning that B150-B156 build on.
