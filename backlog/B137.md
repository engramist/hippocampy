# B137: Reduce Redundant Chunk `register_plan` Churn in the Solve Engine

**Status**: Ready  
**Assigned**: TBD  
**Depends on**: None  
**Complexity**: Small (1–2 hours)  
**Priority**: P2 (MEDIUM — noisy brain writes make plan history harder to audit)  
**Plan**: backlog/plans/B-137-fix-double-register-plan.md

## Problem Statement

The original "double register per solve step" diagnosis is too strong for the current code.

Today:
- `_register_solve_plan()` is already guarded by `self._solve_plan_id is None`, so the top-level solve plan is **not** re-registered every step.
- But `master_timeline.json` still shows repeated `register_plan` writes for similar chunk goals whenever dissonance/staleness resets the active chunk.

So the real issue is **redundant chunk plan churn**, not a universal double-registration bug.

Evidence from the current timeline:
- one top-level solve plan registration appears when the solve plan is first established
- later steps repeatedly register near-identical chunk goals such as `Explore: try unexplored action to gather more information`
- these writes accumulate even when the chunk semantics have barely changed

## What This Card Builds

1. Adds idempotency checks around chunk plan registration.
2. Skips or coalesces re-registration when the new chunk is materially the same as the last one.
3. Emits `plan_registration_skipped` (or equivalent) when a duplicate registration is suppressed.
4. Preserves re-registration when the chunk really changed or dissonance requires a new plan identity.

## Files to Create/Modify

- `agents/arc3/solver.py` — track recent chunk plan signatures and suppress low-value duplicate `register_plan` calls

## Ecosystem / Layer Compliance

- Layer: ARC agent solve engine (`agents/arc3/solver.py`)
- No cross-layer import violations.
- Complies with `docs/arc-harness-rules.md` and `docs/ecosystem-rules.md`.

## Acceptance Criteria

- [ ] Repeated identical or near-identical chunk registrations are reduced substantially
- [ ] `plan_registration_skipped` (or equivalent) appears when deduplication occurs
- [ ] Legitimate new chunk plans still register normally after real strategy change or dissonance reset
- [ ] `pytest -q tests/` passes with no regressions

## Outcome Statement

After this card, the brain plan ledger reflects real chunk transitions instead of noisy churn. Debugging and auditing plan history become much easier.
