# B284 - Bound the Lexical Fallback and Property Scans

State: needs work — reopened 2026-08-03. Real, empirically-confirmed gap in the FTS branch of the lexical fallback in `campy/brain/thalamus/tools/retrieval.py::current_truth` (~L257-300). The pinned kuzu==0.11.3 build installed in this environment DOES have the FTS extension available (confirmed: `KuzuClient.has_fts()` returns `True` on a fresh temp DB via `CALL SHOW_LOADED_EXTENSIONS()`/`LOAD fts;`), contrary to the card's plan assumption ("FTS unavailable, expected on 0.11.3"). When FTS is available, `current_truth` takes the `fts_search()` branch (L263-276), which filters only `node.get("archived")` — it has NO recency/window predicate at all. The CONTAINS fallback branch (L278-287, the `else`) is the ONLY one with `created_at > timestamp($cutoff)`, and it never runs when FTS is loaded. Reproduced end-to-end against a real temp KuzuDB (schema via `init_schema`, no mocks): inserted a Message dated 1 day ago and a Message dated 30 days ago, both containing a unique token, called the real `current_truth()` with `retrieval.lexical_window_days: 14`; actual output — both `msg-recent` AND `msg-old` (30 days old, well outside the 14-day window) returned with `lexical_exact: True`. This directly violates the card's own acceptance criterion "Old Messages outside the window do NOT surface lexically." The existing `tests/test_lexical_fallback.py` never catches this because its `LexicalCaptureDB` mock has no `has_fts` attribute at all — `getattr(db, "has_fts", None)` returns `None`, so the mock is structurally incapable of ever exercising the FTS branch; every test in the file only exercises the CONTAINS branch, which does have the window predicate. `pytest tests/test_lexical_fallback.py tests/test_web.py -q` → 48 passed, 1 xfailed — fully green while the real bug exists, the same MockDB-blind-spot class of failure documented in B278. `KuzuClient.fts_search()` (`kuzu_client.py:163-174`) itself has no window/cutoff parameter at all — a fix needs to either post-filter FTS results by `created_at` in `retrieval.py` the same way the CONTAINS branch does, or add a bounded Cypher predicate to the FTS query path. Not fixed here (requires touching the ranking/filter logic, not a trivial one-line change). Everything else in the card checks out: config-driven window/limit defaults present in `campy/brain/brainstem/config.py`; CONTAINS branch has both the recency predicate and `LIMIT`; recent-message-surfaces behavior confirmed correct in the same repro script.
Priority: P2

Plan: backlog/plans/B-284-lexical-scan-hygiene.md

### Problem

Kuzu 0.11.3 provides only primary-key hash indexes and HNSW vector indexes — no secondary property indexes, no built-in full-text search at our pin. Several hot queries are full table scans:

1. `current_truth`'s lexical fallback: `MATCH (m:Message) WHERE lower(m.text_raw) CONTAINS lower($query)` — scans every Message ever captured, on every recall. Message is the fastest-growing table (every turn writes one). This degrades linearly and runs inside the recall hot path.
2. `WHERE n.archived = false` filters in sweeps and retrieval scan whole tables (acceptable for sweeps, hot-path concern for retrieval).
3. `{gist_class: $domain}` lookups in knowledge-gap detection scan Concept.

### What This Card Does

1. Bounds the lexical Message scan with a recency window: only scan Messages from the last N days (default 14) and/or the current session+quest, with a hard `LIMIT` — recency is the actual intent of the episodic fallback ("what did we just say about X?").
2. Probes whether the pinned Kuzu build ships the FTS extension; if yes, adds an FTS index on `Message.text_raw` behind a feature flag and uses it instead of CONTAINS.
3. Adds a scan-budget regression test asserting the lexical query includes both the recency predicate and LIMIT.
4. Documents which remaining scans are accepted (sweep-time scans are fine — they're background) in a comment block.

### Dependencies

- None.

### Files to Modify

- `campy/brain/thalamus/tools/__init__.py` — `current_truth` lexical fallback (~line 1208)
- `campy/brain/hippocampus/graph/kuzu_client.py` — optional `has_fts()` capability probe + FTS query method (only if extension available)
- `campy/brain/brainstem/config.py` — `retrieval.lexical_window_days` (14), `retrieval.lexical_limit` (10)
- `tests/test_lexical_fallback.py` — NEW

### Acceptance Criteria

- [ ] Lexical fallback query contains `created_at > timestamp($cutoff)` (or FTS CALL) and `LIMIT`
- [ ] Window and limit are config-driven with sane defaults
- [ ] If FTS unavailable (expected on 0.11.3): graceful CONTAINS-with-window path, no errors
- [ ] Behavior preserved for the recency use case: a Message from yesterday containing the query string still surfaces with `lexical_exact: true`
- [ ] Old Messages outside the window do NOT surface lexically (they can still surface via vector search — assert this in a test)
- [ ] `pytest tests/test_lexical_fallback.py tests/test_web.py -q` passes

### Outcome

The recall hot path stops scaling linearly with total conversation history; lexical recall does what it was designed for (recent episodic lookup) at bounded cost.
