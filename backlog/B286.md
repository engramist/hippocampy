# B286 - Consolidate Node Table Registry

State: complete — re-verified 2026-08-03 (caveat noted below): `campy/brain/hippocampus/table_registry.py` genuinely derives `TableInfo(name, pk, has_embedding, vector_index, tags)` from `schema.NODE_TABLES` DDL via regex parsing (PK and `embedding FLOAT[n]` detection), not a third hand-written list. All 5 named consumer sites confirmed importing from it: `sweep.py::SWEEP_TABLES` (`tables_with("sweepable")`), `explore_graph.py::_NODE_TABLES` (`tables_with("traversable")`), `warm_frontier.py::NODE_PK_MAP`/warm_seed (`tables_with("warmable"/"warm_seed")`), `retrieval.py::artifact_tables` (`tables_with("retrievable")`, formerly `thalamus/tools/__init__.py` pre-B292 split). Membership-preservation checked against git history, not just the card's prose estimate: diffed the registry's tag membership at the commit that introduced it (`b6d1a09`) against the pre-refactor hand-written lists (`git show b6d1a09^:...`) — exact match: sweepable=9, traversable=12, warmable=7, retrievable=10, all identical membership to before. (Today `retrievable` shows 12, not 10, but confirmed via `git log --oneline -- table_registry.py` that Plan/Procedure were added afterward by a separate, later, intentional card — `eb4c7a7 feat(retrieval): surface the plan lane in current_truth and compile_context (B299)` — not membership drift introduced by B286 itself.) Consistency test exists and passes for real: `pytest tests/test_table_registry.py -q` → 6 passed, including `test_every_embedding_table_registered`, `test_sweep_tables_match_legacy_literal`, `test_explore_tables_match_legacy_literal`, `test_warm_pk_map_matches_legacy_literal`, `test_retrievable_matches_current_truth_literal`, `test_pk_parsed_for_all_tables`. `pytest tests/ -q -k "sweep or explore or warm or truth or web"` → 165 passed. Card's own grep proof passes literally: `grep -rn "concept_emb_idx" campy/brain --include="*.py"` → zero hits anywhere (even cleaner than the card expected, since `_index_name()` generates index names dynamically rather than as any literal string). CAVEAT: a broader `grep -rn "_emb_idx" campy/brain --include="*.py"` surfaces one hand-written table list NOT wired to the registry — `campy/brain/thalamus/analogical.py`'s `CROSS_QUEST_TABLES` (Decision/Constraint/Requirement/GlobalConstraint/GlobalPreference/Lesson with literal index-name strings) — a 6th list of the same shape the card was written to prevent. This is out of the card's literally-stated scope (only 5 consumer sites were named, and analogical.py's list happens not to contain "concept_emb_idx" so the card's own specified grep check doesn't catch it), so this is a caveat rather than an unmet acceptance criterion, but worth a follow-up card to fold `CROSS_QUEST_TABLES` into the registry as a new tag (e.g. `cross_quest`) to close the drift risk the card exists to eliminate.
Priority: P2

Plan: backlog/plans/B-286-node-table-registry.md

### Problem

The `(table, pk_column, vector_index)` metadata for the graph's node tables is hand-maintained in at least five places, and they have already drifted:

- `hippocampus/schema.py` — `NODE_TABLES` DDL (source of truth, but stringly-typed)
- `brainstem/sweep.py` — `SWEEP_TABLES` (9 tables, includes index names)
- `thalamus/tools/explore_graph.py` — `_NODE_TABLES` (12 tables, includes Lesson + Document + quests)
- `temporal_lobe/warm_frontier.py` — `NODE_PK_MAP` (7 tables)
- `thalamus/tools/__init__.py` — `artifact_tables` lists inside `current_truth` and other tools (10 tables)

Adding a new artifact-bearing node table requires updating all five; missing one produces silent gaps (the new table is invisible to retrieval, traversal, or decay). Relationship types already solved this with `get_relationship_types()` derived from REL_TABLES (B72) — node tables need the same treatment.

### What This Card Does

Creates a single registry in `campy/brain/hippocampus/table_registry.py` derived from the schema DDL, exporting typed metadata per table (name, pk column, vector index name, has_embedding, capability tags like `sweepable`, `retrievable`, `traversable`). All five consumer sites import from it.

### Dependencies

- None. Coordinate with B280/B281/B282/B285 (they touch the same consumer files — land B286 first if possible, since the others can then use the registry).

### Files to Create

- `campy/brain/hippocampus/table_registry.py`
- `tests/test_table_registry.py`

### Files to Modify

- `campy/brain/brainstem/sweep.py` — `SWEEP_TABLES` becomes a registry query (keep the name as a module alias for compat)
- `campy/brain/thalamus/tools/explore_graph.py` — `_NODE_TABLES` from registry
- `campy/brain/temporal_lobe/warm_frontier.py` — `NODE_PK_MAP` from registry
- `campy/brain/thalamus/tools/__init__.py` — `artifact_tables` lists from registry

### Acceptance Criteria

- [ ] Registry is derived from `schema.py`'s `NODE_TABLES` dict (parses PK from DDL) — not a third hand-written list
- [ ] Each entry: `TableInfo(name, pk, vector_index, has_embedding, tags)` (dataclass or NamedTuple)
- [ ] A consistency test fails if a table in NODE_TABLES with an `embedding` column lacks a registry entry with `has_embedding=True`
- [ ] All five consumer sites import from the registry; their previous behavior is preserved exactly (same table sets — encode current membership as explicit tags, e.g. Lesson is `traversable` but not `sweepable`)
- [ ] Grep proves no orphaned hand-written lists remain: `rg "concept_emb_idx" campy/brain --include='*.py'` hits only the registry and schema
- [ ] Full test suite subset passes: `pytest tests/ -q -k "sweep or explore or warm or truth or web"`

### Outcome

Adding a node table is a one-place change; retrieval/traversal/decay coverage can no longer silently drift.
