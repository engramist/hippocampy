# B190: Cross-Puzzle Learning via Task Graph

**Status**: ready
**Assigned**: TBD
**Depends on**: B189 (scheduler for ordering)
**Complexity**: Medium (2-4 hours)
**Priority**: P3 (long-term knowledge accumulation)
**Plan**: backlog/plans/B-190-cross-puzzle-learning.md

## Problem Statement

Each puzzle is solved independently. When puzzle A is solved, the lessons learned (action semantics, entity roles, game rules) are not available to structurally similar puzzle B. The existing task graph API (`register_task_graph`, `get_ready_tasks`, `advance_task` from B127/B128) is built but never connected to the ARC runner.

Enterprise orchestration enables "merge and coordination" — work from one agent informs another. The ARC analog is cross-puzzle learning: solved puzzle lessons feed downstream similar puzzles via the knowledge graph.

## Root Causes

1. `BrainClientProtocol` defines task graph methods (lines 62-76 of adapter.py) but ARC runner never calls them
2. `analogical_search()` retrieves similar puzzles but only for archetype classification, not for strategy transfer
3. No mechanism to order puzzles so that solved ones inform unsolved similar ones

## What This Card Does

1. **Create a task graph** where each puzzle is a `TaskNode` with structural similarity edges
2. **Order execution** so structurally similar puzzles are adjacent (solver already uses `analogical_search`)
3. **After solving a puzzle**, persist lessons (successful strategy, discovered roles, game rules) as `Lesson` nodes linked to puzzle's task node
4. **Before solving a puzzle**, query for lessons from similar solved puzzles and inject into orchestrator context
5. Uses existing `BrainClientProtocol.register_task_graph()`, `advance_task()`, `recall_relevant_lessons()`

## Ecosystem Layer

- `agents/arc3/runner.py` (Agent Runtime — wire task graph to puzzle loop)
- `benchmarks/arc3/adapter.py` (Eval Infra — ensure task graph methods work in all clients)

**Cross-layer compliance**: No cross-layer violations. Uses existing tool handlers.

## Files to Create/Modify

- **MODIFY** `agents/arc3/runner.py` — Create task graph at batch start, advance tasks after each puzzle, query lessons before each puzzle
- **MODIFY** `benchmarks/arc3/adapter.py` — Ensure `NoOpBrainClient` and `LedgerBrainClient` handle task graph calls
- **CREATE** `tests/test_b190_cross_puzzle_learning.py` — Tests for lesson persistence, retrieval, and strategy transfer
- No changes to `docs/tool-catalog.md`

## Acceptance Criteria

- [ ] Task graph created at batch start with all puzzles as TaskNodes
- [ ] After solving puzzle A, lessons persisted as Lesson nodes
- [ ] Before solving puzzle B (similar to A), lessons from A retrieved and injected into context
- [ ] NoOpBrainClient handles task graph calls without error
- [ ] Solve rate improves for puzzles that have similar solved predecessors (measured over batch)
- [ ] All existing ARC tests pass

## Outcome

The agent accumulates knowledge across puzzles within a batch. Solving one puzzle makes similar puzzles easier. This is the "durable memory" concept — the agent gets smarter over time within a run, not just within a puzzle.
