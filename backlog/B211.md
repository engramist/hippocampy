# B211 — Structured ActionEffect Writes: Graph-Native Action Outcome Storage

- **State:** ready
- **Priority:** P1
- **Plan:** backlog/plans/B-211-structured-action-effect-writes.md
- **Ecosystem Layer(s):** Agent Runtime, SideQuests Brain (write path)
- **Cross-layer violations:** None — writes go through existing `brain.notify_turn` and `brain.upsert_lesson` MCP tools; no direct KuzuDB access from agent code.
- **Dependencies:** B213 (clean codebase), B202 (per-step perceive wiring).

## Problem Statement

The current `ingest_step` / `notify_turn` flow stores action outcomes as **narrative text strings**:

```
"[m0r0-dadda488:task123] Episode 1 · Step 3: ACTION2 at the grid sets None · ACTION1 changed 1 pixel."
```

SideQuests ingests this into `Concept` nodes via NER and consolidation. The result is semantic text that can be retrieved by text similarity — but not by **structural graph pattern**. You cannot ask:

```
"Find ActionEffect nodes where entity_type=COMPACT_OBJECT and action=INTERACT and effect_class=TRANSFORMATION"
```

Because those structured nodes don't exist. There is no graph path from "current puzzle has a compact non-background object" → "ACTION5 applied to compact objects causes transformation" → "this puzzle likely needs ACTION5."

The knowledge is there (the agent observed it) but it's stored in a form that requires the agent to already know the right text query to retrieve it. That's circular.

**Root cause:** `ActionEffect` nodes in the graph schema exist, but the agent writes them with insufficient structure. `ingest_step` emits a narrative that consolidates to Concept nodes — it never writes an `ActionEffect` with typed fields `entity_type`, `action`, `effect_class`, `spatial_role`, `prerequisite`.

## What It Does (Scope)

After every `record_step_result()` call (in `perceive_step_response()`, which runs post-EVALUATE), write a structured `ActionEffect` record to SideQuests via `upsert_lesson` with typed fields:

```python
{
    "lesson_type": "action_effect",
    "action": "ACTION5",
    "entity_type": "compact_object",    # from GridEntity role classification
    "entity_color": 10,
    "effect_class": "transformation",   # derived from FrameDelta
    "n_cells_changed": 48,
    "reward_signal": 0.0,
    "spatial_role": "trigger",          # from ObjectRoleMapper if available
    "puzzle_archetype": "space",
    "session_id": "...",
    "task_id": "...",
    "step": 5,
}
```

The `effect_class` is derived from existing `FrameDelta` fields already computed by `record_step_result()`:
- `n_cells_changed > 30` → `"large_transformation"`
- `n_cells_changed > 0 and apparent_effect != "no_effect"` → `"local_change"`  
- `n_cells_changed == 0` → `"no_effect"`
- `direction` present → `"directional_movement"`

The `entity_type` comes from `ObjectRoleMapper` roles already in `_solve_context["roles"]` — the entity at the position the action targeted.

This structured write happens **in addition to** the existing narrative `notify_turn`. It does not replace any existing write path.

### Not in scope
- Reading these records back (that's B212).
- Changing KuzuDB schema (ActionEffect node already exists in schema).
- Writing to KuzuDB directly from agent code (goes through `upsert_lesson` MCP tool).
- Inferring `entity_type` with new LLM calls (use existing `_solve_context` only).

## Files to Create/Modify

| File | Action |
|------|--------|
| `agents/arc3/orchestrator.py` | **Modify** — Add `_write_action_effect_record()` helper (~40 lines); call it from `perceive_step_response()` after the `notify_turn` call |

### Adapter/allow-list impact
- Uses existing `upsert_lesson` tool — already in the MCP surface.
- Add `"perceive"` to `upsert_lesson` allowed_phases in `_build_orchestration_report` (update B203's tool_rules patch if needed).

## Acceptance Criteria

1. After every step, `brain.upsert_lesson` is called with a dict containing `lesson_type="action_effect"`, `action`, `effect_class`, `n_cells_changed`, `step`, `task_id`.
2. `effect_class` is one of: `"no_effect"`, `"local_change"`, `"large_transformation"`, `"directional_movement"`.
3. When `_solve_context["roles"]` contains a role for the current player position or action target, `entity_type` and `spatial_role` are populated from it.
4. `upsert_lesson` is not called for step 0 (bootstrap perceive) — only for `step > 0`.
5. `pytest tests/test_arc3_orchestrator.py::test_perceive_step_response_writes_action_effect` passes.
6. Existing tests pass.

## Outcome

Every action outcome is stored in KuzuDB with typed structural fields. B212 can then query "find lessons where lesson_type=action_effect and entity_type=compact_object and effect_class=large_transformation" to retrieve grounded evidence for the hypothesis that compact objects are interaction triggers — without any puzzle-specific keywords.
