# B168: Graph-Based Exploration Agent — Build Neural Substrate Before Goal-Seeking

**Status**: ready
**Assigned**: TBD
**Depends on**: B150 (grid diff engine), B119 (bootstrap entity discovery), B166 (autopilot)
**Complexity**: Large (6–8 hours)
**Priority**: P0 (CRITICAL — without a knowledge foundation, all downstream heuristics operate blind)
**Plan**: backlog/plans/B-168-graph-exploration-agent.md

## Problem Statement

Our ARC agents fail not because the heuristics are wrong, but because they have **no knowledge foundation to draw from**. When a puzzle starts, the graph is empty of contextual data. The ObjectRoleMapper guesses "smallest = player, largest = goal" — a heuristic with zero grounding. After B167, the agent burned 119 steps with zero progress: it misidentified background (color 0) as player, never discovered intermediates, and had 0.0 pattern similarity on every step.

This is analogous to the human brain: a baby cannot solve a puzzle because it hasn't developed the neurons and pathways needed for heuristic decision-making. The baby builds that foundation through **exploration and curiosity**. We need to emulate this — treat our agents as babies that need an Explorer/Curiosity agent to build up nodes and relationships that other agents will use and build on.

The heuristics stay. They just need a neural substrate to work with.

## Root Causes

1. **No entity-level graph model**: Each color is analyzed in isolation via centroids. No entity relationships are captured (adjacency, containment, structural similarity).
2. **No behavioral exploration phase**: The agent jumps straight to goal-seeking at step 1. It never systematically tests what each action does to discover which entities are mobile, static, or interactive.
3. **Heuristic bootstrap is uninformed**: `seed_bootstrap_roles()` guesses player/goal from pixel count alone, producing wrong results on complex grids (e.g., color 0 = "player" at 0.90 confidence).
4. **No persistent puzzle understanding**: Even when the grid has rich structure (190 regions, 7 colors, symmetry), none of it is modeled in the knowledge graph for downstream agents to query.

## What This Card Does

### New: Explorer/Curiosity Agent with Two-Phase Exploration

**Phase 1 — Static Analysis** (pre-action, step 0, no steps consumed):
1. Extract all connected components from the initial grid → create `GridEntity` nodes in KuzuDB
2. Compute pairwise spatial relationships → `ADJACENT_TO`, `CONTAINS` edges
3. Compute structural similarity between regions → `STRUCTURALLY_SIMILAR` edges
4. Flag background entities (color 0 or >50% coverage)
5. Detect structural groups (entities that look alike = likely same semantic role)
6. Create a `GridSnapshot` node anchoring all entities at step 0

**Phase 2a — Deterministic Sweep** (~4–8 steps depending on available actions):
- Try each available action once and record what changes via frame diff
- For each action: create `ActionEffect` node, identify which entities moved/changed/appeared/disappeared
- Create behavioral edges: `MOVED_BY`, `RESPONDS_TO`, `BLOCKS`
- After sweep: graph knows which entities are mobile, static, interactive

**Phase 2b — LLM-Guided Follow-up** (~4 steps):
- Present graph summary to LLM with curiosity-focused prompt
- LLM picks targeted experiments to fill graph gaps
- Each result updates graph with new edges/properties

**Handoff**: Graph-inferred roles (player, goal, wall, intermediate) fed to existing ObjectRoleMapper as high-confidence starting points. Existing agents take over with rich knowledge substrate.

## Ecosystem Layer

- `agents/arc3` (ARC agent layer — entity_graph.py, runner.py, orchestrator.py)
- `mcp_engine` (Data Products & Memory — schema.py for node/relationship types)
- `benchmarks/arc3` (adapter.py — expose db property)

**Cross-layer compliance**: New `GridEntity`/`GridSnapshot`/`ActionEffect` nodes are Data Products (layer 5). The exploration agent logic lives in Agent Runtime (layer 2). Graph reads/writes go through `KuzuClient` (layer 4). No cross-layer violations.

## Files to Create/Modify

- **CREATE** `agents/arc3/entity_graph.py` — `EntityGraphBuilder` class with bootstrap, update, inference methods (~350 lines)
- **MODIFY** `mcp_engine/schema.py` — Add `GridEntity`, `GridSnapshot`, `ActionEffect` node types and 9 relationship types
- **MODIFY** `agents/arc3/runner.py` — Insert exploration phase between perceive and main action loop
- **MODIFY** `agents/arc3/orchestrator.py` — Add `merge_graph_roles()` method to accept graph-inferred roles
- **MODIFY** `benchmarks/arc3/adapter.py` — Expose `db` property on `BrainClientProtocol`, `NoOpBrainClient`, `LedgerBrainClient`
- **CREATE** `tests/test_b168_graph_exploration.py` — Tests for EntityGraphBuilder, inference queries, role merging
- No changes to `docs/tool-catalog.md` (no new MCP tools introduced; graph ops are internal to the agent)

## Acceptance Criteria

- [ ] `EntityGraphBuilder.bootstrap(grid, level, observation)` creates `GridEntity` nodes for all connected regions with correct properties (centroid, bbox, compactness, is_background)
- [ ] `ADJACENT_TO` edges exist between entities whose bounding boxes are within 2 cells
- [ ] `STRUCTURALLY_SIMILAR` edges exist between regions with similarity >= 0.5
- [ ] Background entities (color 0 or >50% grid coverage) have `is_background = true` and are never inferred as player
- [ ] After deterministic sweep, `MOVED_BY` edges correctly identify which entities moved in response to which actions
- [ ] `infer_player()` returns the most mobile non-background entity (not color 0)
- [ ] `infer_walls()` returns large, elongated, stationary entities
- [ ] `infer_intermediates()` returns small, stationary, structurally-grouped entities
- [ ] Graph-inferred roles are merged into ObjectRoleMapper's output with higher confidence winning
- [ ] Exploration phase consumes ≤ 12 steps (< 10% of typical 119+ budget)
- [ ] When `_entity_graph` is None (NoOp mode), all graph operations are skipped — existing heuristic path unchanged
- [ ] All existing ARC tests pass (`pytest tests/test_arc3_*.py tests/test_b16*.py tests/test_b167*.py -q`)
- [ ] Smoke test shows player != color 0 and exploration data in trace

## Outcome

The agent builds a knowledge substrate through deliberate exploration before attempting to solve the puzzle. Entity relationships, behavioral patterns, and structural groups are modeled in the graph. Downstream heuristics (ObjectRoleMapper, autopilot, strategy selection) finally have a neural base to draw from — like a baby that has explored enough to form the pathways needed for puzzle-solving.
