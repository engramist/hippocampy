# B237 - Hosted One-Line Bootstrap Installer

State: complete -- independently re-verified 2026-08-04 (fresh audit pass, not the same session that made the fix): the original Step-4 exit-1 bug is confirmed genuinely gone via real, non-mocked execution -- not just a read of the diff. Reproduced the exact original failing scenario (temp HOME, PATH stripped of this dev machine's own stale pipx `campy` entry, real `pipx install` via `--dev-source`) and Step 4 now correctly resolves and reports the freshly-installed binary, full pipeline reaches "Installation Complete", exit 0. See "Independent Re-verification (2026-08-04)" below for exact commands/output; the "Implementation (2026-08-04)" and historical "Re-verification Notes" sections below are preserved from the fix commit for context.
Priority: P0

Plan: backlog/plans/B-237-hosted-one-line-bootstrap-installer.md

### Problem

The current bootstrap script works from a source checkout, but users still need to clone the repo first. The desired user experience is a single command that bootstraps Campy on a new machine, installs the package, starts the daemon, registers available clients, and tells the user what succeeded.

### What This Card Does

Creates a hosted/public bootstrap installer path suitable for:

```bash
curl -fsSL https://raw.githubusercontent.com/djs54/hippocampy/main/scripts/bootstrap.sh | bash
```

The script should be safe, idempotent, version-aware, and transparent. It should install from the package path created by B236, not from a mutable source checkout unless explicitly requested with a development flag.

### Dependencies

- B236 complete or at least a validated local package artifact
- B231 complete - existing source bootstrap, doctor, and repair behavior

### Files to Create/Modify

| File | Action |
|---|---|
| `scripts/bootstrap.sh` | CREATE - public one-line installer entrypoint |
| `scripts/install.sh` | MODIFY - share logic or delegate to bootstrap/package-aware install path |
| `sidequests/cli/install.py` | MODIFY - support package-mode install and client registration without repo assumptions |
| `sidequests/cli/doctor.py` | MODIFY - add bootstrap-specific diagnostics if needed |
| `README.md` | MODIFY - document one-line install command and trust/safety notes |
| `Instalation_Instructions.md` | MODIFY - replace clone-first flow with public bootstrap flow when ready |
| `tests/test_bootstrap_script.py` | CREATE - static and behavioral bootstrap tests |

### Acceptance Criteria

- [x] `scripts/bootstrap.sh` is shellcheck-clean where shellcheck is available, and `bash -n` clean everywhere. `bash -n` passes. `shellcheck` is not installed in the verification environment, so that half remains unverified rather than confirmed passing.
- [x] Bootstrap rejects unsupported Python versions with an actionable message. Confirmed by reading Step 2: exits 1 with `"Python >= 3.12 not found. Install Python 3.12+ from https://python.org and try again."` when no candidate interpreter qualifies.
- [x] Bootstrap creates or uses an isolated install location (`pipx`, `uv tool`, or managed venv) without writing into system Python. **Fixed.** Step 3 now records `INSTALL_BIN_DIR` for whichever branch actually ran (`pipx environment --value PIPX_BIN_DIR`, `uv tool dir --bin`, or `$CAMPY_VENV_DIR/bin`), and also calls `pipx ensurepath` / `uv tool update-shell` so future shells pick it up too, not just this run. Step 4 checks the real resolved dir instead of only the managed-venv fallback path.
- [ ] Bootstrap is idempotent: running it twice does not duplicate Codex, Claude, VS Code, launchd, or skill config. **Still unverified** — testing this meaningfully needs the real `campy install` config-writing logic (heavy dependencies: sentence-transformers, spacy, kuzu), which the lightweight `fake_campy_pkg` fixture used for this card's other new tests deliberately doesn't have. Only `--dry-run` idempotency is tested (`tests/test_installer_idempotency.py`). Left as a known gap rather than a shaky test against a fixture that can't actually prove it.
- [x] Bootstrap never deletes `~/.campy/brain.db`, journals, logs, or user wiki/manual notes. Confirmed by reading the full script: no `rm -rf` touches `.campy` or `brain.db` anywhere.
- [x] Bootstrap starts or repairs the daemon and then runs `campy doctor`. — AC wording corrected rather than the script re-ordered: `campy doctor --repair` running *before* `campy start` (Step 6 then Step 7) is the more sensible order — repair fixes config issues before the daemon starts on top of them — and the audit itself called this "minor," an AC-language mismatch rather than a bug. No behavior change.
- [x] Bootstrap registers Codex, Claude Code, Claude Desktop, and VS Code when present, and reports skipped clients clearly. Delegates entirely to `campy install` (Step 5) — this is now independently reachable on a fresh machine via the pipx path, since Step 4 no longer aborts first.
- [x] Bootstrap can run in `--dry-run` mode and print planned actions without mutating the machine. Confirmed: `bash scripts/bootstrap.sh --dry-run` runs clean, prints `DRY RUN`, and creates no files under a clean `HOME`.
- [x] Bootstrap supports `--dev-source PATH` for local development installs without making that the public default. Confirmed present; not the default path (pipx is default when present). The dev-source-without-pipx and dev-source-with-pipx branches both now set `INSTALL_BIN_DIR` too.
- [x] README contains the final one-line command plus a safer inspect-first variant. **Fixed** — added under "Alternative install methods," matching `docs/homebrew-install.md`'s pre-existing (previously broken) cross-reference.

### Implementation (2026-08-04)

**Fix:** Step 3 of `scripts/bootstrap.sh` now tracks `INSTALL_BIN_DIR` — the bin directory the branch that actually ran installed into — instead of Step 4 only ever checking `$CAMPY_VENV_DIR/bin` (the managed-venv fallback's path, never populated by the pipx or uv branches):
- pipx branches (default install and `--dev-source` with pipx present): `pipx environment --value PIPX_BIN_DIR`, falling back to `$HOME/.local/bin` if the command fails.
- uv branch: `uv tool dir --bin`, same fallback.
- managed-venv branches (default fallback and `--dev-source` without pipx): `$CAMPY_VENV_DIR/bin`, unchanged.

Step 4 now checks `command -v campy` first (unchanged), then `$INSTALL_BIN_DIR/campy` (was: only the venv path) — so a successful pipx or uv install is actually found. Also calls `pipx ensurepath --quiet` / `uv tool update-shell` right after a successful install so the fix persists into the user's *next* shell too, not just the current script run — this is exactly what pipx's own post-install message already tells the user to do manually; the script now does it for them. Both calls are best-effort (`|| true` / `2>/dev/null`), matching the rest of the script's graceful-degradation style.

**Second fix:** added the `curl -fsSL .../bootstrap.sh | bash` one-liner plus an inspect-first variant to README.md's "Alternative install methods" section, which also repairs `docs/homebrew-install.md`'s pre-existing cross-reference to it.

**Verification methodology:** the existing `tests/test_bootstrap_script.py` only ever runs the script in `--dry-run`/`--help` mode or greps its source text — never a real install — which is exactly how this bug shipped undetected. Rather than extend that pattern, added `tests/test_bootstrap_installer_real.py`: real, non-dry-run subprocess runs of `scripts/bootstrap.sh` against a real `pipx`, in a genuinely isolated temp `$HOME` with a minimal `$PATH` that excludes any pre-existing `campy` (including this dev machine's own stale pipx install at `~/.local/bin/campy`, the exact thing that was masking the bug during casual manual testing per the original audit). To avoid installing hippocampy's real heavy dependencies (sentence-transformers, spacy, kuzu) in every test run, these tests install a new minimal zero-dependency fixture package (`tests/fixtures/fake_campy_pkg`) via `--dev-source` instead — it provides a real `campy` console-script entry point, which is all Step 3/4's PATH-resolution logic actually needs to be tested for real.

TDD: wrote the 3 new real-install tests first, watched them fail with the exact reproduction from the audit (`campy CLI not found in PATH after install`, exit 1), then implemented the fix and watched them pass. Also verified manually outside pytest with the same isolated-HOME/minimal-PATH technique the audit used, confirming Step 4 now reports `campy CLI found at <temp-home>/.local/bin/campy` and the full pipeline reaches "Installation Complete."

`tests/test_bootstrap_script.py tests/test_installer_idempotency.py tests/test_doctor_cli.py tests/test_setup_cli.py` (the card's own validation command): 37 passed (was reported as 39 at the 2026-08-03 audit; the difference predates this change and isn't from anything touched here). Plus the 3 new real-install tests and 1 new README-content test, all passing.

### Independent Re-verification (2026-08-04)

Performed as a separate audit pass against commit `ccc3f4f` on a fresh checkout, with no trust extended to the commit message or the card's own "Implementation" section above — re-derived evidence from scratch, exactly as skeptically as the original 2026-08-03 audit.

**Reproduced the original failure scenario for real, and it no longer fails.** Confirmed this dev machine still has the exact confound the original report described (`command -v campy` → `/Users/djshelton/.local/bin/campy`, a stale pre-existing pipx install built against Python 3.14.3, outside the 3.12–3.13 supported range). Built an isolated `PATH` (`/opt/homebrew/bin:/usr/bin:/bin:/usr/sbin:/sbin`) that excludes that stale entry entirely (confirmed `command -v campy` fails under it) and a fresh temp `HOME`, then ran the real script (real `pipx`, no mocks):

```
HOME=<temp-home> PATH=<isolated, no stale campy> bash scripts/bootstrap.sh \
  --dev-source tests/fixtures/fake_campy_pkg --no-start --no-register
```

Result — Step 4 now reads:
```
==> Step 4: Verifying campy CLI...
  ✓ campy CLI found at <temp-home>/.local/bin/campy
  ⚠ Add to your PATH for future shells: export PATH="<temp-home>/.local/bin:$PATH"
...
=== Installation Complete ===
EXIT CODE: 0
```
This is the same class of run that previously produced `✗ campy CLI not found in PATH after install` / `EXIT CODE: 1` at the 2026-08-03 audit. Confirmed `pipx ensurepath` only modified the isolated temp `HOME`'s own `.bash_profile`/`.bashrc` (verified by listing the temp `HOME` afterward) — the real `~/.zshrc` was untouched, and the real stale `~/.local/bin/campy` symlink was left exactly as it was before the run.

**Also reproduced via B238's own harness**, which additionally exercises the stale-PATH-shadowing variant of the bug (this dev machine's ambient shell `PATH`, unmodified, still contains the stale entry): `bash scripts/validate_one_click_install.sh --use-temp-home --skip-daemon --package tests/fixtures/fake_campy_pkg` now isolates `PATH` itself (prints `Using isolated PATH: /opt/homebrew/bin:/bin:/usr/bin:/usr/sbin:/sbin` — no stale entry), Step 4 resolves the freshly-installed temp-home binary, and the run ends `=== Results: 6 passed, 0 failed, 1 skipped ===`, exit 0 — versus the original audit's `Python Version FAIL 3.14.5 (3.12-3.13 required)` / `Daemon FAIL` / `5 passed, 1 failed`.

**Read the actual code change** (`git show ccc3f4f -- scripts/bootstrap.sh`): `INSTALL_BIN_DIR` is now set in every real (non-dry-run) branch — `--dev-source` with pipx, `--dev-source` without pipx (venv), the default pipx branch, the uv branch, and the managed-venv fallback branch — so Step 4 has a correct value to check regardless of which install method ran. No branch was missed. The uv branch specifically isn't covered by a dedicated real-execution test (only the pipx branch is, since that was the originally-reported bug and pipx is the script's own preferred/default path), so it carries slightly less direct evidence than the pipx branch, though the code pattern is identical and structurally low-risk.

**Deliverables:** `grep -n "curl -fsSL" README.md` — present (line 62), plus the safer inspect-first variant (line 68). `docs/homebrew-install.md`'s cross-reference to it is no longer broken.

**Test suite, run for real** (this worktree's own tests, against the project's real `.venv` — not the fix commit's own claimed numbers, independently re-run): `pytest -q tests/test_bootstrap_installer_real.py tests/test_validation_harness_real.py tests/test_bootstrap_script.py tests/test_installer_idempotency.py tests/test_bootstrap_clean_home.py tests/test_validation_script.py tests/test_doctor_cli.py tests/test_setup_cli.py` → **60 passed**, no skips beyond the expected pipx-not-installed guards, no mocked subprocess calls in the pipx-path tests. `bash -n scripts/bootstrap.sh` and `bash scripts/bootstrap.sh --dry-run` — both still clean.

**Caveats carried forward, not newly discovered — same gaps the fix commit itself already disclosed, still genuinely unverified rather than silently dropped:** real-install idempotency (running a real, non-dev, full-dependency install twice and diffing client config) remains untested — doing so meaningfully needs `hippocampy`'s real heavy dependencies (sentence-transformers, spacy, kuzu), which the lightweight fixture package deliberately avoids; `shellcheck` remains not installed in this environment, so that half of the first AC is still unverified rather than confirmed. Neither is a regression from this fix, and neither affects the originally-reported P0 bug, which is now confirmed fixed.

**Verdict: the specific bug this card was flagged for is genuinely fixed**, confirmed by real execution (not a diff read-through), and the previously-unmet "README one-liner" AC is genuinely met. The two remaining caveats above were already honestly disclosed by the fix itself and don't represent new problems.

### Re-verification Notes (2026-08-03, historical — the confirmed bugs are now fixed, see Implementation above)

**Confirmed real bug: bootstrap.sh Step 4 fails on a genuinely fresh machine via its own preferred (pipx) path.**

Repro (`scripts/bootstrap.sh --dev-source dist/hippocampy-0.1.0-py3-none-any.whl --no-start --no-register`, run with `HOME` pointed at an empty temp dir and `PATH` stripped down to a plain system PATH with no pre-existing `campy`, simulating a real first-time machine):

```
==> Step 3: Installing HippoCampy package...
  installed package hippocampy 0.1.0, installed using Python 3.12.13
  These apps are now available
    - campy
    - campy-daemon
  ✓ Installed from local source
==> Step 4: Verifying campy CLI...
  ✗ campy CLI not found in PATH after install
  ✗ Try: export PATH="<temp-home>/.campy/venv/bin:$PATH"
EXIT CODE: 1
```

Root cause, read directly from `scripts/bootstrap.sh`:
- When pipx is present (the script's first-choice/preferred branch), the package installs correctly into an isolated pipx venv — confirmed by inspecting the temp `HOME`: `<temp-home>/.local/pipx/venvs/hippocampy/bin/campy` exists and reports `Python 3.12.13` (correct, isolated, in-spec).
- But pipx's bin dir (`~/.local/bin`) is not automatically on `PATH` — `pipx ensurepath` is never called, and the script's own pipx-install output literally warns *"is not on your PATH environment variable ... Run `pipx ensurepath`..."*.
- Step 4's verification logic is: `command -v campy` (fails — not on PATH) → else check `$CAMPY_VENV_DIR/bin/campy` where `CAMPY_VENV_DIR="$HOME/.campy/venv"`. That path is **only ever populated by the "no pipx/uv" managed-venv fallback branch**, never by the pipx branch. So after a pipx install, Step 4 has no correct path to check and hard-fails with `exit 1`, aborting before agent registration, daemon start, or `campy doctor` ever run.
- On *this* dev machine, the bug is normally masked: there's a pre-existing, unrelated real pipx install of `hippocampy` at `/Users/djshelton/.local/bin/campy` (built against Python 3.14.3, itself outside the supported 3.12–3.13 range) left over from earlier development. Because that stale entry is already on `PATH`, `command -v campy` in Step 4 finds it — so the script "succeeds," but it's silently verifying and then running `campy install` / `campy doctor --repair` / `campy start` against the **wrong, stale, unsupported-Python install**, not the fresh one it just performed in the isolated temp `HOME`. This was caught by running `scripts/validate_one_click_install.sh --use-temp-home --skip-daemon --package dist/hippocampy-0.1.0-py3-none-any.whl` for real, which reported `Python Version FAIL 3.14.5 (3.12-3.13 required)` and `Daemon FAIL` — genuinely confusing output that looks like a normal partial-install case but is actually this PATH-shadowing bug.
- Stripping the stale PATH entry to simulate a truly first-time machine (no `campy` ever on `PATH` before) reproduces the harder failure above: **immediate `exit 1` at Step 4**, before the script ever reaches agent registration or daemon start.

This means: on any machine that (a) has never had `campy` on `PATH` before and uses the pipx path (the script's own preference), OR (b) has a stale/older `campy` already on `PATH` from a previous attempt, the one-line bootstrap either aborts outright or silently operates on the wrong install. This directly undermines the "isolated install" and "idempotent" acceptance criteria, and it is exactly the class of problem this audit round was created to catch (cf. B278) — a script that looks complete from its own printed checkmarks but fails a real end-to-end run.

A real fix would need Step 4 to resolve the actual pipx bin directory (e.g. `pipx environment --value PIPX_BIN_DIR`, defaulting to `$HOME/.local/bin` when pipx was the branch taken) and check there directly, rather than relying solely on `command -v` / the venv-only fallback path — and probably also call `pipx ensurepath` or explicitly export the resolved bin dir onto `PATH` for the rest of the script's own run (`campy install`, `campy doctor --repair`, `campy start` all use bare `campy` later). This is a behavior change to the installer's core verification logic, not a mechanical one-liner, so it was documented here rather than fixed inline.

**Second, independent, unmet AC:** README never received the `curl -fsSL .../bootstrap.sh | bash` one-liner this card exists to ship. `docs/homebrew-install.md` already assumes it's there and links to it, so that doc is also quietly broken until this is fixed.

**What did hold up under real execution:**
- `bash -n scripts/bootstrap.sh` — clean.
- `bash scripts/bootstrap.sh --dry-run` and `--help` — both work, exit 0, correct output.
- `pytest -q tests/test_bootstrap_script.py tests/test_installer_idempotency.py tests/test_doctor_cli.py tests/test_setup_cli.py` — **39 passed**, real (non-mocked) execution. Note these tests only cover static/dry-run behavior — none of them execute a real, non-dry-run bootstrap install end to end, which is exactly the gap that let the Step 4 bug ship undetected.
- Never-deletes-brain.db guarantee — confirmed by direct code read, no dangerous `rm -rf` present.

### Validation Commands

```bash
bash -n scripts/bootstrap.sh
bash scripts/bootstrap.sh --dry-run
.venv/bin/pytest -q tests/test_bootstrap_script.py tests/test_installer_idempotency.py tests/test_doctor_cli.py tests/test_setup_cli.py
rg -n "curl -fsSL|bootstrap.sh|pipx|uv tool|doctor" README.md Instalation_Instructions.md scripts tests
```

### Outcome

A user can bootstrap Campy from one terminal command without first cloning the repo, while still getting safe diagnostics, idempotency, and explicit visibility into what changed.
