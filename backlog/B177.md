# B177: No-Progress Escalation Resets Without Triggering Dissonance — Fix Tier Ladder

**Status**: ready
**Assigned**: TBD
**Depends on**: None
**Complexity**: Small (1-2 hours)
**Priority**: P1 (root cause: 146 zero-reward steps without B142 evidence floor firing)
**Plan**: backlog/plans/B-177-no-progress-dissonance-trigger.md

## Problem Statement

The no-progress escalation ladder in `orchestrator.py:1674-1702` has three tiers:
- Tier 1 (3 steps): sets `_solve_context["dissonance"] = True` and emits `force_replan`
- Tier 2 (5 steps): blocks the last action via `_blocked_actions.add()`
- Tier 3 (8 steps): abandons chunk and **resets `_consecutive_no_progress_steps` to 0**

The reset at tier 3 (line 1702) means the ladder restarts from zero every 8 steps. Over 146 steps, the ladder cycled ~18 times but never triggered a meaningful behavior change because:

1. **Tier 1 dissonance is ignored**: Setting `_solve_context["dissonance"] = True` at line 1677 only affects the *next* `_should_check_loop` evaluation — it doesn't reach the SolveEngine's `DissonanceDetector`. The SolveEngine has its own `should_replan` logic at `solver.py:2384` that operates independently.
2. **Tier 2 blocks one action but oscillation continues**: Blocking ACTION1 just shifts to ACTION4, which then gets blocked, shifting back to ACTION1 (previous block expires when counter resets at tier 3).
3. **Tier 3 reset prevents escalation**: The counter never accumulates past 8, so the system never triggers stronger interventions.

## Root Causes

1. `_solve_context["dissonance"]` (tier 1) is not connected to `SolveEngine.DissonanceDetector.update()` — they're parallel systems
2. Tier 2 blocks are ephemeral — `_blocked_actions` isn't checked by autopilot or plateau lock
3. Tier 3 resets the counter, preventing cumulative escalation
4. No absolute step-count threshold triggers a strategy reset

## What This Card Does

1. **Connect tier 1 to SolveEngine dissonance**: When tier 1 fires, set a flag that SolveEngine reads in `solve()` to force `should_replan = True`
2. **Make tier 2 blocks persistent and checked by autopilot**: `_try_autopilot()` must skip blocked actions; blocks persist until a positive reward is received
3. **Replace tier 3 reset with escalating intervention**: Instead of resetting to 0, escalate to "full strategy reset" — clear plateau lock, clear autopilot blocked axes, force LLM replan with explicit "all prior approaches failed" context
4. **Add absolute threshold** (e.g., 20 cumulative zero-progress steps): Trigger archetype re-evaluation and role confidence downgrade

## Ecosystem Layer

- `agents/arc3/orchestrator.py` (Agent Runtime — no-progress escalation)
- `agents/arc3/solver.py` (Agent Runtime — dissonance detector input)

**Cross-layer compliance**: No cross-layer violations. Both files are Agent Runtime.

## Files to Create/Modify

- **MODIFY** `agents/arc3/orchestrator.py` — Fix escalation tiers, connect to SolveEngine, persist blocked actions, add absolute threshold
- **MODIFY** `agents/arc3/solver.py` — Accept orchestrator dissonance signal in `solve()` method
- **CREATE** `tests/test_b177_no_progress_escalation.py` — Tests for tier behavior, dissonance propagation, persistent blocks, absolute threshold
- No changes to `docs/tool-catalog.md`

## Acceptance Criteria

- [ ] Tier 1 dissonance flag reaches SolveEngine's `solve()` and forces `should_replan = True`
- [ ] Tier 2 blocked actions are checked by `_try_autopilot()` — autopilot skips blocked actions
- [ ] Tier 3 does NOT reset counter to 0; instead escalates to full strategy reset
- [ ] Absolute threshold (20+ cumulative) triggers archetype re-evaluation
- [ ] Blocked actions persist until positive reward (not until counter reset)
- [ ] Trace events show escalation tier and intervention taken
- [ ] All existing ARC tests pass

## Outcome

The escalation ladder produces real behavior changes instead of cycling endlessly. After 20 zero-progress steps, the agent has tried all actions, reset its strategy, and re-evaluated its archetype — instead of repeating the same two actions 73 times each.
