# B126 - ARC Verification Sub-Agent for Decision Guard

State: complete
Completed: 2026-04-01
Validation: 151/151 tests passed (test_b126_verification.py + orchestrator + solver + B118 + B111 + B124 + B92 regressions)

Plan: backlog/plans/B-126-verification-sub-agent.md

Ecosystem Layer: Runtime (Layer 3) — ARC Agent reasoning

### Problem

The current Decision Guard (B115) is a rules-based filter — it checks for known loops and guidance
violations. But it cannot reason about *novel* failure modes. The guard and the solver share the
same LLM prompt, so the model tends to confirm its own choice rather than challenge it.

Adversarial verification (a separate prompt whose only job is to find reasons the move might fail)
is a well-known technique for improving decision quality.

### What This Card Builds

A dedicated verification sub-prompt within the Decision Guard:

1. After the solver proposes a candidate action, a separate "Verifier" prompt is constructed.
   The Verifier sees: the candidate action, the mental sandbox results (if any), the current
   observation, and the last 3 action outcomes.
2. The Verifier's only job is to output a rejection reason or "APPROVE." It does not propose
   alternatives.
3. If rejected, the solver gets one retry with the rejection reason appended to context.
   If rejected twice, the original candidate executes (bounded cost).
4. Verifier is a single LLM call with a short system prompt — no tool access, no sandbox.
5. Verification result appears in `thinking_trace` and `decision_guard_log`.

### Depends on

- B115 (Decision Guard must exist as the integration point)
- B114 (Mental Sandbox — Verifier consumes sandbox results)

### Priority

P2 — Separation of concerns: solver thinks of moves, verifier tries to break them.

## Files to Create/Modify

- `agents/arc3/orchestrator.py` — **MODIFY**: add `_verify_candidate_action()` method, integrate into decision guard flow
- `agents/arc3/prompts.py` — **MODIFY**: add `VERIFIER_SYSTEM_PROMPT` and `VERIFIER_PROMPT_TEMPLATE` (Layer 4 compliant)
- `tests/test_arc3_orchestrator.py` — **MODIFY**: test verifier approval, rejection+retry, double-rejection fallthrough

## Acceptance Criteria

1. After solver proposes an action, a separate Verifier prompt evaluates it.
2. Verifier can return "APPROVE" or a rejection reason (no alternative actions proposed).
3. On rejection, solver retries once with rejection context.
4. On double rejection, original action executes (no infinite loops).
5. Verifier results appear in `thinking_trace` and are exported in debug output.
6. Verifier adds at most 1 additional LLM call per decision (2 on rejection).
7. All existing tests pass unchanged.

## Outcome

The agent's decisions are stress-tested by an adversarial prompt before execution,
catching novel failure modes that rules-based guards miss.
