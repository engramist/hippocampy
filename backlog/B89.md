# B89 - ARC Prompt Budget and Retrieval Budget Benchmark

State: complete

Plan: backlog/plans/B-89-arc-prompt-budget-benchmark.md

### B89 · ARC Prompt Budget and Retrieval Budget Benchmark

**Problem:** We now have a clearer prompt philosophy for ARC, but we need hard evidence that smaller prompts, richer first-input structure, and bounded retrieval actually improve the agent. Recent live runs showed that prompt shape changes can materially reduce runtime, but action quality still stalls without a repeatable way to compare prompt budgets, retrieval budgets, and initial observation detail. Without a benchmark, prompt changes are opinion-driven instead of evidence-driven.

**What this card builds:** A lightweight benchmarking and reporting pass for ARC prompt strategy. This card measures the effect of prompt compression and retrieval budgets on:
- input token count
- runtime per puzzle
- action validity
- number of no-op or no-progress steps
- final puzzle outcome
- first-input richness
- retrieval payload size per trigger
- whether the prompt asks for a decision from observed effects versus generic pattern talk

**Scope:**
- Add prompt-budget metrics to the ARC single-run and/or benchmark path
- Produce a small comparison report between the legacy-style prompt shape and the compressed prompt shape
- Compare a compact baseline prompt against a more detailed first-input shape so we can see whether more structured initial context helps retrieval without reintroducing bloat
- Establish target budgets that future ARC prompt changes must respect

**Depends on:** B87 (orchestrator ✅)

## Files to Create/Modify

- `benchmarks/arc3/model_eval.py`
- `benchmarks/arc3/README.md`
- `benchmarks/arc3/PROMPT_STRATEGY.md`
- `docs/token-efficiency-side-effect.md`
- `tests/test_arc3_durable_runner.py`

## Acceptance Criteria

1. ARC runs record prompt-shape metrics that distinguish compressed vs. expanded prompting
2. The report includes at least:
   - tokens input
   - runtime seconds
   - step count
   - invalid-action count
   - no-progress step count
   - first-input detail level or proxy metric
3. At least one reproducible comparison is documented for puzzle 1
4. A target prompt budget and retrieval budget are written into ARC docs
5. Tests cover any new metrics or result fields added by this card

## Outcome

ARC prompt design becomes measurable. Future prompt and retrieval changes can be evaluated against a concrete token/runtime/action-quality baseline instead of intuition alone.

## Validation Note (2026-03-31)

- Implemented files: `benchmarks/arc3/model_eval.py`, `benchmarks/arc3/README.md`, `benchmarks/arc3/PROMPT_STRATEGY.md`, `docs/token-efficiency-side-effect.md`, `tests/test_arc3_durable_runner.py`
- Tests run:
  - `python3 -m py_compile benchmarks/arc3/model_eval.py`
  - `.venv/bin/pytest -q tests/test_arc3_durable_runner.py`
  - `.venv/bin/pytest -q tests/test_arc3_hypothesis.py tests/test_arc3_orchestrator.py tests/test_arc3_durable_runner.py tests/test_arc3_solver.py`
- Result: `126 passed, 1 warning` on final integration pass
- Regression checks: exported ARC rows preserve benchmark metrics and compact-vs-rich prompt comparisons are now reproducible from puzzle-1 result rows
