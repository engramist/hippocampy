# B184: Circuit Breaker for LLM Calls

**Status**: ready
**Assigned**: TBD
**Depends on**: None
**Complexity**: Small (1-2 hours)
**Priority**: P1 (prevents puzzle crashes from LLM flakiness)
**Plan**: backlog/plans/B-184-circuit-breaker.md

## Problem Statement

LLM calls in the orchestrator have no retry or failure isolation. If Ollama times out or returns an error, the entire puzzle crashes with an unhandled exception. There's no exponential backoff, no circuit breaker, and no graceful degradation.

The orchestrator makes LLM calls via `self.llm.chat()` in multiple places (solve prompts, victory hypothesis, game rule hypothesis, REPL verification). Each is a potential crash point.

## Root Causes

1. No retry logic around `self.llm.chat()` calls
2. No circuit breaker to prevent cascading failures when LLM is down
3. No exponential backoff — failed calls are not retried at all
4. A single LLM timeout kills the entire puzzle solve

## What This Card Does

1. **New `CircuitBreakerLLMClient` wrapper** in `agents/arc3/circuit_breaker.py`
2. Three states: CLOSED (normal), OPEN (fail-fast for N seconds), HALF_OPEN (allow one probe)
3. Exponential backoff on retries: 1s, 2s, 4s, max 3 retries
4. Wraps the existing `llm_client` injected at `orchestrator.py:129`
5. When circuit is OPEN, returns a safe default response instead of crashing

## Ecosystem Layer

- `agents/arc3/circuit_breaker.py` (Agent Runtime — new file)
- `agents/arc3/orchestrator.py` (Agent Runtime — wrap llm_client)

**Cross-layer compliance**: No cross-layer violations.

## Files to Create/Modify

- **CREATE** `agents/arc3/circuit_breaker.py` — CircuitBreakerLLMClient with states, backoff, retry logic
- **MODIFY** `agents/arc3/orchestrator.py` — Wrap `self.llm` with `CircuitBreakerLLMClient(llm_client)` in `__init__`
- **CREATE** `tests/test_b184_circuit_breaker.py` — Tests for state transitions, backoff timing, safe defaults
- No changes to `docs/tool-catalog.md`

## Acceptance Criteria

- [ ] After 3 consecutive failures, circuit opens (fail-fast for 30 seconds)
- [ ] After cooldown, circuit enters HALF_OPEN and allows one probe call
- [ ] Successful probe closes circuit (back to normal)
- [ ] Exponential backoff: 1s, 2s, 4s between retries
- [ ] When circuit is open, returns safe default (empty response, no crash)
- [ ] Trace events log state transitions (CLOSED→OPEN, OPEN→HALF_OPEN, etc.)
- [ ] All existing ARC tests pass

## Outcome

LLM flakiness no longer crashes puzzles. Transient failures are retried with backoff. Sustained outages are detected and fail-fast, saving time. The supervisor (B183) can rely on robust LLM calls.
