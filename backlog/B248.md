# B248 - Update ARC_AGI Consumer to HippoCampy/Campy Namespace

State: done
Priority: P0

Plan: backlog/plans/B-248-update-arc-agi-consumer-to-hippocampy-campy.md

### Problem

ARC_AGI is the known external consumer that still calls SideQuests/`sidequests` integration paths. As HippoCampy migrates public names, import namespaces, MCP server names, CLI commands, and config filenames, ARC_AGI must be updated deliberately so ARC smoke tests and live benchmark workflows continue to use the shared memory system.

### What This Card Does

Defines the cross-repo migration work for the sibling `ARC_AGI` repository. ARC_AGI should prefer Campy/HippoCampy names for new calls while remaining compatible with older local SideQuests installs during the transition.

### Dependencies

- B241 complete - HippoCampy/Campy public branding and CLI aliases exist
- B242 recommended - `campy` Python import namespace shim exists
- B244 recommended before final cutover - MCP/installer module paths use `campy.*`
- ARC_AGI repository available as a sibling repo at `../ARC_AGI`

### Files to Inspect in ARC_AGI

| Path Pattern | Reason |
|---|---|
| `run_single_puzzle.py` | Live smoke runner and brain socket/MCP integration |
| `benchmarks/**/*.py` | ARC benchmark consumers and memory calls |
| `tests/**/*.py` | Tests expecting `sidequests` or old MCP paths |
| `README.md`, `docs/**` | User-facing setup instructions |
| `.env*`, config templates | Old socket/config/env names |
| `pyproject.toml`, scripts, Makefile | CLI/module invocation references |

### Acceptance Criteria

- [ ] ARC_AGI prefers `campy` CLI commands in docs and scripts.
- [ ] ARC_AGI prefers `campy` MCP server name where it configures or documents MCP usage.
- [ ] ARC_AGI supports `CAMPY_BRAIN_SOCKET`, `CAMPY_SOCKET_PATH`, or current Campy transport defaults.
- [ ] ARC_AGI keeps fallback support for legacy `SIDEQUESTS_BRAIN_SOCKET`, `SIDEQUESTS_SOCKET_PATH`, or `~/.sidequests/brain.sock` during the transition.
- [ ] ARC_AGI imports `campy` where Python import access is needed, with `sidequests` fallback only if required for compatibility.
- [ ] ARC_AGI live smoke tests still fail clearly if no daemon socket exists, with error text telling the user to run `campy start` or `campy setup`.
- [ ] ARC_AGI tests cover both new Campy names and legacy fallback names.
- [ ] No ARC_AGI code creates a second durable memory store; it must continue using the shared Campy/KuzuDB memory service.

### Validation Commands

Run from the sibling ARC_AGI repo after implementation:

```bash
cd ../ARC_AGI
rg -n "sidequests|SideQuests|sidequests-brain|SIDEQUESTS|~/.sidequests" .
pytest -q tests/test_arc3_durable_runner.py tests/test_b185_failure_taxonomy.py
pytest -q
```

Run from this HippoCampy repo after implementation if ARC_AGI invokes local adapters:

```bash
cd ../sidequests-brain
.venv/bin/pytest -q tests/test_adapters.py tests/test_mcp_server_adapter.py tests/test_installer_idempotency.py
```

### Outcome

ARC_AGI becomes a first-class Campy consumer without losing compatibility with currently installed SideQuests-era local memory setups.

### Investigation Notes (2026-08-02) — State left as `ready`, not marked complete

The sibling repo was found at `/Users/djshelton/Desktop/GitProjects/ARC_AGI` and inspected (read-only). Its migration to Campy naming already appears substantially done — but by prior, undocumented work in that repo, not by any action taken in this session. No files in ARC_AGI were modified here: it's a separate live git checkout with no branch/worktree isolation available to this session, and the coordinator directed this task's changes to land as a rollback-able PR — a mechanism this session has no way to provide for a different repository. Per this card's own instruction ("do not fabricate that it happened"), state is left as `ready` rather than `complete`, since the ARC_AGI-side work was not verifiably done *by this task*, even though it appears to already exist.

What was verified as already present in ARC_AGI:
- `sidequest_mcp_client/readiness.py::_check_brain_socket` already checks `CAMPY_BRAIN_SOCKET`/`CAMPY_SOCKET_PATH` first, falls back to `SIDEQUESTS_BRAIN_SOCKET`/`SIDEQUESTS_SOCKET_PATH`/`~/.sidequests/brain.sock`, and raises a `ReadinessError` telling the user to run `campy start` or `campy setup` when no socket is found.
- `_cmd_from_env` prefers `CAMPY_MCP_CMD`, falls back to `SIDEQUESTS_MCP_CMD`, and rewrites any `sidequests-brain` python path to `hippocampy` before invoking `-m campy.adapters.mcp_server`.
- `README.md`, `pyproject.toml` (`name = "arc-agi-campy"`, dependency `hippocampy>=0.1.0rc1`), and `Makefile` (`CAMPY_MCP_CMD`, `CAMPY_REPO ?= ../hippocampy`) all already prefer Campy naming.
- ARC_AGI's own `CLAUDE.md`/`GEMINI.md`/`ARCHITECTURE.md` document a deliberate MCP-seam-only architecture (no direct `campy.*`/`sidequests.*` imports in production runtime code, enforced by their own `tests/test_import_boundary.py`), which is consistent with "no second durable memory store" and makes the "imports campy where needed" bullet largely moot by design.

Two real gaps found but **not fixed** (left for an ARC_AGI-side follow-up):
1. `ARC_AGI/ARCHITECTURE.md` lines ~352 and ~365 still reference the old local folder name `sidequests-brain/` in example paths (e.g. `export CAMPY_MCP_CMD="../sidequests-brain/.venv/bin/python -m campy.adapters.mcp_server"`) instead of the actual current folder name `hippocampy/` — `README.md` in the same repo already got this right, `ARCHITECTURE.md` didn't.
2. `sidequest_mcp_client/test_compat/runtime.py` imports `mcp_engine.config`, `mcp_engine.graph.kuzu_client`, and `mcp_engine.schema` via an obfuscated `importlib` call (`base = "m" + "cp_" + "engine"`). `mcp_engine` no longer exists in the `hippocampy` repo — it was renamed to `campy.brain.brainstem.*` / `campy.brain.hippocampus.graph.*` by a separate, unrelated codebase-anatomy refactor. This test-only compat helper would fail if actually exercised. Not a "sidequests naming" issue per se, but a real cross-repo breakage discovered in passing.

Did not run ARC_AGI's own test suite (`pytest -q tests/test_arc3_durable_runner.py tests/test_b185_failure_taxonomy.py`, full `pytest -q`) since that requires ARC_AGI's own venv/environment, which was out of scope to set up for a read-only verification pass.

### Completion (2026-08-18) — State moved to `done`

A later session had genuine read+write access to the sibling repo (`engramist/ARC-AGI` on
GitHub, not just a local path) and closed both gaps left open above:

1. Re-verified both gaps still existed exactly as described (nothing had drifted since the
   2026-08-02 investigation).
2. Fixed `ARC-AGI/ARCHITECTURE.md`'s two stale `sidequests-brain/` references to `hippocampy/`,
   matching what `README.md` already had.
3. Fixed `sidequest_mcp_client/test_compat/runtime.py`'s broken `mcp_engine.*` import to the
   correct current module paths (`campy.brain.brainstem.config`,
   `campy.brain.hippocampus.graph.kuzu_client`, `campy.brain.hippocampus.schema`), and verified
   the fix by actually importing `load_config`/`KuzuClient`/`init_schema` through it against a
   real hippocampy checkout — not just a syntax check.
4. Ran ARC-AGI's full test suite: **581 passed, 4 skipped**, no regressions (the compat helper's
   only caller is archived, uncollected code, so this wasn't causing live test failures, but was
   genuinely broken for anyone who exercised it).
5. Landed as `engramist/ARC-AGI#34`, merged.

Every other acceptance criterion in this card was already satisfied by prior work in ARC_AGI,
as the 2026-08-02 investigation found and this pass re-confirmed. All items in this card are
now verifiably complete, by an action actually taken (not just observed as already present).
