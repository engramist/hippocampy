# B178: ACTION4 Misidentified as Directional Movement — Action Semantics Discovery

**Status**: ready
**Assigned**: TBD
**Depends on**: B168 (exploration agent provides action effect data)
**Complexity**: Medium (2-4 hours)
**Priority**: P1 (root cause: agent assumes ACTION4 = "move right" but it triggers gravity/scroll)
**Plan**: backlog/plans/B-178-action-semantics-discovery.md

## Problem Statement

Autopilot at `orchestrator.py:451` maps actions to directions by convention:
- ACTION1 = up (dr < 0), ACTION2 = down (dr > 0)
- ACTION3 = left (dc < 0), ACTION4 = right (dc > 0)

This mapping is hardcoded and assumed universal. In the traced puzzle:
- ACTION4 caused 42 entity updates (likely gravity or scroll) but moved the player 0 rows in the intended direction
- ACTION1 moved the player's column (31.5 → 19.5), a column-axis change — not "up" as assumed
- ACTION2/ACTION3 were never tried, so their actual semantics are unknown

The hardcoded mapping is wrong for this puzzle. The B168 exploration agent (Phase 2a) discovers actual action effects, but this data is stored in `ActionEffect` nodes in KuzuDB and never fed back to autopilot's direction mapping.

## Root Causes

1. Autopilot uses a hardcoded ACTION→direction mapping (line 447-452) with no per-puzzle discovery
2. B168 exploration records `ActionEffect` nodes with `direction_row` and `direction_col` but autopilot doesn't query them
3. No mechanism to override the default mapping when exploration data contradicts it

## What This Card Does

1. **Query ActionEffect nodes before autopilot runs**: At the start of `_try_autopilot()`, query KuzuDB for `ActionEffect` nodes for the current task/level to get observed direction deltas
2. **Build a per-puzzle action→direction map**: Replace the hardcoded convention with discovered mappings. If `ACTION1` observed delta is (0, -12), it's a "left" action not "up"
3. **Fallback to default**: If no ActionEffect data exists (B168 exploration didn't run), fall back to the current hardcoded convention
4. **Persist action semantics**: Store the discovered mapping as a property on the `GridSnapshot` node for the current level

## Ecosystem Layer

- `agents/arc3/orchestrator.py` (Agent Runtime — autopilot direction mapping)
- `agents/arc3/entity_graph.py` (Agent Runtime — ActionEffect query)

**Cross-layer compliance**: No cross-layer violations. Reads from KuzuDB (correct pattern per No Shadow Stores rule).

## Files to Create/Modify

- **MODIFY** `agents/arc3/orchestrator.py` — Replace hardcoded direction mapping in `_try_autopilot()` with ActionEffect-based discovery; add `_action_direction_map` cache
- **MODIFY** `agents/arc3/entity_graph.py` — Add `get_action_directions(task_id, level)` query method
- **CREATE** `tests/test_b178_action_semantics.py` — Tests for direction discovery, fallback, and autopilot integration
- No changes to `docs/tool-catalog.md`

## Acceptance Criteria

- [ ] Autopilot queries ActionEffect nodes to determine actual action→direction mappings
- [ ] When ACTION4 causes column change but no row change, autopilot maps it to column-axis (not assumed "right")
- [ ] When no ActionEffect data exists, falls back to default convention
- [ ] Action direction map updates after each exploration step
- [ ] Trace events log discovered vs default action mappings
- [ ] All existing ARC tests pass

## Outcome

Autopilot uses empirically discovered action semantics instead of hardcoded conventions. If ACTION2 is the only action that moves the player upward, autopilot will use ACTION2 to navigate up — instead of using ACTION1 (which actually moves left) and wondering why the player isn't getting closer.
