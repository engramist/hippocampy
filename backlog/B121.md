# B121 - Orchestrator Entity Discovery Gate and Failure Handling

State: complete
Completed: 2026-04-01
Validation: 143/143 tests passed (test_arc3_orchestrator.py + solver + B118 + B111 + B124 + B92 regressions)

Plan: backlog/plans/B-121-orchestrator-entity-gate.md

### Problem

Currently the orchestrator transitions from bootstrap to hypothesize/act with no check on whether
entity discovery succeeded. If entity mapping returns all UNKNOWN, the downstream phases proceed
blind. There is no retry, no log, and no visibility into the failure.

The orchestrator must enforce that entity discovery completes before advancing. If it fails, it
should log why and retry. This is the durable-workflow guarantee: no phase transition without the
prerequisite data.

### What This Card Builds

Orchestrator enforcement of entity discovery completeness:

1. After `_bootstrap_entity_discovery()`, check that at least one role is non-UNKNOWN (unless grid
   is single-color background).
2. If check fails, retry entity discovery with a re-observation (re-fetch grid state).
3. Bounded retries (max 2). If still failing, log failure reason and proceed with degraded context
   (do not crash).
4. Write trace records the gate result: pass/fail/retry-count/reason.
5. `orchestration_report` includes entity gate status.

### Depends on

- B119 (entity discovery must exist)

## Files to Create/Modify

- `agents/arc3/runner.py` — add entity gate check after bootstrap entity discovery
- `agents/arc3/orchestrator.py` — add `_check_entity_gate()` method, integrate with perceive/bootstrap flow
- `tests/test_arc3_durable_runner.py` — test gate pass, gate fail+retry, gate fail+degrade
- `tests/test_arc3_orchestrator.py` — test `_check_entity_gate()` logic

## Acceptance Criteria

1. When entity map has at least one non-UNKNOWN role, gate passes and phase transition proceeds.
2. When entity map is all UNKNOWN on a multi-color grid, gate triggers retry (up to 2).
3. After max retries, orchestrator logs failure reason and proceeds with degraded context.
4. Gate result (pass/fail/retry-count/reason) appears in `bootstrap_write_trace`.
5. `orchestration_report` includes `entity_gate_status` field.
6. Tests cover: gate-pass, gate-retry-then-pass, gate-fail-degrade, single-color-grid-skip.

### Adapter Allow-List Check

No new tools introduced. No tool-catalog update needed.

## Outcome

The orchestrator guarantees entity awareness before reasoning begins. Failures are visible,
bounded, and non-fatal — the system degrades gracefully instead of running blind.
