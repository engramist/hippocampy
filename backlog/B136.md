# B136: Add Top-Level Replan Trigger for Persistent Dissonance

**Status**: complete ✅ DONE (2026-04-01)
**Assigned**: TBD  
**Depends on**: None  
**Complexity**: Small-Medium (1–2 hours)  
**Priority**: P2 (MEDIUM — chunk-level recovery exists, but top-level planning is still one-shot)  
**Plan**: backlog/plans/B-136-dissonance-replan.md

## Problem Statement

This card is still directionally valid, but the original wording overstated the bug.

Today, `SolveEngine.solve()` **does** react to dissonance at the chunk level:
- it reports negative outcome for the stalled chunk,
- marks the chunk as failed,
- clears the active chunk,
- and generates a new chunk.

What is still missing is a **top-level plan refresh**. In `agents/arc3/runner.py`, `await orchestrator.plan(observation, memory_context)` still runs only once during bootstrap. So when dissonance persists across several steps, the agent adapts locally but never refreshes the higher-level plan/reflex context.

Evidence from the current code and timeline:
- `runner.py` calls `plan(...)` before the step loop, not inside it
- `solve_phase_end` shows repeated `dissonance=True` later in the run
- `plan phase_start` still appears only at startup in the unified timeline

## What This Card Builds

1. Adds a runner-level check for persistent dissonance after solve.
2. Re-triggers `orchestrator.plan(...)` only when chunk-level recovery is not enough.
3. Applies a small backoff so re-planning does not thrash every step.
4. Emits `replan_triggered` / `replan_skipped` trace events with reason.

## Files to Create/Modify

- `agents/arc3/runner.py` — add persistent-dissonance replan logic and backoff metadata

## Ecosystem / Layer Compliance

- Layer: ARC harness runner (`agents/arc3/runner.py`)
- No cross-layer import violations.
- Complies with `docs/arc-harness-rules.md` and `docs/ecosystem-rules.md`.

## Acceptance Criteria

- [ ] Re-planning fires when dissonance persists across multiple steps, not just a single transient warning
- [ ] `replan_triggered` appears in `master_timeline.json` with step number and reason
- [ ] Backoff prevents plan thrashing
- [ ] `pytest -q tests/` passes with no regressions

## Outcome Statement

After this card, the agent keeps its existing chunk-level recovery while also refreshing the higher-level plan when confusion persists. Strategy updates become adaptive instead of bootstrap-only.
