# B210 — Correct Per-Step PERCEIVE Action Attribution

- **State:** ready
- **Priority:** P2
- **Plan:** backlog/plans/B-210-perceive-action-attribution.md
- **Ecosystem Layer(s):** Agent Runtime & Harness
- **Cross-layer violations:** None — perception/timeline content fix only.
- **Dependencies:** B202, B205

## Problem Statement

Per-step perceive summaries sometimes ask about the wrong action (for example, `Did ACTION2 advance...` after step action `ACTION6`). This desynchronizes the observation narrative from the actual executed move and pollutes memory ingestion quality.

## What It Does (Scope)

Ensure per-step perception uses the actual action executed for the current evaluated step.

Behavior:
- Bind `action_id` in `perceive_step_response()` to the just-executed step action (single source of truth from step record / execute result).
- Prevent stale reuse of previous action from history tail.
- Add a consistency assertion/trace when `phase_answer` action and `step.action_id` diverge.

Out of scope:
- Changing action selection policy itself.
- Changing phase-transition order.

## Files to Create/Modify

| File | Action |
|------|--------|
| `agents/arc3/runner.py` | **Modify** — pass canonical step action_id into per-step perceive call |
| `agents/arc3/orchestrator.py` | **Modify** — consume canonical action_id and guard against stale fallback |
| `tests/test_arc3_orchestrator.py` | **Modify** — verify phase question references current step action |
| `tests/test_arc3_durable_runner.py` | **Modify** — integration assertion for step/perceive action consistency |

### Adapter/allow-list impact
- No tool additions.
- No `docs/tool-catalog.md` changes required.

## Acceptance Criteria

1. Per-step perceive question references the same action as the corresponding `step.action_id`.
2. No stale previous-step action appears in perceive phase_answer for current step.
3. A trace marker is emitted when action attribution mismatch is detected (defensive monitoring).
4. New tests covering ACTION6/other non-default actions pass.
5. Smoke test shows aligned action ids between `step` snapshots and immediate `evaluate->perceive` phase entries.

## Outcome Statement

Perception narratives become step-accurate, improving debugging fidelity and SideQuests memory quality.