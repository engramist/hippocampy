# B98 — Calibrate ARC Wall Detection to Avoid Overclassification

- **State:** complete
- **Priority:** P1
- **Plan:** backlog/plans/B-98-arc-wall-detection-calibration.md
- **Dependencies:** B95, B97

## Problem Statement

The latest live solve run promoted too many colors to `wall`, including colors that may simply be
stable background structure or puzzle content. Over-eager wall assignment contaminates the solve
context, weakens goal inference, and makes `VictoryHypothesizer` produce brittle narratives like
"navigate through the walls" without enough evidence.

## What It Does

Tighten wall detection so a color becomes `WALL` only when multiple signals agree:

- strong static-row or invariant coverage
- repeated immobility across transitions
- low changed-region participation
- low count-delta / motion evidence relative to active colors

This card should explicitly separate:

- `WALL`: obstacle / boundary evidence is strong
- `STRUCTURE`: stable but semantically unknown board content
- `UNKNOWN`: not enough evidence yet

## Acceptance Criteria

1. Stable colors are not promoted to `wall` unless they satisfy at least two independent wall signals.
2. Colors that repeatedly participate in changed regions do not become `wall`.
3. The live solve summary no longer collapses most colors into `wall` by default.
4. `test_object_role_wall_on_static_frame` still passes.
5. Add at least 2 new tests covering false-positive wall prevention.

## Outcome

The solve phase stops treating most of the board as obstacle geometry and preserves uncertainty
until the evidence is truly wall-like.

## Validation Note (2026-03-31)

- Implemented files: `agents/arc3/solver.py`, `tests/test_arc3_solver.py`
- Tests run: focused ARC solver suite
- Result: passing locally during delegated integration
- Regression checks: live solve summaries stopped collapsing most colors into `wall` by default
