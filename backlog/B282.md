# B282 - Batch Sweep Writes with UNWIND

State: complete — re-verified 2026-08-03 against a real temp KuzuDB (no mocks), specifically because the card's own acceptance criterion names a mock-DB test which per the current audit standard is not sufficient evidence on its own. Standalone script: seeded 1200 real Concept nodes (600 below the 0.05 archive threshold, 600 above) via real schema (`init_schema`), called the real `_decay_and_archive(db, {}, interval_days=1.0, archive_threshold=0.05)` — returned `decayed=1200, archived=600, errors=0`, and an independent real read-back (`MATCH (n:Concept) WHERE n.archived = true RETURN count(n)`) confirmed exactly 600 archived and 600 still active in the actual database (chunked into 3 batches of 500 by `_batch_write`, confirmed via `_BATCH_SIZE = 500`). `_resurrect_archived(db, resurrection_threshold=0.9)` on a planted archived near-duplicate of an active anchor node correctly flipped `archived: false` and reset `pathway_strength` to exactly 0.9 on real read-back. `_hebbian_promote` (per-pair, not UNWIND-batched — reasonably so, since each write depends on a preceding per-pair LLM call and pair counts are threshold-gated, not O(all nodes) like decay/archive) was exercised with a fake LLM client returning a fixed relation; the resulting `ENABLES` edge with `inferred_by: 'LLM'` was confirmed to actually persist via a real read-back query. `pytest tests/test_sweep_batching.py -q` → 5 passed; `pytest tests/ -q -k "sweep or dreaming or basal"` → 84 passed. Decayed-count AC confirmed: `_decay_and_archive` does exactly one `execute_write` (decay SET) + one `execute` read per table, no separate `count(n)` scan — the read serves both the decayed count and the archive-candidate list. Sweep stats dict shape unchanged (`decayed`/`archived`/`resurrected` keys intact in `run_sweep`). No B278-class persistence gap found in this card's own write paths (contrast with B283, audited alongside this card, where a real gap WAS found and fixed in `write_co_occurs_with`).
Priority: P2

Plan: backlog/plans/B-282-batch-sweep-writes.md

### Problem

The background sweep performs per-row writes, each acquiring the global asyncio write lock and dispatching a separate Cypher statement. In `campy/brain/brainstem/sweep.py::_decay_and_archive` (~line 553), nodes below the archive threshold are archived one `execute_write` per node. The same one-row-per-query pattern appears in `_resurrect_archived`, `_hebbian_promote` edge writes, and the per-table `count(n)` full scans run every sweep. On a graph with thousands of decayed nodes, a single sweep serializes thousands of lock acquisitions and blocks the GCL loop's writes.

### What This Card Does

Converts per-row sweep writes to batched `UNWIND` statements (chunked), and removes the redundant per-sweep `count(n)` scans by computing counts from data the sweep already has.

### Dependencies

- None. (Touches `sweep.py` — coordinate with B277 follow-ups if any are in flight.)

### Files to Modify

- `campy/brain/brainstem/sweep.py` — `_decay_and_archive`, `_resurrect_archived`, `_hebbian_promote`
- `campy/brain/basal_ganglia/frustration_clusters.py` — DISTILLED_FROM edge writes if per-row
- `tests/test_sweep_batching.py` — NEW: batching regression tests

### Acceptance Criteria

- [ ] `_decay_and_archive` archives all below-threshold nodes for a table in ≤ ceil(n/500) write statements via `UNWIND $ids AS id MATCH (n:{table} {pk: id}) SET n.archived = true`
- [ ] The decayed-count no longer requires a separate `count(n)` scan per table (read the count from the archive-candidate query + one cheap count, or RETURN count from the bulk decay if Kuzu supports it — otherwise keep a single count query but document why)
- [ ] `_resurrect_archived` batch-updates resurrection candidates the same way
- [ ] A test with a mock DB asserts: 1,200 archive candidates → ≤ 3 write calls (chunk 500)
- [ ] Existing sweep tests pass: `pytest tests/ -q -k "sweep or dreaming or basal"`
- [ ] Sweep stats dict shape unchanged (callers/tests depend on keys)

### Outcome

Sweeps hold the write lock for a bounded number of statements instead of O(nodes), eliminating lock contention between background maintenance and live message processing.
