# B138: Expose SolveEngine Brain I/O in `agent_trace`

**Status**: Ready  
**Assigned**: TBD  
**Depends on**: None  
**Complexity**: Medium (2–4 hours)  
**Priority**: P2 (MEDIUM — solve debugging still requires cross-referencing trace streams)  
**Plan**: backlog/plans/B-138-solve-engine-trace.md

## Problem Statement

This card is still valid, but the current blind spot is narrower than originally described.

`master_timeline.json` already includes `agent_trace` events for some non-solve retrieval work such as bootstrap/perceive operations. The remaining gap is that **solve-internal brain I/O** — especially calls triggered inside `SolveEngine.solve()` such as `report_outcome` and chunk `register_plan` — is still much clearer in `arc_server` than in `agent_trace`.

`SolveEngine.__init__()` currently accepts only:
```python
(brain_client, llm_client, session_id)
```
so it still lacks a dedicated trace callback for its internal async brain operations.

## What This Card Builds

1. Adds an optional `emit_trace_event` callback to `SolveEngine`.
2. Emits `agent_trace` events around solve-internal brain calls such as:
   - `report_outcome`
   - chunk `register_plan`
   - any solve-time recall calls that currently only surface in the ledger/arc stream
3. Keeps the callback optional so existing call sites remain safe.
4. Produces a more complete unified timeline without needing manual cross-reference.

## Files to Create/Modify

- `agents/arc3/solver.py` — accept and invoke an optional trace callback for solve-internal brain I/O
- `agents/arc3/orchestrator.py` — pass `self._emit_trace_event` into `SolveEngine`

## Ecosystem / Layer Compliance

- Layer: ARC agent solve engine (`agents/arc3/solver.py`) + orchestrator integration
- No cross-layer import violations. A callback keeps the dependency direction clean.
- Complies with `docs/arc-harness-rules.md` and `docs/ecosystem-rules.md`.

## Acceptance Criteria

- [ ] `agent_trace` shows solve-internal brain I/O events alongside the existing phase markers
- [ ] Solve timing in the unified timeline more closely matches the real async work being performed
- [ ] `SolveEngine` continues to work when the callback is `None`
- [ ] `pytest -q tests/` passes with no regressions

## Outcome Statement

After this card, solve-time memory/planning activity is visible in the same trace stream as the rest of the agent lifecycle. Debugging becomes much faster because fewer important events are hidden behind the ledger-only view.
