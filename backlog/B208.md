# B208 — Split Victory Inference Cooldowns by Trigger Type

- **State:** ready
- **Priority:** P3
- **Plan:** backlog/plans/B-208-victory-replan-cooldown-split.md
- **Ecosystem Layer(s):** Agent Runtime
- **Cross-layer violations:** None — solver trigger timing only.
- **Dependencies:** B179

## Problem Statement

Victory inference retries are currently gated by one global cooldown. Replan-triggered retries are blocked by the same long window used for normal archetype-threshold calls, delaying recovery when strategy is already failing.

## What It Does (Scope)

Introduce trigger-specific cooldowns for victory inference attempts.

Behavior:
- Keep longer cooldown for routine archetype-threshold calls.
- Use shorter cooldown for replan-triggered retries.
- Track last attempt step per trigger class.

Out of scope:
- Changing VictoryHypothesizer prompt contents.
- New persistence schema.

## Files to Create/Modify

| File | Action |
|------|--------|
| `agents/arc3/solver.py` | **Modify** — split cooldown checks/trackers for victory inference triggers |
| `tests/test_arc3_solver.py` | **Modify** — add trigger-specific cooldown test |

### Adapter/allow-list impact
- No tool changes.
- No `docs/tool-catalog.md` updates required.

## Acceptance Criteria

1. Archetype-threshold trigger keeps existing conservative cooldown behavior.
2. Replan trigger can retry sooner (short cooldown) without violating guardrails.
3. Cooldown bookkeeping is explicit per trigger type.
4. New test validates both cooldown paths.
5. Existing victory inference tests continue passing.

## Outcome Statement

Victory inference becomes responsive during replans while preserving cost control for routine solve flow.
