# B4 - Publish to PyPI

State: complete

### B4 · Publish to PyPI — ✅ DONE (2026-03-28)
**Problem:** SideQuests is not installable via standard Python tooling, blocking developer adoption and Smithery discoverability.

**What it does:** Make `pip install sidequests-brain` and `uvx sidequests-brain` work.

- Create `pyproject.toml` with proper metadata, entry points, and dependency declarations
- `sidequests setup` as the primary CLI entry point
- Test `uvx sidequests-brain setup` end-to-end

Notes:
- Provisional patent filed March 25, 2026 (Application #64/017,066); publication is no longer blocked by pre-filing disclosure constraints.
- `uvx` is the target developer install story

**Acceptance Criteria (Evaluation):**
- `pip install sidequests-brain` in a clean Python 3.12/3.13 venv succeeds and `sidequests --help` runs.
- `uvx sidequests-brain setup` completes the full setup flow end-to-end.
- `twine check` passes on the published wheel and sdist with no warnings.
- Python 3.14 limitation is documented in the package README with mitigation steps.

**Outcome:** Any developer can install and set up SideQuests Brain with a single pip/uvx command.

### Validation Note (2026-03-28)
- Packaging artifacts generated from current tree:
	- `dist/sidequests_brain-0.1.0rc1-py3-none-any.whl`
	- `dist/sidequests_brain-0.1.0rc1.tar.gz`
- Verification run:
	- `python -m build --wheel --sdist`
	- `twine check dist/*`
	- Result: both distributions passed `twine check`

### Correction (2026-08-18)

This card's "complete" / "DONE" status was never actually accurate: the
artifacts above were only ever *built and checked* locally under the old
`sidequests-brain` name — nothing was ever uploaded to PyPI. The real,
first-ever publish happened today under the renamed `hippocampy` package
(0.1.0, https://pypi.org/project/hippocampy/0.1.0/), tracked by
[B327](B327.md). B236 carried the actual publish-readiness work forward
under the new name; this card is left `State: complete` for its own
narrow, now-superseded scope (pyproject.toml/entry points/uvx under the old
name), with this note correcting the historical record rather than
re-opening it.

---
