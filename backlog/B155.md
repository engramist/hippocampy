# B155: ARC Cross-Game Strategy Memory — Store and Retrieve Level-Solving Strategies

**Status**: ready
**Assigned**: TBD
**Depends on**: B150, B151, B157
**Complexity**: Medium (2–4 hours)
**Priority**: P2 (cross-game learning)
**Related**: M1/M2 from codereviewOpus2.md
**Plan**: backlog/plans/B-155-arc-transformation-memory.md
**Phase**: In-game — stores after game completion, retrieves at game start

## Problem Statement

The current memory integration stores game-playing lessons ("tried ACTION6, got zero reward") and retrieves by text similarity to generic queries like "ARC game space win condition." The submission results show `retrieval_count: 1, total_retrieval_size_bytes: 6` — memory contributed nothing.

For ARC-AGI-3 interactive games, useful cross-game knowledge includes:
- What actions do in different game types (ACTION1-4 = directional, ACTION5 = interact, ACTION6 = coordinate, ACTION7 = undo)
- Game rule patterns across similar games (e.g., "games with color-shifting grids usually require matching a target pattern")
- Level-solving strategies that worked (e.g., "on later levels, focus on changed cells only")
- How many levels the game had, what the difficulty progression looked like

## What This Card Builds

1. **Structured game strategy storage** after completing a game:
   - Store via `brain.notify_turn()` with structured format:
     ```
     ARC GAME STRATEGY:
     Levels: {total_levels}, Solved: {levels_solved}
     Actions: {action_semantics_summary}
     Game rule: {rule_hypothesis}
     Rule confidence: {confidence}
     Strategy: {level_by_level_summary}
     Effective actions by level: {per_level_action_patterns}
     ```
   - Stored once per game (not per level) — captures the full game knowledge

2. **Game-characteristic-based retrieval query** at game start:
   - Query based on initial grid characteristics: grid size, color count, available actions
   - Retrieves strategies from structurally similar past games
   - Prior to level 1, gives the agent a head start on action semantics

3. **Strategy extraction from memory results**:
   - Parse retrieved strategies to extract action semantics and game rule hypotheses
   - Feed as candidate hypotheses to GameRuleHypothesizer (B151)
   - Prior successful strategies get a confidence boost
   - Speeds up level 1 exploration by pre-seeding action knowledge

4. **Integration points**:
   - `evaluate()` — store the full-game strategy after game completion
   - `perceive()` at step 0 of level 1 — retrieve using initial grid characteristics
   - B151 GameRuleHypothesizer — feed memory strategies as candidate hypotheses

## Files to Create/Modify

- `agents/arc3/orchestrator.py` — Modify `evaluate()` for game strategy storage; modify `_memory_query()` for game characteristics; add strategy parsing
- `agents/arc3/grid_analysis.py` (from B150) — Add `grid_characteristic_summary(grid)` helper
- `tests/test_b155_game_strategy_memory.py` — NEW: test strategy storage, retrieval, parsing

## Ecosystem / Layer Compliance

- Layer: Agent Runtime & Harness (orchestrator) ↔ Data Products & Memory (SideQuests via BrainClientProtocol)
- Uses existing SideQuests tools (`notify_turn`, `current_truth`, `recall_relevant_lessons`) — no new tools needed
- No cross-layer violations. Communication via BrainClientProtocol only.
- Complies with `docs/arc-harness-rules.md` and `docs/ecosystem-rules.md`.

## Acceptance Criteria

- [ ] After completing a game, a structured game strategy is stored via `notify_turn`
- [ ] The strategy includes action semantics, game rule, per-level summary
- [ ] Memory queries use initial grid characteristics (size, colors, actions)
- [ ] Retrieved strategies are parsed and fed as candidate hypotheses
- [ ] Pre-game memory retrieval seeds level 1 with action knowledge from similar games
- [ ] Failed games store negative-valence strategies to avoid repeating mistakes
- [ ] All tests pass, no regressions

## Outcome Statement

After this card, the agent learns across games. After playing game A, the strategy is stored. When game B starts with a similar grid, the agent retrieves game A's strategy — pre-seeding action semantics and game rule hypotheses. Level 1 exploration becomes more targeted because the agent has prior knowledge about similar game types.
