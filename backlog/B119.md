# B119 - Bootstrap Entity Discovery and SideQuests Persistence

State: complete
Completed: 2026-04-01
Validation: 97/97 tests passed (test_b119_bootstrap_discovery.py + solver/orchestrator + B109-B115 regressions)

Plan: backlog/plans/B-119-bootstrap-entity-discovery.md

### Problem

`ObjectRoleMapper` (which assigns PLAYER, ENEMY, GOAL, WALL, COLLECTIBLE, EXIT roles to color
groups) only runs during the `solve` phase. During bootstrap, the agent only sees raw color counts,
grid dimensions, and a spatial sketch. It has zero entity-level context.

When the environment constrains `available_actions` to a single action, the agent cannot reason
about **why** that constraint exists because it has never identified what's on the grid.

This is the root cause of the "agent chose ACTION6 all 15 steps with zero reward" failure: no
entity awareness means no strategic reasoning.

### What This Card Builds

Run initial entity discovery during bootstrap and persist results to SideQuests:

1. Call `ObjectRoleMapper.assign_roles()` on the initial observation during bootstrap.
2. Build a structured entity map: `{color_id → role, confidence, estimated_position}`.
3. Save entity map to SideQuests via `notify_turn` so it's available for recall/planning.
4. Make entity map accessible on the orchestrator (`self._entity_map`) from step 0.

### Depends on

- B95 (SolveEngine exists)
- B117 (typed decision packets)

## Files to Create/Modify

- `agents/arc3/orchestrator.py` — add `_bootstrap_entity_discovery()` method, call from `perceive()` when step=0
- `agents/arc3/solver.py` — ensure `ObjectRoleMapper.assign_roles()` can run standalone (no solve prerequisite)
- `agents/arc3/runner.py` — call entity discovery during bootstrap phase, before plan
- `tests/test_arc3_orchestrator.py` — test entity map is populated after bootstrap perceive
- `tests/test_arc3_durable_runner.py` — test entity map appears in bootstrap_write_trace

## Acceptance Criteria

1. After bootstrap `perceive()`, `orchestrator._entity_map` contains at least one non-UNKNOWN role when the grid has multiple color groups.
2. Entity map is saved to SideQuests via `notify_turn` with `[ENTITY MAP]` tag.
3. Entity map is accessible in `memory_context` for subsequent steps.
4. If grid has only one color (background), entity map is empty but does not error.
5. Entity discovery write event appears in `bootstrap_write_trace`.
6. Tests validate entity map contents for multi-color and single-color grids.

### Adapter Allow-List Check

No new tools introduced. Uses existing `notify_turn` (already in allow-lists).
No tool-catalog update needed.

## Outcome

The agent has entity-level context from step 0, enabling strategic reasoning even when
`available_actions` is constrained.
