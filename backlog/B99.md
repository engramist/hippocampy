# B99 — Strengthen ARC Goal Detection from Target Evidence

- **State:** complete
- **Priority:** P1
- **Plan:** backlog/plans/B-99-arc-goal-detection.md
- **Dependencies:** B95, B97, B98

## Problem Statement

The solve phase can now infer a `player` in some live runs, but it still fails to identify a
credible `goal`. Without a target object, the victory hypothesis stays generic and `PlanChunker`
cannot graduate into directed movement.

## What It Does

Improve `GOAL` inference using richer target evidence:

- small stationary object heuristics
- proximity of repeated changed-region fronts to a candidate
- path hypotheses converging on a region or color
- persistence outside HUD/static rows
- separation between active actor colors and inert target colors

This card should avoid hard-coding game semantics and instead score goal candidates from evidence.

## Acceptance Criteria

1. Goal candidates are ranked instead of remaining fully `unknown`.
2. A `GOAL` label is only assigned when evidence is stronger than competing explanations.
3. The victory hypothesis prompt can reference a concrete target color/region when available.
4. Add at least 3 tests for goal-candidate ranking and promotion.
5. Live solve summaries show either a concrete goal candidate or an explicit ranked goal-uncertainty state.

## Outcome

The solve phase gains a real target, not just a generic idea of "reach_goal."

## Validation Note (2026-03-31)

- Implemented files: `agents/arc3/solver.py`, `tests/test_arc3_solver.py`
- Tests run: focused ARC solver suite
- Result: passing locally during delegated integration
- Regression checks: live solve runs now infer concrete goal candidates without relying on reward
