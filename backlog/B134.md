# B134: Gate Unconditional `current_truth` Recall During Ingest

**Status**: Ready  
**Assigned**: TBD  
**Depends on**: None  
**Complexity**: Small (< 1 hour)  
**Priority**: P1 (HIGH — still adds unnecessary per-step latency and trace noise)  
**Plan**: backlog/plans/B-134-gate-unconditional-recall.md

## Problem Statement

`agents/arc3/runner.py` still unconditionally sets:

```python
recall_query = "What did I learn from similar puzzles?"
await adapter.ingest_step(..., recall_query=recall_query)
```

inside the main step loop. That means the same broad recall fires after essentially every action, whether or not the agent is stuck.

In the latest `master_timeline.json`, this query appears repeatedly across the run. The call is no longer always empty — some steps now return stored items — but it is still **ungated, repetitive, and noisy**.

## What This Card Builds

1. Replaces the unconditional recall with a conditional gate — only fire when context suggests it is useful, such as:
   - first step,
   - loop detected,
   - repeated no-progress streak,
   - or a dissonance transition.
2. Adds a trace event such as `recall_skipped` with a reason when the query is bypassed.
3. Preserves the feature while reducing low-value retrieval churn.

## Files to Create/Modify

- `agents/arc3/runner.py` — gate the `recall_query` passed into `adapter.ingest_step()`

## Ecosystem / Layer Compliance

- Layer: ARC harness runner (`agents/arc3/runner.py`)
- No cross-layer import violations.
- Complies with `docs/arc-harness-rules.md` and `docs/ecosystem-rules.md`.

## Acceptance Criteria

- [ ] A normal 15-step run produces substantially fewer ingest-time recall calls
- [ ] `recall_skipped` (or equivalent) appears when the gate bypasses retrieval
- [ ] Recall still fires when loop/dissonance conditions indicate it may be helpful
- [ ] `pytest -q tests/` passes with no regressions

## Outcome Statement

After this card, ingest-time recall becomes selective instead of mechanical. The timeline is easier to read, and runtime loses a repeated source of avoidable latency.
