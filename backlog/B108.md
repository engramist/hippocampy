# B108 - Reduce Repeated SideQuests Overhead on ARC Contract Ingestion

State: complete

Plan: backlog/plans/B-108-arc-contract-ingestion-caching.md

### Problem

Recent live ARC smoke runs show that a large share of wall-clock time is being spent in repeated
SideQuests concept classification and relation extraction for recurring ARC contract concepts such
as:

- `ARC-AGI-3 API Contract`
- `ACTION1`, `ACTION2`, ...
- `RESET+first`
- stable coordinate/range concepts like `0-63`

This cognition is real, but it is being repeated too often during live puzzle runs and is not
currently translating into better puzzle outcomes. The harness is paying a large cost for
re-ingesting stable ARC protocol concepts that should be cached, suppressed, or pre-labeled.

### What This Card Builds

A bounded strategy for reducing SideQuests overhead on repeated ARC contract ingestion during live
ARC harness runs:

- detect stable ARC contract concepts that should not be reclassified every run
- cache or pre-seed their gist/schema classifications
- suppress repeated relation extraction when the same ARC contract fragment has already been
  normalized
- preserve the useful puzzle-specific write path while shrinking repeated protocol overhead

### Depends on

- B87
- B89

## Files to Create/Modify

- `agents/arc3/api_knowledge.py`
- `run_single_puzzle.py`
- `docs/ARCHITECTURE.md`
- `benchmarks/arc3/README.md`
- `tests/test_arc3_durable_runner.py`

## Acceptance Criteria

1. Stable ARC contract concepts can be identified and excluded from repeated full ingestion during
   live runs.
2. The live runner can reuse cached/pre-seeded ARC contract knowledge instead of reclassifying the
   same protocol concepts each run.
3. Puzzle-specific observations and promoted facts still flow through normal SideQuests writes.
4. At least one smoke/regression path shows reduced ARC setup overhead without changing puzzle
   semantics.
5. Tests cover:
   - repeated ARC contract concept suppression or caching
   - puzzle-specific writes still preserved
   - no breakage to live-run export shape

## Outcome

The ARC harness spends more time solving the puzzle and less time re-learning the same ARC protocol
facts every run.
