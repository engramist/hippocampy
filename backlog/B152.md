# B152: ARC Level-Replay Verification — Validate Rules Against Solved Levels

**Status**: ready
**Assigned**: TBD
**Depends on**: B150, B151, B157
**Complexity**: Large (4–6 hours)
**Priority**: P0 (primary verification mechanism)
**Related**: A1 from codereviewOpus2.md, B123 (existing REPL sandbox)
**Plan**: backlog/plans/B-152-arc-repl-verification-loop.md
**Phase**: In-game — runs between levels (verifies hypotheses against solved level pairs)

## Problem Statement

The REPL sandbox (B123) exists but is only used as an optional "peek" tool during action selection. The agent needs a way to verify game rule hypotheses against known-good data before applying them to harder levels.

After solving levels 1-N, the agent has N (start_grid, end_grid, action_sequence) triples. These are ground truth: the agent KNOWS these are correct because they resulted in a WIN. A rule hypothesis can be tested by asking: "Does this rule correctly predict end_grid from start_grid for all solved levels?"

### Interactive Game Model

B152 runs at level transitions (after B151 generates hypotheses):
- **Input**: Game rule hypotheses from B151 + solved level pairs from B157
- **Verification**: Execute hypothesis logic in REPL against solved level start→end pairs
- **Output**: Verified confidence score. High confidence → apply rule aggressively on next level. Low confidence → fall back to exploration.
- This costs **zero game steps** — all verification runs locally in the sandbox.

## What This Card Builds

1. **`LevelReplayVerifier` class** in `agents/arc3/repl_verification.py`:
   - `verify_against_solved_levels(hypothesis: GameRuleHypothesis, solved_levels: List[Dict])` -> `VerificationResult`
   - For each solved level:
     a. Constructs a Python script that simulates the hypothesis's predicted behavior
     b. Executes via the existing `execute_repl()` sandbox
     c. Compares predicted end state to actual end_grid
     d. Records match/mismatch with structured diff
   - Returns `VerificationResult` with confidence score

2. **`RuleRefinementLoop`** — orchestrates the hypothesize→verify→refine cycle between levels:
   - Step 1: Get hypotheses from GameRuleHypothesizer (B151)
   - Step 2: Verify each against all solved levels via LevelReplayVerifier
   - Step 3: If verified: boost confidence, set as active rule for next level
   - Step 4: If not verified: feed mismatch details to LLM for refinement
   - Step 5: Repeat up to MAX_REFINEMENT_ROUNDS (default: 2, tight budget between levels)
   - Returns the best-scoring hypothesis

3. **Action sequence replay**: For each solved level, the verifier can replay the recorded action sequence against the start grid to verify action semantics:
   - "If ACTION1 = move up, does replaying the action sequence from start_grid produce end_grid?"
   - This validates action_semantics in the hypothesis

4. **Integration with orchestrator**: Called at level transitions (B157's `_on_level_transition()`). Verification result sets confidence level that influences next level's exploration policy (B154) and prompt mode (B153).

## Files to Create/Modify

- `agents/arc3/repl_verification.py` — NEW: LevelReplayVerifier, RuleRefinementLoop, VerificationResult
- `agents/arc3/orchestrator.py` — Call verification at level transitions
- `agents/arc3/prompts.py` — Add RULE_REFINEMENT_TEMPLATE
- `tests/test_b152_level_replay_verification.py` — NEW: test verification against solved levels

## Ecosystem / Layer Compliance

- Layer: Agent Runtime & Harness (reasoning + verification)
- Uses existing `repl_sandbox.py` (Layer 4: Deterministic Tools)
- No cross-layer violations. No imports from `mcp_engine/`.
- REPL sandbox already has security constraints (no file/network, 2s timeout)
- Complies with `docs/arc-harness-rules.md` and `docs/ecosystem-rules.md`.

## Acceptance Criteria

- [x] Verification correctly tests hypotheses against solved level start→end pairs
- [x] Action sequence replay validates action semantics
- [x] Mismatch details are structured and can feed back to LLM for refinement
- [x] Handles Python errors gracefully (syntax errors, runtime errors, timeouts)
- [x] Refinement loop terminates after MAX_REFINEMENT_ROUNDS
- [x] Verified hypotheses boost confidence for next-level strategy
- [x] All verification costs zero game steps (pure REPL + LLM)
- [x] All tests pass, no regressions

## Outcome Statement

After this card, the agent validates its understanding of the game before applying it to harder levels. Solved levels serve as a test suite — if the rule correctly predicts all solved levels, it's likely correct for the next level. This verification runs between levels at zero game-step cost, giving the agent calibrated confidence.
