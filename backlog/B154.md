# B154: ARC Level-Progressive Exploration Policy

**Status**: complete ✅ DONE (2026-04-01)
**Assigned**: TBD
**Depends on**: B157
**Complexity**: Small (1–2 hours)
**Priority**: P1 (prevents wasted steps)
**Related**: A2/A3/C1 from codereviewOpus2.md
**Plan**: backlog/plans/B-154-arc-exploration-relaxation.md
**Phase**: In-game — exploration policy adapts per level

## Problem Statement

The exploration policy in `_enforce_action_policy()` forces the agent to test every available action before it can exploit known-good actions. With 7 available actions and a tight per-level step budget (B157 distributes steps across 7-8 levels), the agent spends most of its budget on blind exploration — even on later levels where it already knows what each action does from earlier levels.

Three specific issues:

1. **Mandatory exhaustive exploration** (orchestrator.py:2181-2193): If ANY action remains untested, the policy overrides the LLM's choice. On level 5, the agent already knows all 7 actions from levels 1-4 — but the policy forces re-exploration anyway.

2. **DissonanceDetector thresholds too high** (solver.py:970-1010): `STALL_THRESHOLD=6` and `MAX_CHUNK_STEPS=15` mean the agent waits 6 steps before replanning. With ~2-3 steps per level, this never fires.

3. **No level awareness**: The policy treats level 1 and level 8 identically. Level 1 SHOULD explore (it's a tutorial). Level 8 should exploit knowledge from 7 prior levels.

### Interactive Game Model

Exploration policy must adapt by level:
- **Level 1** (tutorial): Full exploration encouraged — this is how the agent learns actions
- **Level 2-3** (early): Reduced exploration — use knowledge from level 1, explore only unknowns
- **Level 4+** (mid-late): Minimal exploration — action effects should be well-established
- **High-confidence rule**: Zero forced exploration — apply the known rule directly

## What This Card Builds

1. **Level-progressive exploration enforcement**:
   - **Level 1**: Allow up to `max_forced_exploration_steps` (default: 5) forced steps. This IS the tutorial.
   - **Level 2-3**: Allow up to 2 forced steps (verify action effects carry over)
   - **Level 4+**: Allow 0 forced steps if action effects are known, 1 if unknown actions remain
   - **High-confidence rule available**: Skip all forced exploration regardless of level

2. **Carry-over action knowledge**: ActionFacts learned in level 1-3 should persist across levels (B157 ensures this). The exploration policy checks `observed_action_effects` — if an action has been tested in a prior level, it doesn't need re-testing.

3. **Reduced DissonanceDetector thresholds**:
   - `STALL_THRESHOLD: 6 → 2` (per-level budgets are tiny)
   - `REWARD_STALL_THRESHOLD: 8 → 3`
   - `MAX_CHUNK_STEPS: 15 → 4`

4. **Remove hardcoded action-to-direction mapping** (solver.py:1296-1306): The fallback that assumes ACTION1=up etc. contradicts empirical learning. The agent should learn action semantics from level 1 play.

## Files to Create/Modify

- `agents/arc3/orchestrator.py` — Modify `_enforce_action_policy()` for level-progressive enforcement
- `agents/arc3/solver.py` — Reduce DissonanceDetector thresholds; remove hardcoded direction mapping
- `tests/test_b154_exploration_relaxation.py` — NEW: test level-progressive enforcement

## Ecosystem / Layer Compliance

- Layer: Agent Runtime & Harness (policy tuning)
- No cross-layer violations. No imports from `mcp_engine/`.
- Complies with `docs/arc-harness-rules.md` and `docs/ecosystem-rules.md`.

## Acceptance Criteria

- [x] Level 1 allows full exploration (up to 5 forced steps)
- [x] Level 2-3 allows up to 2 forced steps
- [x] Level 4+ allows 0-1 forced steps based on action knowledge
- [x] High-confidence rule: zero forced exploration regardless of level
- [x] ActionFacts from prior levels are consulted before forcing exploration
- [x] DissonanceDetector fires after 2 stalled steps (not 6)
- [x] Hardcoded ACTION→direction mapping removed
- [x] All tests pass, no regressions

## Outcome Statement

After this card, exploration scales with the level. Level 1 is the tutorial — full exploration. Later levels leverage prior knowledge. The agent stops wasting half its per-level budget on re-exploring actions it already understands from earlier levels.
