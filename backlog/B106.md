# B106 - ARC Meta-Harness Candidate Runner and Evaluation Loop

State: complete

Plan: backlog/plans/B-106-arc-meta-harness-candidate-runner.md

### Problem

We still improve the harness manually. There is no outer-loop runner that can take a harness
candidate, execute it on a search set, collect comparable results, and write the outcome back into
the experience store.

### What This Card Builds

The first executable outer loop for ARC harness optimization:

- define a harness candidate artifact
- run the candidate against a bounded ARC search/eval set
- record scores, traces, and budget metrics in a comparable form
- write parent/child lineage for candidate evolution
- keep candidate evaluation outside the proposer itself

### Depends on

- B104
- B105

## Files to Create/Modify

- `benchmarks/arc3/harness.py`
- `benchmarks/arc3/model_eval.py`
- `benchmarks/arc3/README.md`
- `tests/test_arc3_durable_runner.py`

## Acceptance Criteria

1. A harness candidate can be identified, run, and scored independently of the proposer.
2. Candidate runs write a comparable result bundle with:
   - score
   - runtime
   - token budget
   - failure summary
   - lineage metadata
3. The runner can compare a candidate against a baseline on a fixed search set.
4. The evaluation path is automated outside the proposer loop.
5. Tests cover candidate result bundling and baseline-vs-candidate comparison.

## Outcome

ARC gets a real outer evaluation loop, which is the minimum executable substrate for a future
meta-harness.

## Validation Note (2026-03-31)

- Implemented files: `benchmarks/arc3/model_eval.py`, `benchmarks/arc3/README.md`, `tests/test_arc3_durable_runner.py`
- Tests run:
  - `.venv/bin/pytest -q tests/test_arc3_durable_runner.py tests/test_arc3_meta_harness_query.py`
- Result: `19 passed, 1 warning`
- Regression checks: candidate evaluation now aggregates solve rate, token budget, latency, and failure clusters in a comparable result bundle
