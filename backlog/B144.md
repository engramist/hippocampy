# B144: Plateau-Aware Exploitation Policy After Grounding Succeeds

**Status**: complete ✅ DONE (2026-04-01)
**Assigned**: TBD  
**Depends on**: B141, B142, B143  
**Complexity**: Medium (3–5 hours)  
**Priority**: P0 (remaining solver-quality blocker after grounding and graduation fixes)  
**Related**: B139, B141, B142, B143  
**Plan**: backlog/plans/B-144-plateau-aware-exploitation-policy.md

## Problem Statement

The latest live ARC run shows that the earlier blockers are now largely resolved:
- player and goal grounding succeed (`PRIMARY ROLES: player=5, goal=10`)
- chunk graduation no longer reinflates (`GRADUATION: stay explore: score=0.20 < 0.72`)
- ACTION6 coordinate fallback remains available when needed

However, the solver still spends the full 15-step budget in **late-stage exploration churn** instead of exploiting the strongest remaining hypothesis.

Evidence from `live_qwen25_7b_playerfix_1775247593`:
- `correct = false`, `steps = 15`, `final_state = NOT_FINISHED`
- `no_progress_step_count = 15`
- Steps 10–15 still bounce across `ACTION6`, `ACTION7`, `ACTION6`, `ACTION6`, `ACTION2`, `ACTION3`
- All of those late actions produced `reward = 0.0`
- Final strategy summary remained `Explore: try unexplored action to gather more information`

This means the bottleneck has shifted from **entity grounding** to **policy quality**: once the puzzle is grounded enough to act intentionally, the agent still lacks a plateau-aware exploitation policy that commits to the most promising action family or abandons weak families sooner.

## What This Card Builds

1. **Plateau-aware exploitation trigger**: when player and goal are grounded and the run has accumulated a sustained zero-reward streak, leave generic exploration mode and switch to a bounded exploitation policy.

2. **Action-family credit assignment**: score each action family using existing evidence (`reward`, `meaningful_change_score`, `tentative_progress`, guard blocks, repeat failures) so the best-supported family rises above generic fallback churn.

3. **Bounded switch budget**: once exploitation mode starts, limit switching across unrelated action families unless fresh evidence appears.

4. **Trace visibility**: emit fields such as exploitation mode / plateau reason / ranked action families in the solve trace so live behavior is auditable.

## Files to Create/Modify

- `agents/arc3/orchestrator.py` — enforce plateau-aware action-family selection and bounded late-run switching
- `agents/arc3/solver.py` — surface exploitation readiness / ranked action-family support into solve context
- `tests/test_arc3_orchestrator.py` — regression tests for late-run plateau behavior and bounded switching
- `tests/test_arc3_solver.py` — regression tests for exploitation-mode gating and strategy summary visibility

## Ecosystem / Layer Compliance

- Layer: ARC solve engine (`agents/arc3/solver.py`) + ARC orchestration policy (`agents/arc3/orchestrator.py`)
- No new tool schemas, adapter allow-list changes, or docs/tool-catalog changes are expected
- Additive policy refinement only; no cross-layer import violations
- Must comply with `docs/arc-harness-rules.md` and `docs/ecosystem-rules.md`

## Acceptance Criteria

- [ ] After 5+ zero-reward steps with grounded player/goal, the solver exits generic late-run exploration churn and enters a plateau-aware exploitation mode
- [ ] Action families with repeated zero reward and no meaningful change are explicitly deprioritized
- [ ] The final five steps do not switch across more than two unrelated action families unless new evidence appears
- [ ] If `ACTION6` is still the best-supported family, B143 coordinate policy remains intact while generic churn is reduced
- [ ] Solve trace / strategy summary exposes the plateau reason and ranked action-family support
- [ ] Focused ARC tests and broader regression suite pass with no regressions

## Outcome Statement

When complete, grounded `space/reach_goal` runs will either commit to a small set of best-supported actions or explicitly abandon the current hypothesis, instead of spending the remaining budget in low-value exploration churn.