# B100 — Graduate ARC Plan Chunks from Explore to Directed Movement

- **State:** complete
- **Priority:** P1
- **Plan:** backlog/plans/B-100-arc-directed-chunking.md
- **Dependencies:** B95, B97, B99

## Problem Statement

Even after the solve phase infers an archetype and victory condition, `PlanChunker` still often
defaults to `Explore: try unexplored action to gather more information`. That means the agent keeps
exploring after the conditions for directed movement have started to appear.

## What It Does

Teach chunk generation to switch modes once the solve context is strong enough:

- if `player` exists and a goal candidate exists, prefer directed chunks
- if a path or region trend exists, build a short movement chunk toward it
- if solve evidence is weak, remain in explore mode explicitly
- make the chunk mode visible in exported solve summaries

This is the bridge from "I know something about the puzzle" to "I am now trying to solve it."

## Acceptance Criteria

1. `PlanChunker` only stays in explore mode when solve evidence is genuinely insufficient.
2. When `player` and `goal` evidence exist, chunk generation prefers directed movement over pure exploration.
3. Solve summaries clearly show whether the chunk is `explore` or `directed`.
4. Add at least 3 tests for mode transition into directed chunking.
5. A live run with player+goal evidence no longer reports `ACTIVE CHUNK: Explore...` by default.

## Outcome

The solve phase starts using what it knows instead of endlessly gathering more evidence.

## Validation Note (2026-03-31)

- Implemented files: `agents/arc3/solver.py`, `tests/test_arc3_solver.py`
- Tests run: focused ARC solver suite
- Result: passing locally during delegated integration
- Regression checks: live solve runs now produce directed chunks in cases where player+goal evidence is strong enough
