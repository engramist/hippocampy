# B297 - README + Install Path: Make the Front Door Work and Sell

State: complete (2026-07-13) — see PR against main from branch feat/b297-readme-install
Priority: P0

Plan: (inline — this card is self-contained)

### Problem

`README.md` (93 lines) is the launch surface and it currently:

1. **Points at a dead repo**: quick-install curls `https://raw.githubusercontent.com/djs54/hippocampy/main/scripts/install.sh` — the account is now `engramist` (rename already done); the link 404s or redirects unreliably.
2. **Leads with `curl | sh` as the RECOMMENDED install** — for a daemon that reads AI conversations, security-conscious devs (the target audience) won't pipe a script to shell. `pipx install hippocampy` must be the headline path — and the sdist test failing (`test_public_release_manifest::TestSdistDistribution::test_sdist_exists`) suggests packaging isn't proven.
3. **Opens with the patent notice** before saying what the product does — inverted priorities for a landing page; OSS audiences react badly to patent-forward framing.
4. **Doesn't sell the wedge**: no demo, no "why", no quickstart-to-first-win. The differentiator (cross-agent continuity: hit token limit in Claude Code → open Codex → resume line appears) isn't shown.

### What This Card Does

**Part A — packaging proof (do first, it gates the install docs):**
1. `python -m build` (install `build` in the venv if needed). Fix whatever breaks until BOTH sdist and wheel build cleanly. Likely suspects: `pyproject.toml` package discovery picking up stray top-level dirs (`mcp_engine` — gone after B294, `benchmarks`, `agents`, `sidequests`), missing package-data for `plugin/skills/**` and `adapters/**` hook templates, README path in project metadata.
2. Smoke the wheel in a scratch venv: `python -m venv /tmp/smoke && /tmp/smoke/bin/pip install dist/*.whl && /tmp/smoke/bin/campy --help` → must print help. Also `/tmp/smoke/bin/python -c "import campy.brain_daemon"`.
3. Make the sdist test pass honestly (coordinate with B295 cluster F — whichever card lands second inherits the check; don't double-fix).
4. Do NOT publish to PyPI in this card — but verify the name: `pip index versions hippocampy` / check pypi.org for squatting. If the name is taken, STOP and report (maintainer decision).

**Part B — README rewrite.** Structure (keep total ≤ 160 lines, link out for depth):
1. **H1 + one-liner**: "One memory for all your coding agents." Sub-line: local-first, graph-native (Kùzu), works with Claude Code, Codex, Gemini CLI, VS Code Copilot via MCP.
2. **Demo block**: placeholder for the handoff GIF (`docs/assets/handoff-demo.gif` — record separately; commit an HTML comment `<!-- TODO: record per docs/demo-script.md -->` plus a text transcript of the demo so the section works before the GIF exists). Write `docs/demo-script.md` with the exact 30-second demo storyboard: (1) Claude Code session working on a card, token limit hits → (2) open Codex → (3) session_start prints `[Campy] Working on B29x (branch: feat/x · abc123). Next: ...` → (4) work continues.
3. **Quickstart**: `pipx install hippocampy` → `campy setup` → open your agent. Three commands max to first value. Verify each command against the actual CLI (`campy --help`) — do not document commands that don't exist.
4. **How it works** (5 lines + link to `docs/ARCHITECTURE.md`): notify_turn capture → Gated Consolidation Loop → Kùzu graph → recall tools + CONTEXT.md file bridge + resume line.
5. **What makes it different** (3 bullets): cross-agent continuity (CWS); local-first/private (no server, no cloud); memory arrives without tool calls (layer cake).
6. **Install alternatives**: pip, and the `install.sh` DEMOTED to "alternative" with the inspect-first variant listed FIRST and the URL fixed to `engramist/hippocampy` (single source: consider `https://hippocampy.dev` later; for now the raw URL, correct).
7. **Status & contributing**: link CONTRIBUTING.md (three-gate pipeline), badges once CI exists (coordinate with B295's `tests.yml` — add the badge if the workflow is merged).
8. **License & patent**: bottom of the page, two lines: MIT (or per B296/legal outcome) + "Patent pending — see PATENTS.md" (file created by B296; if B296 hasn't landed, link `docs/nonprovisional-strategy.md` unchanged and leave a TODO comment).
9. Fix every other `djs54` reference repo-wide: `grep -rn "djs54" --include="*.md" --include="*.sh" --include="*.toml" .` → update each to `engramist` (or org, per final home).

**Part C — install.sh sanity:** read `scripts/install.sh` top to bottom; fix its repo URL(s); ensure it fails loudly (`set -euo pipefail`) and states what it will do before doing it. If it installs hooks into `~/.claude`/`~/.codex`, it must echo each write.

### Dependencies

- B296 (PATENTS.md, tree decisions) and B295 (CI badge, sdist test) interact — this card can land first with TODO markers, but final README text assumes their outcomes.
- B294 helps Part A (removes stray package from discovery).

### Files to Modify

- `README.md` — full rewrite per structure above
- `pyproject.toml` — packaging fixes
- `scripts/install.sh` — URL + safety pass
- Create: `docs/demo-script.md`
- Repo-wide `djs54` reference sweep

### Acceptance Criteria

- [ ] `python -m build` produces sdist + wheel with zero errors
- [ ] Wheel smoke-test in a clean venv: `campy --help` works
- [ ] `grep -rn "djs54" .` (excluding `.git/`) → zero hits
- [ ] README: quickstart commands each verified against the real CLI; patent mention only at bottom; ≤160 lines; `curl | sh` not the recommended path
- [ ] `docs/demo-script.md` exists with the 4-step storyboard
- [ ] `bash -n scripts/install.sh` passes; URLs corrected; announces writes
- [ ] PyPI name availability checked and result recorded in the report

### Outcome

The front door works (correct links, provable install) and sells the actual wedge — cross-agent continuity — instead of leading with legal notices and a pipe-to-shell.
