# B171: Add ActionFact Node Type and Persist to KuzuDB

**Status**: ready
**Assigned**: TBD
**Depends on**: B170 (hypothesis persistence — ActionFacts feed into hypotheses)
**Complexity**: Small-Medium (2-3 hours)
**Priority**: P1 (missing schema — deterministic action knowledge has no graph representation)
**Plan**: backlog/plans/B-171-action-fact-kuzu.md

## Problem Statement

`HypothesisManager.action_facts` (`hypothesis.py:336`) stores deterministic action facts (e.g. "ACTION4 moves player right by 1 cell") in a Python dict. These facts are the most reliable knowledge the agent produces — they're deterministic observations, not guesses.

There is **no `ActionFact` node type in the schema at all**. This knowledge is invisible to graph queries and lost between solves.

## Root Causes

1. ActionFact was added as a dataclass (`hypothesis.py:94-100`) without corresponding schema work
2. No card planned the persistence — it was treated as ephemeral despite being deterministic ground truth

## What This Card Does

1. **Add `ActionFact` node type to schema**: `action_id`, `task_id`, `level`, `effect_description`, `delta_row`, `delta_col`, `n_cells_changed`, `confidence`, `observation_count`, `created_at`
2. **Add `DERIVED_FROM_FACT` relationship**: `ActionFact → ActionEffect` (links fact to the exploration observations that produced it)
3. **Add `SUPPORTS_HYPOTHESIS` relationship**: `ActionFact → Hypothesis` (links deterministic facts to hypotheses they support)
4. **Persist facts to KuzuDB**: When `HypothesisManager` creates or updates an ActionFact, write to KuzuDB
5. **Load facts from KuzuDB**: At start of solve, reload known action facts (enables cross-level memory within a puzzle)

## Ecosystem Layer

- `agents/arc3/hypothesis.py` (Agent Runtime — fact writer)
- `mcp_engine/schema.py` (Data Products — new node type + 2 relationship types)

**Cross-layer compliance**: Same pattern as B170. No cross-layer violations.

## Files to Create/Modify

- **MODIFY** `mcp_engine/schema.py` — Add `ActionFact` node type and `DERIVED_FROM_FACT`, `SUPPORTS_HYPOTHESIS` relationships
- **MODIFY** `agents/arc3/hypothesis.py` — Persist ActionFacts to KuzuDB on create/update
- **CREATE** `tests/test_b171_action_fact_persistence.py` — Tests for ActionFact CRUD, relationship creation
- No changes to `docs/tool-catalog.md`

## Acceptance Criteria

- [ ] `ActionFact` node type exists in schema with all required fields
- [ ] Creating an ActionFact writes a node to KuzuDB
- [ ] Updating observation_count updates the node in KuzuDB
- [ ] `DERIVED_FROM_FACT` edges link ActionFacts to ActionEffect nodes from B168 exploration
- [ ] When KuzuDB unavailable, falls back to in-memory-only
- [ ] All existing ARC tests pass

## Outcome

Deterministic action knowledge is persisted to the knowledge graph. The agent can query "what does ACTION4 do?" from KuzuDB instead of rediscovering it each solve.
