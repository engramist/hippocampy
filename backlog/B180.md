# B180: Token Cost Tracker and Budget Enforcer

**Status**: ready
**Assigned**: TBD
**Depends on**: None
**Complexity**: Medium (2-4 hours)
**Priority**: P1 (foundation for all FinOps and efficiency metrics)
**Plan**: backlog/plans/B-180-cost-tracker-budget.md

## Problem Statement

The ARC agent tracks token counts per step (`_prompt_tokens_per_step` in orchestrator.py) but has no cost awareness. When running against cloud LLMs, a single puzzle can consume unlimited tokens with no budget enforcement. The agent burned 146 steps on a puzzle it would never solve — with no signal to stop spending.

There is no cost-per-puzzle metric, no budget exhaustion detection, and no pricing table. The only token data captured is `tokens_input`/`tokens_output` in submission results, with no dollar conversion.

## Root Causes

1. No pricing table mapping model names to $/token
2. No accumulator tracking running cost during a puzzle solve
3. No budget enforcement — runner never checks whether to abandon a puzzle
4. `LedgerBrainClient._record()` captures latency but not token cost

## What This Card Does

1. **New `CostTracker` class** in `agents/arc3/cost_tracker.py`: Accumulates `tokens_in`, `tokens_out`, computes `cost_usd` from a pricing table. Exposes `budget_exhausted` property.
2. **Pricing table** in `sidequests.toml` under `[cost]` section: Maps model name to input/output price per million tokens. Ollama models = $0 but tokens still tracked for efficiency.
3. **Budget enforcement** in `runner.py`: After each step, check `cost_tracker.budget_exhausted`. If true, abandon puzzle with status `"budget_exceeded"`.
4. **KuzuDB persistence**: Store `PuzzleCostSummary` node per puzzle (total_tokens_in, total_tokens_out, cost_usd, outcome, model).
5. **Extend `LedgerBrainClient._record()`** with token count and cost per call.

## Ecosystem Layer

- `agents/arc3/cost_tracker.py` (Agent Runtime — new file)
- `agents/arc3/runner.py` (Agent Runtime — budget check hook)
- `agents/arc3/orchestrator.py` (Agent Runtime — feed tokens to tracker)
- `benchmarks/arc3/adapter.py` (Eval Infra — extend ledger recording)
- `mcp_engine/schema.py` (Data Products — PuzzleCostSummary node)
- `sidequests.toml` (Config — pricing table)

**Cross-layer compliance**: No cross-layer violations. Cost tracker is Agent Runtime; schema addition is Data Products.

## Files to Create/Modify

- **CREATE** `agents/arc3/cost_tracker.py` — CostTracker class with pricing table, accumulator, budget check
- **MODIFY** `agents/arc3/orchestrator.py` — Feed token counts to CostTracker after each LLM call
- **MODIFY** `agents/arc3/runner.py` — Check `budget_exhausted` after each step; abandon with status if hit
- **MODIFY** `benchmarks/arc3/adapter.py` — Extend `LedgerBrainClient._record()` with token/cost fields
- **MODIFY** `mcp_engine/schema.py` — Add `PuzzleCostSummary` node type
- **MODIFY** `sidequests.toml` — Add `[cost]` section with pricing table and per-puzzle budget
- **CREATE** `tests/test_b180_cost_tracker.py` — Tests for accumulation, budget enforcement, pricing lookup
- No changes to `docs/tool-catalog.md`

## Acceptance Criteria

- [ ] `CostTracker.total_cost_usd` accurate within 5% of manual calculation
- [ ] Budget enforcement stops puzzles when `cost_usd > budget_per_puzzle`
- [ ] Ollama models track tokens at $0 cost (efficiency metrics still work)
- [ ] `PuzzleCostSummary` node written to KuzuDB after each puzzle
- [ ] `LedgerBrainClient` records include token count and cost per call
- [ ] Submission results include `cost_usd` field
- [ ] All existing ARC tests pass

## Outcome

Every puzzle solve has a dollar cost attached. Hopeless puzzles are abandoned when budget is exhausted instead of burning unlimited tokens. Efficiency metrics (tokens/solve, cost/solve) become available for the enhanced ABHarness (B182).
