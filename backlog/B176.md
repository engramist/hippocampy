# B176: Plateau Policy Scores All Untried Actions at 0.0 — Force Exploration of Unused Actions

**Status**: ready
**Assigned**: TBD
**Depends on**: None
**Complexity**: Small (1-2 hours)
**Priority**: P1 (root cause: ACTION2/ACTION3 never tried despite 146 zero-reward steps)
**Plan**: backlog/plans/B-176-plateau-explore-untried-actions.md

## Problem Statement

`_score_action_families()` in `solver.py:2275` scores each available action based on `observed_action_effects` from the hypothesis context. Actions that have never been tried have no entries in `observed_action_effects`, so they score 0.0 (the default at line 2277).

Actions that *have* been tried but produce zero reward also score 0.0 (or slightly negative due to the zero-streak penalty). This means untried actions rank *equal to or above* known-bad actions — but never *higher* than actions that coincidentally produced some grid change (`avg_meaningful_change > 0`).

In the trace, ACTION4 scored highest because it caused 42 cells to change (gravity effect), even though it never moved the player. ACTION2 and ACTION3 scored 0.0 because they'd never been tried. Plateau mode locked onto ACTION4 and never explored alternatives.

## Root Causes

1. `_score_action_families()` initializes all scores to 0.0 — untried actions are indistinguishable from known-bad ones
2. No exploration bonus for untried actions during plateau mode
3. Plateau lock (B146) commits to the first locked family and requires a 0.5 score differential to unlock — impossible when untried actions are at 0.0

## What This Card Does

1. **Exploration bonus for untried actions**: In `_score_action_families()`, actions with zero entries in `observed_action_effects` get a positive exploration bonus (e.g., +0.3) during plateau mode
2. **Decay tried-but-failed actions faster**: Actions with zero-reward streak >= 5 get an additional penalty proportional to streak length
3. **Plateau lock relaxation**: After N steps locked to a family with zero progress, reduce the unlock threshold from 0.5 to 0.1 so alternatives can compete

## Ecosystem Layer

- `agents/arc3/solver.py` (Agent Runtime — plateau policy and action scoring)

**Cross-layer compliance**: No cross-layer violations.

## Files to Create/Modify

- **MODIFY** `agents/arc3/solver.py` — Update `_score_action_families()` to add exploration bonus; update plateau lock threshold decay in `solve()` around line 2641
- **CREATE** `tests/test_b176_plateau_explore_untried.py` — Tests for exploration bonus, lock relaxation, and score differentiation
- No changes to `docs/tool-catalog.md`

## Acceptance Criteria

- [ ] Untried actions score higher than tried-and-failed actions during plateau mode
- [ ] Exploration bonus is positive (>= 0.2) for actions with no observed effects
- [ ] Plateau lock threshold decays after N consecutive zero-reward steps with locked family
- [ ] After threshold decay, an untried action can win the lock
- [ ] Trace events show exploration bonus and lock relaxation when triggered
- [ ] All existing ARC tests pass

## Outcome

Plateau mode actively explores untried actions instead of locking onto a known-ineffective action. ACTION2/ACTION3 get attempted within the first 10-15 plateau steps instead of never.
