# B110 - ARC Prompt Dedup and Section Ownership

State: complete
Completed: 2026-04-01
Validation: 90/90 tests passed (orchestrator/solver + B109-B115 regression suites)

Plan: backlog/plans/B-110-arc-prompt-dedup-and-section-ownership.md

### Problem

Recent live ARC smoke runs show that the harness prompt is still too repetitive even after major
prompt-shaping work. The same action evidence, board facts, and exploration state are being
restated across multiple sections:

- `ACTION FACTS`
- `PATH HYPOTHESES`
- `OBSERVED EFFECTS`
- `PLAN`
- `HISTORY`
- `OBSERVATION`
- final `INSTRUCTION`

This is not random duplication. It is a prompt-composition problem: each subsystem appends useful
context, but there is no final dedup/compression pass enforcing a "one fact, one place" rule.

### What This Card Builds

A prompt-composition cleanup pass for the ARC harness:

- define clear ownership for each prompt section
- deduplicate repeated action evidence and board-state summaries
- ensure the final instruction references facts instead of restating them
- make the prompt read like one intentionally edited message instead of a stack of subsystem dumps

### Depends on

- B88
- B90
- B93
- B94

## Files to Create/Modify

- `agents/arc3/orchestrator.py`
- `benchmarks/arc3/PROMPT_STRATEGY.md`
- `tests/test_arc3_orchestrator.py`

## Acceptance Criteria

1. The prompt builder enforces section ownership:
   - `ACTION FACTS` = operator facts only
   - `PATH HYPOTHESES` = sequence/path facts only
   - `OBSERVED EFFECTS` = latest transition evidence only
   - `SOLVE CONTEXT` / `PLAN` = high-level goal/chunk only
   - `INSTRUCTION` = decision rule only, without re-dumping earlier facts
2. Repeated action evidence does not appear in multiple sections unless the later reference is
   explicitly compressed.
3. If `OBSERVED EFFECTS` already provides sufficient board-change context, `OBSERVATION` is reduced
   or suppressed.
4. Tests cover:
   - duplicate action fact suppression
   - duplicate board summary suppression
   - instruction compression
5. A live smoke run shows a noticeably smaller, less repetitive prompt while preserving decision
   quality.

## Outcome

The ARC harness prompt becomes intentionally composed instead of cumulatively assembled, which
should reduce token waste and make the model’s decision signal clearer.
