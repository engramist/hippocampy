# B203 — Fix Stale tool_rules and phase_owner Mappings in Orchestration Report

- **State:** ready
- **Priority:** P1
- **Plan:** backlog/plans/B-203-fix-tool-rules.md
- **Ecosystem Layer(s):** Agent Runtime & Harness
- **Cross-layer violations:** None — change is confined to runner.py reporting logic.
- **Dependencies:** B201 (Durable Phase-State Machine) — must be complete (it is).

## Problem Statement

`DurableARCRunner._build_orchestration_report()` (runner.py, line ~2094) contains three dicts that still use **pre-B201 phase name strings** (`"act"`, `"ingest"`, `"solve"`) instead of the B201 enum values (`"execute"`, `"evaluate"`, `"route"`).

This mismatch causes **120 false phase_violation entries** in `orchestration_report.violations` on every run — nearly every SideQuests tool call is flagged as illegal even when the orchestrator is behaving correctly.

**Evidence from smoke test (2026-04-11):** `submission_results_single.json` shows 120 violations, `status: "violation"`. Breakdown:
- `notify_turn`: 38 violations — allowed_phases still lists `"act"`, `"ingest"` instead of `"execute"`, `"evaluate"`
- `current_truth`: 44 violations — allowed_phases still lists `"act"`, `"ingest"`, `"solve"` instead of `"execute"`, `"evaluate"`, `"route"`
- `register_plan`: 25 violations — allowed_phases still lists `"solve"` instead of `"route"`
- `recall_lessons`: 9 violations — same stale names
- `report_outcome`: 3 violations — allowed `"solve"` instead of `"route"`
- `branch_quest`: 1 violation — phase was `"unknown"` before bootstrap wiring

Stale sections:
1. `phase_owner` dict (line ~2095) — maps `"solve"`, `"act"`, `"ingest"` to owners
2. `decision_flow` dict (line ~2105) — maps `"solve"`, `"act"`, `"ingest"` to proposer/executor
3. `tool_rules` dict (line ~2115) — `allowed_phases` lists for each tool

Additionally, B202 adds `perceive` as a per-step phase — `notify_turn` and `current_truth` will now fire in `perceive` phase and must be allowed there.

## What It Does (Scope)

Update the three stale dicts in `_build_orchestration_report()` to use B201 phase names and include `perceive` where appropriate:

### phase_owner (rename keys)
- `"solve"` → `"route"`
- `"act"` → `"execute"`
- `"ingest"` → remove (now part of `"evaluate"`)

### decision_flow (rename keys)
- `"solve"` → `"route"`
- `"act"` → `"execute"`
- `"ingest"` → remove (now part of `"evaluate"`)

### tool_rules allowed_phases (update values)

| Tool | Current (stale) | New (correct) |
|------|----------------|---------------|
| `notify_turn` | `["bootstrap", "act", "ingest", "evaluate", "finalization"]` | `["bootstrap", "perceive", "execute", "evaluate", "finalization"]` |
| `current_truth` | `["bootstrap", "act", "ingest", "solve"]` | `["bootstrap", "perceive", "execute", "evaluate", "route"]` |
| `recall_lessons` | `["bootstrap", "solve", "ingest"]` | `["bootstrap", "perceive", "route", "evaluate"]` |
| `register_plan` | `["bootstrap", "solve"]` | `["bootstrap", "route"]` |
| `report_outcome` | `["evaluate", "solve", "finalization"]` | `["evaluate", "route", "finalization"]` |

### Not in scope
- Changing which tools are called in which phases (behavior unchanged).
- Adding new tools or tool catalog entries.

## Files to Create/Modify

| File | Action |
|------|--------|
| `agents/arc3/runner.py` | **Modify** — Update `phase_owner`, `decision_flow`, and `tool_rules` dicts in `_build_orchestration_report()` (lines ~2094-2122) |

### Adapter/allow-list impact
- No new tools introduced.
- No tool-catalog changes.

## Acceptance Criteria

1. `phase_owner` dict contains `"route"`, `"execute"`, `"perceive"` keys and does not contain `"solve"`, `"act"`, or `"ingest"`.
2. `decision_flow` dict contains `"route"`, `"execute"` keys and does not contain `"solve"`, `"act"`, or `"ingest"`.
3. After a smoke test run, `orchestration_report.violations` is empty (or contains only legitimate violations, not stale-name false positives).
4. `orchestration_report.status` is `"ok"` on a clean run.
5. `notify_turn` allowed_phases includes `"perceive"` and `"execute"` (not `"act"` or `"ingest"`).
6. `current_truth` allowed_phases includes `"perceive"`, `"route"`, `"evaluate"` (not `"solve"`, `"act"`, `"ingest"`).
7. `register_plan` allowed_phases includes `"route"` (not `"solve"`).
8. `pytest tests/test_arc3_durable_runner.py` passes.

## Outcome

The orchestration report accurately reflects the B201 phase model. False violations are eliminated, making `status: "ok"` the normal output on a correctly-behaving run. Violations in the report now indicate real problems, not stale string mismatches.
