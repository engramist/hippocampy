# B124 - Structured Task Ledger for ARC SolveContext

State: complete
Completed: 2026-04-01
Validation: 84/84 tests passed (tests/test_arc3_solver.py + tests/test_arc3_orchestrator.py + tests/test_b124_chunk_ledger.py)

Plan: backlog/plans/B-124-structured-task-ledger.md

Ecosystem Layer: Runtime (Layer 3) — ARC Agent reasoning state

### Problem

The ARC SolveContext tracks plan progress via a single `active_chunk: Optional[PlanChunk]` field.
Once a chunk completes, it disappears from the agent's view. Over 15+ steps the agent forgets
which chunks are done, which are pending, and which failed — causing exploration loops where it
re-discovers strategies it already tried and abandoned.

The current `_build_solve_section()` only shows the active chunk. There is no structured view
of completed vs. pending work.

### What This Card Builds

A structured task ledger in SolveContext that gives the agent a Claude-style todo view:

1. `SolveContext.chunk_ledger: List[ChunkLedgerEntry]` — each entry has chunk description,
   status (completed / active / pending / failed), steps used, and outcome summary.
2. `_build_solve_section()` renders the ledger as a compact table:
   `[✓] Move to goal (3 steps, +0.4 progress)` / `[→] Avoid wall (active, step 2/4)` / `[ ] Collect item (pending)`
3. When a chunk completes or fails, it moves to the appropriate status with an outcome note.
4. The ledger is capped at last 8 entries to stay within token budget.
5. Ledger state is included in checkpoint export for resume.

### Depends on

- None (works with existing PlanChunk infrastructure)

### Priority

P1 — Prevents the "forgetful loop" which is the second most common failure mode after ACTION6
blindness.

## Files to Create/Modify

- `agents/arc3/solver.py` — **MODIFY**: add `ChunkLedgerEntry` dataclass, add `chunk_ledger` to `SolveContext`, update `_build_solve_section()` to render ledger, update chunk lifecycle methods
- `agents/arc3/checkpoint.py` — **MODIFY**: include `chunk_ledger` in checkpoint serialization
- `tests/test_arc3_solver.py` — **MODIFY**: test ledger accumulation, cap, rendering, checkpoint roundtrip

## Acceptance Criteria

1. `SolveContext.chunk_ledger` tracks completed, active, pending, and failed chunks.
2. `_build_solve_section()` renders a compact status table visible in prompt trace.
3. Ledger entries include: description, status, steps_used, outcome_summary.
4. Ledger is capped at 8 entries (oldest completed entries dropped first).
5. Checkpoint save/restore includes ledger state.
6. Existing tests pass unchanged — no behavioral diff except richer solve section output.

## Outcome

The agent always knows what it has tried, what worked, what failed, and what remains — eliminating
the forgetful exploration loop.
