# B188: Cross-Run Regression Detection

**Status**: ready
**Assigned**: TBD
**Depends on**: B182 (enhanced metrics)
**Complexity**: Small (1-2 hours)
**Priority**: P2 (system monitoring eval layer)
**Plan**: backlog/plans/B-188-regression-monitor.md

## Problem Statement

There's no automated detection of quality degradation across runs. A code change that drops solve rate by 20% is only noticed by manual comparison. The "System Monitoring" eval layer watches for regressions at scale, across real usage over time.

## Root Causes

1. No historical metrics storage beyond individual result files
2. No baseline comparison across runs
3. No alerting when metrics degrade

## What This Card Does

1. **New `RegressionMonitor` class** in `benchmarks/arc3/regression_monitor.py`
2. Reads historical results from `benchmarks/results/` directory
3. Compares current run against rolling 3-run average
4. Flags regression when:
   - Solve rate drops > 10%
   - Avg cost per puzzle increases > 50%
   - Crash rate increases > 5%
   - Judge score avg drops > 1.0 point
5. Tracks per-run metadata: model version, config hash, git commit hash
6. Emits structured alert (JSON) with regression details

## Ecosystem Layer

- `benchmarks/arc3/regression_monitor.py` (Eval Infra — new file)

**Cross-layer compliance**: No cross-layer violations. Pure eval infrastructure.

## Files to Create/Modify

- **CREATE** `benchmarks/arc3/regression_monitor.py` — RegressionMonitor class, alert format
- **MODIFY** `agents/arc3/runner.py` — Call regression check after batch run completes
- **CREATE** `tests/test_b188_regression_monitor.py` — Tests with synthetic historical data, regression thresholds
- No changes to `docs/tool-catalog.md`

## Acceptance Criteria

- [ ] Regression flagged when solve rate drops > 10% vs 3-run average
- [ ] No false positive when metrics fluctuate within normal range
- [ ] Per-run metadata (model, config hash, commit) stored with results
- [ ] Structured alert output with regression type, magnitude, and affected metrics
- [ ] Works with < 3 historical runs (skip comparison, no crash)
- [ ] All existing ARC tests pass

## Outcome

Quality degradation is caught immediately after a run, not days later during manual review. The feedback loop for code changes becomes: change → run → regression check → fix or ship.
