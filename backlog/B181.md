# B181: Outcome Eval — LLM-as-Judge for Near-Miss Grading

**Status**: ready
**Assigned**: TBD
**Depends on**: None
**Complexity**: Medium (2-4 hours)
**Priority**: P1 (replaces binary correct/incorrect with gradient signal)
**Plan**: backlog/plans/B-181-outcome-judge.md

## Problem Statement

Puzzle outcomes are binary: `correct: true/false`. A puzzle where the agent gets 90% of cells right is scored identically to one where it never moved. This makes it impossible to tell which strategies are promising vs which are random.

The agent needs a richer outcome signal — a rubric-based score that captures *how close* the agent got, not just whether it won.

## Root Causes

1. `ABTaskResult.correct` is a boolean — no partial credit
2. No post-solve analysis of final grid vs expected solution
3. No LLM judgment of reasoning quality from the trajectory

## What This Card Does

1. **New `OutcomeJudge` class** in `benchmarks/arc3/outcome_judge.py`: After puzzle completes, scores on a fixed rubric:
   - **Structural correctness** (0-5): Right dimensions, right color palette, grid structure
   - **Partial match** (0-5): Fraction of cells matching expected solution
   - **Reasoning quality** (0-5): Did the agent's trajectory narrative show understanding
2. **`JudgeVerdict` dataclass**: Holds scores + explanation text per dimension
3. **Integration in runner.py**: Called after puzzle loop exits, before result recording
4. **ABTaskResult extension**: Add `judge_verdict` field with composite score
5. **Configurable judge model**: Separate from the agent's LLM (can use cheaper model)

## Ecosystem Layer

- `benchmarks/arc3/outcome_judge.py` (Eval Infra — new file)
- `agents/arc3/runner.py` (Agent Runtime — call judge post-solve)
- `benchmarks/ab_harness.py` (Eval Infra — ABTaskResult extension)

**Cross-layer compliance**: Judge is Eval Infra; runner integration is a hook call, not a cross-layer violation.

## Files to Create/Modify

- **CREATE** `benchmarks/arc3/outcome_judge.py` — OutcomeJudge class, JudgeVerdict dataclass, rubric prompts
- **MODIFY** `agents/arc3/runner.py` — Call OutcomeJudge after puzzle loop, add verdict to result payload
- **MODIFY** `benchmarks/ab_harness.py` — Add `judge_verdict` to ABTaskResult, include in metrics
- **CREATE** `tests/test_b181_outcome_judge.py` — Tests for rubric scoring, verdict format, judge integration
- No changes to `docs/tool-catalog.md`

## Acceptance Criteria

- [ ] `OutcomeJudge` returns `JudgeVerdict` with 3 dimension scores (0-5 each)
- [ ] Near-miss puzzles (>70% cell match) score >= 3 on partial match dimension
- [ ] Complete failures score 0-1 on all dimensions
- [ ] Judge model is configurable separately from agent model
- [ ] `ABTaskResult` includes `judge_verdict` with composite score
- [ ] Judge works when expected solution is not available (returns None, no crash)
- [ ] All existing ARC tests pass

## Outcome

Every puzzle has a quality gradient, not just pass/fail. Strategy comparisons can detect "this approach gets close but fails at the final step" vs "this approach is completely lost." Near-miss rate becomes a key metric in B182.
