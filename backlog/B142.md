# B142: Chunk Graduation Must Respect Evidence Floor

**Status**: Backlog  
**Assigned**: TBD  
**Depends on**: None (but B141 bail-out provides defense-in-depth)  
**Complexity**: Small–Medium (2–3 hours)  
**Priority**: P0 (root cause — inflated graduation prevents dissonance and replanning)  
**Related**: B139 (reopened acceptance criteria), B124 (task ledger), B141 (bail-out safety net)  
**Plan**: backlog/plans/B-142-graduation-evidence-floor.md

## Problem Statement

The chunk graduation logic produces a score of **0.87** despite:
- `evidence = 0.10`
- `chunk_progress = 0.00`
- 15 consecutive zero-reward steps

High player confidence (0.90), goal confidence (0.83), and coverage (1.00) dominate the formula, masking the fact the agent has **zero empirical evidence** that its plan works. This inflated graduation score prevents `dissonance_detected` from ever becoming `True`, which blocks replanning entirely.

The agent correctly identifies the puzzle as `space / reach_goal` but then never questions its plan because the graduation score stays high regardless of actual outcomes.

Evidence from `live_gemma4_e4b_timeout_1775221087`:
- Graduation score remained ~0.87 across all 15 steps
- `dissonance_detected` stayed `False` the entire run
- Chunk progress stayed at `0.00` — plan was never re-evaluated

## What This Card Builds

1. **Evidence floor gate**: If `evidence < 0.3` AND `chunk_progress == 0.0` after `≥ 3` steps using the chunk, cap the graduation score at `max(0.4, evidence * 2)` regardless of confidence scores.

2. **Progress-decay penalty**: Graduation score decays by `0.05` per consecutive zero-reward step while the chunk is active, floored at `0.2`.

3. **Dissonance trigger on graduation drop**: When graduation drops below `0.5` (the existing dissonance threshold), set `dissonance_detected = True` to trigger replanning.

4. **Trace evidence**: Emit `graduation_capped_reason`, `evidence_floor_applied`, and `progress_decay_applied` fields in the step trace so the policy shift is auditable.

## Files to Create/Modify

- `agents/arc3/solver.py` — modify chunk graduation scoring to enforce evidence floor and progress-decay penalty
- `agents/arc3/orchestrator.py` — wire graduation-drop → dissonance trigger
- `tests/test_b142_graduation_evidence.py` — regression tests: high-confidence but zero-evidence chunks must degrade

## Ecosystem / Layer Compliance

- Layer: ARC agent solve engine (`agents/arc3/solver.py`) + orchestration policy (`agents/arc3/orchestrator.py`)
- No cross-layer import violations
- Builds on existing graduation scoring — additive change, not rewrite
- Complies with `docs/arc-harness-rules.md` and `docs/ecosystem-rules.md`

## Acceptance Criteria

- [ ] A chunk with `evidence < 0.3` and `chunk_progress == 0.0` after 3+ steps has graduation capped at ≤ 0.4
- [ ] Graduation decays per consecutive zero-reward step (visible in trace)
- [ ] When graduation drops below 0.5, `dissonance_detected` becomes `True`
- [ ] Dissonance triggers solver re-evaluation (new chunk or modified plan)
- [ ] Trace output includes `graduation_capped_reason` and `progress_decay_applied`
- [ ] Existing test suites pass with no regressions
- [ ] High-evidence chunks (evidence ≥ 0.3, progress > 0) are **not** penalized (no false positives)
