# B185: Failure Classification Taxonomy

**Status**: ready
**Assigned**: TBD
**Depends on**: None
**Complexity**: Small (1-2 hours)
**Priority**: P1 (feeds robustness metrics in B182)
**Plan**: backlog/plans/B-185-failure-taxonomy.md

## Problem Statement

When a puzzle fails, the runner captures `str(exc)` and records it as a generic error message. All failures look the same in results — there's no distinction between an LLM timeout, an API error, a strategy exhaustion, or a crash. This makes it impossible to diagnose whether failures are infrastructure problems or intelligence problems.

## Root Causes

1. Runner's `except` block at line ~135 captures raw exception string with no classification
2. `ABTaskResult.error_message` is free-form text, not a structured category
3. No failure taxonomy defined anywhere in the codebase

## What This Card Does

1. **New `FailureTaxonomy` enum** in `agents/arc3/failure_taxonomy.py`:
   - `LLM_TIMEOUT` — LLM didn't respond within timeout
   - `LLM_PARSE_ERROR` — LLM returned unparseable output
   - `API_ERROR` — ARC API returned error (400, 500, etc.)
   - `BUDGET_EXCEEDED` — Token/cost budget hit (from B180)
   - `STRATEGY_EXHAUSTED` — All strategies tried, none worked
   - `STUCK_IN_LOOP` — Detected loop, couldn't escape
   - `MAX_STEPS_REACHED` — Hit step limit without winning
   - `CRASH` — Unhandled exception
2. **`classify_failure()` function**: Takes exception + orchestrator state, returns `FailureTaxonomy`
3. **ABTaskResult extension**: Add `failure_class: Optional[str]` field

## Ecosystem Layer

- `agents/arc3/failure_taxonomy.py` (Agent Runtime — new file)
- `agents/arc3/runner.py` (Agent Runtime — classify failures)
- `benchmarks/ab_harness.py` (Eval Infra — ABTaskResult extension)

**Cross-layer compliance**: No cross-layer violations.

## Files to Create/Modify

- **CREATE** `agents/arc3/failure_taxonomy.py` — FailureTaxonomy enum, classify_failure() function
- **MODIFY** `agents/arc3/runner.py` — Replace `str(exc)` with `classify_failure(exc, orchestrator_state)`
- **MODIFY** `benchmarks/ab_harness.py` — Add `failure_class` to ABTaskResult
- **CREATE** `tests/test_b185_failure_taxonomy.py` — Tests for classification of each failure type
- No changes to `docs/tool-catalog.md`

## Acceptance Criteria

- [ ] Each of the 8 failure types is correctly classified from representative exceptions
- [ ] `classify_failure()` never raises — always returns a valid category (falls back to CRASH)
- [ ] `ABTaskResult.failure_class` populated for all failed puzzles
- [ ] Submission results include `failure_class` field
- [ ] All existing ARC tests pass

## Outcome

Every puzzle failure has a category. Robustness metrics (B182) can report crash_rate, timeout_rate, budget_exceeded_rate separately. Infrastructure failures (LLM_TIMEOUT) are distinguished from intelligence failures (STRATEGY_EXHAUSTED).
