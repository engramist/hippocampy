# B187: Parallel Strategy Racing

**Status**: ready
**Assigned**: TBD
**Depends on**: B180 (budget enforcement), B183 (supervisor)
**Complexity**: Large (4-6 hours)
**Priority**: P2 (highest potential impact, highest complexity)
**Plan**: backlog/plans/B-187-strategy-racing.md

## Problem Statement

The ARC runner processes one strategy per puzzle sequentially. Many puzzles that fail with one approach succeed with another. The agent has no way to try multiple strategies concurrently and pick the winner.

Enterprise orchestration calls for "merge and coordination" — parallel agents working on related tasks with conflict detection. The ARC analog is racing 2-3 strategy variants per puzzle and keeping the best result.

## Root Causes

1. `DurableARCRunner._run_puzzle()` creates a single `ARCOrchestrator` per puzzle
2. No mechanism to run multiple orchestrators concurrently
3. No merge logic to combine or select results from parallel attempts

## What This Card Does

1. **New `StrategyRacer` class** in `agents/arc3/strategy_racer.py`
2. Per puzzle, launches 2-3 strategy variants via `asyncio.gather()`:
   - **Variant A**: Normal solve (current behavior)
   - **Variant B**: Exploration-heavy (double behavioral sweep budget from B168)
   - **Variant C**: Pattern-first (force REPL verification, enter execution mode early)
3. First-to-solve wins. Others cancelled via `asyncio.Task.cancel()`.
4. Each variant gets its own `ARCOrchestrator` instance with 1/3 of per-puzzle token budget (B180).
5. Each variant gets its own ARC API guid (session isolation).
6. Winner's KuzuDB writes kept; losers' writes cleaned up.

## Ecosystem Layer

- `agents/arc3/strategy_racer.py` (Agent Runtime — new file)
- `agents/arc3/runner.py` (Agent Runtime — use racer instead of single orchestrator)

**Cross-layer compliance**: No cross-layer violations. KuzuDB writes serialized via queue to prevent concurrent write conflicts.

## Files to Create/Modify

- **CREATE** `agents/arc3/strategy_racer.py` — StrategyRacer class with variant configs, asyncio.gather, result merge
- **MODIFY** `agents/arc3/runner.py` — Replace single orchestrator creation with `StrategyRacer.race()` call
- **MODIFY** `benchmarks/arc3/adapter.py` — Support multiple concurrent sessions (guid isolation)
- **CREATE** `tests/test_b187_strategy_racer.py` — Tests for variant launching, cancellation, result selection, budget splitting
- No changes to `docs/tool-catalog.md`

## Acceptance Criteria

- [ ] 2-3 variants launched concurrently per puzzle
- [ ] First-to-solve cancels other variants
- [ ] Each variant respects 1/3 of per-puzzle token budget
- [ ] Each variant has isolated ARC API session (own guid)
- [ ] KuzuDB writes from losing variants are cleaned up
- [ ] Result includes which variant won
- [ ] Configurable: can disable racing (run single variant) via config
- [ ] All existing ARC tests pass

## Outcome

Puzzles that fail with one strategy can succeed with another — tried concurrently. The ARC analog of enterprise "parallel agents with merge queues." Solve rate improves without increasing per-puzzle step budget.
