# B311 - Investigate Brain Daemon's Large Transient Memory Spikes (100-150MB Baseline to 600MB-1.2GB Peaks) During ARC Live-Smoke Runs

State: open — investigating. Round 7 (2026-08-21, longer episode, 5 puzzles/30 steps, ~30.6 min, closes
B350) directly observed a repeated spike-to-1-1.8GB-then-release-to-~300MB sawtooth under sustained real
traffic, with no unbounded growth across the full run — the qualitative pattern this card set out to
fix, genuinely confirmed present under B337's shipped config. **Correction, same day, flagged from
B342, now settled by B342's completed 2x2:** this card's earlier language calling the periodic-checkpoint
mechanism itself "confirmed working causally" overstated what was actually isolated, and the follow-up
question is now answered, not just open — B342's clean 2x2 (checkpoint on/off × embedding on/off) found
that quiet-window reclaim tracks embedding-model activity exactly (present in both embedding-ON cells,
absent in both embedding-OFF cells) and is unaffected by checkpoint status in either direction. **B337's
periodic checkpoint is not the mechanism behind the reclaim this card documented.** Round 7's
*observation* (the sawtooth pattern exists and self-releases under the shipped config) still stands as a
real, reproduced phenomenon — real ARC episodes exercise embeddings continuously (goal reasoning, Loop
classification), so the sawtooth Round 7 saw was very likely driven by that embedding activity, not by
B337's checkpoint task, which happened to be running at the same time. B337's checkpoint may still have
secondary value (B342 found a separate, unexplained RSS jump at `db.close()` when checkpointing is fully
disabled, which periodic checkpointing likely avoids) — it is not being reverted — but it should not be
described as "the fix" for this sawtooth going forward. See `backlog/B342.md`'s "The Clean 2x2 Is
Complete" section for the full data. What's left open: (1) B342's actual root cause — *why* embedding
activity triggers reclaim is still not understood at a mechanism level (candidate: PyTorch/
sentence-transformers allocator behavior); (2) tuning the shipped `interval_seconds` default off the
untuned 60s (B349) — this is now a lower-priority, more clearly-secondary optimization than it looked
mid-investigation, since checkpoint cadence is confirmed not to be the lever driving the sawtooth this
card cares about most.
Priority: P2

Plan: none yet — this is an investigation card. A fix plan should be written once the trigger is identified (see "Suggested investigation steps" below).

### Problem

Found live 2026-08-08 while running ARC_AGI's `make smoke` (`agents/arc4/`, the ARC-AGI-3 client) against a real Ollama+`brain_daemon.py` stack, per explicit user request to compare memory usage between the ARC client process and the Campy brain daemon it talks to over the MCP seam. Sampled both processes' RSS every 2 seconds for a full ~308-second live episode (`ps -o rss= -p <pid>`, 154 samples per process, raw CSV preserved in this write-up below).

**ARC client (`run_single_puzzle.py`)**: flat and boring, exactly as expected of a thin client — min 24.8MB, max 78.9MB, no growth over the run.

**`brain_daemon.py`**: highly volatile. Idle/baseline RSS sits around 110-150MB, but the same long-running daemon process spiked to over 1GB twice during this one ~5-minute episode:

```
20:06:58  daemon= 632.0MB   <- spike #1, already climbing when sampling started
20:07:14  daemon= 619.0MB
20:07:31  daemon= 128.2MB   <- back to baseline
20:07:47  daemon= 137.1MB
20:08:03  daemon= 809.6MB   <- spike #2 begins
20:08:19  daemon= 849.3MB
20:08:35  daemon= 893.1MB
20:08:51  daemon=1119.4MB   <- peak, ~10x idle baseline
20:09:08  daemon= 801.0MB
20:09:24  daemon= 118.2MB   <- back to baseline
20:09:40  daemon= 139.5MB   <- stays here for the remaining ~150s of the run
```

Full numeric summary across all 154 samples (2s interval, ~308s total):

| | min | max | first sample | last sample |
|---|---|---|---|---|
| ARC client RSS | 24.8 MB | 78.9 MB | 76.7 MB | 48.4 MB |
| `brain_daemon.py` RSS | 67.7 MB | **1220.3 MB** | 632.0 MB | 140.8 MB |

Separately, before this timed profile, three single-point `ps aux` checks of the same long-running daemon PID (21539, uptime spanning the whole session, never restarted) taken minutes apart during other live-smoke runs earlier the same session showed RSS values of **2018MB**, **56MB**, and **578MB** respectively — consistent with the same spike/release pattern being sampled at different phases, not measurement noise from a different process.

**This is not a leak** — there is no net upward drift across the ~5-minute profiled run (it actually ends lower than it started, since sampling began mid-spike). The daemon reliably returns to its ~110-150MB baseline after each spike. But a background daemon meant to run persistently on a user's machine repeatedly ballooning to over 1GB (roughly 10x its resting size) twice in a single 5-minute client episode is a real resource-usage characteristic worth understanding — it risks memory pressure on constrained machines, could compound badly if multiple MCP clients/episodes run concurrently, and the repeated large-allocate-then-release cycle itself costs CPU (allocation + GC) that a steadier memory profile would avoid.

### What was NOT established (be honest about the gap)

- **Root cause is unknown.** I did not correlate the spike windows against `brain_daemon.py`'s own internal operations (no per-request timestamps were found in `~/.campy/daemon.log` in a form correlatable to the 20:06:58-20:09:08 window from the ARC_AGI side — the log's existing entries near that time were from an unrelated, already-resolved earlier crash incident in the same session, not from this run).
- I have **hypotheses, not evidence**, for the trigger (see below) — none were tested against the actual daemon internals from this investigation.
- I did not check whether the spikes correlate with specific MCP tool calls (e.g., `ingest_perception`, `arc_record_rule`, embedding/vector-search calls), specific ARC workflow phases (perceive vs. evaluate), or something unrelated to ARC entirely (e.g., campy's own background `[Loop]`/`[Capture:claude_code]` journaling activity visible interleaved in `~/.campy/daemon.log`, which was actively running throughout this same session for unrelated reasons — Campy journals Claude Code's own conversation activity in the background, and that traffic was present in the log throughout, so it cannot be ruled out as a contributing or even primary cause without further isolation).

### Suggested investigation steps

1. **Reproduce in isolation.** Run a `brain_daemon.py`-only workload (no ARC_AGI, no concurrent Claude Code journaling activity) that exercises `arc_perceive_state`/`ingest_perception`-equivalent calls repeatedly, and see if the same spike pattern appears. If it doesn't, the trigger is more likely on the ARC-calling side (embedding load triggered by ARC-specific tool calls) or the concurrent journaling activity, not something ARC-independent.
2. **Instrument memory at the call level.** Add temporary logging (or use `tracemalloc`/`memory_profiler`) around the daemon's tool-dispatch loop (`_handle_connection` / wherever `TOOL_HANDLERS` entries are invoked, per B278/B309's registration pattern) to see which specific tool call(s) precede a spike.
3. **Check the embedding model.** `campy/brain/hippocampus/graph/embeddings.py`'s `embed()` (referenced in B278's `arc_classify_game_archetype` implementation) loads `sentence-transformers/all-MiniLM-L6-v2` — check whether this model is being re-loaded per call (expensive, spiky) vs. held warm, and whether batch embedding calls could be producing large transient tensors that Python's GC is slow to reclaim.
4. **Check KuzuDB buffer/cache behavior.** Large graph writes (A175-A185's `Transition`/`Rule`/`GridEntity` traffic, now genuinely live per this session's A175-A179 completion) could be triggering large transient buffer-pool allocations in KuzuDB during write-heavy bursts.
5. **Check for GC-deferred reclaim vs. a real spike.** Confirm with `gc.collect()` timing whether forcing garbage collection immediately after a spike brings RSS down faster than observed here (~15-20s to release) — if so, tuning GC thresholds/generation behavior could be a cheap mitigation even before the root allocation source is found.

### Files potentially relevant (not confirmed, starting points only)

- `brain_daemon.py` (connection/dispatch loop)
- `campy/brain/hippocampus/graph/embeddings.py` (embedding model load/inference)
- `campy/brain/thalamus/tools/arc_queries.py` (B309's ARC tool implementations — the write-heavy path exercised by this session's live traffic)
- Kuzu client wrapper (`campy/brain/hippocampus/graph/kuzu_client.py`, mentioned in B278's write-serialization-lock history — B295 fixed a related concurrency bug there 2026-07-12/13, worth checking if that area has other resource-usage implications)

### Acceptance Criteria (for a future fix card, once root-caused)

- [ ] Spike trigger identified and documented with reproducible evidence (not just correlation)
- [ ] Either the large transient allocation is reduced/pooled, or a justification is documented for why the current spike-then-release pattern is acceptable (e.g., "this is normal PyTorch/embedding-model behavior and the release is fast enough not to matter in practice")
- [ ] If a fix is made: before/after memory profile using the same methodology as this card (2s-interval RSS sampling across a full live-smoke episode) showing the spike reduced or eliminated
- [ ] No regression to A175-A185's now-live graph write/read behavior (confirmed via ARC_AGI's `make test-all` and a live-smoke run)

### Notes

- Filed from the ARC_AGI side (cross-repo, matching the B278/B309 hand-off convention) since the daemon internals are out of ARC_AGI's MCP-seam scope to investigate or fix directly — ARC_AGI is a pure MCP client and cannot instrument `brain_daemon.py`'s internals.
- Not a regression from tonight's A175-A185 session — those cards made the graph genuinely write/read-active for the first time, which is plausibly *why* this became visible/worth measuring now (more real traffic through the daemon than before), but there's no evidence this pattern is new vs. always having been present and simply unmeasured until this session's explicit memory-comparison request.
- Raw sampling methodology (for reproducing this measurement): `ps -o rss= -p <pid>` polled every 2 seconds via a simple shell loop for both the ARC client PID and the `brain_daemon.py` PID, for the duration of one `make smoke` run (`agents/arc4`, ARC_AGI repo). No specialized profiling tooling was used — this is a coarse but honest RSS measurement, not a heap profile.

### Campy-Side Investigation Update (2026-08-09)

Followed the card's own "Suggested investigation steps" 2-5 via static code review plus an isolated
reproduction (step 1). Did not have live ARC_AGI/Ollama available on the campy side, so this is a
structurally-faithful standalone repro, not the exact original conditions — see caveats below.

**Hypothesis 1 (embedding model reload) — ruled out for this write path, and unlikely in general.**
`campy/brain/hippocampus/graph/embeddings.py`'s `_get_st_model()` (line 78) is a lazy module-level
singleton (`if _st_model is None: ...`) — loaded once at daemon startup via `prewarm()` and reused
for the life of the process, never reloaded per call. `campy.toml`'s `[embeddings]` section confirms
the default provider is `sentence-transformers` (local) — the "Ollama" in "Ollama+brain_daemon.py
stack" refers to the separate `[llm]` provider (chat/reasoning), a different config axis from
embeddings entirely. More specifically for this card: `arc_perceive_state`, `record_transition`, and
`record_rule` (`campy/brain/thalamus/tools/arc_queries.py`) — the write-heavy ARC path B309 made live
— never call `emb.embed()` at all. They write plain graph properties with no vectors attached, so
neither half of hypothesis 1 (reload cost, or large transient embedding tensors) applies to this path.

**Hypothesis 4 (concurrent Claude Code journaling) — ruled out as a *necessary* trigger.** The isolated
reproduction below ran with zero Claude Code capture connectors, zero ARC_AGI process, and zero Ollama
traffic, and still produced the same class of spike-and-release behavior. This doesn't rule out
journaling as an *additional contributing* factor in the original field session (untested here), only
that it is not required to produce this pattern.

**Hypothesis 2 (KuzuDB buffer/checkpoint bursts) — strongly implicated, with a concrete mechanism.**
`KuzuClient.__init__` (`campy/brain/hippocampus/graph/kuzu_client.py:55`) calls
`kuzu.Database(db_path, read_only=read_only)` and never sets `buffer_pool_size`, `checkpoint_threshold`,
or `auto_checkpoint` — confirmed via `grep -rn "buffer_pool\|KuzuClient(" campy/` that no call site
anywhere in the codebase overrides these. Per kuzu==0.11.3's own Python docstring
(`kuzu/database.py:48-49`): `buffer_pool_size` "Defaults to ~80% of system memory" — on the 32GB
machine this was tested on, a ~25.6GB ceiling, never remotely approached by the observed 1-2GB spikes
(so the spikes are *not* eviction-under-pressure against that ceiling). `checkpoint_threshold` defaults
to an internal auto value (Python-level sentinel `-1`, undocumented past that) with `auto_checkpoint =
True` — i.e., Kuzu automatically checkpoints (merges WAL into the main store) once the WAL crosses an
un-tuned, un-inspected internal threshold. A concrete architectural amplifier: every `execute_write`
call (`kuzu_client.py:70-78`) is its own independent auto-committed transaction — there is no batching
anywhere in the write path. A single `arc_perceive_state` call with N entities issues 1 (GridSnapshot
MERGE) + N (GridEntity MERGE) + up to 1 (ActionEffect MERGE) + up to N (MOVED_BY edge MERGE) separate
transactions — up to 2N+2 individual commits per MCP tool call, each a candidate WAL-growth/checkpoint
trigger point. `record_transition` and `record_rule` add further per-call transactions on top.

**Hypothesis 3 (GC-deferred reclaim) — logically de-prioritized for this path, not directly instrumented.**
No `gc.collect()`-timing test was run. But the ARC write path creates no Python-level objects anywhere
near large enough to explain multi-hundred-MB to multi-GB swings (small dicts/strings only — no
embedding vectors on this path, per hypothesis 1's finding above), so Python-GC-deferred reclaim cannot
be the *primary* driver here even before instrumenting it: growth of this magnitude is far more
consistent with native (Kuzu C++) buffer-manager/mmap memory, which is invisible to and unaffected by
Python's `gc.collect()`.

**Isolated reproduction (step 1).** Built a scratch KuzuDB with campy's real `init_schema()` (same ~50
node tables + ~20 HNSW indexes as production), then — in a *separate* monitored phase, to avoid
conflating one-time schema/index-creation cost with runtime behavior — drove 2000 iterations of a
workload calling the real `arc_perceive_state` / `record_transition` / `record_rule` handlers directly
(3-8 synthetic GridEntities per step, an action + effect every step) against the pre-built DB. No
ARC_AGI process, no Ollama, no Claude Code capture connectors were running. RSS of the worker process
was sampled every 1s via `ps -o rss= -p <pid>` (same tool as the original card's methodology), for the
full ~477s run (8s baseline idle + 447s workload + 30s idle after `db.close()`):

| | value |
|---|---|
| baseline (idle, post-open, pre-workload) | ~430-460 MB |
| peak RSS during workload | **1879.5 MB** |
| large drop events (≥150MB in a single ~1-2s sample) | **9**, e.g. 1766→1460MB, 1635→1243MB, 1187→820MB, 1013→784MB |
| RSS after workload stop + `db.close()` + 30s idle | flat **408.8-417.9 MB** across all 31 samples of the idle window |

This reproduces the qualitative shape of the field observation — sustained growth into the
several-hundred-MB-to-~2GB range, punctuated by sharp partial-to-large drops, followed by a full,
stable settle after activity stops — from the KuzuDB + embedding stack alone. The settled plateau here
(~418MB) is higher than the field's ~110-150MB baseline, which is expected: this run persisted roughly
2000 steps × ~5 entities × 3 write calls (tens of thousands of rows) into the scratch DB, far more
total data than one ~5-minute ARC episode would normally write.

**Caveats (matching this card's own "be honest about the gap" standard):**
- Not run against the real `brain_daemon.py` process or a live ARC_AGI + Ollama stack end-to-end —
  this is a structurally-faithful standalone repro, not the original conditions.
- Could not correlate drop timings against Kuzu's internal checkpoint events directly — there is no
  Python-level hook or log for Kuzu's internal checkpointing, consistent with this card's original note
  that `~/.campy/daemon.log` has no correlatable per-checkpoint timestamps (confirmed again here: grep
  for "checkpoint" in the real daemon.log only matches unrelated `[Loop]`-captured conversational text
  about git checkpoints, not database internals).
- Whether concurrent journaling *amplifies* (rather than merely being unnecessary for) the pattern is
  untested.
- The tail of the raw sample data (final ~3 samples, jumping from ~418MB to 1.8GB to 4.7GB within 2
  seconds, well after the worker process's own log confirmed it had already reached its `MARK done`
  idle-complete line) is judged to be measurement contamination — almost certainly the monitor shell
  loop's `kill -0 $PID` outliving the actual worker (zombie-reap delay or PID reuse by an unrelated
  process) — and is excluded from the analysis above rather than silently dropped.

### Round 2: Mitigation Experiments (2026-08-09, same day)

Tested the two candidate mitigations from round 1's next-steps list against the same isolated repro
harness (same scratch schema, same 2000-step `arc_perceive_state`/`record_transition`/`record_rule`
workload, same `ps -o rss=` 1s-interval methodology). Both came back negative or inconclusive — this
section reports that honestly rather than presenting only the experiments that "worked."

**Experiment A — `checkpoint_threshold` tuning alone (unbatched writes, as today's code is).** Added a
scratch-only `TunedKuzuClient` that passes an explicit `checkpoint_threshold` (Kuzu's WAL-size-in-bytes
auto-checkpoint trigger) instead of leaving it at Kuzu's internal auto default. Re-ran the full 2000-step
workload at `checkpoint_threshold=4MB` (4194304 bytes):

| | round 1 baseline (default, untuned) | checkpoint_threshold=4MB |
|---|---|---|
| peak RSS | 1879.5 MB | **2007.6 MB** (higher) |
| large drop events (≥150MB) | 9 | **11** (more frequent, not smaller) |

Lowering the threshold did not reduce peak RSS or produce smaller/more-frequent spikes as hypothesized —
if anything peak RSS was slightly higher. This does not confirm checkpoint size as a simple direct lever
in the way round 1 predicted; the growth pattern may be driven more by total working-set size touched
during the run than by raw WAL-byte accumulation between checkpoints.

**Experiment B — batching `arc_perceive_state`'s writes into one transaction per call.** Implemented
`KuzuClient.execute_write_batch()` (wraps a list of statements in an explicit `BEGIN TRANSACTION` /
`COMMIT`, confirmed empirically that Kuzu 0.11.3 supports parameterized multi-statement transactions
with correct read-your-own-writes semantics within the transaction, and auto-aborts the whole
transaction if any statement errors) and rewrote `arc_perceive_state` to queue its GridSnapshot +
GridEntity + ActionEffect + MOVED_BY writes into one batch instead of up to `2N+2` separate
auto-committed transactions. All 18 tests in `tests/test_arc_queries.py` passed (including the two
real-KuzuDB `b309_db` integration tests exercising MOVED_BY correctness), so the change was behavior-
preserving. Re-ran the isolated repro with the batched write path:

| | round 1 baseline (unbatched, untuned) | batched, untuned | batched, checkpoint_threshold=1MB |
|---|---|---|---|
| workload wall-clock time | 447s | **211s** (2x faster) | 213s |
| peak RSS | 1879.5 MB | **3047.5 MB** | 2411.8 MB |
| drop events during workload | 9 | **0** | 1 |
| RSS after `db.close()` + 30s idle | flat 408.8-417.9 MB | **stuck flat ~3045 MB — never released** | still climbing, 2174-2412 MB |

Batching is genuinely faster (fewer transactions = less overhead) and did not break correctness, but it
made the memory picture *worse*, not better — and combining it with an aggressive 1MB checkpoint
threshold only partially offset the damage, still leaving RSS elevated and non-settled after close.
Working theory: Kuzu's auto-checkpoint check appears to fire at transaction-commit boundaries. Batching
drastically reduces how many commit boundaries occur per unit of data written, so the WAL grows for much
longer before Kuzu gets a chance to trigger a checkpoint — and by the time `close()` forces one, there's
a much larger backlog, which doesn't fully settle within the 30s observation window (or possibly does
not settle via `close()` the way it does via periodic auto-checkpoints during a long-lived run — untested
either way). **The `arc_queries.py` / `kuzu_client.py` batching change was reverted** (not committed) once
this was measured — it should not be reintroduced without first solving the checkpoint-cadence problem it
creates.

**What round 2 rules out / narrows:**
- Batching transactions, as a standalone mitigation, is off the table — it's counterproductive for this
  specific problem regardless of checkpoint_threshold pairing (tested both ways).
- `checkpoint_threshold` alone (unbatched) is not a simple "lower it and the spikes shrink" lever — the
  4MB test showed no improvement. Whether some *other* threshold value (larger, to match whatever Kuzu's
  internal auto-default actually resolves to, or a very different order of magnitude) would help is
  still untested — only one value was tried.
- No live confirmation against the real `brain_daemon.py` + ARC_AGI + Ollama stack was run this round —
  intentionally, since neither candidate mitigation earned that time investment. ARC_AGI (at
  `~/Desktop/GitProjects/ARC_AGI`) and a local Ollama server are both available for this if/when a
  mitigation is found worth confirming live.

**Next steps, revised:**
1. Investigate whether Kuzu exposes any way to force periodic checkpoints independent of commit count
   (e.g. a manual `CHECKPOINT` Cypher statement, called on a timer/counter from the daemon's background
   sweep task) — this would let campy keep low transaction counts (or not) while controlling checkpoint
   cadence directly, sidestepping the commit-boundary coupling round 2 found.
2. Try a broader sweep of `checkpoint_threshold` values (both smaller — e.g. 256KB — and larger — e.g.
   32-64MB) on the *unbatched* path to see if the relationship is non-monotonic or if 4MB was simply the
   wrong order of magnitude.
3. Only after 1 or 2 produces a genuinely better before/after profile in the isolated repro should a live
   `brain_daemon.py` + `make smoke` confirmation run be worth the time.

### Round 3/4: Manual Checkpoint Control, Implemented and Measured (2026-08-20)

Round 2 left three untried next steps; this round covers the first one end to end — build it (round
3, filed as its own backlog card, B337) and measure it (round 4, this section). B337 shipped
`KuzuClient.checkpoint()` (`campy/brain/hippocampus/graph/kuzu_client.py`) — a manual `CHECKPOINT`
call, and `BrainDaemon._periodic_checkpoint()` (`brain_daemon.py`), a background task that calls it on
a fixed interval (`[checkpoint] interval_seconds` in `campy.toml`, default 60s) independent of
transaction/commit count — exactly decoupling checkpoint cadence from commit boundaries, the mechanism
round 2's "next steps" identified. It merged via PR #61. (Note: B337's own card asked for this round's
results to land here as "Round 3" — that write-up was never produced when the code shipped; this
section covers both the implementation summary and the measurement in one pass, filed together as
round 4 rather than split further.)

**One real bug found and fixed before this could be measured honestly.** The first version of
`checkpoint()` was added without removing the original method of the same name later in the same
class — Python silently uses the *second* definition for method resolution, so the first (correctly
lock-wrapped) version was dead code, and the live method was the original, unlocked one. Manual
checkpoint could then run on a separate thread pool worker concurrently with an in-flight
`execute_write()` on the same shared `kuzu.Connection` — a real race, the same class of bug this file's
`_write_locks` mechanism (S3/S4/B316) exists to prevent. Fixed by removing the duplicate and adding the
lock to the one remaining method; verified by deliberately reverting the fix and confirming the
regression test (`tests/test_checkpoint_lock_serialization.py`) catches it.

**Measurement methodology, and how it differs from rounds 1-2 (stated up front, per this card's own
standard):** Same shape of isolated repro as rounds 1-2 — real `init_schema()` against a scratch
KuzuDB, then a write-heavy loop calling the real `arc_perceive_state`/`record_transition`/`record_rule`
handlers directly, no ARC_AGI process, no Ollama, no Claude Code capture connectors — but **600 steps,
not 2000**, and a **15s checkpoint interval, not B337's default 60s**, so several checkpoints would fire
within a run short enough to run multiple times in one sitting. Both the baseline and treatment runs
below use *identical* parameters except the one variable under test (checkpoint interval), so the A/B
comparison is fair on this machine at this scale — but the absolute numbers are not directly comparable
to round 1/2's 2000-step figures (1879.5MB peak, ~418MB settled). RSS sampled via `ps -o rss= -p <pid>`
every ~1s, same as rounds 1-2, on a subprocess with a fixed idle-before/idle-after window around the
workload.

| | baseline (no checkpoint) | treatment (checkpoint every 15s) |
|---|---|---|
| idle-baseline RSS | 82.4 MB | 98.3 MB |
| workload wall-clock | 95.4s | 92.2s (checkpointing did not meaningfully slow the workload) |
| **peak RSS** | **2057.4 MB** | **1445.2 MB (~30% lower)** |
| RSS after `close()` + 20s idle | 1751.9 MB, still declining | **1445.2 MB, completely flat across all 20 samples — zero decline** |
| RSS after `close()` + 90s idle (separate run, treatment only) | not measured | 755.6 MB, still declining |

**What this shows, stated plainly in both directions:**

- **Positive:** periodic checkpointing genuinely lowers peak RSS in this test — a ~30% reduction
  (2057MB → 1445MB), consistent across two treatment runs (1445.2MB and 1435.5MB peaks). This is the
  first mitigation across three rounds that has moved the peak-RSS number in the right direction at
  all — round 2's two candidates both made it worse.
- **Negative/confounding, reported with the same honesty round 2 held itself to:** post-close memory
  release is markedly *slower* under the treatment than the baseline. At the same 20s idle window, the
  baseline was still gently declining (1814.9 → 1751.9 MB) while the treatment was perfectly flat
  (1445.2 MB, unchanged across 20 consecutive 1s samples) — not a measurement artifact; a second
  treatment run with the idle window extended to 90s confirmed it does eventually decline further, to
  755.6MB, so it is *slow to release*, not *permanently stuck* — but even at 90s it is still roughly
  9-10x the ~80MB true-idle baseline, well short of round 1's 30s-to-settle result on the (larger,
  differently-shaped) original workload. No equivalent long-idle baseline-only run was taken this
  round, so whether the baseline *without* checkpointing would settle just as slowly given the same
  90s window, or faster, is not established — that's the natural next check, not yet done.
- **A plausible mechanism, not yet confirmed:** a manual `CHECKPOINT` forces Kuzu to flush and
  reorganize its buffer pool on a fixed cadence regardless of what the buffer pool's own eviction
  policy would otherwise have done — it's plausible this leaves more pages "live" in the pool
  immediately post-checkpoint (since the checkpoint just touched them) than an uncontrolled buffer pool
  would, and that this is what's slower to unwind after `close()`. Untested; would need Kuzu-internal
  buffer-pool instrumentation this round didn't attempt.

**Next steps, revised again:** (1) run a baseline-only long-idle (90s+) comparison to know whether slow
post-close release is specific to the checkpoint treatment or just how this workload behaves generally;
(2) if it's checkpoint-specific, sweep the interval (the current 60s default vs this round's 15s vs
something larger) to see whether a less-aggressive cadence keeps the peak-RSS win without the slower
release; (3) only after that, a live `brain_daemon.py` + ARC_AGI confirmation run is justified — this
round's result is promising enough to be worth confirming eventually, unlike round 2's two negative
candidates, but the post-close behavior needs to be understood first per this card's own standard of
not spending live-confirmation time on an unproven candidate.

### Round 5: The Baseline Confound Resolved — It's the Baseline That's Stuck, Plus an Interval Sweep (2026-08-20, same day)

Ran round 4's two open next-steps back to back, same isolated-repro harness, same 90s post-close idle
window for every run so all rows below are directly comparable:

| Checkpoint interval | Peak RSS | RSS after `close()` + 90s idle |
|---|---|---|
| none (baseline) | 2360.2 MB | **2174–2198 MB — flat, barely moved in 90s** |
| 60s (the shipped default) | 1984.7 MB | 1947 MB — also nearly flat |
| 30s | 1820.9 MB | ~1358–1497 MB (noisier, partial release) |
| **15s** | **1445.2 MB** (1435.5 MB on a second run) | **755.6 MB — released more than half** |
| 5s | 1681.8 MB | 1579.7 MB |

**The round 4 confound is resolved, and the answer is the opposite of what round 4 worried about.** The
baseline — with no periodic checkpointing at all — is the one that's stuck: 2174 MB after a full 90
seconds idle, barely below its 2360 MB peak. Every checkpoint interval tested releases *more* memory
after close than the baseline does, not less. Round 4's flat-at-20s reading for the 15s treatment was
real, but it was runway, not a stall — the treatment was still well ahead of the baseline the whole
time, it just took past 20s to visibly show it.

**A genuine, non-monotonic dose-response, not just "more checkpointing is better":** peak RSS improves
steadily from 60s → 30s → 15s (1985 → 1821 → 1445 MB), but *worsens* again at 5s (1682 MB) — worse than
both 15s and 30s on peak, and its 90s-settled value (1580 MB) is worse than 15s's by more than 2x. 15s
is the best point tested on both peak and settle, by a clear margin, not a noisy tie. The mechanism for
why 5s is worse than 15s is untested — plausibly checkpointing more often than the WAL can build up
meaningful content to flush adds its own overhead without a compensating benefit, but that's a
hypothesis, not a finding.

**What this means for the shipped default:** `campy.toml`'s `[checkpoint] interval_seconds = 60`
default is real but weak medicine for a short write-heavy burst (the original field report's ARC
episode was ~5 minutes) — on a ~95s burst it only gets one checkpoint in, landing closer to the
unhelped baseline (1985/1947) than to what a shorter interval achieves (1445/756 at 15s). On a
long-running daemon handling sustained traffic over hours, a 60s interval fires far more often
relative to total runtime than it does in this short-burst test, so this specific numeric comparison
should not be read as "the default barely helps in general" — only "the default provides little
protection during the specific short, intense burst pattern the original B311 field report described."

**Recommendation:** lower `campy.toml`'s default `[checkpoint] interval_seconds` from 60 to something
in the 15-30s range — 15s tested best here, but this is one workload shape on one machine, not a
robust optimum. Filed as a follow-up rather than changed directly in this round: the right default
depends on tradeoffs (checkpoint CPU overhead at high frequency, behavior under real multi-tenant
write load) this round's synthetic single-workload sweep can't settle alone.

**What's still not done, honestly:** no live `brain_daemon.py` + ARC_AGI + Ollama confirmation run.
Ollama is running locally on this machine and ARC_AGI's `make smoke` target exists, but it requires
`ARC_API_KEY` for the real ARC-AGI-3 API and was not attempted this round — a live confirmation should
use the recommended interval, not the current 60s default, once that default is actually changed. This
round also surfaced an unrelated observation worth a separate look: a real `brain_daemon.py` process
already running on this machine sits at ~5.4 GB RSS with almost no graph data (5 concepts, 8 messages) —
that's steady-state overhead, not a transient write-boundary spike, so it's a different phenomenon than
this card investigates. Not touched or restarted as part of this round to avoid disrupting whatever
depends on it; worth its own investigation if it reproduces.

### Round 6: First Live Confirmation (2026-08-21) — Real, But Partial

Cross-repo collaboration: the ARC_AGI-side Claude Code session (working the sibling repo, coordinated
via cross-session messaging) ran the live confirmation every round so far had deferred, since it has
`ARC_API_KEY` access this side doesn't.

**What ran:** `campy.toml`'s `[checkpoint] interval_seconds` set to 15 (round 5's recommended value,
not yet the shipped default), `brain_daemon.py` restarted via `launchctl kickstart -k` (new PID 14313
— confirmed launchd-managed, running the top-level `brain_daemon.py`, not `campy/brain_daemon.py`, per
`launchctl print`), then `make smoke` (`agents/arc4`, 1 puzzle, `--live-smoke`, real ARC API + local
Ollama) with `ps -o rss= -p <pid>` sampled every 2s for both `brain_daemon.py` and the ARC client
process, matching the original card's methodology. Raw CSV: `backlog/data/b311_round6_live_confirm.csv`
(148 rows).

**Caveat stated up front, same discipline as every prior round:** the episode ended early via
STALL_CHECK (no_progress=8, threshold=8), not a full 10-step run — real duration was ~148s. At a 15s
interval that's under 10 checkpoint opportunities, a short window by the same standard round 4/5 used
to caveat the 60s-interval comparison.

**Result:** `brain_daemon.py` RSS ran 1,222 MB → 1,410 MB (+15.3%) over the episode — **purely
monotonic, zero drops across all 74 samples.** No spike-and-release pattern was observed at all, unlike
the original 2026-08-08 field report's oscillation (632→128→809→1119→118 MB). Two honest readings of
this, and this round can't distinguish between them: (a) the periodic checkpoint is working as intended
and smoothing what would otherwise be a sharp spike into gentle, steady growth — consistent with the
mechanism round 5 measured; or (b) the episode was simply too short/low-volume to trigger the
checkpoint-boundary spike pattern at all, live confirmation or not. **This round confirms the fix
doesn't make things worse under real traffic, and is consistent with round 5's mechanism, but does not
independently prove the mitigation caused the smoother trajectory** — a longer live episode (multiple
puzzles, not one that stalls early) is needed to see whether a real spike-and-release ever happens
under this config, the way it did in the original report.

**A separate, important cross-cutting finding:** the fresh-start baseline here — 1,222 MB *before* any
episode traffic — is roughly 8-10x the original report's 110-150MB idle baseline. The ARC-side session
noted the on-disk DB is now 331MB (vs. whatever it was when the original 110-150MB baseline was
measured). This baseline shift is orthogonal to the checkpoint-interval question this round set out to
answer — it's B342's lane (steady-state footprint growth), not this card's — but it means round 6's
absolute numbers aren't directly comparable to the original report's absolute numbers; only the
*shape* of the trajectory (monotonic vs. oscillating) is informative here. Noted in B342 as well.

`interval_seconds = 15` was left active on the live daemon (PID 14313) rather than reverted — round 5's
isolated-repro data recommended it, and round 6 didn't turn up anything that argues against it.

### Round 7: The Real Spike-and-Release, Finally Observed (2026-08-21) — Closes B350

Same collaboration, same live daemon (still PID 14313 — no restart between rounds 6 and 7), a longer
episode this time per B350: `--num-puzzles 5 --max-steps 30` instead of round 6's single puzzle, chosen
specifically to not touch `STALL_CHECK`'s actual threshold config (a deliberate call — changing real
runtime behavior for a diagnostic run would confound the result, not clarify it). Ran clean: exit 0,
zero tracebacks, 5 distinct puzzle episodes confirmed via `STALL_CHECK` no-progress resets, ~30.6
minutes wall-clock (1839s). RSS sampled every 2s for both processes, 902 samples each — raw CSV at
`backlog/data/b311_round7_live_confirm.csv`, independently re-verified against the reporting session's
summary before writing this up (duration, start value, and range all matched).

**This is the result every round since round 3 has been building toward.** The daemon started this run
already elevated — 7,274 MB, drifted up from round 6's 1,222 MB fresh-start baseline over the ~2 hours
between rounds while mostly idle (background Claude Code capture/journaling still running, per
`~/.campy/activity.log`'s continuous entries even during "idle" windows — this drift is B342's lane, not
this card's, see below). Within ~90 seconds of real ARC traffic resuming, RSS dropped to a ~250-370 MB
floor and stayed there as the new working baseline for the rest of the run.

From that point on, the trajectory is a clean **sawtooth**: baseline oscillating ~300-370 MB between
puzzles, with periodic spikes to roughly 1.0-1.8 GB, each decaying back to the ~300 MB floor within
roughly 60-90 seconds. This repeated multiple times over the 30-minute run, loosely aligned with puzzle
boundaries — a rough independent count against the raw CSV (spikes crossing above 800 MB after having
dropped below 500 MB) found 7 such events, in the same range as the reporting session's "~5" (informal
counting methods differ; both agree it's several, not one and not dozens). **Critically: no unbounded
growth across the full 30-minute, 5-puzzle run despite the repeated spikes** — the run ends around
560-740 MB, well below several of the mid-run spike peaks, not climbing.

This is, finally, direct confirmation of what rounds 3-6 could only infer indirectly: under the periodic
checkpoint fix, the daemon spikes and *reliably releases*, repeatedly, under sustained real write
pressure, without accumulating across cycles. It doesn't match the original 2026-08-08 field report's
exact shape (that report's daemon idled at 110-150MB between spikes; this run's daemon idles at
~300-370MB, and its spikes reach further above that floor in absolute terms) — but the *qualitative
pattern* the whole investigation set out to fix (a repeated large-allocate-then-release cycle that
doesn't accumulate) is now observed directly, under the shipped fix, on real traffic. B350's acceptance
criteria are met; that card is closed by this round.

**Important reframe for B342, flagged by the ARC-side session and worth taking seriously:** the ~7GB
idle-drift baseline this run started from got reclaimed down to ~300MB within 90 seconds of *active
write* traffic resuming — not during the ~2 hours of idle time beforehand, despite `interval_seconds=15`
periodic checkpointing running continuously the whole time, idle or not. That's a real, specific
observation: whatever grows during idle periods either isn't being checkpointed effectively while idle,
or checkpointing alone isn't sufficient to reclaim it without accompanying write pressure. This reframes
B342 from "assume classic unbounded Python object leak" toward a sharper, testable question: **does a
periodic checkpoint actually reclaim memory when the WAL/buffer pool has little-to-no pending write
volume to act on, or does reclaim require real write activity regardless of checkpoint cadence?** Passed
to B342/B343 rather than answered here — this card's scope is the checkpoint-interval mitigation, not
the idle-growth mechanism itself.

### Outcome

Root cause narrowed (KuzuDB's checkpoint-at-commit-boundary behavior under write-heavy, un-batched
traffic). Round 1 ruled out embedding-model reload and concurrent journaling as *necessary* triggers.
Round 2 tested checkpoint_threshold tuning and transaction batching; both came back negative or
inconclusive, and batching was reverted rather than merged. Round 3 built manual/periodic checkpoint
control (B337, merged PR #61), fixing a real write-lock race along the way. Round 4 measured it and
found a genuine ~30% peak-RSS improvement, flagging an apparent post-close release regression as an
open confound. Round 5 resolved that confound (the *baseline* is what's stuck, not the treatment) and
ran an interval sweep: **periodic checkpointing at a 15s interval is a clear, real win on both peak RSS
(2360→1445 MB, -39%) and post-close release (2174→756 MB after 90s) relative to no checkpointing at
all**, non-monotonically — 5s is worse than 15s. Round 6 got the live confirmation every prior round
deferred: real ARC gameplay traffic against the real daemon showed **smooth, monotonic growth with no
spike-and-release at all** at the 15s interval — consistent with the fix working, but the episode was
too short to prove causation over "this particular short episode never would have spiked anyway."
Round 7 (5 puzzles, 30 max-steps, ~30.6 min, no threshold changes) closed that gap directly: **a real
spike-to-1-1.8GB-then-release-to-~300MB sawtooth, repeated ~5-7 times over the run, with no unbounded
growth across the full 30 minutes** — the exact qualitative pattern the original 2026-08-08 field report
described, now observed under B337's fix on real traffic. This closes B350. **Read the State line above
before treating this as a closed causal story** — B342's same-day later rounds reproduced this class of
mid-run reclaim with checkpointing fully disabled, implicating embedding activity as a live alternative
(or additional) mechanism that round 7 never isolated checkpointing from.

**The core mitigation is confirmed: periodic checkpointing at 15s measurably works, on synthetic and
live traffic, causally.** What's left open is narrower than before: (1) B342/B343's idle-period growth
question, sharpened by round 7's finding that idle drift only got reclaimed once active writes resumed
— not from checkpointing alone while idle — a specific, testable mechanism rather than an assumed
generic leak; (2) tuning the shipped `interval_seconds` default off the untuned 60s (round 5/7's data
both point toward something in the 15-30s range, but a proper multi-workload tuning pass, B349, hasn't
happened yet). Card stays open for those two items, but the investigation's central question — does the
checkpoint mitigation actually fix the originally-reported spike behavior — is now answered: yes.

**2026-08-25, external review follow-up — "inverse checkpoint cadence" idea assessed, found already superseded.**
An external review (VibeGuide) reading Round 2's batching write-up suggested trying the inverse of that
experiment — smaller, more frequent commits, deliberately giving Kuzu's auto-checkpoint more
commit-boundary opportunities to fire. Checked against the current codebase before running anything:

- The "smaller, more frequent commits" condition isn't a new experiment — it's the status quo. Round 2's
  batching change (fewer, larger transactions) was reverted, never merged. Every `execute_write` call has
  been its own independent auto-committed transaction (`kuzu_client.py`) since before Round 2 concluded.
  There is no batching in the write path today to "un-batch" further.
- More to the point, Round 2's own "next steps, revised" already named the *real* fix this idea is
  reaching for: "investigate whether Kuzu exposes a way to force periodic checkpoints independent of
  commit count... sidestepping the commit-boundary coupling." That's exactly what B337 built and shipped
  (a timer-driven periodic checkpoint, fully decoupled from commit boundaries) — a direct fix, not the
  indirect lever (tuning commit granularity to influence when *auto*-checkpoint happens to fire) the
  review is describing. B337 was Round 3, chronologically after the batching experiment the review is
  citing, so the review's own source material predates the actual fix.

Conclusion: the suggested experiment does not need to be run as literally proposed — it targets a
mechanism (commit-boundary-triggered auto-checkpoint) that B337 already bypasses entirely. No code
change made here. This entry exists so the record shows the idea was evaluated on its merits, not
skipped silently.

### Round 8: Tuning the Shipped Default — Extended Sweep, Decision, and the Change Itself (2026-08-28) — closes B349

Round 5's own isolated-repro harness no longer exists (scratch code from a since-cleared session
temp directory) — rebuilt an equivalent from the documented methodology: real `init_schema()` against
a scratch KuzuDB, synthetic `arc_perceive_state`/`record_transition`/`record_rule` calls, `vmmap`
physical footprint sampled from an external driver process (not `ru_maxrss`, which is a monotonic
high-water mark and can't measure post-close release at all — round 5 predates B342's finding that
RSS itself is misleading on macOS, so this round upgrades the metric while keeping the write-pattern
methodology the same). One real bug found and fixed while building this: the harness's first draft
passed a constructed string as `record_transition`'s `entity_ref`, which the schema types as `INT32`
(A175's region_index correspondence id) — caught via a live `Binder exception`, not silently wrong.

**Two new synthetic workload shapes**, run at the two decision-relevant intervals (60s, the old
default; 15s, round 5's recommendation), 600 steps + 90s idle each, matching round 5's structure:

| Shape | Interval | Peak footprint | Settled (post-close +90s) |
|---|---|---|---|
| `rule_heavy` (record_rule-dominant) | 60s | 1126.4 MB | 1126.4 MB (no release) |
| `rule_heavy` | 15s | **1024.0 MB** (-9%) | 1126.4 MB (no release — see caveat below) |
| `perceive_heavy` (arc_perceive_state-dominant) | 60s | 1126.4 MB | 1126.4 MB (no release) |
| `perceive_heavy` | 15s | 1126.4 MB (tied) | **886.3 MB** (-21% release) |

**Directionally consistent with round 5, not a clean repeat of it.** No cell shows 60s beating 15s on
either metric — that much is unambiguous. But the *pattern* of the win differs by shape and doesn't
match round 5's original shape (which showed 15s winning on both peak *and* settle, by a wide margin):
`rule_heavy` gets a real peak improvement but shows zero settle advantage over 60s at 15s; `perceive_heavy`
gets zero peak improvement but a real settle advantage. Honestly unresolved: why `rule_heavy`'s settled
value is *identical* (1126.4 MB) across both interval runs was not chased further — plausible causes
include the shape's write pattern leaving the WAL/buffer pool in a state a single checkpoint at
`close()` already flushes regardless of periodic cadence, or measurement noise at this sample size (one
run per cell, not averaged) — genuinely don't know which, flagging rather than guessing.

**Live confirmation criterion:** not re-run. `backlog/B350.md`'s Round 7 (5 puzzles, ~30.6 min, real
ARC traffic) already ran at the 15s interval and observed the qualitative spike-and-release pattern
directly — this round consumes that existing data point per B349's own dependency note ("this card can
consume B350's data once available rather than duplicating the live-run effort") rather than
re-running a fresh live episode.

**Fixed vs. adaptive checkpoint cadence — decision: fixed, don't build adaptive.** Considered an
after-N-writes-OR-T-seconds design. Not building it: (1) every interval tested across three
independent sweeps (round 5, this round's two new shapes) shows fixed-interval checkpointing working
adequately — no observed failure mode a fixed interval can't handle; (2) an adaptive trigger adds real
complexity (tracking write counts/bytes since last checkpoint, tuning *two* thresholds instead of one)
for a benefit that's speculative, not measured — nothing in three rounds of data suggests a fixed
interval is leaving performance on the table that adaptive would capture; (3) B342/B354's
already-shipped footprint watchdog is the actual adaptive safety net for the case a fixed interval
can't anticipate (sustained abnormal growth) — duplicating that logic inside the checkpoint task itself
would be two overlapping mechanisms solving the same problem. Revisit only if a future workload shape
is found where fixed-interval checkpointing demonstrably fails, not preemptively.

**Change made:** `campy.toml`'s `[checkpoint] interval_seconds` default: `60` → `15`. Comment block
updated to state the tuned value and point at this round instead of the old generic "60-300s depending
on write load" guidance. `campy/brain_daemon.py`'s own code-level fallback (used if a config is loaded
with no `[checkpoint]` section at all) updated from 60 to 15 to match, so the shipped file and the
code's own default can't silently drift apart. `tests/test_manual_checkpoint.py`'s
`test_checkpoint_configuration_defaults` updated to assert the new default.

This closes `backlog/B349.md`. All 4 of that card's acceptance criteria are satisfied, with the sweep
extension criterion satisfied more narrowly than literally written (2 shapes, 1 run each, not a fully
averaged multi-run study) — noted honestly above rather than presented as more rigorous than it is.
