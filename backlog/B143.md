# B143: Geometry-Aware Coordinate Policy for space/reach_goal Puzzles

**Status**: complete ✅ DONE (2026-04-01)
**Assigned**: TBD  
**Depends on**: B141 (bail-out ensures bad coordinates get abandoned), B142 (graduation fix enables replanning)  
**Complexity**: Medium (2–4 hours)  
**Priority**: P1 (quality improvement — makes the "right" choice smarter once B141/B142 stop the "wrong" choice)  
**Related**: B139 (reopened acceptance criteria)  
**Plan**: backlog/plans/B-143-geometry-coordinate-policy.md

## Problem Statement

`_candidate_action6_coordinates()` in `orchestrator.py` (~L1633-1681) builds a naive candidate list:
1. All non-background pixels
2. Grid center
3. All 4 corners (clamped 0-63)

`_infer_action6_coordinates()` then cycles through candidates via modulo: `start_index = len(action6_attempts) % len(candidates)`. This produces effectively random coordinate exploration with no geometric reasoning.

For `space` + `reach_goal` puzzles where player and goal positions are both known with high confidence, the agent should bias toward coordinates that reduce player→goal distance or continue a consistent movement direction.

Evidence from `live_gemma4_e4b_timeout_1775221087`:
- Coordinates visited: (0,0), (33,2), (2,0), (0,3), (0,4), (0,5), (35,0), (36,1), (37,2), (37,1), (38,1), (38,2), (38,1)
- Pattern: center → corners → edges → nearby player cluster — never tested player→goal corridor
- Player role identified at position ~(33-38, 0-2) with confidence 0.90
- Goal role identified at position ~(9) with confidence 0.83
- No coordinates tested between player and goal regions

## What This Card Builds

1. **Goal-directed candidate sorting**: When `archetype=space` AND `victory=reach_goal` AND both player/goal roles are known, sort candidates by priority:
   - Tier 1: Coordinates on the player→goal vector (within ±2 cells of the line)
   - Tier 2: Coordinates that reduce Manhattan distance from player to goal
   - Tier 3: Original heuristic candidates (non-background, center, corners)

2. **Directional momentum bias**: If the last 2+ actions produced a consistent player movement direction (detected via frame delta), bias next coordinate to continue that direction rather than reset.

3. **Anti-clustering guard**: If 3+ consecutive coordinates fall within a 3×3 bounding box and all produced zero reward, skip that region and jump to the next tier.

4. **Trace evidence**: Emit `coordinate_policy`, `goal_distance_before`, `goal_distance_after`, `directional_bias` in step trace for auditability.

## Files to Create/Modify

- `agents/arc3/orchestrator.py` — replace `_candidate_action6_coordinates()` and `_infer_action6_coordinates()` with geometry-aware versions when space/reach_goal conditions are met; fallback to existing logic for other archetypes
- `tests/test_b143_coordinate_policy.py` — regression tests for goal-directed sorting, momentum bias, and anti-clustering

## Ecosystem / Layer Compliance

- Layer: ARC agent orchestration policy (`agents/arc3/orchestrator.py`)
- No cross-layer import violations
- Additive to existing coordinate logic — existing behavior preserved for non-space archetypes
- Complies with `docs/arc-harness-rules.md` and `docs/ecosystem-rules.md`

## Acceptance Criteria

- [ ] For `space/reach_goal` puzzles with known player+goal, first 3 coordinate candidates lie on or near the player→goal vector
- [ ] Directional momentum is detected from frame deltas and biases next coordinate choice
- [ ] Anti-clustering skips regions after 3+ zero-reward attempts in a 3×3 box
- [ ] Non-space archetypes use the existing coordinate policy unchanged
- [ ] Trace output includes `coordinate_policy`, `goal_distance_before/after`
- [ ] Existing test suites pass with no regressions
- [ ] A fresh live smoke shows coordinates biased toward the goal rather than random cycling
