# B135: Replace Hardcoded `step_num > 3` Loop Check With Evidence-Based Trigger

**Status**: Ready  
**Assigned**: TBD  
**Depends on**: None  
**Complexity**: Small (< 1 hour)  
**Priority**: P1 (HIGH — the loop query still fires on nearly every late step)  
**Plan**: backlog/plans/B-135-gate-loop-check-trigger.md

## Problem Statement

`agents/arc3/orchestrator.py` still contains a step-count-based loop query gate:

```python
if step_num > 3:
    await self.brain.current_truth(query="Am I looping?", ...)
```

So after step 3, the check runs on essentially every act phase whether or not there is concrete evidence of a loop.

The current `master_timeline.json` still shows repeated `"Am I looping?"` calls across later steps. Some of those checks may be justified, but the trigger itself is still **mechanical rather than evidence-driven**.

## What This Card Builds

1. Replaces `step_num > 3` with a gate based on actual loop indicators, such as:
   - repeated frame hash,
   - consecutive no-progress steps,
   - repeated zero-reward action patterns,
   - or explicit dissonance/loop signals from the solve context.
2. Adds a `loop_check_skipped` event with a reason when the query is bypassed.
3. Preserves true loop detection while reducing noise and unnecessary latency.

## Files to Create/Modify

- `agents/arc3/orchestrator.py` — replace the step-based loop gate and add trace metadata

## Ecosystem / Layer Compliance

- Layer: ARC orchestration internals (`agents/arc3/`)
- No cross-layer import violations.
- Complies with `docs/arc-harness-rules.md` and `docs/ecosystem-rules.md`.

## Acceptance Criteria

- [ ] A run with no strong loop evidence produces far fewer `"Am I looping?"` calls
- [ ] `loop_check_skipped` appears when the query is bypassed
- [ ] A repeated frame or zero-progress streak still triggers the loop check promptly
- [ ] `pytest -q tests/` passes with no regressions

## Outcome Statement

After this card, loop checks are driven by evidence instead of an arbitrary step threshold. The timeline becomes more meaningful, and the loop signal is easier to trust.
