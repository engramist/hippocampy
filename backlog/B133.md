# B133: Add Decision-Source Tracing for Guard Overrides and Fallback Actions

**Status**: complete ✅ DONE (2026-04-01)
**Assigned**: TBD  
**Depends on**: None  
**Complexity**: Small-Medium (1–2 hours)  
**Priority**: P2 (MEDIUM — current action provenance is hard to audit)  
**Plan**: TBD

## Problem Statement

The earlier "round-robin 1→2→3→4→5" diagnosis no longer matches the current system state. The latest timeline is dominated by `ACTION6`, not the old sequential cycle.

What is still missing is **decision provenance**. The act phase currently does not clearly surface whether the final executed action came from:
- the LLM/sandbox directly,
- a sandbox fallback path,
- `_enforce_action_policy()`, or
- the final decision guard override.

A code search against `agents/**` shows no existing `llm_decision_source` or `guard_override_reason` fields. That makes it difficult to judge whether low-quality moves come from the model, from fallback behavior, or from policy enforcement.

## What This Card Builds

1. Adds a per-step decision source field such as:
   - `"sandbox"`
   - `"sandbox_recovered"`
   - `"fallback_query"`
   - `"policy_override"`
   - `"guard_override"`
2. Records both the **candidate** action and the **executed** action in trace output.
3. Emits an explicit `guard_override_reason` event whenever the guard changes or blocks a move.
4. Makes it easy to audit whether policy layers are helping or harming solve quality.

## Files to Create/Modify

- `agents/arc3/orchestrator.py` — enrich action trace metadata around `_mental_sandbox()`, `_enforce_action_policy()`, and guard execution

## Ecosystem / Layer Compliance

- Layer: ARC orchestration internals (`agents/arc3/`)
- No cross-layer import violations.
- Complies with `docs/arc-harness-rules.md` and `docs/ecosystem-rules.md`.

## Acceptance Criteria

- [ ] Each step records both the candidate and executed action
- [ ] Each executed step includes a decision-source field
- [ ] When the guard changes a move, `guard_override_reason` is visible in the trace
- [ ] `pytest -q tests/` passes with no regressions

## Outcome Statement

After this card, action selection is auditable. Future solver regressions can be attributed to the correct layer instead of guessing whether the LLM, fallback path, or guard logic was responsible.
