# B200: ARC Agent — Post-Solve Lesson Persistence (Outcome-Driven Learning)

**Status**: ready
**Assigned**: TBD
**Depends on**: B181 (outcome judge), B185 (failure taxonomy)
**Complexity**: Medium (2-4 hours)
**Priority**: P1 (closes the learning loop — every solve attempt produces knowledge)
**Plan**: backlog/plans/B-200-arc-post-solve-learning.md

## Problem Statement

After a puzzle completes, the agent records results to JSON but doesn't persist structured lessons back to SideQuests. The brain never learns from puzzle outcomes. The `report_outcome` tool exists and auto-creates Lessons when |valence| > 0.7, but the ARC runner doesn't call it with rich enough context to produce useful Lessons.

The learning loop is broken: solve → result → JSON file → nothing feeds back into the brain.

## Root Causes

1. `runner.py` writes results to JSON but doesn't call `report_outcome` with structured strategy data
2. `report_outcome` is called but with minimal context (just "success" or "failure")
3. No structured lesson extraction from the solve trace (what worked, what failed, why)
4. Failure lessons (negative valence) are especially valuable but never generated

## What This Card Does

1. **Structured outcome reporting**: After puzzle completes, call `report_outcome` with rich context:
   - Archetype and confidence
   - Strategy summary (which chunks worked, which failed)
   - Action semantics discovered (B178 data)
   - Entity roles discovered (B168 data)
   - Judge verdict (B181 score)
   - Failure class (B185 taxonomy)
2. **Explicit lesson creation**: Call `upsert_lesson` with domain = archetype, summarizing the most important learning from this puzzle
3. **Success lessons**: "Exploration-first strategy solved spatial navigation in 15 steps"
4. **Failure lessons**: "Goal-seeking without exploration failed after 146 steps; ACTION4 is not directional movement"
5. **Link to puzzle**: Lesson `APPLIES_TO` the archetype Concept

## Ecosystem Layer

- `agents/arc3/runner.py` (Agent Runtime — post-solve reporting)
- `benchmarks/arc3/adapter.py` (Eval Infra — ensure upsert_lesson in protocol)

**Cross-layer compliance**: Agent Runtime writes to Data Products via BrainClientProtocol.
**No shadow stores**: All learning persisted to KuzuDB via existing tools.

## Files to Create/Modify

- **MODIFY** `agents/arc3/runner.py` — Add structured `report_outcome` and `upsert_lesson` calls after puzzle completion
- **MODIFY** `benchmarks/arc3/adapter.py` — Ensure `upsert_lesson` in all BrainClientProtocol implementations
- **CREATE** `tests/test_b200_post_solve_learning.py` — Tests for lesson creation from success, failure, near-miss
- No changes to `docs/tool-catalog.md`

## Acceptance Criteria

- [ ] Every puzzle solve (success or failure) generates a structured `report_outcome` call
- [ ] Success puzzles create positive-valence Lessons with strategy details
- [ ] Failed puzzles create negative-valence Lessons with failure class and what didn't work
- [ ] Near-miss puzzles (B181 judge score >= 3) create Lessons noting what was almost right
- [ ] Lessons tagged with `domain = archetype` for recall by `recall_relevant_lessons`
- [ ] `upsert_lesson` creates `APPLIES_TO` edge to archetype Concept
- [ ] NoOpBrainClient handles all calls without error
- [ ] All existing ARC tests pass

## Outcome

The learning loop is closed. Every puzzle attempt — success or failure — produces knowledge that feeds back into the brain. After 50 runs, the brain has a rich library of what works and what doesn't for each archetype, enabling B191 (dreaming) to synthesize meta-lessons and B194 (procedures) to create reusable recipes.
