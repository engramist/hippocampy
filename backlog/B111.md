# B111 - ARC SideQuests Call Ledger in Debug Export

State: complete
Completed: 2026-04-01
Validation: 118/118 tests passed (test_b111_sidequests_ledger.py + solver + orchestrator + B124 + B92 regressions)

Plan: backlog/plans/B-111-arc-sidequests-call-ledger.md

### Problem

Current ARC debug output gives only partial visibility into SideQuests activity:

- `benchmark_metrics.retrieval_count`
- `benchmark_metrics.total_retrieval_size_bytes`
- `write_trace`
- prompt-included memory snippets

That is not enough to answer the operational questions we now care about during live ARC runs:

- how many times did the harness call SideQuests?
- which phase made the call?
- was it a read or a write?
- what was the input/query?
- what came back?
- how long did it take?
- did the result actually influence the next decision?

Without a true call ledger, we are still inferring overhead and usefulness from scattered traces.

### What This Card Builds

A step-level SideQuests call ledger for ARC debug exports:

- record each significant SideQuests read/write interaction
- capture phase, call type, compact input, compact result summary, and latency
- attach the ledger to `submission_results_single.json`
- make it easy to answer whether SideQuests is helping or just adding runtime cost

### Depends on

- B92
- B108

## Files to Create/Modify

- `benchmarks/arc3/adapter.py`
- `agents/arc3/orchestrator.py`
- `agents/arc3/runner.py`
- `tests/test_arc3_durable_runner.py`
- `tests/test_arc3_orchestrator.py`

## Acceptance Criteria

1. Each live ARC run emits a `sidequests_call_log` or similarly named export field with per-call entries.
2. Each entry includes at minimum:
   - `step`
   - `phase`
   - `call_type`
   - `mode` (`read` / `write`)
   - `input_summary`
   - `result_summary`
   - `latency_ms`
3. Calls are compactly summarized so the export remains readable.
4. The log distinguishes bootstrap/finalization calls from per-step calls.
5. Tests verify the ledger shape and that representative calls are captured.
6. A live smoke run makes it possible to answer:
   - how many SideQuests calls happened
   - which calls were most expensive
   - which calls returned information later used by the harness

## Outcome

ARC debug output gains a first-class observability layer for SideQuests usage, making runtime cost and decision impact visible instead of inferred.
