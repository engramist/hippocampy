# B90 - ARC Uncertainty-Gated Retrieval

State: complete

Plan: backlog/plans/B-90-arc-uncertainty-gated-retrieval.md

### B90 · ARC Uncertainty-Gated Retrieval

**Problem:** The current ARC orchestrator still retrieves memory too eagerly and the prompt language is still too generic. Even after prompt slimming, retrieval happens as part of the normal action loop rather than because the agent has a concrete information need, and the model is still being nudged toward pattern talk instead of decisions grounded in observed effects. This keeps SideQuests acting like a context pusher instead of a targeted decision-support system.

**What this card builds:** An uncertainty-gated retrieval policy for ARC. The agent should only ask SideQuests for more context when a bounded trigger fires, such as:
- start-of-puzzle initialization
- repeated no-progress steps
- loop suspicion
- large state change that invalidates prior assumptions
- low-confidence or fallback action selection
- a decision prompt that cannot be answered from the current observation and short history alone

**Scope:**
- Add explicit retrieval triggers to the ARC orchestrator
- Separate baseline working context from just-in-time retrieval
- Ensure the prompt only expands when a trigger justifies it
- Rephrase the action request so it asks for the next decision based on observed effects, not generic pattern commentary
- Keep the triggered context compact and evidence-oriented

**Depends on:** B87 (orchestrator ✅), B89 (measurement card)

## Files to Create/Modify

- `agents/arc3/orchestrator.py`
- `benchmarks/arc3/PROMPT_STRATEGY.md`
- `docs/ARCHITECTURE.md`
- `tests/test_arc3_orchestrator.py`

## Acceptance Criteria

1. The orchestrator defines explicit retrieval triggers instead of always injecting retrieved context
2. Retrieval can be skipped on a step when no trigger fires
3. When a trigger does fire, the resulting prompt shows only the newly justified retrieval context
4. The action prompt asks for a decision from observed effects, not a generic pattern description
5. Tests cover:
   - no-trigger path
   - no-progress trigger
   - invalid-action/fallback trigger
   - loop-warning trigger (or placeholder trigger if loop signal is pending)
6. Docs explain the rule: memory should enter the prompt only when it can change the next action

## Outcome

SideQuests moves closer to a symbiotic retrieval partner: minimal default context, targeted retrieval only when the agent is actually blocked or uncertain.

## Validation Note (2026-03-31)

- Implemented files: `agents/arc3/orchestrator.py`, `benchmarks/arc3/PROMPT_STRATEGY.md`, `tests/test_arc3_orchestrator.py`
- Tests run:
  - `.venv/bin/pytest -q tests/test_arc3_orchestrator.py tests/test_arc3_durable_runner.py`
- Result: `54 passed, 1 warning`
- Regression checks: retrieval is now gated behind explicit no-progress, invalid-action, loop, state-shift, and evidence-gap triggers
