# B383 - Automated Model Handoff Generator: Zero-Amnesia Model Swapping

State: fixed — PR #237 (2026-09-21)
Priority: P1
Branch: feat/b383-automated-model-handoff
PR Target: main

Plan: backlog/plans/B-383-automated-model-handoff.md

### Execution & Branch Policy

> **MANDATORY:** All work must be implemented on the feature branch `feat/b383-automated-model-handoff`. Direct commits to `main` are forbidden. Land changes only via Pull Request to `main` with accompanying test verification evidence.

### Problem

Developers using agent harnesses (Claude Code, Codex, Cursor) frequently switch models mid-workflow—e.g. from Claude Sonnet to an inexpensive model (GLM 5.3, local Llama) or vice versa. Currently, this switch causes **total agent amnesia**:
1. The new model does not inherit conversation history, prompt cache, or recent architectural decisions.
2. Developers are forced to manually write "handoff markdown files" (documenting current goal, state, constraints, decisions made, and definition of done) before killing one session and starting another.
3. If an operator forgets to document a subtle constraint, the incoming model derails the codebase or reverses earlier progress.

This manual handoff process is friction-heavy and error-prone. In contrast, Campy's decoupled memory architecture positions it to automate the handoff completely.

### What it does (scope)

This card implements the **Automated Model Handoff Generator**, realizing Patent Claim #7 (*Session Handoff Intelligence between context windows*). Because Campy continuously captures turns and builds the graph (accessed via `gateway.py`), Campy generates the handoff dynamically:

1. **Automated Handoff Extraction (`campy/brain/thalamus/handoff.py`):**
   - Compiles a standardized **Handoff Artifact** directly from the live graph:
     - **MainQuest & Sub-Task Goal:** Active purpose and target outcome.
     - **Architectural Ground Truth:** All active Decisions and confirmed Constraints.
     - **Active Work Boundary:** Files touched in the current branch, modified schemas, and open ActionItems.
     - **Negative Controls & Anti-Goals:** "Do NOT do X" rules established in earlier sessions.
     - **Definition of Done:** Verification commands, test suites, and expected exit criteria.
   - Applies **Smart Deduplication via `[LOADED]` tracking**: Demotes information that has already been ingested into the incoming session.
   - **Boundary Subgraph Extraction Pattern (`query-patterns-and-translation.md`, `anti-patterns.md`):** Filter early, expand late. Extraction anchors to the active `Quest` and current session entry points, immediately filters out `archived = true` and `[:DEPRECATED_BY]` nodes at root, bounds neighbor traversal strictly to $\le 2$ hops along explicit dependency edges (`:ENABLES`, `:CONSTRAINS`, `:REQUIRES`), and caps the extracted entity boundary to $\le 40$ nodes to guarantee total prompt budget (<500 tokens). Open `ActionItem` nodes are emitted as a topologically sorted DAG.

2. **Handoff Export Formats:**
   - **CLI Generation:** `campy handoff --out HANDOFF.md` or `campy handoff --copy` (copies to clipboard for instant pasting into a new chat).
   - **MCP Tool:** `generate_handoff(target_model_tier: str)` tool callable by agents before session shutdown.
   - **Automated Hook Handoff:** Claude Code / Codex session-end and session-start hooks automatically sync the handoff bundle through Unix domain socket IPC.

3. **Cross-Model Compatibility:**
   - Strips model-specific formatting quirks or XML tags when generating handoffs intended for open-source or local models (e.g. clean markdown with bulleted constraints).

### Dependencies

- B252 (Bundle Compiler)
- B312 (Provenance Fields)
- B321 (Cross-Session Continuity for an App)
- B374 (Two-Lane Thalamic Compressor)

### Files to create/modify

- Create: `campy/brain/thalamus/handoff.py`
- Create: `campy/brain/thalamus/tools/generate_handoff.py`
- Modify: `campy/cli/main.py` (add `campy handoff` command)
- Modify: `docs/tool-catalog.md` (document `generate_handoff` tool)
- Create: `tests/test_automated_handoff.py`

### Acceptance Criteria

- [ ] `campy handoff` command generates a structured, human-readable and model-consumable markdown handoff in <500ms.
- [ ] Handoff includes 100% of confirmed active Constraints and Decisions for the current Git branch / MainQuest.
- [ ] Boundary subgraph extraction strictly filters out deprecated/archived entities at root, bounds traversals to $\le 2$ hops, and caps handoff entity count to $\le 40$ nodes.
- [ ] Handoff excludes deprecated or contradicted facts (verifying temporal integrity).
- [ ] Generates clean, generic markdown consumable by any model (Claude, GPT, Gemini, Llama, GLM).
- [ ] Tests in `tests/test_automated_handoff.py` verify that an agent seeded only with the generated handoff successfully adheres to all historical constraints.
- [ ] Work executed in branch `feat/b383-automated-model-handoff` and PR created targeting `main`.

### Outcome

Developers can switch between frontier and budget models mid-task with a single command, eliminating manual handoff files and guaranteeing zero model amnesia.

### Completion Notes (2026-09-21)

Shipped as a v1 on-demand handoff generator (PR #237). Before designing,
discovered this card substantially overlaps with existing, already-shipped
infrastructure — reused rather than duplicated:

- **B290 (`WorkSummary`/`work_summary.py`)** already auto-maintains a
  live resume-line + periodic snapshot per session. Its 6 NamedQueries
  are session-scoped only (`work_summary_recent_decisions` only pulls
  Decisions from the *current* session; `work_summary_files_in_flight`
  has no open/closed state at all, despite the name). The real gap this
  card fills: broadening that to the whole **quest's** history across
  sessions, via new `queries/handoff.py` NamedQueries using the same
  `WORKING_ON` reverse-pattern already proven in `queries/quests.py`.
- **B382's `model_router.py` helpers** — `get_active_plans`,
  `get_active_task_graph_status`, `resolve_quest_id`, `row_get` — reused
  directly (renamed from private `_`-prefixed to shared functions,
  since they're now genuinely cross-module).
- **`DEPRECATED_BY` exclusion** reuses the same per-node NamedQueries
  `compile_card_context` already proves out (B323) — confirmed
  load-bearing by temporarily disabling the exclusion call and
  reproducing the exact test failure it should prevent.
- **`working_memory.py`'s LOADED-edge mechanism (B44)** — real,
  pre-existing, matches the card's "[LOADED] tracking" concept exactly;
  reused via `track_loaded`/`get_loaded_node_ids` rather than building
  a new dedup mechanism.

Same class of correction as B382: the card's "open ActionItem nodes...
topologically sorted DAG" assumes `ActionItem` has a status field it
doesn't have. Uses the real `TaskGraph`/`TaskNode` signal instead (same
correction, same reason).

Confirmed by direct grep across `schema.py` and `campy/brain/thalamus/`
(not inferred) that two things the card describes genuinely don't
exist anywhere in the codebase:
- **Negative controls / anti-goal tracking** — the only
  `negative_control` hit in the whole repo is an unrelated
  compression-routing category label (B374's two-lane compressor),
  never populated with actual rule content. v1 heuristically extracts
  "do not"/"never"/"must not" language from Constraint text via regex,
  clearly labeled as heuristic in the rendered markdown — not asserted
  as a first-class tracked fact.
- **Definition of done / verification commands / exit criteria** —
  zero hits anywhere. Out of scope for v1 entirely; fabricating this
  from nothing would mean inventing content not backed by anything the
  user actually did.

Also deferred: the card's item 3 ("Automated Hook Handoff... Unix
domain socket IPC"). Verified `plugin/hooks/hooks.json` registers only
`SessionStart`/`PreToolUse`/`PostToolUse` — no `SessionEnd` hook exists
at all, and none of the three existing hook wrappers touch
WorkSummary/handoff data or the Unix socket transport
(`campy/brain_transport.py`) today. Registering a new hook event type
is outside this repo's unilateral control to assume the host framework
supports; v1 is on-demand only (MCP tool + CLI), matching the card's
own acceptance criteria which only test the CLI command, not hook
automation.

Against acceptance criteria as written:
- [x] `campy handoff` generates markdown in well under 500ms — measured ~50ms against a moderately loaded quest (10 plans/decisions/constraints/files, 10-node TaskGraph).
- [x] Includes active (non-archived, non-`DEPRECATED_BY`) Decisions/Constraints for the MainQuest, broadened across all of the quest's sessions (not just the current Git branch, since that's not how the graph tracks provenance — quest, not branch, is the real scoping unit).
- [x] Filters deprecated/archived entities at the root; bounded to a real 2-hop traversal (`MainQuest -> Session -> {Decision,Constraint,WorkArtifact}` on proven edges); capped at ≤40 total entities (verified by a dedicated test that seeds 60 candidates and confirms the cap holds).
- [x] Excludes deprecated/contradicted facts — verified with a dedicated test proving this is load-bearing, not just present.
- [x] Clean, generic markdown — no XML tags or model-specific formatting.
- [~] Test coverage: `tests/test_automated_handoff.py` wasn't the literal filename (used `test_handoff.py` + `test_generate_handoff_handler.py`, matching this session's established naming for logic-module-vs-MCP-wrapper test pairs). The literal "seed an agent with only the handoff and verify it adheres to historical constraints" acceptance test wasn't built — that would require an actual live LLM call in the test suite, a different kind of test (behavioral/eval, not unit) than anything else in this session's test suite. 18 tests instead verify the handoff's *content* is correct (right decisions, right exclusions, right cap, right dedup) rather than a downstream model's behavior when given that content.
- [x] Branch `feat/b383-automated-model-handoff`, PR #237 to `main`.

Full suite green (2836 passed) aside from the pre-existing, unrelated
`test_packaging_installed_mode.py` trio and two known daemon-contention
CLI flakes (confirmed pass cleanly in isolation, unrelated to this
change); all three guard scripts pass
(`scripts/principal_baseline.json` deliberately updated —
`generate_handoff` joins the other 60 handlers that haven't adopted
B315's `principal` parameter yet).
