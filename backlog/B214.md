# B214 — Fix upsert_lesson Silent Failure in LedgerBrainClient

- **State:** ready
- **Priority:** P0 (blocking — kills in-run learning; recall_lessons always returns 0)
- **Plan:** backlog/plans/B-214-fix-upsert-lesson-silent-failure.md
- **Ecosystem Layer(s):** Agent Runtime, Benchmark Adapter
- **Cross-layer violations:** None.
- **Dependencies:** None.

## Problem Statement

Every `upsert_lesson` call during a run returns `lesson_id=None`. This means the agent's learning loop is completely broken: lessons written in `model` and `perceive` phases are never persisted, and `recall_lessons` finds 0 results on every step for the entire 20-step run.

Root cause: `LedgerBrainClient.upsert_lesson` (`benchmarks/arc3/adapter.py:684`) wraps the inner call in a bare `except Exception: resp = {"lesson_id": None}`. If KuzuDB, the embedding step, or any other inner call throws, the exception is swallowed silently and the returned `lesson_id` is `None`. The same silent-fallback pattern exists in `LocalBrainClient.upsert_lesson` and `LocalBrainClient.store_lesson` (lines 283–299).

Confirmed from smoke test `master_timeline.json`: all 15 `upsert_lesson` calls show `lesson_id=None`; all `recall_lessons` calls across 20 steps return `found 0 lessons`.

## What It Does (Scope)

1. Add exception logging (at minimum `logger.exception`) inside the except blocks so failures are visible.
2. Add a round-trip smoke assertion: after a successful `upsert_lesson`, a `recall_lessons` query with matching domain/type returns ≥ 1 result.
3. Diagnose and fix the actual root cause (likely a KuzuDB write failure or embedding error in the test DB path).

Out of scope:
- Changing lesson retention policy or lesson schema.
- Cross-session lesson persistence (separate concern).

## Files to Create/Modify

| File | Action |
|------|--------|
| `benchmarks/arc3/adapter.py` | **Modify** — add `logger.exception` in both `upsert_lesson` except blocks and `store_lesson` fallback |
| `tests/test_arc3_durable_runner.py` | **Modify** — add round-trip test: write lesson → recall → assert found ≥ 1 |

### Adapter/allow-list impact
- No tool schema changes.
- No `docs/tool-catalog.md` updates required.

## Acceptance Criteria

1. A `upsert_lesson` call on a live `LocalBrainClient` returns `lesson_id` that is not `None`.
2. A `recall_lessons` call immediately after returns `found ≥ 1 lessons` for the same domain.
3. Any exception during lesson write is logged at ERROR level with full traceback (not silently swallowed).
4. Smoke run `master_timeline.json` shows non-None `lesson_id` values for all `upsert_lesson` entries.
5. Existing tests continue passing.

## Outcome Statement

The agent's in-run learning loop is restored: lessons written during `model` and `perceive` phases are persisted and retrievable by `recall_lessons`, enabling the agent to accumulate knowledge across steps within a single puzzle run.
