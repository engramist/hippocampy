# B257 - Upgrade Plugin Skills with Memory OS Tools

State: complete -- re-verified 2026-08-03: all 5 skill files read in full; every tool call referenced across them (17 distinct names) cross-checked programmatically against the live TOOL_HANDLERS registry (53 tools) with zero misses -- see Verification Findings
Priority: P0

Plan: backlog/plans/B-257-upgrade-plugin-skills.md

### Problem

The 4 plugin skills at `plugin/skills/` predate the Memory OS work (B249-B254). The recall skill only knows about `current_truth`. It doesn't mention `compile_context`, `memory_decision`, `ingest_data`, `recall_procedures`, `recall_relevant_lessons`, or other retrieval tools added since the plugin was written.

### What This Card Does

Updates all 4 existing plugin skills and adds a new `session-start` skill with the explicit session-start recall protocol.

### Dependencies

- B256 complete (tools must be reachable before we teach agents to use them)

### Design Decisions

| Decision | Choice | Rationale |
|---|---|---|
| New skill added | `plugin/skills/session-start/SKILL.md` | Session-start recall protocol is the critical missing piece |
| Skill tone | Assertive mandatory triggers, not passive suggestions | Agents interpret "you can..." as "you don't have to..." |
| Tool coverage | All retrieval tools documented in recall skill | Agents need a complete menu |

### Changes Per Skill

**`plugin/skills/recall/SKILL.md`:**
- Add `compile_context` for multi-entity queries
- Add `memory_decision` as the "not sure?" entry point
- Add `recall_procedures`, `recall_relevant_lessons`, `reconstruct_timeline`
- Add explicit trigger phrases: "Before answering architecture questions → current_truth"

**`plugin/skills/memory-awareness/SKILL.md`:**
- Add tabular data ingestion mentions (`ingest_data`)
- Update insight surfacing for bundle compilation

**`plugin/skills/status/SKILL.md`:**
- Add `compile_context` token budget guidance
- Add bundle truncation awareness

**`plugin/skills/quest-management/SKILL.md`:**
- Minor: reference `diff_since` when switching quests

**`plugin/skills/session-start/SKILL.md` (NEW):**
- Step 1: Call `memory_decision` with user's first message
- Step 2: If recommended, call the suggested recall tool
- Step 3: If continuing an existing quest, call `diff_since`
- Step 4: Surface what the Brain knows before working blind

### Acceptance Criteria

- [x] All 5 plugin skills are present and reference current tools (`plugin/skills/{recall,memory-awareness,status,quest-management,session-start}/SKILL.md` all exist and read in full)
- [x] `session-start/SKILL.md` contains explicit step-by-step protocol (Steps 1-4, matches plan)
- [x] No references to old tool names or missing tools — programmatically verified (see below)
- [x] Trigger language is assertive ("ALWAYS call..." not "you can call...") — confirmed via direct read of all 5 files; see also B258 findings for the repo-wide passive-language grep

### Verification Findings (2026-08-03)

- Extracted every `tool_name(` call pattern referenced across the 5 skill files (17 distinct real tool names: `analogical_search`, `branch_quest`, `compile_context`, `complete_quest`, `context_status`, `current_truth`, `diff_since`, `get_open_loops`, `ingest_data`, `memory_decision`, `notify_turn`, `recall_plans`, `recall_procedures`, `recall_relevant_lessons`, `reconstruct_timeline`, `set_quest` — plus incidental Python stdlib calls like `isoformat`/`timedelta`/`utcnow` from the session-start example code block, which are not tool names).
- Cross-checked that set against the live `TOOL_HANDLERS` registry (`campy/brain/thalamus/tools/__init__.py`, 53 tools total) with: `PYTHONPATH=<worktree> .venv/bin/python -c "from campy.brain.thalamus.tools import TOOL_HANDLERS; ..."` → **0 missing**. Every tool the skills tell agents to call actually exists.
- `memory-awareness/SKILL.md` confirmed to have the `ingest_data` tabular-ingestion section and "Bundle Compilation Awareness" section per the plan's Task 2.
- `status/SKILL.md` confirmed to have the "Token Budget Guidance" table and "Bundle Truncation Awareness" section per the plan's Task 3.
- `quest-management/SKILL.md` confirmed to have the "Switching Context with Memory" section referencing `diff_since` and `reconstruct_timeline` per the plan's Task 4.
