# B122 - Extract Hardcoded Prompts to Shared Prompt Module

State: complete
Completed: 2026-04-01
Validation: 99/99 tests passed (test_arc3_prompts.py + test_arc3_orchestrator.py + test_arc3_solver.py)

Plan: backlog/plans/B-122-extract-shared-prompts.md

Ecosystem Layer: Prompt & Knowledge (Layer 4)

### Problem

Several prompt strings are hardcoded inline in `orchestrator.py` and `solver.py`. Per
`docs/arc-harness-rules.md` and `docs/ecosystem-rules.md`, prompts belong in Layer 4
(Prompt & Knowledge) and must not be embedded in runtime logic (Layer 3) or control plane
(Layer 2). This makes prompts hard to audit, version, and test independently.

Hardcoded locations:
- `orchestrator.py` ~line 622: SYSTEM block ("You are an ARC puzzle solver...")
- `orchestrator.py` ~line 671-703: INSTRUCTION block (effect-oriented decision prompt)
- `orchestrator.py` ~line 845: Mental sandbox instruction string
- `orchestrator.py` ~line 911: `_query_llm` system message ("You are an ARC reasoning assistant.")
- `solver.py` ~line 783: `VictoryHypothesizer.PROMPT_TEMPLATE`

### What This Card Builds

1. New module `agents/arc3/prompts.py` holding all extracted prompt constants/templates.
2. Each prompt is a named constant (e.g., `SYSTEM_PROMPT`, `INSTRUCTION_TEMPLATE`,
   `SANDBOX_INSTRUCTION`, `QUERY_LLM_SYSTEM`, `VICTORY_HYPOTHESIS_TEMPLATE`).
3. `orchestrator.py` and `solver.py` import from `agents/arc3/prompts.py` instead of
   defining inline strings.
4. No behavioral change — exact same prompt text, just relocated.

### Depends on

None (can run independently of B119-B121).

### Priority

P2 — Cleanup/compliance. Prioritize after B119-B121 entity discovery chain.

## Files to Create/Modify

- `agents/arc3/prompts.py` — **CREATE**: all prompt constants extracted here
- `agents/arc3/orchestrator.py` — **MODIFY**: replace inline strings with imports from prompts.py
- `agents/arc3/solver.py` — **MODIFY**: replace `PROMPT_TEMPLATE` with import from prompts.py
- `tests/test_arc3_prompts.py` — **CREATE**: verify prompt constants are non-empty strings, verify orchestrator/solver use them

## Acceptance Criteria

1. `agents/arc3/prompts.py` exists and contains all 5 prompt constants.
2. `orchestrator.py` has zero hardcoded multi-line prompt strings — all come from `prompts.py`.
3. `solver.py` `VictoryHypothesizer` references `prompts.VICTORY_HYPOTHESIS_TEMPLATE` instead of inline `PROMPT_TEMPLATE`.
4. `grep -rn "You are an ARC" agents/arc3/orchestrator.py agents/arc3/solver.py` returns zero hits (prompts live in prompts.py only).
5. All existing tests pass unchanged — no behavioral diff.
6. `pytest tests/test_arc3_prompts.py -q` passes.

## Outcome

All ARC agent prompts live in a single auditable module per ecosystem-rules.md Layer 4, making prompt evolution, A/B testing, and compliance review straightforward.
