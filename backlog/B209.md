# B209 — Enforce Route→Execute Plan Adherence and Action Provenance

- **State:** ready
- **Priority:** P1
- **Plan:** backlog/plans/B-209-route-execute-plan-adherence.md
- **Ecosystem Layer(s):** Agent Runtime & Harness
- **Cross-layer violations:** None — runtime policy + reporting only.
- **Dependencies:** B203, B205

## Problem Statement

Live smoke output shows repeated planner/executor drift: route phase chooses a concrete chunk/action family (for example, `Plateau Exploitation: commit to top-ranked ACTION3`), while execute/act repeatedly emits a different action (`ACTION2`) via autopilot rationale. This creates poor plan adherence and produces `orchestration_status: violation` despite valid low-level calls.

## What It Does (Scope)

Add a route→execute adherence contract so the selected action aligns with the active chunk unless a documented override condition is met.

Behavior:
- Treat active chunk `estimated_actions[0]` (or locked family) as preferred action.
- If execute proposes a different action, allow it only with explicit override reason (`safety`, `invalid_action`, `blocked_transition`, `policy_override`) and emit structured trace metadata.
- Record `expected_action`, `selected_action`, `override_reason`, and `adherence_ok` in solve context for downstream evaluation.
- Tighten orchestration report classification so this drift is measured as planner/executor mismatch (not tool-phase mismatch).

Out of scope:
- Rewriting trajectory scoring formulas.
- Changing ARC adapter protocol or SideQuests tool schemas.

## Files to Create/Modify

| File | Action |
|------|--------|
| `agents/arc3/orchestrator.py` | **Modify** — enforce action adherence policy between route output and execute action |
| `agents/arc3/solver.py` | **Modify** — expose expected action/family in solve context for execute policy |
| `agents/arc3/runner.py` | **Modify** — include planner/executor adherence metadata in orchestration report |
| `tests/test_arc3_orchestrator.py` | **Modify** — add unit tests for adherence and explicit override paths |
| `tests/test_arc3_durable_runner.py` | **Modify** — validate orchestration report fields for adherence mismatch |

### Adapter/allow-list impact
- No tool additions.
- No `docs/tool-catalog.md` changes required.

## Acceptance Criteria

1. When a chunk recommends `ACTIONX`, execute emits `ACTIONX` unless a recognized override reason exists.
2. Any override emits structured metadata (`expected_action`, `selected_action`, `override_reason`) in trace/summary.
3. Planner/executor mismatch is surfaced explicitly in orchestration report diagnostics.
4. Smoke test no longer shows silent ACTION3→ACTION2 drift without override reason.
5. New/updated tests for adherence and override behavior pass.

## Outcome Statement

Route intent and execute behavior become contract-aligned, improving plan adherence and making true orchestration violations actionable instead of ambiguous.