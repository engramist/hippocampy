# B159: Topological Entity Resolution — Graph-Neighborhood Overlap in Step 5

**Status**: ready
**Assigned**: TBD
**Depends on**: None
**Complexity**: Small (1-2 hours)
**Priority**: P4 (targeted improvement to entity resolution accuracy)
**Plan**: backlog/plans/B-159-topological-entity-resolution.md

## Problem Statement

Step 5 (Candidate Retrieval) uses **only embedding similarity** to determine if an incoming concept matches an existing one. The thresholds are: <0.75 = no match, 0.75-0.92 = gray zone (arbitration), >0.92 = strong match (additive).

This misses cases where two concepts have low text similarity but high structural overlap in the graph. Example: "The DB" and "PostgreSQL" have low vector similarity (~0.4), but if both `REQUIRES` the same config files and `CO_OCCURS_WITH` the same concepts, they're almost certainly the same entity.

The warm frontier boost (B91) and CO_OCCURS_WITH edges already capture some structural signal, but they operate downstream of Step 5's similarity check. By the time the graph context is considered, the candidate has already been rejected as "no match" because the embedding similarity was below 0.75.

## What This Card Builds

1. **Topological overlap score** computed in `retrieve_candidates()`:
   - After the HNSW vector search returns candidates above a relaxed floor (0.55 instead of 0.75)
   - For each candidate in the 0.55-0.75 range: compute 1-hop Jaccard overlap
   - Jaccard = |shared_neighbors| / |union_of_neighbors| using outgoing named edges (REQUIRES, ENABLES, etc.) and CO_OCCURS_WITH (count >= 3)
   - If `embedding_similarity + (jaccard_overlap * 0.3) >= MATCH_THRESHOLD (0.75)`: promote candidate into the gray zone for Step 6 arbitration

2. **Modified threshold logic** in Step 5:
   - Vector search fetch floor: lowered from 0.75 to 0.55 (only for topological check, not for final matching)
   - Candidates in 0.55-0.75 with high topological overlap get their effective similarity boosted
   - Candidates still in 0.55-0.75 after boost: filtered out (no match, same as before)
   - No change to the 0.75-0.92 and >0.92 thresholds

3. **Efficient neighbor query**: Single Cypher query per candidate to get 1-hop neighbor IDs:
   ```
   MATCH (c:Concept {concept_id: $cid})-[r]->(n:Concept)
   WHERE n.archived = false
   RETURN n.concept_id
   ```

## Files to Create/Modify

- `mcp_engine/loop/step5_retrieval.py` — Add `_compute_topological_overlap()`, modify `retrieve_candidates()` to check structural overlap for sub-threshold candidates
- `tests/test_topological_overlap.py` — NEW: test Jaccard computation, boost logic, threshold behavior

## Ecosystem / Layer Compliance

- Layer: Data Products & Memory (retrieval logic)
- No ARC code touched. No imports from `agents/` or `benchmarks/`.
- Changes are internal to Step 5 — no tool contract changes, no schema changes.
- Complies with `docs/ecosystem-rules.md`.

## Acceptance Criteria

- [ ] Candidates with embedding similarity 0.55-0.75 are checked for topological overlap
- [ ] Jaccard overlap of 1-hop neighbors correctly computed
- [ ] High topological overlap (>0.5 Jaccard) promotes candidates into gray zone
- [ ] No change to behavior for candidates already above 0.75 (existing path unchanged)
- [ ] Candidates with low embedding similarity AND low topological overlap remain "no match"
- [ ] Performance: topological check adds <50ms per candidate (single Cypher query)
- [ ] All tests pass, no regressions

## Outcome Statement

After this card, the entity resolution pipeline catches structural matches that pure vector similarity misses. Two concepts that share graph neighbors but have different surface text (abbreviations, jargon, domain-specific aliases) are promoted into the arbitration zone where the LLM can make the final call. This reduces duplicate nodes without increasing false merges.
