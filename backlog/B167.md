# B167: Pattern-Aware Puzzle Strategy — Region Comparison and Intermediate Goals

**Status**: ready
**Assigned**: TBD
**Depends on**: B166 (autopilot infrastructure), B150 (grid diff engine)
**Complexity**: Large (4–6 hours)
**Priority**: P0 (CRITICAL — without this, the agent can't solve any puzzle that requires pattern matching or intermediate steps)
**Plan**: backlog/plans/B-167-pattern-aware-strategy.md

## Problem Statement

ARC-AGI-3 puzzles are **multi-phase pattern-matching challenges**, not simple navigation tasks. A typical puzzle requires:

1. A **reference pattern** exists somewhere on the grid (e.g., bottom-left corner) showing what the goal should look like
2. A **goal pattern** exists elsewhere that must be transformed to match the reference
3. **Intermediate objects** (e.g., visual crosses, colored markers) exist on the grid — the player must navigate to these and interact to trigger state changes in the goal pattern
4. Once the goal pattern **matches** the reference pattern, the player navigates to the goal and uses ACTION5 (interact) to pass the level

The current agent has no concept of any of this. The autopilot (B166) drives straight to the "goal" color, which is useless — the goal isn't solvable until intermediate steps are completed. The SolveEngine classifies everything as player/goal/decoration/unknown, hiding functionally critical intermediate objects under "decoration."

This pattern-matching mechanic is common across many ARC-AGI-3 puzzles, not just one specific game.

## Root Causes

1. **No region comparison**: The agent can't compare two grid sub-regions to check if they match
2. **No intermediate goal discovery**: Objects classified as "decoration" may be interactive triggers the player must visit
3. **No phase awareness**: The agent doesn't know whether it's in explore/intermediate/finish phase
4. **Autopilot targets only "goal" role**: It drives to the final goal, ignoring intermediate objectives
5. **No progress metric for pattern completion**: The agent can't tell if its actions are making the goal pattern closer to the reference
6. **No memory persistence of grid understanding**: The agent discovers the puzzle structure (reference pattern, intermediates, goal) from scratch every level. It should save that understanding to SideQuests and recall it on subsequent levels

## What This Card Does

1. **Region extraction and comparison** — Identify distinct bounded regions on the grid and compare them for similarity (exact match, partial match, color-shifted match)
2. **Reference/goal region pairing** — Detect which regions are "reference" (static, in a corner) vs "goal" (dynamic, changes when player interacts with objects)
3. **Intermediate object identification** — Identify small, isolated colored objects that aren't player/goal/background as potential interaction targets
4. **Phase-aware autopilot targeting** — Extend B166 autopilot to navigate to intermediate targets first, then to the goal only when pattern comparison shows a match
5. **Pattern match progress tracking** — After each action, compare goal region to reference region and report similarity score (0-1)
6. **Save grid understanding to SideQuests memory** — After the first level's discovery phase, save a structured "puzzle model" to SideQuests (via `report_outcome` / `notify_turn`) describing what was learned:
   - "Reference pattern is always in bottom-left corner"
   - "Yellow-bordered markers are intermediate targets — visit all before approaching goal"
   - "Goal pattern transforms when player interacts with intermediate markers"
   - "Goal must match reference pattern before interacting completes the level"
7. **Recall puzzle model on subsequent levels** — On level 2+, `recall_plans` / `current_truth` retrieves the saved puzzle model so the agent skips discovery and immediately enters the intermediate phase

## Ecosystem Layer

- `agents/arc3` (ARC agent layer — grid_analysis.py, solver.py, orchestrator.py)

## Files to Create/Modify

- `agents/arc3/grid_analysis.py` — Add `extract_bounded_regions()`, `compare_regions()`, `find_reference_goal_pair()`
- `agents/arc3/solver.py` — Add "intermediate" role to ObjectRoleMapper, add `PatternMatchTracker` to SolveEngine
- `agents/arc3/orchestrator.py` — Extend `_try_autopilot()` to use phase-aware targeting, add pattern match progress to trace
- `tests/test_b167_pattern_strategy.py` — Tests for region comparison, intermediate discovery, phase-aware targeting

## Acceptance Criteria

- [ ] `compare_regions(region_a, region_b)` returns a similarity score (0.0 = completely different, 1.0 = exact match), with tolerance for position offset
- [ ] `find_reference_goal_pair(grid)` identifies the static reference region and the dynamic goal region using heuristics (corner position, size similarity, color palette overlap)
- [ ] Small isolated non-player/non-background objects are classified as "intermediate" role (not "decoration")
- [ ] Autopilot navigates to the **nearest intermediate object** when goal-reference similarity < 0.8
- [ ] Autopilot navigates to the **goal** only when goal-reference similarity ≥ 0.8
- [ ] Pattern match progress (similarity score) is logged in the trace after each step
- [ ] When the player reaches an intermediate object, ACTION5 (interact) is automatically tried
- [ ] After completing level 1 (or failing with new understanding), the agent saves a structured puzzle model to SideQuests via `report_outcome`
- [ ] On level 2+, `recall_plans` retrieves the puzzle model and the agent starts in the correct phase (intermediate, not discover)
- [ ] The puzzle model improves across levels — new observations (e.g., "white gates need interact to open") are appended
- [ ] All existing ARC tests pass (`pytest tests/test_arc3_*.py -q`)

## Outcome

The agent understands that puzzles have phases and intermediate objectives. It visits interactive objects to transform the goal pattern, monitors progress via region comparison, and only approaches the final goal once the pattern matches. This is the foundational capability needed to solve ARC-AGI-3 puzzles.
