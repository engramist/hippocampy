# B161: Goal-Directed Navigation — Agent Moves But Wanders Aimlessly

**Status**: ready
**Assigned**: TBD
**Depends on**: None
**Complexity**: Medium (2–4 hours)
**Priority**: P0 (CRITICAL — the agent moves the player but never navigates toward the goal)
**Plan**: backlog/plans/B-161-goal-directed-navigation.md

## Problem Statement

**Corrected diagnosis** (original analysis assumed zero-effect actions; deeper trace review proved otherwise):

The live smoke (`live_qwen25_7b_smoke_1775330224`) shows the agent IS moving the player through the maze — actions produce 1-96 pixel changes per step. But the agent **wanders aimlessly** instead of navigating toward the goal.

Actual per-step pixel changes:
```
Step 1: ACTION1(up)→0    Step 6: ACTION4(right)→72   Step 11: ACTION4(right)→0
Step 2: ACTION2(down)→1  Step 7: ACTION5(interact)→72 Step 12: ACTION1(up)→49
Step 3: ACTION3(left)→48 Step 8: ACTION5(interact)→96 Step 13: ACTION2(down)→0
Step 4: ACTION4(right)→1 Step 9: ACTION5(interact)→0  Step 14: ACTION3(left)→1
Step 5: ACTION5(int)→0   Step 10: ACTION3(left)→1     Step 15: ACTION1(up)→48
```

Key observations from the game UI screenshots:
1. **The game is a maze navigation puzzle** (Level 1/7) — navigate a player (orange+blue piece) through gray corridors to reach a goal
2. **Controls are directional**: ACTION1=up, ACTION2=down, ACTION3=left, ACTION4=right (confirmed by HELP screen)
3. **ACTION5 (interact) produces large effects** (72-96 pixels) — likely pushing objects or triggering game mechanics
4. **There's an energy bar** that depletes with each action — the agent has a limited step budget
5. **The HELP button shows**: "Available Controls: ↑↓←→. You need to play the game to discover controls, rules, and goal."
6. **7 levels total**, each progressively harder

The agent already knows action mappings (via `api_knowledge.py`) but this knowledge **doesn't translate into goal-directed behavior**. The LLM picks actions without understanding the maze layout or planning a path.

## Root Causes

1. **No spatial reasoning in the prompt**: The LLM sees color counts and action history but never a walkable-path analysis
2. **No player/goal position tracking per step**: The solver identifies player=color_8 and goal roles but doesn't track where they are on the grid each step
3. **No directional intent**: The prompt asks "which action?" without providing "player is at (31,30), goal appears to be at (15,20), you need to go up-left"
4. **ACTION5 effects aren't understood**: Large pixel changes from interact suggest significant game mechanics the agent doesn't leverage

## What This Card Does

1. **Adds per-step player position tracking**: After each action, compute the centroid of the player color to know where the player is now
2. **Adds directional guidance to the prompt**: When player and goal positions are known, inject "Player is at row X col Y. Goal appears near row A col B. Move toward it."
3. **Adds ACTION5 effect analysis**: When ACTION5 produces large pixel changes, log what changed and provide that as context to the LLM
4. **Adds a movement history summary**: "You moved: →right(72px), ↑up(0px=wall), ←left(48px)" so the LLM understands which directions are passable

## Ecosystem Layer

- `agents/arc3` (ARC agent layer — orchestrator.py, solver.py)

## Files to Create/Modify

- `agents/arc3/orchestrator.py` — player position tracking, directional prompt injection, movement history
- `agents/arc3/solver.py` — per-step centroid update in ObjectRoleMapper
- `tests/test_b161_goal_directed_nav.py` — test directional guidance appears in prompt, test position tracking

## Acceptance Criteria

- [ ] After each step, `_player_position` is updated with the centroid of the player color
- [ ] When player and goal positions are both known, the INSTRUCTION block includes directional guidance
- [ ] Movement history is included in the prompt: which directions produced movement vs hit walls
- [ ] When ACTION5 produces >30 pixel changes, the effect is logged and included in next prompt's ACTION_FACTS
- [ ] All existing ARC tests pass (`pytest tests/test_arc3_*.py -q`)

## Outcome

The agent stops wandering and starts moving toward the goal, using spatial awareness and movement history to navigate the maze.
