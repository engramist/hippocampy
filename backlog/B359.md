# B359 - Entity-Neighborhood Graph Query for ARC (arc_get_entity_neighborhood)

State: ready
Priority: P2

Plan: none yet.

Parent/context: B278 (ARC-specific MCP tool surface). Requested by the ARC_AGI-side Claude Code session
(`arc-agi-45`, 2026-08-22) following an ARC-side architecture review mapping the graph-guided
investigation loop against ARC_AGI's actual query surface. Full context lives in ARC_AGI's own tree at
`docs/handoff/B278-entity-neighborhood-query.md` and `backlog/A192.md` (not this repo) — this card is the
Campy-side content of that handoff, reworded to this project's own card conventions rather than pasted
verbatim.

### Problem

ARC_AGI's planner never asks the graph "what do we already know about this specific object" before
deciding whether to act on it — only action-family-level evidence (`fetch_per_action_evidence`,
`fetch_rules_for_action`) and coordinate-level attempt/falsification counts exist today. The one
entity-scoped query already exposed, `get_entity_history`, is used once client-side for a flat +0.08
goal-confidence bump, not for hypothesis generation. ARC's own A175 already gives every entity a stable
`entity_ref` across frames; it just never reaches a query that returns "what hypotheses/mechanics touch
this entity."

### What it does (scope)

Add one new read tool: `arc_get_entity_neighborhood`.

Request: `{"task_id": "...", "entity_ref": 3}`

Response: `{"hypotheses": [{"hypothesis_id": "...", "claim": "...", "confidence": 0.6, "falsified":
false}], "mechanics": [{"name": "...", "confidence": 0.5}]}` — live (unfalsified) hypotheses and
mechanics already associated with the given entity.

Schema options (implementer's call, whichever is cheaper against the current schema — evaluate both
before picking):

1. A new direct edge from `GridEntity` to the `Hypothesis`/`Rule` nodes derived from effects observed on
   that entity (a natural extension of the existing `Transition` nodes, which `Rule` nodes already
   reference via `CONFIRMED_BY`/`FALSIFIED_BY`).
2. Traversal through existing nodes: `GridEntity` already has `MOVED_BY` edges to `ActionEffect` (per
   `arc_get_entity_movement`'s existing pattern) — traverse from there out to whatever `Rule`/`Hypothesis`
   nodes reference those same effects, no new edge type needed.

### Non-blocking finding to fold in or spin off separately (flagged by arc-agi-45, not part of this card's scope)

While tracing `arc_get_causal_path`'s implementation for this request, arc-agi-45 found it matches
`ActionFact` by bare `action_id` only (`campy/brain/thalamus/tools/arc_queries.py::arc_get_causal_path`) —
no coordinate/`book_id` granularity. For ACTION6, that's the same family-vs-instance conflation ARC_AGI
spent six cards (A182-A189) fixing client-side, except this instance lives in this repo's own Cypher and
hasn't been touched. Worth its own look, but explicitly not required to land this card.

### Dependencies

ARC-side status (as of 2026-08-22, per arc-agi-45): `entity_ref` threading and the client-side consumer
(`agents/arc4/graph_queries.py::fetch_entity_neighborhood`, degrades cleanly on `capability_missing`) are
specified in ARC_AGI's A192 but not yet implemented — will ship independently and correctly whether or
not this lands first, matching the existing non-strict-MCP pattern already used for B278's other tools.

### Files to create/modify

- New MCP tool declaration (follow B278's existing pattern — check the 3+ declaration sites this project
  requires for any new MCP tool surface: `TOOL_HANDLERS`, `tool_schemas.TOOLS`, `index.ts`, and
  `test_adapters.EXPECTED_TOOLS`, per this project's own established lesson from prior MCP tool additions).
- `campy/brain/thalamus/tools/arc_queries.py` (or wherever `arc_get_entity_movement`/`arc_get_causal_path`
  currently live) — the new query implementation, following whichever schema option is chosen above.

### Acceptance Criteria

- [ ] `arc_get_entity_neighborhood` tool added through all required declaration sites (not just the
      handler).
- [ ] Schema choice (new edge vs. traversal through existing `MOVED_BY`/effect nodes) made and documented
      with reasoning.
- [ ] Live smoke episode with a click-target entity that has an existing confirmed rule/hypothesis from a
      prior step: confirm `arc_get_entity_neighborhood` stops returning `capability_missing` and the
      corresponding ARC-side candidate score shows a nonzero entity-neighborhood contribution distinct
      from its action-family contribution.
- [ ] Coordinate with `arc-agi-45`/whichever ARC_AGI-side session is live at implementation time (check
      `ListAgents` for the current name) to confirm A192's client-side consumer is ready for the live
      smoke test, or land this independently and note that the smoke test is deferred until it is.
