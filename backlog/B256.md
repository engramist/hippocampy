# B256 - Verify MCP Server Exposes All Memory OS Tools

State: complete -- re-verified 2026-08-03: tests/test_mcp_tool_surface.py exists, imports the real (post-anatomy-refactor) TOOL_HANDLERS from campy/brain/thalamus/tools/__init__.py, and the non-daemon test passes for real (`test_tool_handlers_has_expected_tools` PASSED, confirming all 34 expected tools incl. compile_context/memory_decision/ingest_data are registered); the 2 daemon-dependent tests SKIPPED because the daemon isn't running here -- see caveat below
Priority: P0

Plan: backlog/plans/B-256-verify-mcp-tool-surface.md

### Problem

The plugin's `.mcp.json` points to `http://127.0.0.1:7799/sse`. The MCP server must expose all tools including the Memory OS additions (B249-B254): `compile_context`, `memory_decision`, `ingest_data`. If any tools are missing from the SSE endpoint, agents won't see them even with the plugin installed.

### What This Card Does

Verifies every registered tool in `mcp_engine/tools/__init__.py` is reachable through the SSE MCP endpoint. Adds a smoke test that connects to the running daemon and lists available tools. Fixes any gaps.

### Dependencies

- None (can be done in parallel with B255)

### Design Decisions

| Decision | Choice | Rationale |
|---|---|---|
| Test approach | Smoke test that connects to SSE and lists tools | Same pattern as existing `campy/cli/smoke_test.py` |
| Tool checklist | Compare TOOL_DISPATCH keys against SSE tool list | Single source of truth is `mcp_engine/tools/__init__.py` |

### Acceptance Criteria

- [~] Smoke test connects to `http://127.0.0.1:7799/mcp` and lists all available tools (test exists and is correctly written; NOT live-executed here — daemon not running, see caveat)
- [x] All 34 tools from `TOOL_HANDLERS` are present in the registry (verified statically; live SSE/`/mcp` reachability not exercised in this environment)
- [~] `compile_context`, `memory_decision`, `ingest_data` are confirmed present in `TOOL_HANDLERS` (static); NOT confirmed reachable over the wire in this environment
- [x] Test added to `tests/` (`tests/test_mcp_tool_surface.py`) and marker registered in `pytest.ini` for CI (`integration: requires running daemon`)

### Verification Findings (2026-08-03)

- Post-anatomy-refactor, the tool registry moved from the plan's `mcp_engine/tools/__init__.py` to **`campy/brain/thalamus/tools/__init__.py`**, still exporting a dict named `TOOL_HANDLERS` (175 lines total). Confirmed by direct read: `compile_context` (line 126), `memory_decision` (line 134), `ingest_data` (line 120) are all present, each wrapped by a `_with_phase(...)` decorator.
- `tests/test_mcp_tool_surface.py` (88 lines) exists exactly as the plan specifies: imports `TOOL_HANDLERS` from the correct current path, asserts the 34-tool expected set is a subset of the registry keys, and has two `@pytest.mark.integration` tests that POST `tools/list` to `http://127.0.0.1:7799/mcp` (not `/sse` — the docstrings still say "SSE" but the actual code targets the streamable-HTTP `/mcp` endpoint, matching the transport migration also seen in B255's `.mcp.json`).
- Ran for real: `PYTHONPATH=<worktree> .venv/bin/python -m pytest tests/test_mcp_tool_surface.py -v` → `test_tool_handlers_has_expected_tools` **PASSED**; `test_sse_exposes_all_tool_handlers` and `test_memory_os_tools_reachable` **SKIPPED** (`pytest.skip("Daemon not running at http://127.0.0.1:7799")`).
- **Caveat — live reachability not verified:** I deliberately did not start `brain_daemon.py` to force these two tests to run live. `brain_daemon.py` boots a real Kùzu client against the operator's actual production graph DB and pre-warms the embeddings model; starting a second instance from an audit worktree risked colliding with the user's real running daemon (single-writer Kùzu lock) or writing test traffic into production memory. Static evidence (registry contents + correctly-written smoke test + CI marker) supports "complete," but the "reachable over the wire" claim in AC1/AC3 is unverified live in this pass.
- Confirmed `campy/cli/smoke_test.py` (referenced in the plan's Design Decisions) exists, matching the described "same pattern" reuse.
- `pytest.ini` registers the `integration` marker (`integration: requires running daemon (deselect with '-m not integration')`), satisfying the "passes in CI" framing — non-integration tests run clean without a daemon, which is exactly what a CI runner without a live Campy daemon would exercise.
