# B213 — Revert Puzzle-Specific Heuristics and Fix Action/Autopilot Override Failures

- **State:** ready
- **Priority:** P0 (blocking — corrupts solve behavior on all non-matching puzzles)
- **Plan:** backlog/plans/B-213-revert-and-fix-action-policy.md
- **Ecosystem Layer(s):** Agent Runtime
- **Cross-layer violations:** None.
- **Dependencies:** None (prerequisite for B202, B211, B212).

## Problem Statement

Three separate issues were introduced into `orchestrator.py` that must be addressed together:

### Issue 1: Puzzle-specific white-cross heuristic (must revert)

`_detect_split_map_rotate_cross()` (orchestrator.py, line 4763) detects a specific visual pattern — two large connected components plus a plus-shaped object — and directly overrides action selection to force ACTION5 toward the detected coordinate. This heuristic is wired into three places:
- `_enforce_action_policy()` — forces ACTION5 when cross detected and no-progress ≥ 2
- `perceive_step_response()` — injects retrieval terms "rotate trigger cross", "ACTION5 interact rotation"
- `_try_autopilot()` — navigates to cross coordinate under no-progress

**Why this must be reverted:** It solves one puzzle by giving the agent the answer, not by making the agent smarter. On any puzzle without a plus-shaped object, the heuristic is dead code at best and misfires at worst. It permanently skews ACTION5 usage and retrieval queries across all puzzles.

### Issue 2: Action policy override blocks fresh unexplored actions (must fix)

The `_should_skip_chunk_action` guard at line 4220 fires when an action is stale (`value_status == "low_value"`, `zero_reward_streak >= 3`, etc.). It then selects a `replacement`. The replacement search at line 4231 iterates `unexplored` with `candidate != action_id` — but if the LLM **chose an unexplored action** as `action_id`, this guard fires on the _wrong target_: the guard is evaluating `observed_effects.get(action_id)`, which returns `None` for a truly unexplored action.

**Wait** — `_should_skip_chunk_action(None)` returns `False`. So the guard shouldn't fire for genuinely unexplored actions. The actual bug is upstream: the guard fires because `observed_effects.get(action_id)` is not `None` — the action has been seen and stamped `low_value` from a prior run. But the LLM is choosing it again because it wants to **re-test** it under different conditions. The policy interprets "was stale before" as "is stale now" and overrides.

**Evidence from live trace (2026-04-11):**
- Step 5: LLM rationale "I want to try ACTION5 because it hasn't been tried yet" → policy override to ACTION3
- Step 19: LLM explicitly: "I will try ACTION5 because it is a new action that hasn't been tried yet" → policy override to ACTION2

The fix: the policy must **not override the LLM when the LLM is choosing an unexplored or explicitly re-test action**. Exploration intent from the LLM is higher-authority than the decay guard.

### Issue 3: Autopilot spatial lock burns steps after no progress (must fix)

Recent traces show autopilot repeatedly navigating toward an incorrect intermediate/goal mapping during sustained zero-reward streaks. This creates a control loop where:
- no reward is observed,
- confidence does not degrade fast enough,
- autopilot continues burning steps on the same geometry.

The fix: add a confidence gate so autopilot cannot repeatedly commit to an intermediate/goal mapping after consecutive zero-reward steps. Under low confidence, defer back to planner exploration instead of forcing geometric continuation.

## What It Does (Scope)

### Part A: Remove `_detect_split_map_rotate_cross` and all call sites

Remove the method entirely and revert the three places it was called:
1. `_enforce_action_policy()` — remove the rotate_hint block (lines ~4180-4207)
2. `perceive_step_response()` — remove the rotate_hint injection block
3. `_try_autopilot()` — remove the cross-coordinate navigation block

### Part B: Fix action policy override inversion

In `_enforce_action_policy()`, before the `_should_skip_chunk_action` override block (line 4220), add an exploration-intent guard:

```python
# Never override the LLM's action when it is choosing to explore.
# Exploration intent: action is in unexplored OR LLM rationale contains
# explicit exploration language. The decay guard is for exploitation decisions,
# not discovery steps.
is_exploration_intent = (
    action_id in unexplored
    or any(kw in rationale.lower() for kw in (
        "haven't tried", "not yet tried", "new action", "unexplored",
        "never tried", "hasn't been tried",
    ))
)
if is_exploration_intent:
    # Record that we saw exploration intent and let the action through.
    self._emit_trace_event("operation", "guard_exploration_bypass",
        {"action": action_id}, {"reason": "LLM chose exploration; skipping decay guard"})
    return action
```

This check runs **before** the `_should_skip_chunk_action` block so exploration intent is never suppressed by fatigue state from prior steps.

### Part C: Fix autopilot confidence under no-progress

In `_try_autopilot()`, add a no-progress confidence gate before returning a coordinate decision:

- if `self._consecutive_no_progress_steps >= 2` and proposed target family repeats recent failed coordinates, abort autopilot decision for that step,
- emit a trace event (`autopilot_confidence_drop`) with reason `no_progress_spatial_lock`,
- return `None` so normal policy/planner exploration proceeds.

This is intentionally narrow: it only blocks repeated low-confidence spatial continuation; it does not disable autopilot globally.

### Not in scope
- Changing `_should_skip_chunk_action` logic (the guard itself is correct for exploitation).
- Adding graph inference or structured memory queries (B211, B212).
- Changing any phase machine wiring.

## Files to Create/Modify

| File | Action |
|------|--------|
| `agents/arc3/orchestrator.py` | **Modify** — Remove `_detect_split_map_rotate_cross()` and 3 call sites; add exploration-intent bypass in `_enforce_action_policy()`; add no-progress confidence gate in `_try_autopilot()` |

### Adapter/allow-list impact
- No new tools introduced.
- No tool-catalog changes.

## Acceptance Criteria

1. `_detect_split_map_rotate_cross` does not exist anywhere in orchestrator.py.
2. No references to "rotate_cross_inference", "rotate trigger cross", or "ACTION5 interact rotation" in action policy or perceive code.
3. In `_enforce_action_policy`, when `action_id in unexplored`, the decay guard does not fire and the action is returned unmodified.
4. When LLM rationale contains "new action" or "haven't tried", the decay guard does not fire.
5. In `_try_autopilot`, repeated target continuation is blocked when `consecutive_no_progress_steps >= 2` and recent failed geometry repeats.
6. `pytest tests/test_arc3_orchestrator.py::test_enforce_action_policy_preserves_unexplored_exploration` passes.
7. `pytest tests/test_arc3_orchestrator.py::test_autopilot_confidence_drops_on_no_progress_spatial_lock` passes.
8. `pytest tests/test_arc3_orchestrator.py` — full suite passes.
9. Smoke test: step rationales no longer show "policy override: stale low-value ACTION5 is still decaying; switching to ACTION2" when ACTION5 was the LLM's chosen action.

## Outcome

The agent's short-horizon control is restored to correct behavior: exploration intent from the LLM is honored, decay guards only fire on stale exploitation, and autopilot backs off when spatial confidence collapses under no progress. Puzzle-specific heuristics are removed, leaving the solve path generalizable.
