# B164: Improve LLM Prompt Quality for Small Models (qwen2.5:7b)

**Status**: ready
**Assigned**: TBD
**Depends on**: None
**Complexity**: Medium (2–4 hours)
**Priority**: P1 (HIGH — qwen2.5:7b produces 105 tokens/step average; needs better prompts)
**Plan**: backlog/plans/B-164-small-model-prompt-quality.md

## Problem Statement

The live smoke with qwen2.5:7b shows `tokens_output: 1572` across 15 steps (~105 tokens/step). The LLM is giving extremely terse responses with minimal reasoning. Contributing factors:

1. **Prompt budget is 1200 tokens in "navigation" mode** — this may be too small for meaningful reasoning about a 64x64 grid.
2. **The prompt structure is complex** — 15+ content block types (SYSTEM, ENTITY_CONTEXT, MEMORY, SOLVE_CONTEXT, PLAN, ACTION_FACTS, etc.) with deep headers. A 7B model may struggle to follow this structure.
3. **The JSON output format is demanding** — the model must return structured JSON with specific keys, but terse models often skip the rationale field or return malformed JSON.
4. **No chain-of-thought nudge** — the prompt asks directly for an action without encouraging step-by-step reasoning.

## What This Card Does

1. **Adds a "compact" prompt mode for small models** (≤13B params):
   - Reduces the number of content blocks to the 5 most critical: SYSTEM, OBSERVATION, ACTION_FACTS, HISTORY, INSTRUCTION
   - Uses shorter, more directive headers
   - Increases max output tokens to encourage reasoning

2. **Simplifies the JSON response format** for compact mode:
   - Instead of `{"action_id": "...", "rationale": "..."}`, use `{"action": N, "why": "..."}`
   - Adds a chain-of-thought preamble: "Think step by step. What changed? What should you try next?"

3. **Increases the prompt budget** from 1200 to 1800 tokens for navigation mode.

## Ecosystem Layer

- `agents/arc3` (ARC agent layer — orchestrator.py, prompts.py)

## Files to Create/Modify

- `agents/arc3/prompts.py` — add compact prompt templates
- `agents/arc3/orchestrator.py` — add compact mode detection (model name/param count), adjust prompt budget
- `tests/test_b164_compact_prompts.py` — test compact prompt rendering, JSON parsing of simplified format

## Acceptance Criteria

- [ ] When LLM model name contains "7b" or "3b" (case-insensitive), compact prompt mode activates
- [ ] Compact prompts contain ≤5 content blocks and use simplified JSON format
- [ ] Navigation prompt budget is 1800 tokens (up from 1200)
- [ ] The simplified JSON format `{"action": N, "why": "..."}` is parsed correctly by `_normalize_action_id()`
- [ ] All existing ARC tests pass (`pytest tests/test_arc3_*.py -q`)

## Outcome

Small models produce more useful reasoning per step, and the prompt structure doesn't overwhelm their limited attention budget.
