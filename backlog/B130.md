# B130: Enhanced submission_results logging and observability

**Status**: Backlog  
**Assigned**: TBD  
**Depends on**: None  
**Complexity**: Medium (1-2 day feature)  
**Priority**: P1 (Diagnostic value, aids debugging and performance tuning)
Plan: backlog/plans/B-130-enhanced-logging.md

## What This Card Builds

1. Per-call ARC API request/response logging in submission output.
2. Human-friendly run timeline markers (`elapsed_mmss`) and submission timestamp metadata.
3. Better orchestration observability (decision flow, guard escalation history, failure diagnostics).
4. A **CloudWatch-style but human-friendly** event stream showing exactly when each ARC request started, when the server replied, and what the 1st / 2nd / 3rd responses were.

## Files to Create/Modify

- `agents/arc3/runner.py`
- `benchmarks/arc3/adapter.py`
- `tests/test_arc3_durable_runner.py`
- `tests/test_adapters.py` (if adapter trace schema is surfaced there)
- `docs/arc-harness-rules.md` (if logging contract examples are updated)

## Ecosystem / Layer Compliance

- Layers touched: ARC harness runner + ARC adapter integration layer.
- No cross-layer import violations allowed; implementation must comply with `docs/ecosystem-rules.md`.
- Orchestration behavior and phase ownership reporting must comply with `docs/arc-harness-rules.md`.

## Problem Statement

Current `submission_results.json` output lacks critical observability context:

1. **No submission timestamp** — Can't identify when runs occurred or correlate with system events
2. **Missing raw ARC server responses** — Only summaries are captured; actual API payloads unavailable for compliance/audit
3. **No sequence timeline** — Hard to trace temporal relationships between steps; only wall-clock latencies are present  
4. **Ambiguous orchestration report** — `phase_owner` shows all "ARC Harness" (actually correct by design, but uninformative)
5. **Limited raw frame data** — Frame hash captures state but no pixel-level changes or animation sequences
6. **No HTTP metadata** — Status codes, response sizes, rate limit headers lost
7. **Sparse error context** — When API calls fail, root cause not captured (network error, timeout, invalid action, etc.)

## Goals

✅ Add ISO8601 submission timestamp to results header  
✅ Optionally capture raw ARC API requests/responses (with truncation for size)  
✅ Add `elapsed_mm:ss` sequence marker to each step for visual timeline  
✅ Add a top-level `arc_event_timeline` that reads like a human-friendly CloudWatch stream  
✅ Record request start time, response received time, and total duration for every ARC call  
✅ Make the 1st / 2nd / 3rd server responses trivially visible in order  
✅ Replace uninformative `phase_owner` with actual decision flow (who proposed vs. who executed)  
✅ Capture raw frame pixel deltas or change vectors for AI/debugging analysis  
✅ Log HTTP status, headers, and byte counts for API observability  
✅ Surface API error context (error codes, validation failures, constraint violations)  

## Exact Location Of ARC Responses

ARC-AGI-3 request/response payloads must be logged at these exact paths for each ARC server call:

- `submission_results[*].sidequests_ledger[*].arc_api_io.request`
- `submission_results[*].sidequests_ledger[*].arc_api_io.response`
- `submission_results[*].arc_event_timeline[*]`

Each ARC call must also include strict sequence identifiers:

- `submission_results[*].sidequests_ledger[*].arc_api_io.call_seq`
- `submission_results[*].arc_event_timeline[*].event_seq`

Rules:

- `call_seq` is strictly increasing per run (`1, 2, 3, ...`).
- ARC calls include RESET, action dispatch (`ACTION1` through `ACTION7`), and `GET /api/games` when used.
- Failures still write `arc_api_io.response` with `received=false` and error details.

## Design

### 1. Submission Metadata (`submission_metadata`)
```json
{
  "submission_results": [
    {
      "submission_metadata": {
        "created_at": "2026-04-02T14:32:58.234Z",
        "submission_id": "sub_20260402_143258_arc_eval_001",
        "run_duration_seconds": 2430.3,
        "environment": {
          "llm_model": "llama3.1:8b",
          "llm_endpoint": "http://localhost:11434/v1",
          "memory_backend": "kuzu_0.11.3",
          "arc_api_endpoint": "three.arcprize.org"
        }
      },
      "game_id": "g50t-5849a774",
      "task_id": "arc_eval_001",
      ...
    }
  ]
}
```

### 2. Timeline Sequence Markers (`elapsed_mmss`)
Add to each step, ledger entry, and solve trace:
```json
{
  "step": 10,
  "elapsed_mmss": "03:24",  // 3 min 24 sec from run start
  "timestamp_iso": "2026-04-02T14:35:22.145Z",
  "action_id": "ACTION1",
  "state_after": "NOT_FINISHED",
  "reward": 0.0,
  "latency_ms": 204.1
}
```

### 3. CloudWatch-Style ARC Event Timeline (`arc_event_timeline`)
This is the **primary human-facing log view** for each run. It must make the first, second, third server responses obvious without digging through nested JSON.

```json
{
  "arc_event_timeline": [
    {
      "event_seq": 1,
      "call_seq": 1,
      "kind": "request_started",
      "label": "RESET request started",
      "request_started_iso": "2026-04-02T14:32:58.234Z",
      "elapsed_mmss": "00:00",
      "method": "POST",
      "endpoint": "/api/cmd/RESET"
    },
    {
      "event_seq": 2,
      "call_seq": 1,
      "kind": "response_received",
      "label": "RESET response #1",
      "response_received_iso": "2026-04-02T14:32:58.612Z",
      "elapsed_mmss": "00:00",
      "duration_ms": 378,
      "http_status": 200,
      "response_summary": "Initial frame received; state NOT_FINISHED; available_actions=[ACTION1,ACTION2,ACTION3]"
    },
    {
      "event_seq": 3,
      "call_seq": 2,
      "kind": "request_started",
      "label": "ACTION1 request started",
      "request_started_iso": "2026-04-02T14:33:02.101Z",
      "elapsed_mmss": "00:04",
      "method": "POST",
      "endpoint": "/api/cmd/ACTION1"
    },
    {
      "event_seq": 4,
      "call_seq": 2,
      "kind": "response_received",
      "label": "ACTION1 response #2",
      "response_received_iso": "2026-04-02T14:33:02.420Z",
      "elapsed_mmss": "00:04",
      "duration_ms": 319,
      "http_status": 200,
      "response_summary": "No movement; state unchanged; possible wall collision"
    }
  ]
}
```

### 4. Raw ARC API Trace (`arc_api_io` + `arc_api_trace`)
Required `arc_api_io` field in sidequests_ledger entries for **every ARC server call**:
```json
{
  "step": 10,
  "phase": "act",
  "call_type": "arc_api_action",
  "mode": "write",
  "input_summary": "POST /api/cmd/ACTION1 with guid=..., action_id=1",
  "arc_api_io": {
    "call_seq": 14,
    "request": {
      "method": "POST",
      "endpoint": "/api/cmd/ACTION1"
    },
    "response": {
      "received": true,
      "http_status": 200
    }
  },
  "arc_api_trace": {
    "http_method": "POST",
    "http_endpoint": "/api/cmd/ACTION1",
    "http_status": 200,
    "request_size_bytes": 156,
    "response_size_bytes": 4821,
    "response_headers": {
      "content-type": "application/json",
      "x-ratelimit-remaining": "499",
      "x-ratelimit-reset": "2026-04-02T14:36:00Z"
    },
    "raw_frame_response": {
      "frame": [64x64 pixel array],
      "state": "NOT_FINISHED",
      "available_actions": [1, 2, 3],
      "score": 0,
      "reward": 0.0,
      "time_left": 3600
    }
  },
  "result_summary": "Frame returned, state NOT_FINISHED, no reward",
  "latency_ms": 204.1
}
```

### 4. Frame Delta / Change Vector (`frame_analysis`)
Capture what changed between consecutive frames:
```json
{
  "step": 10,
  "frame_hash": "d3e49253ba71cd4e",
  "frame_analysis": {
    "pixels_changed": 0,
    "bounding_box_change": null,
    "movement_detected": false,
    "new_colors_introduced": [],
    "colors_removed": [],
    "analysis": "Frame unchanged from previous step (wall collision)"
  }
}
```

### 5. Rich Orchestration Report (`decision_flow`)
Replace generic `phase_owner` with actual decision flow:
```json
{
  "orchestration_report": {
    ...
    "decision_flow": {
      "bootstrap": {
        "proposed_by": ["Arc Agent", "SideQuests"],
        "executed_by": "Arc Harness",
        "policy_applied": "sequential entity discovery",
        "enforced": true
      },
      "solve": {
        "proposed_by": ["Arc Agent"],
        "executed_by": "Arc Harness",
        "policy_applied": "chunk enforcement with guard escalation",
        "enforced": true,
        "guard_escalations": [
          {"step": 12, "reason": "loop detected", "guard_state": "warned"},
          {"step": 13, "reason": "distance not shrinking", "guard_state": "warned"}
        ]
      }
    }
  }
}
```

### 6. API Error Context (`api_failure_trace`)
When ARC API returns error or timeout:
```json
{
  "step": 12,
  "phase": "act",
  "api_failure_trace": {
    "error_type": "connection_timeout",
    "http_status": null,
    "error_code": "ECONN_TIMEOUT",
    "error_message": "POST /api/cmd/ACTION1 timed out after 30s",
    "retry_count": 0,
    "retry_backoff_ms": null,
    "wall_time_elapsed_ms": 30000,
    "diagnostics": "Network latency spike; server may be rate-limiting or overloaded"
  }
}
```

### 7. Solve Phase Confidence Breakdown (`solver_decisions`)
More granular reasoning in solve trace:
```json
{
  "solve_phase_summary": {
    "archetype_evolution": [...],
    "victory_evolution": [...],
    "solver_decisions": [
      {
        "step": 1,
        "archetype_candidate": "space",
        "confidence": 0.45,
        "justification": "Detected 2D movement grid; player can move orthogonally",
        "evidence_sources": ["available_actions coverage", "frame analysis"]
      },
      {
        "step": 5,
        "archetype_candidate": "space",
        "confidence": 0.65,
        "justification": "Multiple steps confirm orthogonal movement; no color palettes detected",
        "evidence_sources": ["action semantics", "player role stability"]
      }
    ]
  }
}
```

## Implementation Plan

### Phase 1: Timestamp & Metadata (1 session)
- [ ] Add `submission_metadata` to `_format_submission_row()` in [agents/arc3/runner.py](agents/arc3/runner.py)
- [ ] Add `elapsed_mmss` + `timestamp_iso` to each ledger entry
- [ ] Add `request_started_iso` and `response_received_iso` for ARC calls
- [ ] Pass run start time through DurableARCRunner state
- [ ] Validate timestamps are ISO8601 and monotonically increasing

### Phase 2: Frame Delta Analysis (1 session)
- [ ] Implement `_analyze_frame_delta()` helper to compare consecutive frames
- [ ] Track pixel changes, bounding box deltas, new/removed colors
- [ ] Add `frame_analysis` to solve_phase_summary
- [ ] Test with mock frames (no pixel changes, color swaps, movement)

### Phase 3: Raw ARC Trace Capture (1 session)
- [ ] Intercept HTTP calls in ARC adapter; capture method, endpoint, status, headers
- [ ] Add required `arc_api_io` field to each ARC call ledger entry (`request`, `response`, `call_seq`)
- [ ] Build a top-level `arc_event_timeline` with ordered `event_seq` entries for `request_started` and `response_received`
- [ ] Keep `arc_api_trace` optional as compatibility mirror during migration
- [ ] Capture raw frame response (optional to avoid bloating JSON)
- [ ] Add truncation logic for large payloads (warn if >10MB)

### Phase 4: Decision Flow & Guard Escalation (1 session)
- [ ] Replace `phase_owner` dict with `decision_flow` that tracks who proposed/executed
- [ ] Surface guard escalation history (which step, why, which guard state)
- [ ] Link decision violations back to orchestration_report
- [ ] Add solver confidence breakdown to solve_phase_summary

### Phase 5: Error Context & Diagnostics (1 session)
- [ ] Catch ARC API errors; log error_type, error_code, retry attempts
- [ ] Add network diagnostics (connection resets, timeouts, DNS failures)
- [ ] Surface rate-limit warnings and backoff advice
- [ ] Test with mock server errors

## Testing

## Acceptance Criteria

1. Every ARC call writes `arc_api_io.request`, `arc_api_io.response`, and monotonic `call_seq`.
2. Submission rows include `submission_metadata.created_at`, request start timestamps, and per-entry `elapsed_mmss`.
3. Each run exposes a top-level `arc_event_timeline` where the 1st, 2nd, and 3rd server responses are clearly visible in order.
4. Failures include populated response error details (`received=false` and diagnostic fields).
5. Existing consumers of `submission_results` do not break (additive schema only).
6. Targeted tests pass for runner/adapter logging paths.

**Unit Tests:**
- [ ] `test_submission_metadata_iso8601()` — Verify timestamp format and timezone
- [ ] `test_elapsed_mmss_calculation()` — Verify mm:ss is correctly computed from start_time
- [ ] `test_frame_delta_analysis()` — Test pixel change detection (no-op, movement, color swap)
- [ ] `test_arc_api_trace_capture()` — Mock HTTP call; verify trace structure
- [ ] `test_arc_api_io_per_call_present()` — Verify every ARC call has request+response
- [ ] `test_arc_api_io_call_seq_monotonic()` — Verify `call_seq` strictly increases
- [ ] `test_guard_escalation_history()` — Verify guard state transitions logged
- [ ] `test_arc_api_error_context()` — Mock 500, timeout, connection error

**Integration Tests:**
- [ ] `test_live_submission_metadata()` — Run single puzzle; verify metadata present
- [ ] `test_live_timeline_sequence()` — Verify mm:ss timestamps increase monotonically
- [ ] `test_live_frame_delta_trace()` — Verify frame_analysis present for all steps
- [ ] `test_large_json_truncation()` — Verify raw traces truncated and warned

## Backwards Compatibility

✅ **Non-breaking**: New fields are additive; old fields preserved  
✅ **Optional**: `arc_api_trace` and frame pixel data off by default (config flag)  
✅ **Migration**: Scripts parsing `submission_results.json` unaffected; can opt-in to new fields  

## Why This Matters

1. **Debugging**: Trace exact sequence of agent decisions + API responses in one file
2. **Compliance**: Raw API payloads available for audit (useful if ARC contest ever disputes results)
3. **Performance**: See where time is spent (LLM latency, API latency, memory operations)
4. **Learning**: Understand guard behavior, loop detection triggers, policy override decisions
5. **Visualization**: mm:ss timeline enables waterfall charts of agent reasoning

## Out of Scope (Future)

- Real-time streaming results (batch JSON only for now)
- WebSocket traces (HTTP only)
- Video frame reconstruction (keep as abstract change vectors)
- Agent internal token/embedding traces (too verbose; separate debug API)

---

## Notes

- **Orchestration Report clarification**: The "all ARC Harness" phase_owner is **correct by design** — the harness orchestrates all phases. The new `decision_flow` will show who *proposed* actions (agent, sidequests) vs. who *executed* them (harness), which is more meaningful.
- **Raw API traces**: Default off to avoid 100MB+ JSON files; enable per-run or per-phase with config flag.
- **Frame deltas**: Efficient delta encoding; don't store full 64x64 grids, just the changes + bounding box.

## Outcome

Submission output becomes an auditable per-call timeline where each ARC request has a corresponding response payload record, enabling fast debugging of loops, API behavior, and orchestration decisions.

