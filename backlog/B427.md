# B427 - ~41 Test Files Validate the Retired Kùzu/Cypher Engine, Not the Shipped Oxigraph/SPARQL Engine

State: ready (deprioritized — see Correction below)
Priority: P3
Branch: docs/b427-kuzu-test-suite-migration

### Correction this card starts from

An earlier pass on the 2026-09-11 re-vet mischaracterized `kuzu==0.11.3` as "still a hard
runtime dependency." **That was wrong.** Verified precisely (2026-09-12):

- `kuzu` lives in `[project.optional-dependencies].test` in `pyproject.toml` — a separate,
  opt-in-only extra, never in `[project.dependencies]`.
- `requirements.txt` has no kuzu.
- The documented dev setup (`pip install -e ".[dev]"`) does not pull it in (`dev` and `test`
  are disjoint extras).
- The real end-user install path (`pipx install hippocampy` via `scripts/bootstrap.sh`) never
  requests `[test]`.
- `pip show hippocampy`'s own `Requires:` metadata (checked in a real dev venv) lists 17
  packages; kuzu is not one of them.

So kuzu is **not shipped, not installed by a normal user, and not a runtime dependency** —
it was already correctly isolated as a test-only extra. No packaging fix is needed. The
prior turn's "top priority, fix this" framing does not apply; this card exists for the
separate, real issue found while investigating it.

### The real, separate finding

`tests/kuzu_test_client.py` is a genuine `kuzu.Database`/`kuzu.Connection` client (the
pre-cutover `kuzu_client.py`, relocated under `tests/`). **~41 test files** import it as
their DB backend and exercise it via `.cypher`, while the shipped engine
(`OxigraphClient`) executes `.sparql` (`gateway.py`'s `_is_oxigraph` branch). Only 4 files
are explicitly Kùzu-specific (`test_*_real_kuzu.py`); the other ~37 are ordinary domain
tests (`test_provenance`, `test_authority`, `test_task_dependency`,
`test_bundle_compiler_stages`, `test_fact_ingest`, `test_workspace_router`, …) that just
inherited Kùzu because that is what the shared test fixture happened to be.

Consequence: a meaningful share of the suite validates an engine that is **not what
ships**, and cannot catch drift on the SPARQL path — plausibly part of why the
B412/B418/B420/B421/B422 family of bugs (SPARQL-path defects) shipped despite a large,
green test suite. `test_kuzu_import_isolation.py` already enforces zero Kùzu imports in
`campy/` itself; this card is about the *test* side of that boundary, which currently has
none.

### Scope (measured, 2026-09-12)

- 41 files import `tests.kuzu_test_client` (excl. `conftest.py`, which provides the shared
  fixture).
- Of those, **6 make zero raw `.execute()` calls** (drive the DB only through
  gateway/handler functions) — candidates for a near-mechanical client swap.
- The remaining **35 make ~300 raw `db.execute(cypher, …)` / `conn.execute(...)` calls**
  between them (heaviest: `test_bundle_compiler_stages.py` 35, `test_graph_export.py` 33,
  `test_task_dependency.py` 20, `test_authority.py` 20, `test_provenance.py` 15,
  `test_b404_sweep_pk.py` 15) — each needs the Cypher rewritten to the equivalent SPARQL
  (or, where simpler, test setup switched to `OxigraphClient.write_node()`/`write_edge()`
  and assertions to `_execute_and_collect()` / a SPARQL `SELECT`, the pattern already used
  by `tests/test_b420_entity_link_handlers.py` and siblings).
- `tests/test_checkpoint_lock_serialization.py` specifically exercises `kuzu_test_client`'s
  own `_get_write_lock()` — a Kùzu-specific concurrency primitive with no 1:1 Oxigraph
  analog (the gateway's own `asyncio.Lock` is the real equivalent); needs a rewrite against
  that, not a client swap.
- 4 files (`test_*_real_kuzu.py`) test Kùzu-specific behavior (FTS/vector-index mechanics)
  that has no meaning post-cutover — likely deletion or an Oxigraph-analog rewrite
  (`sqlite-vec`'s FTS5/vec0), decided per file.

### Suggested approach, if picked up

1. Migrate the 6 zero-raw-Cypher files first (near-mechanical; validates the swap pattern).
2. Triage the 4 `real_kuzu` files individually (delete vs. rewrite as Oxigraph-analog).
3. Work through the 35 raw-Cypher files in descending call-count order, in small batches
   with their own PRs — each migration should assert the SAME domain behavior, not just
   "the query runs" (cf. the B412 card's own standard: "asserting NON-EMPTY, CORRECT
   results", not merely no-exception).
4. Once all 41 are migrated, delete `tests/kuzu_test_client.py`, the `test` extra from
   `pyproject.toml`, and retire `kuzu` from the repo entirely (no more test-only extra).

### Priority note

Downgraded to P3. It is real test-quality debt worth fixing, but — now that the "kuzu is a
shipped dependency" premise is corrected — there is no urgency driver; it competes with
B418b, B401, and other open items on ordinary priority.

### Progress log

- **Batch 1** (2026-09-14, merged #217): the 6→5 zero-raw-Cypher files (excluding
  `test_checkpoint_lock_serialization.py`, still needing its own rewrite against the
  gateway's `asyncio.Lock`). Found and fixed a real bug in the process
  (`gateway.py`'s `capability.create_edge_*` handler wrote to a nonexistent `INVOKES`
  table instead of `FACT_INVOKES`). `test_capability_queries.py` was pulled back out —
  filed as [B431](B431.md), a much larger SPARQL-completeness gap needing its own design
  pass.
- **Batch 2** (2026-09-15): triaged the 4 `test_*_real_kuzu.py` files.
  - `test_lexical_fallback_fts_real_kuzu.py` — migrated cleanly, all 6 pass.
    `OxigraphClient` already has a real `has_fts`/`create_fts_index`/`fts_search()`
    analog (sqlite-vec FTS5).
  - `test_tabular_ingest_real_kuzu.py` — migrated cleanly, all 6 pass.
    `tabular_ingest.py` is fully gateway-routed, no raw Cypher.
  - `test_basal_ganglia_real_kuzu.py` — migrated; 5/7 pass, 2 xfail(strict) referencing a
    newly found bug: [B433](B433.md) — `basal_ganglia.frustration_get_*`'s `sparql=`
    can never bind `?emb` (embeddings live in `vector_store`, not RDF), so
    `detect_frustration_clusters()` always finds zero clusters against the shipped
    engine.
  - `test_explore_graph_real_kuzu.py` — **NOT migrated**, stays on `KuzuClient`. Exposed
    a critical live-tool bug: [B432](B432.md) — `explore_graph` (a registered MCP tool)
    is structurally broken against Oxigraph (start-node lookup returns bare URI strings
    instead of hydrated nodes, crashing silently; frontier expansion bypasses the
    gateway entirely with Kùzu-only Cypher builtins with no SPARQL equivalent). Needs
    its own design pass before this file can be migrated or rewritten.
  - B432 fixed separately (2026-09-16, #219) — `test_explore_graph_real_kuzu.py` fully
    migrated onto `OxigraphClient` as part of that fix (8/8 pass).
- **Batch 3** (2026-09-17, in progress): working through the remaining raw-Cypher files.
  Re-measured scope first (counts above were incomplete — missed `execute_write`/
  `execute_read` calls, only counted bare `.execute(`) and found **3 more files that are
  permanently excluded, not migration candidates**:
  - `tests/patent_claims/conftest.py` — deliberately dual-engine: its `patent_db` fixture
    is `@pytest.fixture(params=["oxigraph", "kuzu"])` and `load_patent_conformance_graph()`
    already branches on `hasattr(db, "write_node")` to support both engines. All 9
    `test_claim_*.py` files in that directory consume this fixture generically (no direct
    `KuzuClient` import) and already run against both engines today. Nothing to fix.
  - `tests/test_migration_roundtrip.py` — a Kùzu→Oxigraph migration **round-trip proof**
    (exports a populated Kùzu graph, imports it into Oxigraph, re-exports, asserts
    set-equality). It legitimately needs a real Kùzu source by design — same category as
    `test_b417_kuzu_migrate.py` (already excluded in batch 1's scope re-measurement).
  - `tests/test_lexical_fallback.py` — its one real `KuzuClient` usage
    (`test_fts_probe_graceful_failure`) tests `KuzuClient.has_fts()`'s own
    probe-and-cache-the-result behavior when the underlying `execute()` raises — a
    Kùzu-client-internals unit test with no Oxigraph analog (`OxigraphClient.has_fts()` is
    a trivial `vector_store is not None` check, no probe/cache to test). Same reasoning as
    `test_checkpoint_lock_serialization.py`/`test_workspace_router.py`'s write-lock case.
    Every other test in the file already uses a hand-rolled mock DB.
  - This brings total permanently-excluded files to 6: `test_capability_queries.py`
    (blocked on B431), `test_checkpoint_lock_serialization.py` +
    `test_workspace_router.py` (Kùzu write-lock internals, no analog),
    `tests/patent_claims/conftest.py` + `test_migration_roundtrip.py` (deliberate
    dual-engine infra), `test_lexical_fallback.py` (Kùzu FTS-probe internals, no analog).
  - Migrated (near-mechanical, both zero-real-domain-logic — only a module-scoped `db`
    fixture + one raw readback helper needed changing): `test_auth_context.py` (46/46
    pass), `test_ingest_data_routing.py` (7/7 pass, same shape as `test_tabular_ingest_
    real_kuzu.py` from batch 2 — custom Dataset DDL dropped, `tabular_ingest.py` is fully
    gateway-routed).
  - Remaining after batch 3a: ~28 files with real raw-Cypher domain logic to rewrite.
    `test_bundle_compiler_stages.py` (35 calls) noted as unusually hard: its fixtures use
    custom ad-hoc `CREATE NODE TABLE` DDL with a fake 4-dim embedding (not the real
    384-dim schema), which `OxigraphClient.write_node()` can't accommodate (it's
    schema-driven off the real global `NODE_COLUMNS`, no per-test dimension override) —
    will need switching to real `init_schema()` + 384-dim fake vectors throughout, not just
    a client swap. Deferred; picked smaller files instead to keep momentum.
  - **Batch 3b** (2026-09-18): migrated `test_idempotent_writes.py` (27/27 pass — also
    replaced a Kùzu-catalog-specific `CALL table_info(...)` introspection call with a
    direct `NODE_COLUMNS` schema check, since there's no SPARQL/RDF equivalent to a
    runtime column catalog) and `test_arc_mechanic_memory.py` (4/4 pass, dropped its
    manual `CREATE NODE/REL TABLE` DDL entirely — real DDL but unnecessary for
    `OxigraphClient`, which needs no create-table step at all).
    Migrating `test_arc_mechanic_memory.py` surfaced **two real, independent bugs** in
    `campy/brain/hippocampus/graph/queries/arc.py`'s production SPARQL, both fixed:
    1. `arc.merge_mechanic`'s `sparql=` had a stray `BIND(NOW() AS ?now)` — `?now` is
       already a declared query parameter (auto-injected via a `VALUES` clause), and
       SPARQL forbids re-binding a variable already in scope in the same group. This was
       a genuine parse error on every real call, and even if it somehow hadn't been,
       `NOW()` would have silently discarded the caller's actual timestamp
       (`arc_mechanics.py` computes and passes a real `now`, unused). Fixed by deleting
       the stray BIND — `?now` already resolves correctly via the parameter injection.
    2. `arc.get_mechanic_failure_modes`'s `RETURN f.name, ..., pol.name, ...` (no
       aliases) gives Kùzu columns literally named `"f.name"`/`"pol.name"`, but
       `RowDict`'s dotted-key lookup falls back to the part *after* the dot when the
       exact key is missing (needed so the same Python read code also works against
       SPARQL's `?name`-style variables, which can't contain dots) — so both `"f.name"`
       and `"pol.name"` collapsed onto the same bare `"name"` SPARQL variable, and every
       recovery policy silently returned its own failure mode's name instead of its own.
       Fixed with explicit aliases (`f_name`/`pol_name`/etc.) in both `cypher=` and
       `sparql=`, matching the B399 pattern already established in `explore.py`. Verified
       the alias change doesn't regress the still-Kùzu-backed callers
       (`test_wiki_projection_arc.py`, `test_arc_world_model_tools_mcp.py`) — 58 tests
       pass across all files touching `arc_mechanics.py`.
    Neither bug had any test coverage until this migration — `arc.py` has no
    SPARQL-conformance test at all (unlike `web.py`/`cli.py`/`explore.py`/`capability.py`
    via `test_b395_sparql.py`, or `backup.py`/.../`temporal_lobe.py` via
    `test_b396_sparql.py`); flagged as a follow-up task (not filed as a numbered card —
    it's a test-infrastructure gap, not a functional defect) since a proper sweep would
    likely surface more of the same defect classes across arc.py's ~150 queries.
  - **Batch 3c** (2026-09-18): 6 more migrated, 4 more exclusions found, 3 more real bugs
    fixed (2 new classes beyond batch 3b's).
    - Migrated: `test_b64_integration.py` (1/1), `test_b219_task_graph_integration.py`
      (1/1, dropped manual TaskGraph/TaskNode DDL entirely), `test_hnsw_hygiene.py` (7/7,
      the one real-Kùzu test used a synthetic 4-dim `HygNode` table incompatible with
      `write_node()`'s schema-driven design — adapted to the real 384-dim `Concept` table,
      only the first 2 dims varying, preserving the exact near/far cosine structure the
      test cares about), `test_ask_eval.py` (11/11), `test_wiki_projection_arc.py` (1/1,
      dropped manual Arc* DDL), `test_arc_world_model_tools_mcp.py` (3/3, same).
    - New exclusions (Kùzu-internals-specific or deliberately dual-engine, same reasoning
      as prior exclusions): `test_manual_checkpoint.py` (tests `KuzuClient`'s own
      checkpoint/durability semantics directly — `OxigraphClient.checkpoint()` is a
      trivial `return True` stub, no comparable internals to test);
      `test_vector_ranking_parity.py` (a *deliberate* Kùzu-HNSW-vs-sqlite-vec ranking
      comparison — migrating it to Oxigraph-only would defeat its purpose; already
      gracefully `skipif`s when kuzu isn't importable); `test_vector_calibration.py`
      (Kùzu `QUERY_VECTOR_INDEX` calibration internals — the equivalent Oxigraph/sqlite-vec
      calibration is already fully covered by `test_vector_store.py`); `test_vector_store.py`
      itself needs no migration — already Oxigraph-native with its own optional
      (`skipif`-guarded) Kùzu parity check.
    - Total permanently-excluded files now 10 (see batch 3 intro for the first 6).
    - **Three more real production bugs found and fixed**, migrating
      `test_wiki_projection_arc.py`:
      1-3. Three more instances of batch 3b's `BIND(NOW() AS ?now)`-over-declared-param
         bug, found by grepping all 11 `BIND(NOW() AS ?now)` occurrences in `arc.py` and
         cross-checking each against its own `params=` tuple: `arc.upsert_artifact`,
         `arc.upsert_run`, `arc.upsert_task` all had it (the other 8 occurrences don't
         declare a `now` param, so those are legitimate). All 3 fixed the same way
         (delete the stray BIND).
      4-5. A new, more severe bug class: `thalamus.wiki_arc_run_wm_summary` and
         `thalamus.wiki_arc_wm_summaries` (in `queries/thalamus.py`) chained ALL of
         `ArcWorldModelSummary`'s properties as REQUIRED (non-`OPTIONAL`) triples in one
         basic graph pattern — but `arc.upsert_wm_summary` (the write side) skips
         asserting a triple entirely for any `None`-valued param (matching Cypher's
         `SET x = NULL` no-op). In Kùzu/LPG, `RETURN s.prop` for a NULL property still
         returns the row (null column); in RDF, a required triple pattern with no
         matching triple drops the WHOLE row. A real-world summary missing even one
         property (e.g. `full_reasoning_cycles_avoided`, absent from this fixture's data)
         silently vanished from every read. Fixed by converting every non-essential
         property to `OPTIONAL { ... }` in both queries.
    - Given the growing count and severity of distinct SPARQL-vs-Cypher defect classes
      found by simply migrating tests (5 in arc.py/thalamus.py alone this session), the
      earlier "arc.py needs a conformance test" follow-up was replaced with a broader
      spawned task covering both bug classes across both files (and flagging that other
      query files — `capability.py`, `basal_ganglia.py`, `explore.py` — already had
      B430-B433 find related divergences, so the required-vs-optional pattern specifically
      may be widespread and needs a real audit, not just a parse-check).
  - **Batch 4** (2026-09-18): the spawned follow-up from batch 3c — sweep `arc.py`/
    `thalamus.py` for more bug-class-1 instances, and do a real (not just grep) audit of
    bug class 2 across both files.
    - **Bug class 1** (`BIND(...AS ?param)` rebinding a variable already declared in that
      query's own `params=` tuple): re-swept with an AST-based extractor (parses every
      `NamedQuery(...)` call in all 21 files under `queries/`, 860 records total) plus a
      non-greedy `BIND\s*\(.*?\bAS\s+\?(\w+)\s*\)` regex (the batch 3b/3c grep used
      `[^)]*` instead of `.*?`, which silently fails to match a `BIND(...)` containing any
      nested function call before its `AS` clause — re-verified the regex against known
      cases before trusting a zero result). **Zero remaining instances, across the whole
      registry, not just these two files** — batches 3b/3c's fixes (`arc.merge_mechanic`,
      `arc.upsert_artifact`, `arc.upsert_run`, `arc.upsert_task`) were a complete sweep
      after all.
    - **Bug class 2** (required-vs-optional divergence): built a candidate-finder (extract
      every non-mutating SELECT's WHERE clause, strip `OPTIONAL { }` blocks, find
      `;`-chained multi-property patterns in what's left) — 68 candidate read queries
      across `arc.py`/`thalamus.py`. Manually cross-referenced each high-signal candidate
      against its write query's real call site (not just the query text — e.g.
      `arc_artifacts.py`'s `_safe_int()`/`_safe_float()` helpers return `None` with no
      default, so any field routed through them is a real nullability risk; a field with a
      non-None default, or set as a SPARQL literal constant, is not). Found and fixed
      **7 queries, 6 real "can go None in real data" bugs + 1 always-broken "never written
      at all" bug**:
      1. `arc.get_entity_centroid` (`centroid_row`/`centroid_col`) — `arc_perceive_state`
         writes these straight from `ent.get("centroid_row")`/`ent.get("centroid_col")`,
         `None` whenever the caller's entity dict doesn't report a centroid that frame. The
         required pattern meant a GridEntity with an unset centroid returned zero rows,
         which `arc_perceive_state`'s own `existing_row is not None` check then misread as
         "entity doesn't exist yet" — silently breaking move detection.
      2. `thalamus.wiki_arc_task_results` (`steps`) — `arc_artifacts.py`'s
         `_extract_task_results()` computes it via `_safe_int(...)`, `None` if the source
         artifact's step-count field is missing/non-numeric.
      3. `thalamus.wiki_arc_events` (`step_index`) — same file's `_extract_events()`, same
         `_safe_int(...)` pattern; `None` for any event not tied to a specific step (e.g. a
         run-level "artifact_ingested" event).
      4. `thalamus.wiki_arc_wm_steps` (`step_index`, `node_count`, `edge_count`,
         `compiled_claim_count` — 4 properties in one query) — same file's
         `_extract_world_model_steps()`, same pattern for all four.
      5. `thalamus.wiki_arc_mechanics` (`terminal_relevance`, `coordinate_relevance`) —
         `arc.merge_mechanic`'s SPARQL writes both as ordinary UNDEF-capable params, same
         as every other property in that query. The one current caller
         (`arc_mechanics.py`'s `publish_mechanic_summary`) happens to floor both through
         its own *locally-defined* `_safe_float(..., default=0.0)` (a different function,
         same name, different default than `arc_artifacts.py`'s) so this field is not
         actually reachable as `None` through today's only caller — fixed anyway since the
         query's own contract already supports `None` structurally and nothing prevents a
         future/second caller from relying on that, but documented in the test as
         defensive/structural, not a currently-live symptom (unlike 1-4 and 6 below, which
         are demonstrably live with the current single caller).
      6. `arc.get_goal_evidence` (`condition_type`) — **the most severe finding**: grepped
         every writer of `VictoryCondition` in the repo and found **zero** ever assert
         `campy:condition_type` (`arc.merge_victory_condition_confidence`, the only writer,
         sets `task_id`/`confidence`/`created_at`/`last_updated` only; the column exists in
         `schema.py`'s DDL and is read back in `arc_get_goal_evidence`, but nothing ever
         writes it). This isn't a sometimes-null case — the required triple pattern meant
         `arc.get_goal_evidence`, and the live, registered `arc_get_goal_evidence` MCP
         tool built on it, returned **zero goals for every task, unconditionally, since the
         query was written**. Same severity class as B430's causal-path finding.
      - Lower-confidence candidates checked and confirmed SAFE (not fixed, no bug):
        `arc.get_max_snapshot_step`, `arc.count_active_hypotheses`,
        `arc.get_distinct_action_ids`, `arc.get_action_facts_summary` (already correctly
        OPTIONAL), `arc.query_causal_chain`/`query_causal_chain_with_goal` (`confidence` —
        floored via `_safe_float(...,0.0)` at the `arc_update_goal_confidence` call site),
        `arc.find_live_rule`/`get_rules_for_action`/`get_transferred_rules` (`confidence`/
        `falsified` are SPARQL literal constants at `arc.create_rule`, never params),
        `arc.get_entity_mechanics`/`recall_mechanics`/`recall_mechanics_with_action_set`
        (all safe per the `arc.merge_mechanic` analysis above — `name`/`confidence`/
        `source_task_ids`/`summary`/`updated_at` are all always-defaulted or derived,
        never bare `None` params).
      - **~55 of the 68 candidates were not individually triaged** (mostly thalamus.py's
        non-ARC core entities — Procedure, Lesson, Concept, Decision, WorkArtifact, the
        Quest/Plan family, Workspace, ActionItem — plus the remaining arc.py entity/rule/
        mechanic read queries not listed above). Real audit of the checked subset above
        found a genuine bug in the majority of arc_artifacts.py-backed queries checked (4
        of 5) and the single most-severe finding in the whole batch, so the unchecked
        remainder is a reasonable place to look next, not a "probably fine, skip it"
        judgment — filed as follow-up, not swept under a false all-clear.
    - **Regression tests**: `tests/test_b427_null_optional_sparql.py`, 8 tests, one per
      fix above (plus one documenting `arc_perceive_state`'s real move-detection recovery
      for finding 1, and one documenting today's actual floored-not-None caller behavior
      for finding 5) — each asserts a non-empty, correct row with the specific realistic
      partial-data condition that used to make it vanish, not just "the query runs" (same
      standard as B412). Verified red-before-fix / green-after-fix for every one by
      stashing the query-file changes and re-running (not just trusting the diff).
    - **New tool**: `scripts/check_sparql_required_vs_written.py` — flags a property
      required (non-`OPTIONAL`) in some read query's SPARQL that is never once the target
      of an `INSERT` triple in ANY registered mutating query, anywhere (the exact shape of
      finding 6 above). Deliberately narrower than a full nullability check (write queries
      almost all route non-PK properties through the same UNDEF-on-None pattern, so
      "sometimes unwritten" is structurally true almost everywhere and would drown a real
      signal in noise) — this only flags the unambiguous "never written at all" case, zero
      guessing required. Report-only, **not wired into CI**: has two known false-positive
      classes documented in its own docstring (node types created via
      `OxigraphClient.write_node()`/`write_edge()` directly from Python rather than a
      NamedQuery's `sparql=` INSERT block, and mutating queries dispatched through
      `gateway.py`'s name-matched `_handle_oxigraph_handler()` instead of generic SPARQL —
      both confirmed by manually checking `GlobalConstraint`/`AgentWorker` and
      `arc.link_mechanic_action_pattern` respectively, all real writers, just invisible to
      a SPARQL-text-only scanner). Currently reports 19 (type, property) findings across
      the full registry after collapsing to a registry-wide (not per-type) written-property
      set; not further triaged this batch beyond the 3 ArcMechanic edge findings (confirmed
      false positives, handler-dispatched) — remaining findings need the same one-by-one
      check before acting on them.
