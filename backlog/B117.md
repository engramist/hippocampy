# B117 - ARC Typed Decision Packets Instead of Ad Hoc Prompt Sections

State: complete
Completed: 2026-04-01
Validation: 94/94 tests passed (orchestrator/solver + B109-B115 regression suites)

Plan: backlog/plans/B-117-arc-typed-decision-packets.md

### Problem

Even after prompt cleanup, the ARC harness still assembles decisions through loosely-coupled text
sections. That makes compaction, guard logic, and future multimodal context harder than it needs
to be.

### What This Card Builds

A typed decision-packet layer for ARC:

- represent observation, action facts, path hypotheses, solve context, chunk guidance, and
  instruction as typed packet/block objects
- render prompt text from those typed packets
- make compaction and guard logic operate on structured data first

### Depends on

- B110
- B114
- B116

## Files to Create/Modify

- `agents/arc3/orchestrator.py`
- `agents/arc3/hypothesis.py`
- `benchmarks/arc3/PROMPT_STRATEGY.md`
- `tests/test_arc3_orchestrator.py`

## Acceptance Criteria

1. The harness builds a typed decision packet before rendering the final prompt.
2. Packet/block types cover at least:
   - observation
   - action facts
   - path hypotheses
   - solve context
   - chunk guidance
   - instruction
3. Prompt rendering is derived from those packets rather than ad hoc string assembly.
4. Tests cover packet construction and rendering.
5. The change preserves existing debug/export visibility.

## Outcome

The ARC harness gets a structured decision representation that makes future compaction, guards, and
multimodal inputs easier to implement.
