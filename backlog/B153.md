# B153: ARC Level-Aware Prompt Restructure — Show Prior Level Insights

**Status**: ready
**Assigned**: TBD
**Depends on**: B150, B151, B157
**Complexity**: Medium (2–4 hours)
**Priority**: P0 (directly impacts LLM reasoning quality)
**Related**: F2/P1/P2/P3 from codereviewOpus2.md
**Plan**: backlog/plans/B-153-arc-prompt-restructure.md
**Phase**: Serves all levels — adapts prompt based on level and knowledge state

## Problem Statement

The current prompt asks the LLM to "choose the next valid action based on observed effects" and fills the context window with 15 structured sections. This frames ARC as a navigation game rather than a multi-level progression where early levels teach the rules.

With qwen2.5:7b, the average prompt is ~1018 tokens. Most of that budget is spent on exploration metadata that describes the wrong problem. The prompt never shows insights from prior levels, even when the agent has already solved levels and knows what the game is about.

### Interactive Game Model

The prompt must adapt to the level and knowledge state:
- **Level 1** (no prior knowledge): Exploration mode — focused on discovering what actions do
- **Level 2+** (prior levels solved): Rule-application mode — show game rule hypothesis, action semantics, prior level patterns. The agent should apply what it learned.
- **Level N with high confidence**: Execution mode — minimal prompt, just "apply this rule". No exploration metadata needed.
- **Any level, low confidence**: Navigation/fallback mode — existing 15-section prompt with ActionFacts, path hypotheses, etc.

## What This Card Builds

1. **New `ARC_LEVEL_INSIGHT_TEMPLATE`** — for levels 2+ when prior levels have been solved:
   ```
   You are playing level {current_level} of {total_levels} in an ARC game.
   From prior levels, you've learned:
   - Action effects: {action_semantics}
   - Game rule: {rule_hypothesis}
   - Confidence: {confidence}
   Apply this knowledge to solve the current level.
   ```

2. **New `ARC_EXPLORATION_TEMPLATE`** — for level 1 (no prior knowledge):
   ```
   You are playing level 1 of an ARC game. You need to discover what each
   action does by experimenting. Try each action and observe the effect.
   ```

3. **Compact grid rendering** — `render_grid_compact(grid)`:
   - Maps each color to a single character (0→'.', 1→'#', 2→'@', etc.)
   - 64x64 grid rendered compactly for LLM consumption
   - Shows current grid state

4. **Four prompt modes** via level and knowledge state:
   - `"exploration"` (NEW) — Level 1, no prior knowledge. Encourage experimentation. <400 tokens.
   - `"rule_application"` (NEW) — Level 2+, game rule hypothesis available. Show insights from prior levels. <500 tokens.
   - `"execution"` (NEW) — Any level with high-confidence verified rule. Minimal: "apply this rule". <200 tokens.
   - `"navigation"` (EXISTING) — Fallback when confidence is low. Uses existing 15-block layout with ActionFacts, exploration metadata. Actively maintained.

5. **Mode auto-selection** based on:
   - `_current_level` (from B157)
   - `_solved_levels` count
   - Rule hypothesis confidence (from B151/B152)

## Files to Create/Modify

- `agents/arc3/prompts.py` — Add ARC_LEVEL_INSIGHT_TEMPLATE, ARC_EXPLORATION_TEMPLATE
- `agents/arc3/orchestrator.py` — Add `render_grid_compact()`, modify `build_action_packet()` for four-mode auto-selection, add level-aware prompt building
- `tests/test_b153_prompt_restructure.py` — NEW: test compact rendering, prompt size, mode auto-selection

## Ecosystem / Layer Compliance

- Layer: Shared Knowledge Assets (prompts) + Agent Runtime (orchestrator rendering)
- No cross-layer violations. No imports from `mcp_engine/`.
- Prompt changes are additive — existing "navigation" mode preserved.
- Complies with `docs/arc-harness-rules.md` and `docs/ecosystem-rules.md`.

## Acceptance Criteria

- [ ] Exploration-mode prompt is under 400 tokens (level 1)
- [ ] Rule-application prompt shows prior level insights and is under 500 tokens
- [ ] Execution-mode prompt is under 200 tokens (high-confidence rule)
- [ ] Compact grid rendering works for 64x64 grids with 0-15 colors
- [ ] Navigation mode preserved and actively maintained as fallback
- [ ] Prompt mode auto-selects based on level number and knowledge state
- [ ] All tests pass, no regressions

## Outcome Statement

After this card, the LLM receives the right prompt for the right level. Level 1 gets an exploration prompt. Level 2+ gets prior level insights. High-confidence levels get a minimal execution prompt. Low-confidence situations get the existing navigation prompt. Knowledge transfers forward through the prompts.
