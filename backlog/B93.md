# B93 - ARC Action Fact Promotion During Explore Phase

State: complete

Plan: backlog/plans/B-93-arc-action-fact-promotion.md

### B93 · ARC Action Fact Promotion During Explore Phase

**Problem:** The new explore phase is more deterministic: test an action, observe the result, repeat if needed, then move on. But we still do not have an explicit artifact for the operator fact the explore phase discovered, such as "ACTION5 causes localized_change in this puzzle family" or "ACTION6 is a no-op from this state." Without that promotion step, SideQuests cannot clearly separate action facts from generic turn text, and the system risks treating those low-level facts as if they were the higher-level hypotheses themselves.

**What this card builds:** A small fact-promotion layer for ARC explore steps. This card is specifically about **action facts**, not path hypotheses. When repeated evidence supports an operator fact, SideQuests should save a concise action fact artifact such as:
- `ACTION5 -> localized_change at lower-right cluster`
- `ACTION6 -> no visible change from this state`
- `ACTION7 -> returns to prior state / loop`

**Scope:**
- Define when an explore-phase observation is strong enough to become an action fact
- Distinguish:
  - deterministic effect facts
  - blocked/no-op facts
  - low-value but repeatable facts
- Promote the fact into a compact SideQuests artifact that later prompts can retrieve
- Keep action facts separate from:
  - valuable actions
  - path hypotheses
  - final strategy conclusions

**Depends on:** B88 (hypothesis engine), B92 (write trace)

## Files to Create/Modify

- `agents/arc3/hypothesis.py`
- `agents/arc3/orchestrator.py`
- `docs/ARCHITECTURE.md`
- `tests/test_arc3_hypothesis.py`

## Acceptance Criteria

1. Repeated deterministic action evidence can be promoted into a compact action fact
2. The promotion rule distinguishes consistent effect from valuable effect
3. No-op and loop facts can also be promoted when they are repeatable
4. Prompt retrieval can surface these promoted facts in later steps
5. Tests cover:
   - deterministic effect fact promotion
   - repeatable no-op fact promotion
   - low-value effect is not mislabeled as success

## Outcome

The explore phase becomes knowledge-building instead of just action sampling. SideQuests can retain concise operator facts that help later path reasoning without pretending weak actions are good actions or treating operator facts as full hypotheses.

## Validation Note (2026-03-31)

- Implemented files: `agents/arc3/hypothesis.py`, `agents/arc3/orchestrator.py`, `tests/test_arc3_action_fact_promotion.py`
- Tests run:
  - `.venv/bin/pytest -q tests/test_arc3_action_fact_promotion.py tests/test_arc3_hypothesis.py tests/test_arc3_orchestrator.py`
- Result: `93 passed, 1 warning`
- Regression checks: low-value but repeatable actions are no longer promoted as successful deterministic effects
