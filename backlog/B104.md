# B104 - ARC Meta-Harness: Structured Experience Store for Harness Evolution

State: complete

Plan: backlog/plans/B-104-arc-meta-harness-experience-store.md

### Problem

The ARC harness now has a meaningful inner loop, but the outer loop is still manual. We improve the
harness by reading logs, comparing runs, and making one-off edits by hand. That does not scale into
a true meta-harness.

The Meta-Harness paper points in the right direction: optimize the harness, not just the base model
prompt. For SideQuests, the opportunity is to make the outer loop structured and queryable instead
of filesystem-first.

### What This Card Builds

An explicit experience-store contract for ARC harness evolution:

- define the graph artifacts for a harness candidate, evaluation run, puzzle trace, score summary,
  and mutation rationale
- separate inner-loop puzzle memory from outer-loop harness-evolution memory
- make prior harness runs queryable by outcome, budget, failure mode, and lineage
- keep this at the SideQuests/benchmark layer; do not change the base ARC puzzle protocol

### Depends on

- B89
- B90
- B95

## Files to Create/Modify

- `docs/ARCHITECTURE.md`
- `benchmarks/arc3/README.md`
- `docs/retrieval-contract.md`
- `backlog/masterBacklogTracker.md`

## Acceptance Criteria

1. The architecture docs explicitly distinguish ARC Harness, Meta-Harness, and SideQuests.
2. The experience-store contract defines the minimum entities and edges needed for harness
   evolution:
   - harness candidate
   - evaluation run
   - puzzle trace
   - score/budget summary
   - parent candidate / mutation lineage
3. The contract explains which data belongs to the inner loop versus the outer loop.
4. The docs specify at least 5 concrete retrieval questions the meta-harness should be able to ask.
5. The contract remains graph-native and does not require SideQuests to become the harness itself.

## Outcome

SideQuests becomes the structured experience layer for harness evolution, not just the memory layer
inside one puzzle run.

## Validation Note (2026-03-31)

- Implemented files: `docs/ARCHITECTURE.md`, `benchmarks/arc3/README.md`, `docs/retrieval-contract.md`
- Tests run:
  - `.venv/bin/pytest -q tests/test_arc3_orchestrator.py tests/test_arc3_hypothesis.py`
- Result: `88 passed, 1 warning`
- Regression checks: doc-only card; verified the ARC Harness / Meta-Harness / SideQuests split, required experience entities, and required retrieval questions are now documented
