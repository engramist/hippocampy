# B114 - ARC Mental Sandbox Loop Before Real Move Execution

State: complete
Completed: 2026-04-01
Validation: 85/85 tests passed (test_b114_mental_sandbox.py + solver/orchestrator + B109/B112/B113 regression suites)

Plan: backlog/plans/B-114-arc-mental-sandbox-loop.md

### Problem

The ARC harness still mostly operates as:

- build prompt
- choose action
- spend a real environment step

That makes each move too expensive and too brittle. The harness needs a short internal reasoning
loop where it can inspect its own hypotheses, chunk guidance, and retrieved evidence before
committing to a real ARC action.

### What This Card Builds

A bounded "mental sandbox" loop for the ARC harness:

- allow a small number of internal reasoning/tool iterations before executing a real move
- keep the sandbox tools harness-local and non-world-mutating
- use the sandbox to test chunk guidance, compare action facts, and resolve ambiguity before a
  real environment step is spent

### Depends on

- B88
- B94
- B109
- B113

## Files to Create/Modify

- `agents/arc3/orchestrator.py`
- `agents/arc3/solver.py`
- `benchmarks/arc3/PROMPT_STRATEGY.md`
- `tests/test_arc3_orchestrator.py`
- `tests/test_arc3_solver.py`

## Acceptance Criteria

1. The harness supports a bounded pre-action internal reasoning loop.
2. Sandbox iterations do not mutate the ARC environment.
3. Sandbox tools can compare action facts, path hypotheses, and active chunk guidance.
4. The final chosen action records whether sandbox reasoning changed the decision.
5. Tests cover:
   - sandbox loop executes without spending a real move
   - sandbox can revise an initial candidate action
   - sandbox stops within a bounded iteration budget
6. A live smoke run shows the harness using the sandbox without breaking export/debug output.

## Outcome

The ARC harness gains a "think before acting" layer so real puzzle moves are spent more
deliberately.
