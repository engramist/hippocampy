# B162: Front-Load Grid Analysis Before First Action

**Status**: ready
**Assigned**: TBD
**Depends on**: None
**Complexity**: Small (1–2 hours)
**Priority**: P1 (HIGH — the agent wastes early steps acting without understanding the grid)
**Plan**: backlog/plans/B-162-frontload-grid-analysis.md

## Problem Statement

The current bootstrap phase does: perceive (structure_ingest + entity_discovery) → plan (2-step fallback) → immediately act. The agent never pauses to analyze the 64x64 grid's spatial structure before choosing its first action.

In the live smoke, the first action (ACTION1) was chosen by the mental sandbox with zero understanding of:
- Where objects are on the grid
- What spatial relationships exist between colored regions
- Whether the puzzle looks like a movement task, a painting task, or a pattern-matching task

The `grid_characteristic_summary` function exists (`agents/arc3/grid_analysis.py`) but is only used during level transitions, not at bootstrap.

## What This Card Does

1. **Runs `grid_characteristic_summary()` on the initial grid** during the bootstrap phase (before the first `act` call).
2. **Injects the analysis into the first prompt** as a `GRID_ANALYSIS` content block so the LLM can make an informed first action choice.
3. **Uses the analysis to pre-populate object role hints** (e.g., "small isolated region = likely player or goal") into the SolveEngine before step 1.

## Ecosystem Layer

- `agents/arc3` (ARC agent layer)

## Files to Create/Modify

- `agents/arc3/orchestrator.py` — call `grid_characteristic_summary()` in bootstrap, inject into first prompt
- `tests/test_b162_frontload_analysis.py` — test that grid analysis is present in step-0 prompt

## Acceptance Criteria

- [ ] The `build_action_packet()` for step 0 includes a `GRID_ANALYSIS` block with spatial summary
- [ ] The grid analysis identifies distinct colored regions and their approximate positions
- [ ] A trace event `"bootstrap_grid_analysis"` is emitted with the summary
- [ ] All existing ARC tests pass (`pytest tests/test_arc3_*.py -q`)

## Outcome

The agent's first action is informed by actual grid structure instead of blind exploration.
