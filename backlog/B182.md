# B182: Enhanced ABHarness Metrics — Four Quality Dimensions

**Status**: ready
**Assigned**: TBD
**Depends on**: B180 (cost data), B181 (judge verdicts), B185 (failure classes)
**Complexity**: Small (1-2 hours)
**Priority**: P1 (aggregates all new signals into actionable dashboard)
**Plan**: backlog/plans/B-182-enhanced-metrics.md

## Problem Statement

The ABHarness computes basic metrics: solve_rate, steps_to_solve, token_efficiency, repeated_mistakes. It's missing the four quality dimensions that make eval actionable: effectiveness (how well), efficiency (at what cost), robustness (under pressure), and safety/alignment (within bounds).

With B180 (cost), B181 (judge), and B185 (failure taxonomy) providing the raw signals, this card aggregates them into structured reporting.

## Root Causes

1. `_compute_metrics` in `ab_harness.py` only covers basic counts
2. No cost-per-solve or efficiency dimension
3. No robustness breakdown (crash rate vs timeout rate vs stuck rate)
4. No near-miss reporting (judge scores for failed puzzles)

## What This Card Does

Extend `_compute_metrics` in `ab_harness.py` to report four dimensions:

1. **Effectiveness**: solve_rate, judge_score_avg, near_miss_rate (judge >= 3 but not solved), judge_score_by_archetype
2. **Efficiency**: avg_steps_per_solve, avg_tokens_per_solve, avg_cost_per_solve, tokens_per_step, cost_per_step
3. **Robustness**: crash_rate, timeout_rate, budget_exceeded_rate, strategy_exhausted_rate, loop_stuck_rate, retry_rate
4. **Safety/Alignment**: invalid_action_rate, dissonance_trigger_rate, hallucination_rate (from judge)

Also extend `ABComparison._compare_results` with delta computation for all new metrics.

## Ecosystem Layer

- `benchmarks/ab_harness.py` (Eval Infra)

**Cross-layer compliance**: No cross-layer violations. Pure eval infrastructure.

## Files to Create/Modify

- **MODIFY** `benchmarks/ab_harness.py` — Extend `_compute_metrics` and `_compare_results` with four quality dimensions
- **CREATE** `tests/test_b182_enhanced_metrics.py` — Tests for each dimension computation, delta comparison
- No changes to `docs/tool-catalog.md`

## Acceptance Criteria

- [ ] All four dimensions reported in ABHarness output
- [ ] Each dimension has at least 3 named metrics
- [ ] ABComparison shows delta for all metrics (SIDEQUESTS vs BASELINE)
- [ ] Graceful handling when B180/B181/B185 data is missing (metrics show "N/A", no crash)
- [ ] All existing ARC tests pass

## Outcome

ABHarness reports provide a complete quality picture across four dimensions. Strategy changes can be evaluated on effectiveness AND efficiency AND robustness, not just solve rate.
