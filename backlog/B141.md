# B141: Enforce No-Progress Bail-Out Escalation in Orchestrator Loop

**Status**: complete ✅ DONE (2026-04-01)
**Assigned**: TBD  
**Depends on**: None  
**Complexity**: Small (1–2 hours)  
**Priority**: P0 (safety net — prevents wasting entire action budget on zero-reward loops)  
**Related**: B139 (reopened acceptance criteria), B124 (task ledger)  
**Plan**: backlog/plans/B-141-no-progress-bailout.md

## Problem Statement

`_consecutive_no_progress_steps` increments correctly but **nothing acts on it**. The loop detector at `orchestrator.py` ~L758 triggers at `≥ 2` steps but only queries memory ("Am I looping?") — it never forces a replan, blocks the current action, or abandons the chunk.

Evidence from the `gemma4:e4b` live smoke (`live_gemma4_e4b_timeout_1775221087`):
- 15 consecutive steps with `reward = 0.0`
- Guard status escalated to "warned" but never "blocked"
- `no_progress_step_count = 15` — the counter worked, nothing consumed it
- Same ACTION6 dispatched every step despite zero reward

## What This Card Builds

A **three-tier escalation** wired into the existing `_consecutive_no_progress_steps` counter:

| Threshold | Escalation | Effect |
|-----------|-----------|--------|
| `≥ 3` | **Force replan** | Set `dissonance_detected = True`, triggering solver re-evaluation of the active chunk |
| `≥ 5` | **Block current action** | Promote guard from "warned" → "blocked" for the current action type; force selection of a different action |
| `≥ 8` | **Abandon chunk** | Mark active chunk as `failed`, fall back to pure exploration with a different action set |

Each tier resets `_consecutive_no_progress_steps` on trigger so the escalation ladder restarts if the new strategy also stalls.

## Files to Create/Modify

- `agents/arc3/orchestrator.py` — wire escalation tiers into the existing `_should_check_loop` block (~L758) and the step-result recording (~L2554)
- `tests/test_b141_no_progress_bailout.py` — regression tests for each escalation tier

## Ecosystem / Layer Compliance

- Layer: ARC agent orchestration policy (`agents/arc3/orchestrator.py`)
- No cross-layer import violations
- Pure control-flow change — no LLM reasoning or prompt modifications
- Complies with `docs/arc-harness-rules.md` and `docs/ecosystem-rules.md`

## Acceptance Criteria

- [ ] At 3 consecutive zero-reward steps, `dissonance_detected` flips to `True` and solver re-evaluates
- [ ] At 5 consecutive zero-reward steps, the current action type is blocked and a different action is chosen
- [ ] At 8 consecutive zero-reward steps, the active chunk is marked `failed` and a new chunk is activated
- [ ] Each escalation tier resets the counter
- [ ] Escalation events are visible in `agent_execution_trace.json` with reason codes
- [ ] Existing test suites pass with no regressions
