# B173: Persist GameRuleHypothesis to KuzuDB via Hypothesis Nodes

**Status**: ready
**Assigned**: TBD
**Depends on**: B170 (hypothesis persistence — GameRuleHypothesis IS a hypothesis)
**Complexity**: Small (1-2 hours)
**Priority**: P1 (dual in-memory store — solver AND orchestrator both hold copies)
**Plan**: backlog/plans/B-173-game-rule-hypothesis-kuzu.md

## Problem Statement

`GameRuleHypothesis` objects are stored in TWO separate in-memory locations:

1. `SolveEngine._game_rule_hypotheses` (`solver.py:1766`) — a `List[GameRuleHypothesis]`
2. `ARCOrchestrator._game_rule_hypothesis` (`orchestrator.py`) — a single `GameRuleHypothesis`

Neither writes to KuzuDB despite `Hypothesis` nodes existing in the schema. These are literally hypotheses about game rules — they should be instances of the `Hypothesis` node type.

## Root Causes

1. `GameRuleHypothesis` was added in B151 as a solver-internal dataclass
2. The orchestrator holds its own copy for prompt construction
3. Neither was connected to the existing Hypothesis schema

## What This Card Does

1. **Map `GameRuleHypothesis` to `Hypothesis` nodes**: Use `category = "game_rule"` to distinguish from other hypothesis types
2. **Persist to KuzuDB**: When solver creates/updates game rule hypotheses, write to KuzuDB
3. **Eliminate dual store**: Orchestrator reads from KuzuDB (or the solver's cache) instead of maintaining its own copy
4. **Wire `GENERALIZES` relationship**: When a game rule hypothesis generalizes across levels (e.g. "ACTION5 = interact works on all levels"), create `GENERALIZES` edges

## Ecosystem Layer

- `agents/arc3/solver.py` (Agent Runtime — game rule hypothesis writer)
- `agents/arc3/orchestrator.py` (Agent Runtime — remove duplicate store, read from solver/KuzuDB)

**Cross-layer compliance**: No cross-layer violations.

## Files to Create/Modify

- **MODIFY** `agents/arc3/solver.py` — Persist GameRuleHypothesis to KuzuDB as Hypothesis nodes with `category = "game_rule"`
- **MODIFY** `agents/arc3/orchestrator.py` — Remove `_game_rule_hypothesis` instance variable; read from `solve_engine._game_rule_hypotheses` or KuzuDB
- **CREATE** `tests/test_b173_game_rule_hypothesis_persistence.py` — Tests for persistence and dual-store elimination
- No changes to `mcp_engine/schema.py` (uses existing Hypothesis node type)
- No changes to `docs/tool-catalog.md`

## Acceptance Criteria

- [ ] GameRuleHypothesis objects are persisted as Hypothesis nodes with `category = "game_rule"`
- [ ] Orchestrator no longer maintains its own `_game_rule_hypothesis` — reads from solver or KuzuDB
- [ ] Only one copy of each game rule hypothesis exists (no dual store)
- [ ] `GENERALIZES` edges created when a rule applies across levels
- [ ] When KuzuDB unavailable, falls back to in-memory-only
- [ ] All existing ARC tests pass

## Outcome

Game rule hypotheses are no longer duplicated across two Python objects. They're persisted as Hypothesis nodes in KuzuDB, enabling graph-based reasoning about game mechanics.
