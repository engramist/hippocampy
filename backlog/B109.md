# B109 - Make Directional Chunks Constrain ARC Action Choice

State: complete
Completed: 2026-04-01
Validation: 73/73 tests passed (test_b109_chunk_enforcement.py + core solver/orchestrator suites)

Plan: backlog/plans/B-109-arc-directional-chunk-enforcement.md

### Problem

The solve layer now often reaches a coherent directional plan:

- player detected
- goal detected
- directional chunk graduated

But live runs still fail to turn that into actual directed movement. The chunk appears in the final
strategy summary, yet the action policy continues to spread across exploratory or locally-ranked
actions, and `chunk_history` remains effectively empty. The harness can describe the intended plan
better than it can follow it.

### What This Card Builds

A stronger execution contract between `PlanChunker` and action selection:

- directional chunks should constrain the candidate action set more aggressively
- policy overrides should be explainable and rare once a directional chunk is active
- chunk execution should produce explicit chunk-step history so we can see whether the plan was
  followed, overridden, or abandoned
- if the chunk repeatedly fails to produce progress, dissonance should fire and trigger replanning

### Depends on

- B95
- B101
- B103

## Files to Create/Modify

- `agents/arc3/solver.py`
- `agents/arc3/orchestrator.py`
- `docs/ARCHITECTURE.md`
- `tests/test_arc3_solver.py`
- `tests/test_arc3_orchestrator.py`

## Acceptance Criteria

1. When a directional chunk is active, action selection is materially constrained by that chunk.
2. The export/debug output records chunk-step execution history rather than only a final chunk
   summary.
3. Policy overrides against an active directional chunk are explicit and bounded.
4. Repeated zero-progress directional chunk execution can trigger dissonance/replanning.
5. Tests cover:
   - directional chunk constrains action choice
   - override path is explicit
   - failed chunk execution triggers replanning signal or dissonance path

## Outcome

The ARC harness stops merely naming a directional plan and starts actually behaving like it has one.
