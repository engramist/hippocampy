# B287 - Principled Score Fusion in current_truth

State: complete -- verified via git log 2026-08-02 (feat: implement B285 HNSW / B287 RRF b00b71e); card file was stale
Priority: P3

Plan: backlog/plans/B-287-score-fusion.md

### Problem

`current_truth` merges results from four signal sources with incompatible scales and no normalization:

- Vector scores per table (nonlinear distance transform — true cosine after B279)
- Lexical exact matches (hardcoded `score: 1.0` — always beats everything)
- `pathway_strength` (unbounded, grows with Hebbian reinforcement)
- Outcome signals / valence boosts

The final ranking is therefore ad hoc: a lexical substring hit on a throwaway message outranks a high-confidence reinforced Decision; pathway_strength's unbounded scale can dominate or be dominated depending on graph age. The ranking semantics are undocumented and will shift again whenever any component's scale changes (as B279 does).

### What This Card Does

1. Documents the current fusion behavior precisely (read the ranking code in `current_truth` and write down what actually wins).
2. Replaces ad-hoc fusion with **Reciprocal Rank Fusion (RRF)** across the source lists (vector per-table, lexical), then applies bounded multiplicative adjustments for pathway_strength and outcome valence.
3. Encodes the ranking contract in a fixture test so future scale changes break loudly.

### Dependencies

- B279 (calibration) MUST land first — fusion should be built on true cosine scores.

### Files to Modify

- `campy/brain/thalamus/tools/__init__.py` — `current_truth` ranking section (after the vector + lexical collection, ~lines 1190-1320; read the whole function first)
- `tests/test_score_fusion.py` — NEW

### Acceptance Criteria

- [ ] Fusion uses RRF: `rrf_score(item) = Σ_sources 1/(k + rank_in_source)` with k=60, sources = each table's vector result list + the lexical list
- [ ] Pathway strength applies as a bounded multiplier: `× (1 + min(pathway_strength, 5.0)/10)` (max 1.5×) — exact bounds tunable, but MUST be bounded
- [ ] Lexical exact hits are boosted, not absolute: they enter RRF as a ranked list, not `score=1.0`
- [ ] Response shape unchanged; each result gains a `ranking_signals` debug dict only if `params.debug_ranking` is true
- [ ] Fixture test: given controlled inputs (strong vector match vs lexical-only hit vs high-pathway weak match), assert the documented ranking order
- [ ] `pytest tests/test_score_fusion.py tests/test_web.py -q` passes

### Outcome

Recall ranking has documented, tested semantics that survive component scale changes, instead of emergent behavior from mixing incompatible scales.
