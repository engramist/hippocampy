# B375 - Pre-Warmed Selective Activation for Anticipatory Retrieval (Cocktail Party Engine)

State: complete
Priority: P1
Branch: b375-warm-frontier-completion
PR Target: main

Plan: backlog/plans/B-375-prewarmed-selective-activation.md

### Completion Notes (2026-09-18)

All 4 concrete gaps closed, on top of the pre-existing B91 foundation
(`warm_frontier.py`) and `current_truth`'s partial integration:

1. **Dense-supernode safeguard** — `_get_artifact_neighbors` in
   `campy/brain/temporal_lobe/warm_frontier.py` now caps a hub node's
   neighbors to the top `SUPERNODE_TOP_N` (default 5) by
   `pathway_strength` once its degree exceeds `SUPERNODE_DEGREE_THRESHOLD`
   (default 50). Covered by `test_supernode_safeguard_caps_neighbors`,
   `test_no_safeguard_below_threshold`, and a config-override test in
   `tests/test_warm_frontier.py`.
2. **`bundle_compiler.py` retrieval integration** — `_stage_semantic_context`
   now looks up `get_warm_nodes(db, session_id)` and shrinks a warm node's
   effective distance via the card's `(1.0 + warm_score * 0.35)` multiplier
   before the top-N cut, threading `node_id` through the 16 hand-written +
   dynamically-generated `thalamus.bundle_semantic_*` NamedQueries (and the
   Oxigraph Python-handler path in `gateway.py`) to make the lookup
   possible. Covered by 3 new real-Kùzu tests in
   `tests/test_bundle_compiler_stages.py` (promotion, cold-start-safe
   fallback, and a baseline sanity check).
   - **Bonus fix, found while wiring this in**: `current_truth`'s own
     advertised warm-boost was dead code — `_apply_fusion_adjustments`
     never received `warm_nodes` at all, so the multiplier only ever
     existed in a debug-only output field. Fixed by threading `warm_nodes`
     through properly; covered by 3 new tests in `tests/test_score_fusion.py`.
3. **Config-driven tuning** — `[retrieval.warm_frontier]` in `campy.toml`
   (documented, commented-out by default) and `_DEFAULT_CONFIG` in
   `campy/brain/brainstem/config.py` now expose `max_warm_nodes`,
   `similarity_weight`, `hops_decay`, `min_activation`,
   `supernode_degree_threshold`, and `supernode_top_n` — all previously
   hardcoded module constants in `warm_frontier.py`. Defaults are
   byte-identical to the prior hardcoded values. Covered by
   `tests/test_config.py` and a config-override test in
   `tests/test_warm_frontier.py`.
4. **Step 4b associative trigger binding** — `check_associative_triggers`
   in `step4b_associative.py` now pre-activates a matched Lesson/Procedure
   into the session's warm frontier via a new `activate_warm_node()`
   helper in `warm_frontier.py`, on every match above the similarity
   threshold (independent of whether a new `trigger_pattern` was also
   bound). This required adding `Lesson`/`Procedure` to the `"warmable"`
   table tag in `campy/brain/hippocampus/table_registry.py` (they lacked
   `warm_link_*`/`warm_get_*`/`warm_neighbor_*` NamedQueries entirely
   before), which grew `TEMPORAL_LOBE_QUERIES` from 612 to 1000 — all
   validated as syntactically-valid SPARQL against a real pyoxigraph store
   in the updated `tests/test_b396_sparql.py` count assertions. Covered by
   3 new tests in `tests/test_step4b_associative.py`.

Full suite: 2780 passed, 35 skipped, 2 xfailed — the only failures are the
pre-existing, unrelated `test_packaging_installed_mode.py` trio. All three
guard scripts (`check_cypher_ratchet.py`, `check_schema_conformance.py`,
`check_principal_ratchet.py`) pass with no new violations.

### Execution & Branch Policy

> **MANDATORY:** All work must be implemented on the feature branch `feat/b375-prewarmed-selective-activation`. Direct commits to `main` are forbidden. Land changes only via Pull Request to `main` with accompanying test verification evidence.

### Problem

Currently, Campy retrieval is purely reactive: an agent or tool must explicitly invoke `current_truth`, `compile_context`, or `ask` to surface memories. This introduces two limitations:
1. **Tool-call latency:** Waiting for an explicit MCP retrieval query pauses agent execution while the graph is scanned and embeddings are evaluated.
2. **Agent amnesia blind spot:** If an agent fails to recognize that a task depends on a decision made 3 weeks ago in a different session, it will not call retrieval at all, repeating mistakes or contradicting established constraints.

In human cognitive architecture (and as claimed in Campy's patent disclosure regarding the **Cocktail Party Effect**), memory is not a passive warehouse. The brain maintains an active "warm frontier": continuous, passive sensory intake (hearing background chatter, seeing file paths or error codes) pre-activates relevant graph neighborhoods *before* conscious recall is requested.

### Biological Alignment: Thalamic Priming & Sensory Pre-Fetching

In biological cognition (as highlighted in Nate B. Jones' analysis of local vs. cloud prosumer architectures), the brain does not wait for an explicit retrieval instruction to search memory. Instead, it uses **Priming**: passive sensory intake (hearing a word, seeing an error code, observing a modified file) immediately triggers subconscious pattern-matching along neural pathways. By the time conscious attention focuses on the object, the relevant knowledge neighborhood is already active.

### What it does (scope)

This card builds the **Anticipatory Pre-Activation Engine**, transforming retrieval from a purely reactive pull into an anticipatory, pre-warmed state machine.

1. **Bounded Warm Frontier Data Structure (`campy/brain/temporal_lobe/warm_frontier.py`):**
   - Maintains an in-memory, bounded set of pre-activated node IDs with activation scores `[0.0, 1.0]`.
   - Bounded to a maximum size (`max_warm_nodes = 50`, configurable in `campy.toml`).
   - Applies continuous temporal decay: activation scores decay over turns/time (`decay_half_life_turns = 5`).
   - **Dense Supernode Safeguard (`performance-and-debugging.md`, `anti-patterns.md`):** If an entry node's degree exceeds 50 (`degree(node) > 50`), bypass open neighbor expansion. Instead, selectively expand strictly the top 5 incident edges sorted by `pathway_strength DESC` to prevent background traversal explosion into giant hubs (enforcing B283 supernode hygiene).

2. **Passive Ingestion Activation Flow:**
   - On every turn ingested via `notify_turn`, the engine extracts recognized entity tokens, git branch context, and modified file paths.
   - Performs a lightweight 1-to-2 hop graph neighbor expansion from matched nodes via `gateway.py` / `oxigraph_client.py`.
   - Boosts activation scores for connected Decisions, Constraints, and Lessons.
   - Zero perceptible latency: neighbor expansion is bounded and executes in <5ms.

3. **Thalamic Retrieval Integration:**
   - `bundle_compiler.py` and `step5_retrieval.py` check the warm frontier during candidate ranking.
   - Candidate nodes present in the warm frontier receive an activation boost multiplier `(1.0 + warm_score * 0.35)` applied to their calibrated vector/lexical score.
   - Cold-start resilience: If the warm frontier is empty or low confidence, retrieval falls back seamlessly to standard dual-scope ranking.

4. **Step 4b Associative Trigger Binding:**
   - When an incoming turn contains error traces, test failures, or key operations (e.g. git rebase, docker deploy), Step 4b checks stored Lessons/Procedures.
   - If similarity exceeds 0.65, it automatically pre-activates the corresponding Lesson in the warm frontier so it is immediately injected into the next prompt bundle.

### Dependencies

- B91 (Passive Graph Pre-Activation Plan)
- B252 (Bundle Compiler)
- B279 (Vector Similarity Calibration)
- B283 (Supernode Hygiene)

### Files to create/modify

- Create: `campy/brain/temporal_lobe/warm_frontier.py`
- Modify: `campy/brain/temporal_lobe/loop/step4b_associative.py` (or loop orchestrator)
- Modify: `campy/brain/thalamus/bundle_compiler.py`
- Modify: `campy/brain/brainstem/config.py` and `campy.toml` (add `[retrieval.warm_frontier]` settings)
- Create: `tests/test_warm_frontier.py`
- Modify: `tests/test_bundle_compiler.py`

### Acceptance Criteria

- [ ] Warm frontier maintains bounded capacity (`max_warm_nodes`) and evicts lowest-scored nodes when capacity is reached.
- [ ] Ingesting turns through `notify_turn` incrementally updates the warm frontier in <5ms without blocking the event loop.
- [ ] High-degree supernodes (>50 degree) do not flood warm frontier; selective expansion strictly capped to top-5 incident edges by `pathway_strength DESC`.
- [ ] Retrieval ranking (`compile_bundle`, `current_truth`) prioritizes warm nodes when relevant while preserving cold-node recall when similarity is high.
- [ ] Associative pattern detection (Step 4b) successfully pre-warms relevant Lessons upon observing matching error or operational tokens.
- [ ] Clean fallback when warm frontier is empty (no degradation of baseline retrieval accuracy).
- [ ] 100% test pass on `tests/test_warm_frontier.py` against graph gateway (`gateway.py`).
- [ ] Work executed in branch `feat/b375-prewarmed-selective-activation` and PR created targeting `main`.

### Outcome

Campy pre-warms relevant memories ahead of explicit demand, lowering retrieval latency and surfacing critical constraints before agents have the opportunity to violate them.
