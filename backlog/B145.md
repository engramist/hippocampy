# B145: Hard-Bind Plateau Mode to the Top-Ranked Action Family

**Status**: complete ✅ DONE (2026-04-01)
**Assigned**: TBD  
**Depends on**: B144  
**Complexity**: Medium (3–5 hours)  
**Priority**: P0 (next solver-quality blocker after B144)  
**Related**: B139, B141, B142, B143, B144  
**Plan**: backlog/plans/B-145-hard-bind-plateau-mode.md

## Problem Statement

B144 successfully surfaced plateau awareness into the solve context, but the latest verified live run shows that plateau mode is still **advisory** instead of **enforced**.

Evidence from `live_qwen25_7b_b144verify_1775249453`:
- `correct = false`, `steps = 15`, `final_state = NOT_FINISHED`
- final strategy summary shows `PLATEAU: sustained zero-reward streak (15 steps) with grounded entities`
- player/goal grounding is present: `PRIMARY ROLES: player=11, goal=9`
- but the final chunk is still `Explore: try unexplored action to gather more information [explore]`
- late steps still bounce across multiple action families (`ACTION3`, `ACTION4`, `ACTION6`, `ACTION7`) despite the plateau trigger being active

This means the remaining bottleneck is now **policy enforcement consistency**: once plateau mode starts, the solver should stop advertising generic exploration and instead commit the remaining budget to the top-ranked action family (or a tightly bounded fallback), with the trace and actual actions staying aligned.

## What This Card Builds

1. **Plateau chunk replacement**: when `plateau_mode=True`, replace the generic `Explore` chunk with an explicit exploitation chunk sourced from the top-ranked family.

2. **Hard family lock with bounded escape hatch**: keep the final steps on the best-supported family unless fresh evidence materially changes the ranking or a small, explicit fallback budget is consumed.

3. **Summary/action consistency**: ensure `strategy_summary`, `active_chunk`, ranked-family trace fields, and actual chosen actions all describe the same plateau policy.

4. **Explicit exhaustion signal**: if the top family and bounded fallback budget both fail, emit a visible `hypothesis_exhausted` / `plateau_family_exhausted` style reason instead of silently reverting to generic churn.

## Files to Create/Modify

- `agents/arc3/solver.py` — construct a real plateau exploitation chunk and keep strategy-summary text aligned with the ranked family
- `agents/arc3/orchestrator.py` — enforce family locking / bounded fallback under plateau mode and surface exhaustion reasons
- `tests/test_arc3_solver.py` — regressions for plateau chunk replacement and strategy-summary consistency
- `tests/test_arc3_orchestrator.py` — regressions for hard family locking and bounded escape-hatch behavior

## Ecosystem / Layer Compliance

- Layer: ARC solve engine (`agents/arc3/solver.py`) + ARC orchestration policy (`agents/arc3/orchestrator.py`)
- No new tools, adapter allow-list changes, or schema migrations expected
- Additive policy tightening only; no cross-layer import violations
- Must comply with `docs/arc-harness-rules.md` and `docs/ecosystem-rules.md`

## Acceptance Criteria

- [ ] When `plateau_mode=True`, the returned `active_chunk` is no longer generic `explore`; it is an exploitation chunk tied to the top-ranked action family
- [ ] The last 5 steps stay on the top-ranked family or at most one explicit fallback family unless new evidence appears
- [ ] `strategy_summary`, trace fields, and actual executed actions remain consistent about which family is being exploited
- [ ] If the family lock is exhausted, the trace emits an explicit exhaustion reason rather than silently reverting to open-ended exploration
- [ ] Existing B143 coordinate policy still works when `ACTION6` is the locked family
- [ ] Focused ARC tests and broader regression suites pass with no regressions

## Outcome Statement

When complete, plateau mode will become a real late-run control policy rather than a descriptive hint: the solver will visibly replace generic exploration with a bounded, auditable exploitation strategy tied to the strongest remaining action family.