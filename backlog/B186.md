# B186: Trajectory Evaluator

**Status**: ready
**Assigned**: TBD
**Depends on**: None
**Complexity**: Medium (2-4 hours)
**Priority**: P2 (offline analysis, no runtime impact)
**Plan**: backlog/plans/B-186-trajectory-eval.md

## Problem Statement

The agent's step-by-step trajectory is logged in `agent_execution_trace.json` and `_step_history`, but there's no automated analysis of trajectory quality. We know *what* the agent did but not *how well* it navigated. Did it explore efficiently? Did its hypotheses converge? Did it follow its own plans?

The "Trajectory" eval layer asks: did the agent take the right steps, even if it didn't solve the puzzle?

## Root Causes

1. Trace data is collected but only analyzed manually
2. No trajectory scoring metric exists
3. No comparison between "good trajectory" (converging) and "bad trajectory" (oscillating)

## What This Card Does

1. **New `TrajectoryEvaluator` class** in `benchmarks/arc3/trajectory_eval.py`
2. Reads `agent_execution_trace.json` (already exported by runner) and computes:
   - **Action diversity** (0-20): How many distinct actions used / total actions available
   - **Hypothesis convergence** (0-20): Did archetype/victory/roles stabilize or oscillate?
   - **Exploration efficiency** (0-20): Novel states discovered / total states visited
   - **Plan adherence** (0-20): Actions matching active chunk's `estimated_actions` / total actions
   - **Escalation appropriateness** (0-20): Escalations triggered at sensible step counts
3. **`TrajectoryScore`** (0-100): Sum of five sub-scores per puzzle
4. Can run as CLI on saved trace files or integrated into runner post-solve
5. **No LLM needed** — pure algorithmic analysis of trace data

## Ecosystem Layer

- `benchmarks/arc3/trajectory_eval.py` (Eval Infra — new file)
- `agents/arc3/runner.py` (Agent Runtime — optional post-solve call)

**Cross-layer compliance**: No cross-layer violations.

## Files to Create/Modify

- **CREATE** `benchmarks/arc3/trajectory_eval.py` — TrajectoryEvaluator class, TrajectoryScore dataclass
- **MODIFY** `agents/arc3/runner.py` — Optional post-solve trajectory evaluation, add score to result payload
- **CREATE** `tests/test_b186_trajectory_eval.py` — Tests for each sub-score with synthetic trace data
- No changes to `docs/tool-catalog.md`

## Acceptance Criteria

- [ ] TrajectoryScore computed for any valid trace file
- [ ] Action diversity = 0 when agent uses only 1 action; = 20 when all actions used equally
- [ ] Hypothesis convergence = 0 when archetype oscillates every step; = 20 when it stabilizes by step 5
- [ ] Works offline on saved trace files via CLI
- [ ] Works integrated into runner post-solve
- [ ] All existing ARC tests pass

## Outcome

Every puzzle has a trajectory quality score. Combined with outcome score (B181), we can identify "good trajectory, bad outcome" (unlucky) vs "bad trajectory, bad outcome" (strategy problem). This is the feedback loop that makes iteration productive.
