# B240 - One-Click Install Release Gate and User-Facing Docs

State: complete -- 2026-08-18: created `docs/one-click-install-release-gate.md` (go/no-go checklist, supported platforms, all validation commands + expected results) and `docs/troubleshooting-install.md` (repair commands per failure mode); reframed `README.md` so the one-click command is documented as recommended-once-published rather than canonical, since B236 has 2 genuinely unmet acceptance criteria (`hippocampy` not on PyPI; no TestPyPI dry-run record); updated `docs/public-release-audit.md` and `backlog/masterBacklogTracker.md`. Verdict: **NO-GO** on promoting the one-click command as canonical, **GO** on continuing to document/validate it as the recommended path. `Instalation_Instructions.md` was intentionally NOT recreated -- it does not exist in this repo; B297 deliberately deleted it (`docs/public-disclosure-boundary.md`: "Stale test-machine Q&A that contradicted the README's install story; typo'd filename... Deleted — README's Install section is the updated version this row called for"). Recreating it would reverse that documented decision, so its required content (quickstart, troubleshooting) was placed in README's Install section and the new troubleshooting doc instead. See "Validation Commands" below for real captured output.
Priority: P0

Plan: backlog/plans/B-240-one-click-release-gate-and-user-facing-docs.md

### Problem

Even after the package path, bootstrap script, and validation harness exist, the project needs a clear go/no-go release gate and user-facing documentation. Without a single release checklist, it is easy to publish a command that works on the developer machine but fails for real users.

### What This Card Does

Creates the final release gate for one-click install: docs, troubleshooting, supported platforms, privacy guarantees, install/uninstall behavior, validation evidence, and public command readiness.

### Dependencies

- B236 - public package path
- B237 - hosted bootstrap script
- B238 - clean-machine validation
- B239 optional - Homebrew path

### Files to Create/Modify

| File | Action |
|---|---|
| `docs/one-click-install-release-gate.md` | CREATE - final go/no-go checklist |
| `README.md` | MODIFY - make one-click install the primary path only when gate passes |
| `Instalation_Instructions.md` | MODIFY - rewrite as user-facing quickstart/troubleshooting |
| `docs/troubleshooting-install.md` | CREATE - common failures and repair commands |
| `docs/public-release-audit.md` | MODIFY - record final audit status |
| `backlog/masterBacklogTracker.md` | MODIFY - track B236-B240 status and dependencies |

### Acceptance Criteria

- [x] Release gate lists every validation command and expected result. See `docs/one-click-install-release-gate.md`, 11 commands with expected results.
- [x] Release gate documents supported platforms and explicitly marks unsupported ones. Same doc, "Supported Platforms" table (macOS arm64/x86_64, Linux x86_64 supported; musl Linux, native Windows unsupported; WSL2/Windows-Intel untested).
- [x] Docs explain where user memory lives (`~/.campy`) and that installer/uninstaller must preserve it by default. README "Where your memory lives" + `docs/troubleshooting-install.md`.
- [x] Docs include inspect-first and one-line install commands. README's bootstrap section (inspect-first listed first, one-liner given); `docs/one-click-validation.md` (pre-existing, unchanged) also has both.
- [x] Docs include repair commands: `campy doctor --repair`, `campy status`, `campy activity --follow`. README "Verify the install" + `docs/troubleshooting-install.md` "Quick diagnostics".
- [x] Docs explain how to verify Codex, Claude Code, Claude Desktop, and VS Code integration. `docs/troubleshooting-install.md` "Verifying each agent integration" — per-client commands for all four.
- [x] Docs include uninstall behavior and data deletion behavior as separate steps. README + `docs/troubleshooting-install.md`: `campy uninstall` (keeps data by default) vs. `campy uninstall --delete-data` (explicit, separate opt-in).
- [x] README does not promote the one-click command as canonical until B236-B238 pass. B236 has 2 unmet ACs (unpublished PyPI package, no TestPyPI dry-run record) — README's Install section now says "No single command is 'canonical' yet," leads with source install, and frames pipx/bootstrap as "recommended... not live yet."
- [x] Public-release audit and patent-pending language remain accurate. `docs/public-release-audit.md` "One-Click Install Gate (B240)" section appended, existing findings/patent language untouched; re-ran `scripts/audit_public_release.sh --release` — passed.
- [x] Backlog tracker reflects which one-click cards are complete and which are still pending. `backlog/masterBacklogTracker.md` B240 row updated; B236-B239 rows already carried accurate complete/complete-with-caveats status from their own re-verifications.

### Validation Commands

```bash
rg -n "one-click|bootstrap.sh|doctor --repair|activity --follow|~/.campy|uninstall|Codex|Claude|VS Code" README.md Instalation_Instructions.md docs
bash scripts/audit_public_release.sh --release
.venv/bin/campy doctor
.venv/bin/pytest -q tests/test_public_release_manifest.py tests/test_packaging_installed_mode.py tests/test_bootstrap_script.py tests/test_bootstrap_clean_home.py
```

### Outcome

The project has a final release gate that tells us when the one-click installer is safe to advertise as the primary install path.
