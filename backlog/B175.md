# B175: Autopilot Wall-Blind Navigation — Detect Stuck Player and Reroute

**Status**: ready
**Assigned**: TBD
**Depends on**: B168 (graph-based exploration provides entity movement data)
**Complexity**: Medium (2-4 hours)
**Priority**: P1 (root cause of 119-step zero-progress solve failure)
**Plan**: backlog/plans/B-175-autopilot-wall-detection.md

## Problem Statement

`_try_autopilot()` in `orchestrator.py:307` navigates toward a target by choosing the axis with the largest delta (`abs(dr) >= abs(dc)` at line 447). When the chosen action doesn't actually move the player (wall collision), autopilot has two disengage checks:

1. **`n_cells_changed == 0`** check (line 374-381): Fails because ACTION4 triggers 42 entity updates (gravity/scroll effect) even though the *player* doesn't move. `n_cells_changed` is total grid change, not player-specific.
2. **Stuck-position** check (line 383-400): Requires 3+ consecutive autopilot steps with identical position, but the orchestrator frequently interleaves autopilot with LLM decisions, resetting the consecutive count.

Result: autopilot keeps picking ACTION1/ACTION4 (column-axis actions) when the player needs row-axis movement (ACTION2/ACTION3). It never detects the wall and never switches axis.

## Root Causes

1. Wall detection uses `n_cells_changed` (whole-grid metric) instead of player centroid delta
2. Stuck-position check requires 3 *consecutive autopilot* steps — interleaved LLM steps break the streak
3. No axis-rotation fallback: after N failed attempts on one axis, autopilot doesn't try the orthogonal axis

## What This Card Does

1. **Player-specific wall detection**: Compare player centroid before/after action. If centroid moved < 1 pixel for 2+ attempts on the same axis, that axis is wall-blocked.
2. **Persistent blocked-axis tracking**: Maintain `_blocked_axes: Set[str]` (row/col) that persists across interleaved LLM steps, not just consecutive autopilot steps.
3. **Axis rotation**: When the primary axis is blocked, autopilot tries the orthogonal axis automatically instead of disengaging.
4. **Emit wall detection events**: Trace `autopilot_wall_detected` with axis, action, and centroid data for debugging.

## Ecosystem Layer

- `agents/arc3/orchestrator.py` (Agent Runtime — autopilot navigation)

**Cross-layer compliance**: No cross-layer violations. All changes within Agent Runtime.

## Files to Create/Modify

- **MODIFY** `agents/arc3/orchestrator.py` — Rewrite wall detection in `_try_autopilot()`, add `_blocked_axes` state, add axis rotation logic
- **CREATE** `tests/test_b175_autopilot_wall_detection.py` — Tests for wall detection, axis rotation, and rerouting
- No changes to `docs/tool-catalog.md`

## Acceptance Criteria

- [ ] Autopilot detects wall collision using player centroid delta (not `n_cells_changed`)
- [ ] Blocked-axis state persists across interleaved LLM steps
- [ ] When primary axis is blocked, autopilot tries orthogonal axis before disengaging
- [ ] Wall detection emits `autopilot_wall_detected` trace event
- [ ] `_blocked_axes` resets when player position changes significantly (wall cleared)
- [ ] All existing ARC tests pass

## Outcome

Autopilot can detect walls via player centroid tracking and reroute to the orthogonal axis, preventing the 119-step deadlock where ACTION1/ACTION4 are repeatedly tried while ACTION2/ACTION3 (the correct row-movement actions) are never attempted.
