# B166: Deterministic Autopilot — Bypass LLM When Player/Goal Positions Are Known

**Status**: ready
**Assigned**: TBD
**Depends on**: None (B161 is complementary but not a blocker)
**Complexity**: Small (1–2 hours)
**Priority**: P0 (CRITICAL — the LLM cannot reason spatially; this is the single highest-impact change)
**Plan**: backlog/plans/B-166-deterministic-autopilot.md

## Problem Statement

After fixing the B139 plateau guard (reward→pixels_changed), the guard no longer blocks the LLM's choices. But the LLM (qwen2.5:7b) **still wanders aimlessly** because it cannot reason about spatial relationships.

Evidence from latest smoke test (game ka59-9f096b4a):
- Player at (32.5, 23.5), goal at (31.0, 28.0) — goal is slightly UP and to the RIGHT
- Agent should predominantly use ACTION4 (right) with occasional ACTION1 (up)
- Instead, the LLM cycles: ACTION2→3→6→1→4→1→2→3→4→6→2→3→6→4 — essentially random
- All 15 steps get `guard_status: approved` — the guard isn't the problem anymore
- The LLM simply cannot map "row 32.5 → row 31.0 = go up" even with position data in the prompt

The SolveEngine already identifies player/goal with high confidence (player=0.76, goal=0.91) and knows their grid positions. We should **bypass the LLM entirely** and compute the correct directional action deterministically.

## Root Cause

qwen2.5:7b (and similar small models) cannot reliably perform multi-step spatial reasoning:
- "Player at row 32, goal at row 31" → should pick ACTION1 (up) but doesn't
- Even with explicit "move up and right" in the prompt (B161 approach), the model ignores it ~80% of the time
- Each wasted step is ~15 seconds of LLM inference + an API action that doesn't help

## What This Card Does

Adds a **deterministic autopilot** that fires in the `act()` method **before** the mental sandbox LLM call. When:
1. The SolveEngine has identified both player AND goal with confidence ≥ 0.7
2. The player/goal positions are known (non-null estimated_position)
3. The distance between them is > 1 row or > 1 column

Then: compute the optimal directional action (ACTION1-4) deterministically and skip the LLM entirely. This saves ~15s per step and guarantees correct navigation.

## Ecosystem Layer

- `agents/arc3` (ARC agent layer — orchestrator.py only)

## Files to Create/Modify

- `agents/arc3/orchestrator.py` — Add `_try_autopilot()` method, call it early in `act()`, trace events
- `tests/test_arc3_orchestrator.py` — Add tests for autopilot activation, deactivation, and edge cases

## Acceptance Criteria

- [ ] When player and goal are both identified with confidence ≥ 0.7 and positions known, `act()` skips the mental sandbox and returns the correct directional action
- [ ] Autopilot picks the axis with the **larger delta** first (e.g., if 10 rows away and 2 columns away, prioritize the row direction)
- [ ] Autopilot traces its decisions: `decision_source: "autopilot"`, includes player/goal positions and computed direction
- [ ] When player and goal are on the same row/col (delta ≤ 1 on that axis), autopilot only moves on the other axis
- [ ] When player and goal are within 1 row AND 1 column (arrived), autopilot tries ACTION5 (interact)
- [ ] Autopilot disengages if it detects 2+ consecutive zero-pixel-change steps (wall collision → yield to LLM)
- [ ] All existing ARC tests pass (`pytest tests/test_arc3_*.py -q`)

## Outcome

The agent navigates directly toward the goal in ~5-10 steps instead of wandering for 15. Each step is faster (no LLM call) and correct (deterministic computation). The LLM is only consulted when the autopilot can't determine the next move (e.g., uncertain positions, wall collisions, or puzzle mechanics beyond simple navigation).
