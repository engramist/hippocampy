# B150: ARC Grid Diff Engine — Level Transition Analysis

**Status**: complete ✅ DONE (2026-04-01)
**Assigned**: TBD
**Depends on**: B157
**Complexity**: Large (4–6 hours)
**Priority**: P0 (foundation for cross-level learning)
**Related**: F1/F3 from codereviewOpus2.md
**Plan**: backlog/plans/B-150-arc-grid-diff-engine.md

## Problem Statement

ARC-AGI-3 is an interactive multi-level game. The agent learns by playing — early levels are simple tutorials, later levels are harder. After solving level N, the agent has a before/after grid pair (level start → level end). These **solved levels are implicit training examples**.

Currently the agent treats every frame as isolated. It never compares "what the grid looked like before I solved level 1" vs "what it looked like after." Without this analysis, knowledge from early levels cannot transfer to later levels.

### ARC-AGI-3 Game Model

- Games have 7-8 levels, progressing from simple to complex
- No static training examples — the agent discovers patterns through play
- Early levels = implicit tutorials. Solved levels = training pairs.
- Scoring is weighted by level index (later levels count more)
- Actions: ACTION1-4 (directional), ACTION5 (interact), ACTION6 (coordinate), ACTION7 (undo)

## What This Card Builds

1. **`GridDiffEngine` class** in `agents/arc3/grid_analysis.py` — pure deterministic analysis, no LLM:
   - `diff_grids(start_grid, end_grid)` → `GridDiff`: cells changed, color mapping, spatial ops
   - `extract_connected_components(grid, color)` → connected components per color
   - `detect_symmetry(grid)` → symmetry type
   - `detect_color_mapping(start_grid, end_grid)` → Dict[int, int] or None
   - `cross_level_consensus(level_diffs: List[GridDiff])` → `LevelPattern`
   - `diff_frames(frame_before, frame_after)` → `FrameDelta` (step-level analysis)

2. **`LevelPattern` dataclass** — what's common across solved levels:
   - `consistent_action_effects: Dict[str, str]` — what each action does
   - `consistent_color_map: Dict[int, int]` — color transformations
   - `consistent_spatial_pattern: Optional[str]` — movement/transformation type
   - `game_rule_summary: str` — human-readable description of the game's rules
   - `confidence: float` — 0-1 based on cross-level agreement

3. **Two analysis modes**:
   - **Step-level**: `diff_frames()` compares grid before and after a single action — feeds ActionFact tracking
   - **Level-level**: `diff_grids()` compares level start vs level end — feeds cross-level learning (B151)

4. **Integration**: Orchestrator calls GridDiffEngine at each level transition (B157 provides the trigger), building up `_solved_level_diffs` for B151 to use.

## Files to Create/Modify

- `agents/arc3/grid_analysis.py` — NEW: GridDiffEngine, GridDiff, LevelPattern, FrameDelta
- `agents/arc3/orchestrator.py` — Call diff engine at level transitions and per-step
- `tests/test_b150_grid_diff_engine.py` — NEW: comprehensive tests

## Acceptance Criteria

- [x] `diff_grids()` correctly identifies changes between two 64x64 grids
- [x] `diff_frames()` captures per-action effects on the grid
- [x] `cross_level_consensus()` identifies common patterns across 2+ solved levels
- [x] Connected component detection works on 64x64 grids with 0-15 color values
- [x] Integration stores diffs at level transitions (requires B157)
- [x] All tests pass, no regressions

## Outcome Statement

After this card, the agent structurally analyzes what changes between game states — both per-action (what did ACTION3 do?) and per-level (what's the pattern across solved levels?). Early solved levels become training data for later levels.
