# B387 - Torch-Free Ingestion Plane: Replace spaCy NER/Parse with ONNX

State: complete
Priority: P0
Branch: feat/b387-torch-free-ingestion
PR Target: main

Plan: backlog/plans/B-387-torch-free-ingestion.md

Supersedes: B384 Phase 2A (B384 is superseded — see B387/B389/B390/B391–B396/B397)

### Execution & Branch Policy

> **MANDATORY:** Work on `feat/b387-torch-free-ingestion`. No direct commits to `main`. Land via PR to `main` with test evidence.

### Problem

`import spacy` pulls `torch` into `sys.modules` (verified). Measured RSS on this machine:

| Imports | RSS |
|---|---|
| bare Python | 14.8 MB |
| `+ kuzu` | 24.3 MB |
| `+ spacy` (pulls torch) | **239.9 MB** |
| `+ fastembed` | 75.1 MB |
| `+ kuzu + fastembed` | **76.1 MB** |

spaCy/torch costs **~225 MB** — essentially the entire memory problem. Removing it
puts the daemon under the <80 MB target on its own.

`torch` is also ~2 GB of wheel for a dependency used only for token classification,
and it dominates install time and container image size (B385 ships to Fargate).

### Current usage — read this before planning

Files are `step1_ner.py` and `step1b_relations.py`. (B384's card text said
`step1_zoning.py` / `step1b_fast_relations.py`; those names do not exist.)

`campy/brain/temporal_lobe/loop/step1_ner.py` uses:
- `spacy.load("en_core_web_md")`, cached in a module-level `_nlp`
- `doc.ents` — named entity recognition
- `doc.noun_chunks` and `chunk.root.dep_` — **dependency parse labels**

`campy/brain/temporal_lobe/loop/step1b_relations.py`:
- `extract_relations(doc, entities)` takes a spaCy `Doc` object directly

### Gate 0 — Feasibility spike (do this FIRST, report before building)

ONNX token classification gives NER. It does **not** give dependency labels or
noun chunks. Before writing production code, timebox a spike and report:

1. Which ONNX NER model is viable, with its **license** (the card requires
   Apache-2.0 or MIT — no CC-BY-NC, no research-only), size on disk, and RSS when
   loaded under `onnxruntime` (already a dependency via fastembed).
2. How the model is distributed. It must work **offline** at runtime — B385 ships a
   zero-egress container. Pre-baked into the image or downloaded at install time,
   never at first inference.
3. **A concrete replacement for `noun_chunks` + `chunk.root.dep_`.** Options to
   evaluate: an ONNX dependency parser; a POS-tagger + shallow-chunk heuristic; or
   dropping chunk-based candidates entirely and relying on NER spans.
4. Measured entity-extraction agreement against the current spaCy output on a
   sample of real turns.

**If no option clears the license + offline + accuracy bar, STOP and report.** Do
not silently degrade extraction quality to make the card pass. Reporting "not
feasible as specified, here are the three options and their tradeoffs" is a
successful outcome for this gate.

### Scope

1. Replace spaCy NER in `step1_ner.py` with the ONNX pipeline chosen at Gate 0,
   preserving the existing return contract of `extract_entities()`.
2. Replace the `Doc`-typed interface between `step1_ner.py` and
   `step1b_relations.py` with an explicit, engine-neutral dataclass (tokens, spans,
   labels). `extract_relations()` must no longer accept a spaCy `Doc`.
3. Remove `torch`, `torchvision`, `thinc`, `spacy` from `pyproject.toml` and
   `requirements.txt`. Remove the spaCy model download from `campy/cli/install.py`
   and from CI.
4. `tests/test_torch_free_import.py`: importing `campy.brain_daemon` leaves zero
   `torch`, `thinc`, `spacy` entries in `sys.modules`.

### Patent-evidence interaction — required

B380's claim tests run through this extraction path and are zero-mock. Patent
Claims **1** (Gated Consolidation Loop), **2** (Shape-First ontological grounding),
and **4** (Cocktail Party attention gate) describe mechanisms this card changes.

- The full `tests/patent_claims/` suite must pass on this branch.
- If any claim test's *behaviour* changes (not just its fixture plumbing), that is
  an escalation, not a test edit. The evidence pack describes a shipped mechanism;
  it cannot silently drift.
- CI currently downloads `en_core_web_md` for these tests (added in PR #153). That
  step is removed here — confirm the suite still runs without it.

### Files

- Modify: `campy/brain/temporal_lobe/loop/step1_ner.py`
- Modify: `campy/brain/temporal_lobe/loop/step1b_relations.py`
- Modify: `campy/cli/install.py` (drop spaCy model download)
- Modify: `pyproject.toml`, `requirements.txt`
- Modify: CI workflow (drop `en_core_web_md` step)
- Create: `tests/test_torch_free_import.py`
- Create/Modify: tests covering the new entity/relation contract

### Acceptance Criteria

- [x] Gate 0 spike reported before implementation began.
- [x] Chosen model is Apache-2.0 (base model `elastic/distilbert-base-cased-
      finetuned-conll03-english`, cardData.license verified directly via the
      HF API), works fully offline (`local_files_only=True` at daemon
      runtime, respects `HF_HOME`), documented in the PR and in
      `onnx_ner_engine.py`'s module docstring. CoNLL-2003/Reuters training-
      data provenance investigated separately (see PR description) — not a
      clean, judicially-settled answer (no pretrained-NLP-model license ever
      is), but consistent with elastic's own explicit grant and with
      long-standing, high-volume industry practice (`dslim/bert-base-NER`,
      MIT, ~2.1M downloads/month, same CoNLL-2003 provenance). Flagged for
      counsel review given the active patent filing; not blocked on it.
- [x] `import campy.brain_daemon` → zero `torch`/`thinc`/`spacy` in
      `sys.modules` (verified via subprocess in `tests/test_torch_free_import.py`).
- [x] Daemon idle RSS measured and reported — **see PR description**. Cold
      (`import campy.brain_daemon`, nothing else touched): ~45 MB. Warm
      (after actually calling `extract_entities()` once, i.e. NER model
      loaded and used, matching real daemon behavior after its first
      message): ~193-209 MB. Fully warm (NER + fastembed embeddings both
      loaded and used, the realistic steady state for a daemon that has
      processed a real message): **~382 MB, not <80 MB.** This is not a
      regression introduced by this card — it reflects that both
      onnxruntime `InferenceSession`s (the new NER model and B355's
      pre-existing fastembed embedding model) each carry ~150-190 MB of
      session overhead that the "kuzu+fastembed: 76.1 MB" baseline this
      card's own problem statement quotes never actually measured (that
      figure is import-time RSS; fastembed's model is lazily constructed,
      so nothing loads until the first real `.embed()` call — verified
      directly: fastembed alone reaches ~266 MB warm RSS, not 75.1 MB). A
      fair, same-methodology old-vs-new comparison (spaCy `en_core_web_md`
      loaded and run once, also warm) measured **~385 MB for spaCy alone**
      — so B387 is still a real, substantial win (~565 MB+ old full daemon
      vs. ~382 MB new full daemon, by the same warm-measurement method),
      just not the <80 MB this card assumed from an already-stale baseline
      number. Follow-up filed to investigate onnxruntime session-memory
      reduction (shared session/arena, platform differences on the actual
      Fargate Linux target) — see PR description.
- [x] `step1b_relations.extract_relations()` no longer takes a spaCy `Doc`
      (`ParsedText` from `shallow_parse.py`).
- [x] Entity/relation extraction tests pass; agreement vs. spaCy baseline
      measured over 46 real sentences (see PR description for the full
      breakdown): entity-span precision 0.434 / recall 0.695 / F1 0.534
      (exact character-offset match); relation-triple agreement 20/46
      identical, +8/46 identical except for spaCy retaining a leading
      article the shallow-parse chunker strips by design (61% effectively
      equivalent), 6/46 spaCy-only (recall loss), 5/46 ONNX-only (some
      genuine gains — the old dep-walk never handled "X depends on Y"
      prepositional-object patterns; some noise), 7/46 both-found-but-
      different.
- [x] Full `tests/patent_claims/` suite passes (17/17); no assertions
      edited, no behavioural change observed.
- [x] `tests/test_loop.py`, `tests/test_retrieval.py` pass (60/60 and
      10/10 respectively — `test_loop.py`'s 5 previously `@_needs_spacy`-
      skipped tests now run for real and pass, since spaCy's Python-3.14
      incompatibility that motivated the skip no longer applies once
      spaCy is gone entirely).
- [x] Cypher ratchet still 0; no new `execute_raw` (`scripts/check_cypher_ratchet.py` → OK).

### Outcome

`torch`/`thinc`/`spacy` are fully removed from the dependency set and from
`sys.modules` — verified by subprocess-level test, not just "not imported
eagerly". The daemon's *cold* (import-only) footprint drops from spaCy's
~240 MB (import-only figure) to ~45 MB. The *warm* (actually-used) picture
is more nuanced than "reaching the featherweight <80 MB target": both the
new ONNX NER model and the pre-existing fastembed embedding model carry
real onnxruntime session overhead that a warm daemon pays regardless of
this card, and the <80 MB target was set from a baseline number that
(it turns out) was never actually measured warm. B387 still delivers a
real, meaningful reduction (~180 MB less resident memory warm, by a
fair same-methodology comparison against real spaCy usage) and removes
~2 GB of wheel/install-time weight, without touching the storage engine.
The RSS-target gap is real and is tracked as follow-up work, not silently
absorbed into this card's "done" claim.
