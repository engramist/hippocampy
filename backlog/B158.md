# B158: Disambiguation Queue — Human-in-the-Loop Entity Curation

**Status**: ready
**Assigned**: TBD
**Depends on**: None
**Complexity**: Medium (2-4 hours)
**Priority**: P3 (power-user feature, improves entity resolution quality)
**Plan**: backlog/plans/B-158-disambiguation-queue.md

## Problem Statement

When Step 6 (Contradiction Arbitration) returns `classification: "uncertain"`, both the new concept and the existing candidate are stored as `confidence_low=true` and left for the background sweep to re-score later. The sweep can auto-promote them based on graph context, but there is no explicit human review path.

Currently `sidequests review` exists as a CLI concept, but it queries `confidence_low` nodes in isolation without showing the *pairing context* — the specific gray-zone candidate that triggered the uncertainty. The user sees a list of low-confidence nodes but has no way to efficiently resolve the entity question: "Are these the same thing or not?"

This is the highest-value gap identified in the entity resolution pipeline. Gray-zone pairs (0.75-0.92 similarity) represent cases where the system genuinely can't decide — exactly where human judgment adds the most value. A human merge decision creates a high-confidence training signal (altLabel synonym + pathway_strength boost) that improves future entity resolution.

## What This Card Builds

1. **`get_disambiguation_queue` MCP tool** — retrieves pending uncertain pairs:
   - Queries all Concept pairs where Step 6 returned "uncertain" and neither node has been resolved
   - Returns pairs with: both concepts' text_raw, similarity score, 1-hop graph context (neighbors, shared edges), creation timestamps
   - Sorted by: recency (newest first), then similarity (closest to decision boundary first)
   - Limit: configurable, default 10 pairs

2. **`resolve_disambiguation` MCP tool** — human resolves a pair:
   - Input: `concept_id_a`, `concept_id_b`, `resolution` (one of: `"merge"`, `"separate"`, `"skip"`)
   - **merge**: Keep the older node as canonical. Create altLabel from newer node's text_raw. Redirect edges from newer node to older. Archive newer node. Bump older node's `pathway_strength` by 0.15 and set `confidence_low=false`.
   - **separate**: Set both nodes to `confidence_low=false`. Create a `DISTINCT_FROM` edge (new edge type) so the system never re-proposes this pair.
   - **skip**: Leave as-is, re-queue for later.

3. **Tracking**: Add `disambiguation_status` field to the orchestrator's uncertain path — when Step 6 returns "uncertain", store a `DisambiguationEvent` node linking both concepts with the original similarity score, so the queue tool can retrieve them efficiently.

4. **Feedback loop**: Merge decisions feed back into the Hebbian system — the altLabel created by merge makes the canonical concept retrievable via both phrasings. Separate decisions create `DISTINCT_FROM` edges that Step 5 can check before sending pairs to arbitration.

## Files to Create/Modify

- `mcp_engine/tools/__init__.py` — Add `get_disambiguation_queue` and `resolve_disambiguation` handlers
- `mcp_engine/tool_schemas.py` — Add tool schemas for both new tools
- `mcp_engine/schema.py` — Add `DisambiguationEvent` node type and `DISTINCT_FROM` relationship type
- `mcp_engine/loop/orchestrator.py` — On "uncertain" result, create DisambiguationEvent node
- `docs/tool-catalog.md` — Document both new tools
- `tests/test_disambiguation_queue.py` — NEW: test queue retrieval, merge/separate resolution, feedback loop
- Adapter allow-lists: propagate new tools to all adapter pass-through lists

## Ecosystem / Layer Compliance

- Layer: Data Products & Memory (tools + schema) + Deterministic Tools & Services (resolution logic)
- No ARC code touched. No imports from `agents/` or `benchmarks/`.
- New tools follow existing MCP tool contract patterns.
- Complies with `docs/ecosystem-rules.md`.

## Acceptance Criteria

- [ ] `get_disambiguation_queue` returns pending uncertain pairs with graph context
- [ ] `resolve_disambiguation` with "merge" creates altLabel, redirects edges, archives duplicate
- [ ] `resolve_disambiguation` with "separate" sets both confident and creates DISTINCT_FROM edge
- [ ] Step 5 checks DISTINCT_FROM edges before sending pairs to arbitration (avoids re-proposing)
- [ ] DisambiguationEvent nodes track the provenance of each uncertain decision
- [ ] Merge boosts pathway_strength and sets confidence_low=false on canonical node
- [ ] New tools appear in adapter allow-lists and tool-catalog.md
- [ ] All tests pass, no regressions

## Outcome Statement

After this card, users can spend 5 minutes clearing the disambiguation queue — clicking "merge" or "separate" on gray-zone entity pairs. Each decision is a high-quality training signal that improves future entity resolution. Merged concepts become retrievable via all historical phrasings. Separated concepts are never re-proposed. The system gets smarter with minimal human effort.
