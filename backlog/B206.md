# B206 — Prevent Archetype Regression to UNKNOWN During Early Steps

- **State:** ready
- **Priority:** P2
- **Plan:** backlog/plans/B-206-archetype-regression-guard.md
- **Ecosystem Layer(s):** Agent Runtime
- **Cross-layer violations:** None — runtime-only change in solver confidence handling.
- **Dependencies:** B205

## Problem Statement

After bootstrap, archetype confidence can temporarily dip and cause classification to revert from a concrete archetype (for example `space`) back to `unknown`. This creates unstable step-to-step reasoning and weakens downstream phase prompts and strategy selection.

## What It Does (Scope)

Add a confidence-hold guard in `SolveEngine.solve()` so a previously grounded archetype is not dropped to `unknown` due to a brief low-signal window.

Behavior:
- If new classifier output is `unknown`, but prior archetype is non-unknown with confidence >= 0.25, keep prior archetype and decay confidence mildly.
- Allow true pivots between concrete archetypes as today.

Out of scope:
- Changing classifier heuristics or thresholds.
- New SideQuests tools or schema changes.

## Files to Create/Modify

| File | Action |
|------|--------|
| `agents/arc3/solver.py` | **Modify** — add non-regression guard in archetype update path |
| `tests/test_arc3_solver.py` | **Modify** — add regression test for unknown fallback suppression |

### Adapter/allow-list impact
- No tool changes.
- No `docs/tool-catalog.md` updates required.

## Acceptance Criteria

1. If previous archetype is non-unknown and >= 0.25 confidence, a transient `unknown` update does not replace it.
2. Confidence decays gradually when held (not frozen).
3. Concrete archetype pivots (for example `space` -> `race`) still work.
4. New solver test covers this scenario and passes.
5. No regressions in existing ARC solver tests.

## Outcome Statement

Archetype identity remains stable across noisy early steps, improving consistency of perception, chunking, and strategy prompts.
