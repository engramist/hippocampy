# B170: Persist Hypotheses to KuzuDB — Eliminate Ghost Schema

**Status**: ready
**Assigned**: TBD
**Depends on**: None (Hypothesis node type already exists in schema.py)
**Complexity**: Medium (3-4 hours)
**Priority**: P0 (ghost schema — node type defined but zero writers exist)
**Plan**: backlog/plans/B-170-hypothesis-kuzu-persistence.md

## Problem Statement

The `Hypothesis` node type is fully defined in `mcp_engine/schema.py` (lines 372-386) with 6 relationship types (`HYPOTHESIZED_IN`, `CONFIRMS`, `CONTRADICTS`, `GENERALIZES`, `PRODUCED_HYPOTHESIS`, `ENTITY_HYPOTHESIS`), but **zero code paths write to it**. All hypothesis data lives exclusively in `HypothesisManager.hypotheses` (a Python dict in `agents/arc3/hypothesis.py:335`).

This is a ghost schema — infrastructure was built but never connected. Hypotheses are core to game-theory reasoning and should persist across solves for cross-puzzle learning (B165 dependency).

## Root Causes

1. `HypothesisManager` was built as a self-contained in-memory system with no KuzuDB awareness
2. The Hypothesis schema was added speculatively but never wired to writers
3. No card or plan ever required the persistence — the in-memory path "worked" for single solves

## What This Card Does

1. **Wire `HypothesisManager` to KuzuDB**: When hypotheses are created, updated, confirmed, refuted, or pruned, persist state to KuzuDB `Hypothesis` nodes
2. **Wire relationship creation**: `CONFIRMS`, `CONTRADICTS`, `GENERALIZES` edges created when evidence supports/contradicts hypotheses
3. **Wire `ENTITY_HYPOTHESIS`**: Connect `GridEntity` nodes to `Hypothesis` nodes when entity observations generate hypotheses
4. **Add `load_hypotheses()` method**: Reload hypotheses from KuzuDB at start of solve (enables cross-puzzle memory)
5. **NoOp fallback**: When KuzuDB unavailable, keep current in-memory-only behavior

## Ecosystem Layer

- `agents/arc3/hypothesis.py` (Agent Runtime — hypothesis writer)
- `agents/arc3/entity_graph.py` (Agent Runtime — ENTITY_HYPOTHESIS edge creation)
- `mcp_engine/schema.py` (Data Products — no changes, schema already exists)

**Cross-layer compliance**: Hypothesis persistence goes through `KuzuClient`. `hypothesis.py` accesses KuzuDB via a reference passed from the runner, same pattern as `entity_graph.py`. No new cross-layer imports.

## Files to Create/Modify

- **MODIFY** `agents/arc3/hypothesis.py` — Add KuzuDB persistence to `HypothesisManager` (create, update, confirm, refute, prune operations)
- **MODIFY** `agents/arc3/runner.py` — Pass KuzuDB reference to `HypothesisManager`
- **MODIFY** `agents/arc3/entity_graph.py` — Create `ENTITY_HYPOTHESIS` edges when entity observations generate hypotheses
- **CREATE** `tests/test_b170_hypothesis_persistence.py` — Tests for hypothesis CRUD in KuzuDB, relationship creation, load/reload
- No changes to `docs/tool-catalog.md` (no new MCP tools)
- No changes to `mcp_engine/schema.py` (schema already exists)

## Acceptance Criteria

- [ ] Creating a hypothesis writes a `Hypothesis` node to KuzuDB
- [ ] Updating hypothesis confidence updates the node in KuzuDB
- [ ] Confirming a hypothesis sets `status = "confirmed"` in KuzuDB and creates `CONFIRMS` edge
- [ ] Refuting a hypothesis sets `status = "refuted"` in KuzuDB and creates `CONTRADICTS` edge
- [ ] `ENTITY_HYPOTHESIS` edges connect GridEntity nodes to Hypothesis nodes
- [ ] `load_hypotheses()` returns hypotheses from KuzuDB matching current task
- [ ] When KuzuDB unavailable, falls back to in-memory-only (existing behavior preserved)
- [ ] All existing ARC tests pass
- [ ] No ghost schema: every Hypothesis relationship type has at least one writer

## Outcome

The Hypothesis node type is no longer a ghost. Game-theory reasoning is persisted to the knowledge graph, enabling cross-puzzle learning and graph-based inference over hypothesis relationships.
