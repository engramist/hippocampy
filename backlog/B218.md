# B218 — Reconstruct Codex Observability Framework

| Field | Value |
|-------|-------|
| **Priority** | P0 |
| **Status** | open |
| **Effort** | M |
| **Dependencies** | B214 (done) |
| **Plan** | backlog/plans/B-218-reconstruct-observability.md |

## Summary

The `mcp_engine/observability.py` module and its integration points (runner.py, adapter.py, orchestrator.py, tests/test_observability.py) were accidentally deleted/reverted in the previous session. The files were never committed so they are not recoverable from git. This card reconstructs that work from the diffs captured in conversation history.

## Acceptance Criteria

1. `mcp_engine/observability.py` exists and exports `Observability`, `canonical_span_name`, `build_observability`.
2. `Observability` degrades to no-op when Phoenix/OTel is unavailable.
3. `DurableARCRunner` and `LedgerBrainClient` accept and propagate an `Observability` instance.
4. `ARCOrchestrator.emit_structured_event` uses observability if available.
5. `tests/test_observability.py` passes with no Phoenix installed.
6. All existing tests continue passing.
