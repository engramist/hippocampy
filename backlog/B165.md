# B165: Persist Lessons Across Runs — Post-Failure Knowledge Retention

**Status**: ready
**Assigned**: TBD
**Depends on**: None
**Complexity**: Medium (2–4 hours)
**Priority**: P2 (every run starts cold with 0 memories, 0 lessons, 0 analogies)
**Plan**: backlog/plans/B-165-cross-run-lessons.md

## Problem Statement

At bootstrap, the agent starts with:
```
memories: 0, lessons: 0, analogies: 0, plans: 0
```

Every run rediscovers the same information from scratch:
- Which actions have no effect
- What the grid structure looks like
- What the puzzle archetype is
- What strategy was attempted and failed

The SideQuests memory system (`current_truth`, `recall_relevant_lessons`, `analogical_search`) returns 0 results because nothing is ever persisted after a failed run.

## What This Card Does

1. **Adds a post-run lesson extraction step** that fires when the puzzle ends (win or lose):
   - Extracts: action effects observed, archetype inferred, victory hypothesis, what worked/failed
   - Stores as a `Lesson` artifact via `brain.store_lesson()`

2. **Adds a post-run "action effect summary" persistence step**:
   - For each action tested, stores: `{action_id, pixels_changed, reward, times_seen, effect_summary}`
   - Uses `brain.notify_turn()` with a structured lesson tag so future `recall_relevant_lessons` can find it

3. **Adds puzzle-structure fingerprinting** for `analogical_search`:
   - At bootstrap, computes a fingerprint: `{grid_size, n_colors, n_regions, region_sizes}`
   - Stores the fingerprint + outcome as an analogy anchor so future similar puzzles can recall it

## Ecosystem Layer

- `agents/arc3` (ARC agent layer — orchestrator.py, runner.py)
- `mcp_engine` (memory layer — for store_lesson calls)

## Files to Create/Modify

- `agents/arc3/orchestrator.py` — add `_extract_run_lessons()` method called at run end
- `agents/arc3/runner.py` — call lesson extraction after run completes
- `tests/test_b165_cross_run_lessons.py` — test that lessons are stored on failed run, test that next run retrieves them

## Acceptance Criteria

- [ ] After a failed run, at least 1 lesson is stored via `brain.store_lesson()` containing action effect data
- [ ] After a failed run, a puzzle fingerprint is stored for future `analogical_search` hits
- [ ] On a subsequent run for the same puzzle, `recall_relevant_lessons` returns ≥1 result
- [ ] On a subsequent run for a structurally similar puzzle, `analogical_search` returns ≥1 result
- [ ] Lesson content includes: which actions had zero effect, archetype inferred, strategy attempted
- [ ] All existing ARC tests pass (`pytest tests/test_arc3_*.py -q`)

## Outcome

Each failed run teaches the next one. The agent stops wasting steps re-exploring actions that are known to have no effect.
