# B151: ARC Cross-Level Game Rule Hypothesizer — Learn Rules from Solved Levels

**Status**: complete ✅ DONE (2026-04-01)
**Assigned**: TBD
**Depends on**: B150, B157
**Complexity**: Large (4–6 hours)
**Priority**: P0 (core reasoning upgrade)
**Related**: F2/H1/H3 from codereviewOpus2.md
**Plan**: backlog/plans/B-151-arc-transformation-hypotheses.md
**Phase**: In-game — runs between levels (after level 1+ solved)

## Problem Statement

ARC-AGI-3 is an interactive multi-level game. There are no static training examples — the agent discovers rules by playing. After solving level N, the agent has a (start_grid, end_grid, action_sequence) triple that is an implicit training example.

The current VictoryHypothesizer classifies puzzles into game categories (reach_goal, collect_all, survive) and the ArchetypeClassifier uses navigation archetypes (race, space, chase). ARC games require discovering the game's rules — what each action does, how the grid transforms, what constitutes a solution. The agent has no mechanism to generate rule hypotheses from solved levels.

### Interactive Game Model

B151 is the reasoning engine that runs between levels:
- **After level 1**: Analyze the level 1 diff (B150) and action sequence → generate initial game rule hypotheses
- **After level 2+**: Cross-reference new level diffs with existing hypotheses → refine understanding
- VictoryHypothesizer is **retained** as fallback for level 1 exploration (before any levels are solved)

## What This Card Builds

1. **`GameRuleHypothesizer` class** in `agents/arc3/solver.py` — generates game rule hypotheses from solved level data:
   - `hypothesize(level_pattern: LevelPattern, solved_levels: List[Dict], llm_client)` -> `List[GameRuleHypothesis]`
   - Uses B150's LevelPattern (cross-level consensus) as structured evidence
   - Generates 1-3 candidate game rule hypotheses ranked by confidence
   - Runs after level 1 is solved (NOT pre-game — there's no data pre-game)

2. **`GameRuleHypothesis` dataclass**:
   - `rule_description: str` — natural language description of the game rule
   - `action_semantics: Dict[str, str]` — what each action does (e.g., ACTION1="move cursor up")
   - `objective_description: str` — what constitutes "winning" a level
   - `level_strategy: str` — suggested approach for the next level
   - `confidence: float` — 0-1, increases with more solved levels
   - `evidence: List[str]` — which level diffs support this hypothesis
   - `contradictions: List[str]` — what doesn't fit

3. **New prompt template** in `agents/arc3/prompts.py`:
   - `GAME_RULE_HYPOTHESIS_TEMPLATE` — presents solved level diffs and action sequences
   - Asks: "Based on these solved levels, what is the game's rule?"
   - Includes action effect summaries from B150's `consistent_action_effects`
   - JSON output schema enforced

4. **Integration**: Orchestrator calls GameRuleHypothesizer at level transitions (B157 provides the trigger). After level 1, initial hypotheses. After level 2+, refined hypotheses with cross-level consensus. VictoryHypothesizer **retained** for level 1 exploration.

## Files to Create/Modify

- `agents/arc3/solver.py` — Add GameRuleHypothesizer, GameRuleHypothesis; modify SolveEngine to use after level transitions
- `agents/arc3/prompts.py` — Add GAME_RULE_HYPOTHESIS_TEMPLATE
- `tests/test_b151_game_rule_hypotheses.py` — NEW: test hypothesis generation from solved level data

## Ecosystem / Layer Compliance

- Layer: Agent Runtime & Harness (reasoning component)
- Adds one LLM call per level transition — bounded by number of levels (7-8)
- No cross-layer violations. No imports from `mcp_engine/`.
- Complies with `docs/arc-harness-rules.md` and `docs/ecosystem-rules.md`.

## Acceptance Criteria

- [x] GameRuleHypothesizer generates 1-3 ranked hypotheses from solved level data
- [x] Each hypothesis includes action semantics, objective description, and next-level strategy
- [x] Confidence increases as more levels are solved and patterns agree
- [x] Prompt template presents level diffs and action sequences, not raw grid dumps
- [x] Runs at level transitions (after level 1+ solved), not pre-game
- [x] VictoryHypothesizer retained for level 1 exploration fallback
- [x] High-confidence patterns produce hypotheses without LLM calls (Fast Path)
- [x] All tests pass, no regressions

## Outcome Statement

After this card, the agent generates game rule hypotheses from solved levels — e.g., "ACTION1-4 move the cursor, ACTION5 toggles the cell color, the goal is to match a target pattern." Each solved level adds evidence, increasing confidence. By level 3-4, the agent should have a solid understanding of the game's mechanics, enabling efficient play on harder later levels.
