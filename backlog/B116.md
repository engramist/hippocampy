# B116 - ARC Exploration Compaction Artifact

State: complete
Completed: 2026-04-01
Validation: 200/200 tests passed (test_arc3_hypothesis.py + orchestrator + solver + B118 + B111 + B124 + B92 + B126 regressions)

Plan: backlog/plans/B-116-arc-exploration-compaction.md

### Problem

The harness still loses too much long-run exploration context once prompt history gets short:

- what was tested
- what failed
- what loops were detected
- which role hypotheses survived

We need a structured exploration summary instead of relying on a few raw recent steps.

### What This Card Builds

A compact exploration artifact:

- summarize tested actions, failed loops, surviving hypotheses, and useful transitions
- persist it as structured harness state
- feed it into later decision-making instead of only raw short history

### Depends on

- B88
- B90
- B93
- B94

## Files to Create/Modify

- `agents/arc3/hypothesis.py`
- `agents/arc3/orchestrator.py`
- `benchmarks/arc3/PROMPT_STRATEGY.md`
- `tests/test_arc3_hypothesis.py`
- `tests/test_arc3_orchestrator.py`

## Acceptance Criteria

1. The harness can compact older exploration into a structured artifact.
2. The artifact captures:
   - tested actions
   - failed loops
   - surviving role/path beliefs
   - useful action facts
3. Later prompts/decisions can consume the compacted artifact instead of relying on long raw
   history.
4. Tests cover compaction content and consumption.
5. A live smoke run shows preserved exploration knowledge without raw prompt bloat.

## Outcome

The ARC harness keeps long-run exploration knowledge in a compact, reusable form.
