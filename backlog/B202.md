# B202 — Per-Step PERCEIVE Phase: Inspect ARC Server Response After Every Action

- **State:** ready
- **Priority:** P1
- **Plan:** backlog/plans/B-202-per-step-perceive.md
- **Ecosystem Layer(s):** Agent Orchestration & Control Plane, Agent Runtime & Harness
- **Cross-layer violations:** None — PhaseController transitions stay in runner.py (Control Plane); orchestrator.py adds a read-only perception method (Agent Runtime).
- **Dependencies:** B201 (Durable Phase-State Machine) — must be complete (it is).

## Problem Statement

The ARC orchestrator's `perceive()` phase only runs once at bootstrap (step 0). After each action, the server response (grid, state, reward, available_actions, etc.) is ingested mechanically via `adapter.ingest_step()` and `orchestrator.record_step_result()`, but never explicitly inspected or interpreted as its own phase.

The phase state machine has no path back to PERCEIVE after bootstrap:
- `EVALUATE -> {HYPOTHESIZE, REPLAN}` — PERCEIVE is unreachable after step 0.

This means:
1. The timeline shows no "ARC response perception" events after the first step.
2. The agent evaluates rewards but doesn't formally observe what changed in the grid.
3. Field-by-field server response interpretation (state, grid delta, new/removed colors, available actions) is never logged as a distinct phase.

**Evidence from smoke test (2026-04-11):** In `master_timeline_filtered.md`, events 4-10 are `perceive` phase, then event 12 transitions `perceive -> model` and PERCEIVE never appears again across 229 events and 20 steps.

## What It Does (Scope)

1. Update the phase transition table so PERCEIVE runs after EVALUATE on every step.
2. Add a lightweight `perceive_step_response()` method to the orchestrator that interprets the server response without LLM calls.
3. Wire the per-step PERCEIVE into both runner methods (`_run_puzzle` and `_run_puzzle_with_brain`).
4. Update ARCHITECTURE.md phase documentation.

### New per-step cycle

```
HYPOTHESIZE -> ROUTE -> EXECUTE -> EVALUATE
  -> PERCEIVE -> HYPOTHESIZE (normal continuation)
  -> REPLAN (stall detected, skips PERCEIVE)
```

### Transition table changes

| Change | From | To |
|--------|------|-----|
| PERCEIVE gains HYPOTHESIZE | `{MODEL}` | `{MODEL, HYPOTHESIZE}` |
| EVALUATE loses HYPOTHESIZE, gains PERCEIVE | `{HYPOTHESIZE, REPLAN}` | `{PERCEIVE, REPLAN}` |

### Not in scope
- LLM calls or entity discovery in per-step perceive (lightweight only).
- Timeline export formatting (see B204).
- Fixing stale tool_rules (see B203).

## Files to Create/Modify

| File | Action |
|------|--------|
| `agents/arc3/phase.py` | **Modify** — Update TRANSITIONS dict: PERCEIVE adds HYPOTHESIZE, EVALUATE replaces HYPOTHESIZE with PERCEIVE |
| `agents/arc3/orchestrator.py` | **Modify** — Add `perceive_step_response()` method (~50 lines) after existing `perceive()`, store result as `_last_response_perception` |
| `agents/arc3/runner.py` | **Modify** — Add per-step PERCEIVE block in both `_run_puzzle` (after ~line 737) and `_run_puzzle_with_brain` (after ~line 1407); restructure REPLAN check to fire before PERCEIVE |
| `docs/ARCHITECTURE.md` | **Modify** — Update phase cycle diagram, PERCEIVE definition, transition table, gate conditions (lines 936-1020) |
| `tests/test_phase_controller.py` | **Modify** — Add 5 new transition tests |
| `tests/test_arc3_orchestrator.py` | **Modify** — Add `test_perceive_step_response` test |

### Adapter/allow-list impact
- No new tools introduced.
- No tool-catalog changes.

## Acceptance Criteria

1. `PhaseController.TRANSITIONS[EVALUATE]` contains `PERCEIVE` and not `HYPOTHESIZE`.
2. `PhaseController.TRANSITIONS[PERCEIVE]` contains both `MODEL` and `HYPOTHESIZE`.
3. `orchestrator.perceive_step_response()` exists, accepts `(observation, step, reward, done, action_id)`, returns a dict with keys: `step`, `state`, `reward`, `done`, `delta`, `available_actions`, `active_colors`.
4. `perceive_step_response()` calls `brain.notify_turn()` with a `[STEP RESPONSE]` prefix.
5. `perceive_step_response()` does NOT make LLM calls or run entity discovery.
6. Result is stored as `orchestrator._last_response_perception`.
7. Both `_run_puzzle` and `_run_puzzle_with_brain` advance to PERCEIVE after EVALUATE (when not replanning or done).
8. REPLAN check fires before PERCEIVE advance — stall path skips per-step perceive.
9. `pytest tests/test_phase_controller.py` passes including: `test_evaluate_to_perceive_transition`, `test_perceive_to_hypothesize_transition`, `test_evaluate_to_hypothesize_now_illegal`, `test_full_per_step_cycle`, `test_replan_bypasses_perceive`.
10. `pytest tests/test_arc3_orchestrator.py` passes including `test_perceive_step_response`.
11. Existing tests in `tests/test_arc3_durable_runner.py` still pass.
12. ARCHITECTURE.md phase documentation is updated.

## Outcome

Every ARC server response is formally inspected in a dedicated PERCEIVE phase, with field-by-field interpretation (state, grid delta, reward, colors, available actions) ingested into SideQuests memory. The timeline shows perceive events after every action, not just at bootstrap.
