# B129 - Align Task Graph Test Imports with B128 API Surface

State: complete ✅ DONE (2026-04-02)

Plan: backlog/plans/B-129-task-graph-api-test-alignment.md

### Problem

The full regression sweep fails during collection with:

- `ImportError: cannot import name 'create_task_graph' from mcp_engine.tools.task_graph`

`tests/test_b127_dag_schema.py` expects a symbol that no longer matches the finalized B128
handler/API surface. This causes an immediate suite stop before runtime regressions can be measured.

### What This Card Builds

1. Align task-graph tests and helper imports with the finalized tool surface.
2. Ensure B127 schema tests validate behavior through current task-graph functions.
3. Keep compatibility across `tests/test_b127_dag_schema.py`, `tests/test_b128_dag_tools.py`,
   and `mcp_engine/tools/task_graph.py`.

### Depends on

- B127
- B128

## Files to Create/Modify

- `mcp_engine/tools/task_graph.py`
- `tests/test_b127_dag_schema.py`
- `tests/test_b128_dag_tools.py` (if needed for consistency)

## Acceptance Criteria

1. `pytest -q tests/test_b127_dag_schema.py` collects and passes.
2. `pytest -q tests/test_b128_dag_tools.py tests/test_adapters.py` still passes.
3. Full sweep no longer fails at collection on task-graph symbol imports.

## Outcome

Task-graph schema/tool tests are aligned to the current API surface, restoring full-suite
collectability and stable regression coverage.