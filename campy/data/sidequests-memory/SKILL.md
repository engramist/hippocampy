# SideQuests Memory Skill (LEGACY)

**Note: This skill has been replaced by `campy-memory`.**

The HippoCampy project has rebranded to **Campy**. All new memory usage policy and agent guidance is now located in the `campy-memory` skill.

## Migration

1. Use `campy setup` to update your adapter registrations.
2. Refer to `skills/campy-memory/SKILL.md` for current guidance.
3. Use `campy activity --follow` to monitor memory behavior.

This legacy skill remains for backward compatibility with existing agent configurations but will not receive further updates.
